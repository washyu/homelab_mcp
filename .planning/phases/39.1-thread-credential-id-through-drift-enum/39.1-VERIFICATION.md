---
phase: 39.1-thread-credential-id-through-drift-enum
verified: 2026-04-28T20:30:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Run 5 round-trip integration tests against a REAL Docker Proxmox target (not in-memory fakes)"
    expected: "All 5 tests in tests/integration/test_credential_binding_round_trip.py pass when run against a live Docker Proxmox container — closes ROADMAP success criterion #2 verbatim ('pass on a real Docker target')"
    why_human: "ROADMAP success criterion #2 explicitly requires 'pass on a real Docker target'. Verifier confirmed all 5 tests pass with in-memory fakes (the suite's default mode) but cannot autonomously start/connect to a Docker Proxmox target. The fakes assert the credential_id contract end-to-end (fake_get_proxmox_client raises if credential_id is missing), which is strong evidence — but the literal ROADMAP wording calls for Docker. Defer to milestone-close UAT per project convention."
---

# Phase 39.1: Thread credential_id through drift enum — Verification Report

**Phase Goal:** Close the Phase 38.1 R6 verification gap surfaced in `38.1-VERIFICATION.md` (2026-04-28). Thread the per-host binding UUID from `scan_drift`'s row loop through `_enumerate_proxmox_vms` to `_enum_one`. Extend AST regression guard so any future `get_proxmox_client(...)` call inside `drift_detection.py` that omits `credential_id=` blocks CI structurally.

**Verified:** 2026-04-28T20:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (5 from PLAN frontmatter must_haves; merged with 3 ROADMAP success criteria)

| #   | Truth                                                                                                                                                | Status     | Evidence |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | `drift_detection._enumerate_proxmox_vms._enum_one` calls `get_proxmox_client(...)` with `credential_id=` threaded from per-host binding (not None, not omitted) | ✓ VERIFIED | `drift_detection.py:430` reads `client = await get_proxmox_client(host=h, session=session, credential_id=binding)`; `binding = host_to_binding.get(h)` at line 428; `host_to_binding` parameter declared at line 385 |
| 2   | 5 round-trip integration tests in `tests/integration/test_credential_binding_round_trip.py` PASS at HEAD (currently 5/5 RED on credential-cleanup HEAD pre-fix) | ✓ VERIFIED | Verifier ran `uv run pytest tests/integration/test_credential_binding_round_trip.py -v --no-cov` → **5 passed in 0.77s**. (See human_verification — ROADMAP wording also calls for "real Docker target") |
| 3   | AST regression guard fails CI if a future Proxmox-touching helper inside `drift_detection.py` omits `credential_id=` on a `get_proxmox_client(...)` call (Phase 38.1 D-16 anticipated extension) | ✓ VERIFIED | `tests/test_ast_regression.py:853` declares `TestPhase39_1NoSkipInDriftEnum`; AST walk at line 887-890 matches `ast.Call` where `func.id == "get_proxmox_client"`; line 901-903 collects violations missing `credential_id` keyword. 2 tests pass under `uv run pytest tests/test_ast_regression.py::TestPhase39_1NoSkipInDriftEnum --no-cov` |
| 4   | Existing 7-key probed_ok record shape contract preserved unchanged (`test_per_row_record_shape_preserved_for_probed_ok` still passes — Option B preserves locked contract) | ✓ VERIFIED | `uv run pytest tests/test_drift_detection.py -k "test_per_row_record_shape_preserved_for_probed_ok" --no-cov` → **1 passed in 0.30s**. Option B (side-map) was selected explicitly to avoid touching the record shape at line 941 |
| 5   | Phase 38.1 D-15 / D-17 AST guards (`TestPhase381CredBinding`) still pass — new guard is additive, not destructive                                    | ✓ VERIFIED | `uv run pytest tests/test_ast_regression.py::TestPhase381CredBinding tests/test_ast_regression.py::TestPhase39DriftCases --no-cov` → **6 passed**. D-12 carve-out comment at lines 957-963 cleanly updated to acknowledge new guard scope |

**Score:** 5/5 truths verified.

### ROADMAP Success Criteria Coverage

| # | ROADMAP SC | Mapped Truth | Status |
|---|-----------|-------------|--------|
| 1 | `drift_detection.py:419` (`_enum_one`) threads `credential_id` from sitemap row through to `get_proxmox_client(...)` | T1 | VERIFIED |
| 2 | 5 round-trip integration tests pass on a **real Docker target** | T2 | VERIFIED via in-memory fakes; **Docker pass deferred to human verification** (see human_verification) |
| 3 | Phase 38.1 AST regression guard extended to cover `_enumerate_proxmox_vms` (D-16 anticipation) | T3 | VERIFIED |

### Required Artifacts

| Artifact                                       | Expected                                                                                                                | Status     | Details |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ---------- | ------- |
| `src/homelab_mcp/drift_detection.py`           | `_enumerate_proxmox_vms` accepts hostname→binding side-map; `_enum_one` threads `credential_id`; contains "credential_id=binding" | ✓ VERIFIED | Substantive (no stub); file is wired (imported across module); `host_to_binding` appears 5x; `credential_id=binding` exact match at line 430; `Mapping` imported at line 30 |
| `tests/test_ast_regression.py`                 | AST guard covering every `get_proxmox_client(...)` call inside `drift_detection.py`; contains "credential_id"           | ✓ VERIFIED | Substantive: 60-line `TestPhase39_1NoSkipInDriftEnum` class with 2 tests; `func.id == "get_proxmox_client"` at lines 890, 932; D-12 comment updated at lines 957-963 |

### Key Link Verification

| From                                                            | To                                                | Via                                                                                                              | Status   | Details |
| --------------------------------------------------------------- | ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | -------- | ------- |
| `drift_detection.py:scan_drift`                                 | `drift_detection.py:_enumerate_proxmox_vms`       | hostname→binding side-map built from same `row.get('proxmox_credential_id')` source-of-truth at line 718         | ✓ WIRED  | Side-map dict comprehension at lines 1012-1014; passed as 3rd arg at line 1015 (`_enumerate_proxmox_vms(probed_ok + changed, session, host_to_binding)`); pattern `_enumerate_proxmox_vms\(probed_ok \+ changed,.*session.*\)` matches |
| `drift_detection.py:_enum_one`                                  | `proxmox_api.py:get_proxmox_client`               | `credential_id=` kwarg threaded from side-map                                                                   | ✓ WIRED  | Line 430: `get_proxmox_client(host=h, session=session, credential_id=binding)`; pattern `get_proxmox_client\([^)]*credential_id=` matches at line 430 |
| `tests/test_ast_regression.py`                                  | `src/homelab_mcp/drift_detection.py`              | AST walk of every `ast.Call` where `func.id == 'get_proxmox_client'`; assert `'credential_id'` in keyword names | ✓ WIRED  | `Path(__file__).parent.parent / "src" / "homelab_mcp" / "drift_detection.py"` at line 883/925; `ast.parse(source)` + `ast.walk(tree)` at line 885/927 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `drift_detection.py:_enum_one` | `binding` (credential_id) | `host_to_binding.get(h)`, with side-map built from `row.get("proxmox_credential_id")` in `scan_drift` (rows fetched from sitemap DB query at upstream caller) | ✓ FLOWING | Source-of-truth chain: SQLite/Postgres `devices.proxmox_credential_id` → `rows` (sitemap query) → side-map dict comp at line 1012-1014 → `_enumerate_proxmox_vms` parameter → `_enum_one` lookup → `get_proxmox_client(credential_id=...)`. None values are valid (cluster-served rows fall through to Tier-1/Tier-2 walk per documented semantics) |

Data-flow is intact end-to-end; round-trip tests prove the binding UUID actually arrives at the resolver via the fake assertion `assert credential_id == expected_credential_id`.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Round-trip integration suite GREEN (in-memory fakes; assertion-as-contract) | `uv run pytest tests/integration/test_credential_binding_round_trip.py -v --no-cov` | 5 passed in 0.77s | ✓ PASS |
| Phase 39.1 + Phase 38.1 + Phase 39 AST guards all green | `uv run pytest tests/test_ast_regression.py::TestPhase39_1NoSkipInDriftEnum tests/test_ast_regression.py::TestPhase381CredBinding tests/test_ast_regression.py::TestPhase39DriftCases -v --no-cov` | 6 passed in 0.05s | ✓ PASS |
| Locked 7-key probed_ok contract preserved | `uv run pytest tests/test_drift_detection.py -k "test_per_row_record_shape_preserved_for_probed_ok" --no-cov` | 1 passed in 0.30s | ✓ PASS |
| Phase 38.1 footprint clean (closes verifier's "5 failed" gap) | `uv run pytest tests/ -k "phase38_1 or phase381 or Phase381 or Phase38_1" --no-cov` | 54 passed, 1 skipped (was 49 passed / 5 failed pre-39.1) | ✓ PASS |
| Quality gates on modified files | `uv run ruff check src/homelab_mcp/drift_detection.py tests/test_ast_regression.py && uv run mypy src/homelab_mcp/drift_detection.py` | All checks passed; Success: no issues | ✓ PASS |
| Round-trip suite against **real Docker target** (ROADMAP SC #2 literal wording) | (cannot autonomously run — requires Docker container) | N/A | ? SKIP — routed to human verification |

### Requirements Coverage

| Requirement       | Source Plan | Description                                                                            | Status      | Evidence |
| ----------------- | ----------- | -------------------------------------------------------------------------------------- | ----------- | -------- |
| `R6-regression`   | 39.1-01     | Closes the Phase 38.1 R6 verification gap surfaced in `38.1-VERIFICATION.md` (2026-04-28). Bisected to Phase 39 commit `e05df24` (DRFT-17 cluster/resources enumeration). | ✓ SATISFIED | All 5 round-trip tests GREEN at HEAD (was 5/5 RED). New AST guard catches the regression class structurally. Phase 38.1 footprint goes from 49p/5f → 54p/0f. |

The `R6-regression` tag is a meta-tag introduced by this phase as the canonical handle for the regression — not a tracked entry in `.planning/REQUIREMENTS.md`'s active requirement IDs (R1-R10 in REQUIREMENTS.md belong to Phase 38.1's SPEC; this phase is a regression-closure of R6 specifically). The ROADMAP entry for Phase 39.1 references it under "Requirements: Closes the Phase 38.1 R6 verification gap..." which matches the PLAN frontmatter `requirements: [R6-regression]`. **No orphaned requirements** — all phase-level requirements accounted for; the parent R6 requirement (Phase 38.1 SPEC) was VERIFIED at HEAD post-fix as part of the Phase 38.1 footprint sanity check (54p/0f).

### Anti-Patterns Found

| File                                       | Line | Pattern                                                                                | Severity | Impact |
| ------------------------------------------ | ---- | -------------------------------------------------------------------------------------- | -------- | ------ |
| `src/homelab_mcp/drift_detection.py`       | 1013 | `(row.get("hostname") or "")` redundant when filter clause already gates on truthy hostname | ℹ️ Info  | Identified in 39.1-REVIEW.md as IN-02; harmless dead defense; matches `sitemap_hostnames` style on line 1004 for diff readability — not load-bearing |
| `src/homelab_mcp/drift_detection.py`       | 1012-1014 | Last-write-wins on duplicate hostnames in side-map dict comp (REVIEW WR-01)            | ⚠️ Warning | REVIEW noted narrow case where cluster-dedupe survivor's binding may differ from row that won the side-map. Tests pass, but undocumented behavior. Documented in 39.1-REVIEW.md; addressing this is OUT-OF-SCOPE for goal-backward verification (the goal — round-trip GREEN + AST guard — is achieved) |
| `tests/test_ast_regression.py`             | 917-940 | `test_phase39_1_guard_catches_added_helper` is a tautology of the floor — does not test what its name promises (REVIEW WR-02) | ℹ️ Info  | Cosmetic; test still asserts a useful invariant (≥2 call sites discovered). Out-of-scope for goal verification |
| `tests/test_ast_regression.py`             | 887-891 | Guard does not cover aliased imports (REVIEW WR-03)                                    | ℹ️ Info  | `from .proxmox_api import get_proxmox_client as gpc` would silently bypass; not currently aliased in `drift_detection.py:42`; documented theoretical gap; out-of-scope for goal verification |

None of the anti-patterns rise to BLOCKER severity. WR-01 is a WARNING surfaced by the REVIEW agent — verifier confirms goal is achieved despite the warning (round-trip tests would surface a real binding-to-host mismatch via the `fake_get_proxmox_client` assertion). The 39.1-REVIEW.md flagged 3 warnings + 2 info findings, all triaged as non-blocking by goal-backward analysis.

### Human Verification Required

#### 1. Run 5 round-trip integration tests against a REAL Docker Proxmox target

**Test:** Start a Docker Proxmox container (per project's integration-test setup), then run `uv run pytest tests/integration/test_credential_binding_round_trip.py -v -m integration` (or whatever flag wires the suite to live targets vs in-memory fakes).
**Expected:** All 5 tests pass against the live Docker target — closes ROADMAP success criterion #2 verbatim ("pass on a real Docker target").
**Why human:** ROADMAP success criterion #2 explicitly says "pass on a real Docker target". The 5 tests at HEAD pass with in-memory fakes (the suite's default mode), and those fakes assert the `credential_id` threading contract end-to-end — which is strong evidence the fix is correct. But the literal ROADMAP wording calls for Docker. Verifier cannot autonomously start a Docker container in this environment; deferring to milestone-close UAT (per project convention captured in MEMORY.md "Manual UAT deferred to milestone close").

### Gaps Summary

The phase goal is **achieved** in the codebase: all 5 must-have truths VERIFIED with substantive evidence. The mechanical fix (`credential_id=binding` threaded into `_enum_one`) is in place at line 430; the side-map source-of-truth chain is intact (sitemap DB → `rows` → dict-comp at 1012-1014 → `_enum_one.binding`); the AST guard structurally catches the regression class via `ast.walk` + `func.id == "get_proxmox_client"` matching; the locked 7-key probed_ok contract is preserved (Option B held the line); Phase 38.1 D-15/D-17 + Phase 39 D-11/D-12 guards remain GREEN.

**One human-verification item open:** ROADMAP success criterion #2 literally calls for "pass on a real Docker target." The in-memory fake suite passes 5/5 (and the fake's assertion is the binding contract itself, so passing fakes prove the contract holds end-to-end), but the verifier cannot autonomously confirm Docker-target pass. Routed to milestone-close UAT per project convention.

**No blockers, no failed truths, no missing artifacts.** Status: `human_needed` purely on the Docker-vs-fake distinction.

---

*Verified: 2026-04-28T20:30:00Z*
*Verifier: Claude (gsd-verifier)*
*Branch: credential-cleanup; commits 798d393 (fix) and 11c0877 (test) confirmed in git log*
