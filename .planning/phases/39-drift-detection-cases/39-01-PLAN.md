---
phase: 39-drift-detection-cases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/homelab_mcp/drift_detection.py
  - src/homelab_mcp/ssh_tools.py
  - tests/conftest.py
  - tests/test_drift_detection.py
autonomous: true
requirements: [DRFT-17, DRFT-18, DRFT-19]
requirements_addressed: [DRFT-17-helpers, DRFT-18-helpers, DRFT-19-helpers]
tags: [drift, fingerprint, ssh-probe, helpers]

must_haves:
  truths:
    - "Universal-core probe block extracted into _probe_universal_core helper used by both ssh_discover_system and drift"
    - "_diff_fingerprints walks two dicts emitting per-leaf diffs with dotted-path keys; only leaves present in BOTH sides are diffed"
    - "_classify_unreachable returns ('missing', message) when last_seen older than threshold, ('unreachable', sanitize_error(exc)) otherwise"
    - "_enumerate_unknown_vms builds per-VM unknown[] rows by case-insensitive vm.name vs sitemap.hostname"
    - "_missing_threshold_days reads HOMELAB_DRIFT_MISSING_THRESHOLD_DAYS, clamps to positive int, default 7"
    - "tests/conftest.py provides drift fixtures (freeze_now, mock probe responses, sitemap row factories)"
    - "Wave 0 RED tests for all helpers fail until implementation lands"
  artifacts:
    - path: "src/homelab_mcp/ssh_tools.py"
      provides: "_probe_universal_core async helper (extracted from ssh_discover_system lines 614-691)"
      contains: "async def _probe_universal_core"
    - path: "src/homelab_mcp/drift_detection.py"
      provides: "_diff_fingerprints, _classify_unreachable, _enumerate_unknown_vms, _missing_threshold_days, _parse_last_seen helpers"
      contains: "def _diff_fingerprints"
    - path: "tests/conftest.py"
      provides: "Shared drift fixtures"
      contains: "def freeze_now"
    - path: "tests/test_drift_detection.py"
      provides: "TestPhase39Helpers class with unit tests"
      contains: "class TestPhase39Helpers"
  key_links:
    - from: "src/homelab_mcp/ssh_tools.py::ssh_discover_system"
      to: "_probe_universal_core"
      via: "function call replacing inline probe block"
      pattern: "await _probe_universal_core\\("
    - from: "tests/test_drift_detection.py::TestPhase39Helpers"
      to: "drift_detection helpers"
      via: "import + direct call"
      pattern: "from homelab_mcp.drift_detection import \\(\\s*_diff_fingerprints"
---

<objective>
Extract the universal-core SSH probe block from `ssh_discover_system` into a reusable `_probe_universal_core(conn, timed_out_commands)` helper, and add four pure-function helpers to `drift_detection.py` that Plans 02 and 03 will compose into `scan_drift`. Write unit tests for every helper. Create the new `tests/conftest.py` with the shared fixtures the rest of the phase depends on.

Purpose: Lock the deterministic, testable substrate (helpers + fixtures + RED unit tests) before the row-loop edits in Plans 02/03. Keeps Plans 02/03 focused on wiring rather than re-deriving helper shapes. Helpers stay loop-free per D-11(b) so the existing AST guard remains green without an allowlist extension.

Output: One extracted helper in `ssh_tools.py`, five new helpers in `drift_detection.py`, one new fixture file `tests/conftest.py`, one new test class `TestPhase39Helpers` (≥ 5 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/39-drift-detection-cases/39-CONTEXT.md
@.planning/phases/39-drift-detection-cases/39-RESEARCH.md
@.planning/phases/39-drift-detection-cases/39-PATTERNS.md
@.planning/phases/39-drift-detection-cases/39-VALIDATION.md

<interfaces>
<!-- Existing contracts the executor must respect. -->

From src/homelab_mcp/ssh_tools.py (lines 614-691, to extract):
The four-probe block currently lives inline inside `ssh_discover_system`. Each probe wraps `conn.run(...)` with `_run_with_timeout(conn, "<cmd>", cmd_name="<key>", timed_out=timed_out_commands)`. The block populates a local `fingerprint_info: dict[str, Any]` from successful probe stdout, then assigns `system_info["fingerprint"] = fingerprint_info` if non-empty.

From src/homelab_mcp/ssh_tools.py:863-889 (used by helper):
```python
async def _run_with_timeout(
    conn: asyncssh.SSHClientConnection,
    command: str,
    *,
    cmd_name: str,
    timed_out: list[str],
    timeout: float = 10.0,
) -> "asyncssh.SSHCompletedProcess | None":
```

From src/homelab_mcp/drift_detection.py (existing helpers — pattern template):
```python
def _classify_credential_failure(exc: CredentialNotFoundError, binding: str | None) -> str: ...
def _reason_message(reason: str, hostname: str, credential_type: str) -> str: ...
```

From src/homelab_mcp/log_filter.py:
```python
def sanitize_error(exc: BaseException) -> str: ...
```

From tests/test_drift_detection.py:1-9 (existing imports):
```python
from unittest.mock import AsyncMock, MagicMock, patch
import aiohttp
import pytest
from homelab_mcp.drift_detection import scan_drift
from homelab_mcp.proxmox_api import CredentialNotFoundError
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — write conftest.py fixtures + RED unit tests for all helpers</name>
  <files>tests/conftest.py, tests/test_drift_detection.py</files>
  <read_first>
    - tests/test_drift_detection.py (lines 1-110: existing imports, TestScanDrift4Bucket pattern; lines 821-870: TestScanDriftNotEligible pattern)
    - .planning/phases/39-drift-detection-cases/39-PATTERNS.md (lines 580-720: conftest fixture shapes)
    - .planning/phases/39-drift-detection-cases/39-RESEARCH.md (lines 454-482: env-var clamp + last_seen parse code examples; lines 696-708: fixtures table)
    - .planning/phases/39-drift-detection-cases/39-VALIDATION.md (lines 39-56: per-task verification map)
  </read_first>
  <behavior>
    Fixtures (in tests/conftest.py — NEW file):
    - `freeze_now`: monkeypatches `homelab_mcp.drift_detection.datetime` to a `_FakeDatetime` whose `.now(tz)` returns `datetime(2026, 4, 27, 12, 0, 0, tzinfo=UTC)` when tz is set. Returns the frozen datetime.
    - `mock_universal_core_probe_response`: returns `{"kernel_name": "Linux", "kernel_version": "6.5.13-1-pve", "os_name": "Proxmox VE", "os_version": "8.2.4", "package_fingerprint": "sha256:abc123"}`.
    - `mock_universal_core_probe_drifted`: same shape, but `kernel_version: "6.8.4-2-pve"` and `package_fingerprint: "sha256:def456"`.
    - `mock_cluster_resources_response`: returns 4-element list — qemu vmid=100 name="ubuntu-prod" node="pve1", qemu vmid=110 name="ubuntu-test" node="pve1", lxc vmid=200 name="pi-hole" node="pve1", node-type record (filtered out by helper).
    - `sitemap_row_old_last_seen` (depends on `freeze_now`): hostname="pi-lab", connection_ip="10.0.0.12", status="success", ssh_credential_id="11111111-1111-1111-1111-111111111111", proxmox_credential_id=None, last_seen=(freeze_now.replace(tzinfo=None) - timedelta(days=12)).isoformat(), fingerprint={}.
    - `sitemap_row_recent_last_seen` (depends on `freeze_now`): same row but last_seen 1 day before frozen now.
    - `sitemap_row_with_stored_fingerprint` (depends on `mock_universal_core_probe_response`): hostname="pve1", with full fingerprint blob INCLUDING `capabilities: {"vulkan": {"available": True}}`.
    - `mock_resolve_ssh_credentials`: returns MagicMock with hostname="10.0.0.12", username="mcp_admin", port=22, password=None, key_path="/tmp/fake-key".
    - `mock_ssh_connect`: returns async-context-manager MagicMock; `__aenter__` returns conn whose `.run` is `AsyncMock(return_value=MagicMock(exit_status=0, stdout="Linux"))`.

    Unit tests (TestPhase39Helpers class in tests/test_drift_detection.py):
    - test_diff_fingerprints_per_leaf_present_in_both: stored has `capabilities.vulkan.available=True`, current has empty `capabilities={}`. Asserts that key NOT in result. (D-09a)
    - test_diff_fingerprints_dotted_path: stored.capabilities.vulkan.available=True, current.capabilities.vulkan.available=False. Asserts result == `{"capabilities.vulkan.available": {"stored": True, "current": False}}`. (D-08)
    - test_diff_fingerprints_top_level_kernel: stored.kernel_version="6.5.13", current.kernel_version="6.8.4". Asserts result == `{"kernel_version": {"stored": "6.5.13", "current": "6.8.4"}}`.
    - test_diff_fingerprints_empty_when_equal: both dicts identical. Asserts result == `{}`.
    - test_classify_unreachable_old_last_seen_returns_missing(freeze_now): row with last_seen 12 days before frozen now, threshold=7, exc=TimeoutError(). Asserts return == ("missing", message_containing("decommission_device")).
    - test_classify_unreachable_recent_last_seen_returns_unreachable(freeze_now): row with last_seen 1 day before, threshold=7, exc=aiohttp.ClientError("connection refused"). Asserts return[0]=="unreachable".
    - test_classify_unreachable_timezone_normalization(freeze_now): row with naive last_seen string (e.g., "2026-04-15T10:00:00", no tzinfo). Must NOT raise TypeError. Returns "missing" when >threshold.
    - test_missing_threshold_days_default: env unset → returns 7. Patches os.environ.
    - test_missing_threshold_days_env_override: env="3" → returns 3.
    - test_missing_threshold_days_invalid_falls_back: env="abc" or "-1" → returns 7.
    - test_parse_last_seen_naive_string: input "2026-04-15T10:00:00" → returns aware datetime with UTC tzinfo.
    - test_parse_last_seen_none_or_malformed: input None/""/garbage → returns None.
    - test_enumerate_unknown_case_insensitive: cluster_vm_map={"pve1": [{type:"qemu", name:"Plex-Server", vmid:100, node:"pve1", status:"running"}]}, sitemap_hostnames={"plex-server"} (lower). Asserts unknown == [] (matched).
    - test_enumerate_unknown_unmatched_vm_in_result: cluster_vm_map has VM name "ubuntu-test" not in sitemap_hostnames. Asserts result has 1 row with vmid, vm_type="qemu", message containing "discover_and_map".
    - test_probe_universal_core_extraction_parity: uses asyncio + mock conn whose `conn.run` returns canned stdouts (one for each cmd_name). Asserts returned dict matches Phase 38 schema (kernel_name, kernel_version, os_name, os_version, package_fingerprint).

    All tests MUST FAIL initially (helpers don't exist yet) — RED stage.
  </behavior>
  <action>
    1. Create `tests/conftest.py` with the 9 fixtures listed above. Imports: `from datetime import UTC, datetime, timedelta`, `from unittest.mock import AsyncMock, MagicMock`, `import pytest`. The `freeze_now` fixture builds a `_FakeDatetime` subclass of `datetime` whose `now(cls, tz=None)` returns `frozen if tz is not None else frozen.replace(tzinfo=None)`; uses `monkeypatch.setattr("homelab_mcp.drift_detection.datetime", _FakeDatetime)`.
    2. Append to `tests/test_drift_detection.py` after the existing imports (line ~9): `from homelab_mcp.drift_detection import (_classify_unreachable, _diff_fingerprints, _enumerate_unknown_vms, _missing_threshold_days, _parse_last_seen)` and `from homelab_mcp.ssh_tools import _probe_universal_core`.
    3. Append a new test class `TestPhase39Helpers` at end of file with the 15 tests above. Use `@pytest.mark.asyncio` only on `test_probe_universal_core_extraction_parity`. Other tests are sync.
    4. For `test_classify_unreachable_*` use `freeze_now` fixture and pass `now=freeze_now` argument. The helper signature is `_classify_unreachable(row, exc, threshold_days, now) -> tuple[Literal["unreachable", "missing"], str]`.
    5. Run `uv run pytest tests/test_drift_detection.py::TestPhase39Helpers -x` and confirm collection passes but tests FAIL with `ImportError: cannot import name '_diff_fingerprints'` etc. — RED.
    6. Commit: `test(39-01): wave 0 RED tests for drift helpers and conftest fixtures`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_drift_detection.py::TestPhase39Helpers -x 2>&1 | grep -E "(ImportError|cannot import name)" || (echo "EXPECTED RED: ImportError on helper imports" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/conftest.py` exists and contains exactly 9 `@pytest.fixture` definitions: `freeze_now`, `mock_universal_core_probe_response`, `mock_universal_core_probe_drifted`, `mock_cluster_resources_response`, `sitemap_row_old_last_seen`, `sitemap_row_recent_last_seen`, `sitemap_row_with_stored_fingerprint`, `mock_resolve_ssh_credentials`, `mock_ssh_connect`. Verify: `grep -c "^@pytest.fixture" tests/conftest.py` returns 9.
    - File `tests/test_drift_detection.py` contains class `TestPhase39Helpers` with ≥ 15 test methods. Verify: `grep -c "    def test_" tests/test_drift_detection.py | head` shows growth ≥ 15.
    - The new test class imports `_diff_fingerprints, _classify_unreachable, _enumerate_unknown_vms, _missing_threshold_days, _parse_last_seen, _probe_universal_core`. Verify: `grep -E "from homelab_mcp.drift_detection import" tests/test_drift_detection.py | grep -E "_diff_fingerprints"`.
    - Running `uv run pytest tests/test_drift_detection.py::TestPhase39Helpers --collect-only` succeeds (no syntax errors).
    - Running `uv run pytest tests/test_drift_detection.py::TestPhase39Helpers -x` fails with ImportError (RED stage confirmed).
  </acceptance_criteria>
  <done>
    Wave 0 fixtures + RED tests committed. `tests/conftest.py` created. `TestPhase39Helpers` collects but fails on missing imports. No implementation exists yet — Tasks 2 and 3 land it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract _probe_universal_core into ssh_tools.py and refactor ssh_discover_system to use it</name>
  <files>src/homelab_mcp/ssh_tools.py</files>
  <read_first>
    - src/homelab_mcp/ssh_tools.py (lines 614-691: the four-probe block to extract; lines 863-889: _run_with_timeout signature)
    - tests/test_ast_regression.py (lines 447-502: test_ssh_discover_system_wraps_every_conn_run_phase35 — verify guard scope is `n.name == "ssh_discover_system"` ONLY so the new sibling helper does NOT need to be added)
    - .planning/phases/39-drift-detection-cases/39-PATTERNS.md (lines 244-347: extraction shape)
    - .planning/phases/38-sitemap-fingerprint-schema/38-CONTEXT.md (D-04 universal-core probe contract: 4 commands)
  </read_first>
  <behavior>
    - `_probe_universal_core(conn, timed_out_commands)` returns a `dict[str, Any]` with up to 5 keys: `kernel_name`, `kernel_version`, `os_name`, `os_version`, `package_fingerprint`. Empty dict if every probe failed.
    - Every `conn.run(...)` call inside the helper is wrapped in `_run_with_timeout(conn, "<cmd>", cmd_name="<name>", timed_out=timed_out_commands)`.
    - On non-zero exit, the cmd_name is appended to `timed_out_commands` (idempotent — only if not already present), so callers can flag `partial: True`.
    - `ssh_discover_system` calls `_probe_universal_core(conn, timed_out_commands)` and assigns `system_info["fingerprint"] = result` only if `result` is non-empty (preserves the existing semantics — Phase 38 D-04).
    - The Phase 35 AST guard (`tests/test_ast_regression.py::test_ssh_discover_system_wraps_every_conn_run_phase35`) MUST stay green. Since the helper is a sibling function (not part of `ssh_discover_system`), the guard's targeted scope at line 466 (`n.name == "ssh_discover_system"`) is unaffected.
    - Existing test for `ssh_discover_system` (any test that mocks the four probes) MUST continue to pass — fixture mock paths (e.g., `conn.run`) are unchanged because the helper still calls `_run_with_timeout` → `conn.run`.
    - `TestPhase39Helpers::test_probe_universal_core_extraction_parity` from Task 1 now PASSES (helper importable; returns expected dict against canned mock conn).
  </behavior>
  <action>
    1. In `src/homelab_mcp/ssh_tools.py`, immediately ABOVE the existing `ssh_discover_system` function definition, add `async def _probe_universal_core(conn: asyncssh.SSHClientConnection, timed_out_commands: list[str]) -> dict[str, Any]:`. Body: lift lines 614-691 verbatim (the four probe blocks for `uname -s`, `uname -r`, `cat /etc/os-release 2>/dev/null`, `LC_ALL=C dpkg -l 2>/dev/null | sort | sha256sum`). Replace the trailing `if fingerprint_info: system_info["fingerprint"] = fingerprint_info` with `return fingerprint_info`. Use a local `fingerprint_info: dict[str, Any] = {}` initialization at the top of the helper. Docstring: `"""Phase 38 D-04 universal-core fingerprint probes. Reused by Phase 39 drift. All four probes wrapped in _run_with_timeout(10s) per Phase 35 D-05; non-zero exits enroll cmd_name into timed_out_commands so callers can flag partial: True."""`.
    2. In `ssh_discover_system`, replace the deleted lines 614-691 with: `_fp = await _probe_universal_core(conn, timed_out_commands)\n    if _fp:\n        system_info["fingerprint"] = _fp`.
    3. Run `uv run pytest tests/test_drift_detection.py::TestPhase39Helpers::test_probe_universal_core_extraction_parity tests/test_ast_regression.py::TestSshDiscoverSystem -x` (or whatever the Phase 35 AST class name is — verify via grep). All must be green.
    4. Run `uv run pytest tests/ -m "not integration" -k "ssh_discover" -x` to confirm existing ssh_discover_system tests still pass (extraction parity).
    5. Run `uv run mypy src/homelab_mcp/ssh_tools.py` to confirm strict type annotations satisfied.
    6. Commit: `refactor(39-01): extract _probe_universal_core helper from ssh_discover_system`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_drift_detection.py::TestPhase39Helpers::test_probe_universal_core_extraction_parity tests/test_ast_regression.py -x -k "ssh_discover_system or phase35" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^async def _probe_universal_core" src/homelab_mcp/ssh_tools.py` returns 1.
    - `_probe_universal_core` body contains 4 calls to `_run_with_timeout`. Verify: `grep -A 80 "async def _probe_universal_core" src/homelab_mcp/ssh_tools.py | grep -c "_run_with_timeout("` returns 4.
    - `ssh_discover_system` no longer contains inline `uname -s` / `uname -r` calls. Verify: `grep -A 200 "async def ssh_discover_system" src/homelab_mcp/ssh_tools.py | head -200 | grep -E "conn.run.*uname|_run_with_timeout.*uname"` returns 0.
    - `ssh_discover_system` calls the new helper. Verify: `grep -A 200 "async def ssh_discover_system" src/homelab_mcp/ssh_tools.py | grep -c "_probe_universal_core(conn"` returns ≥ 1.
    - Phase 35 AST guard test passes: `uv run pytest tests/test_ast_regression.py -x -k "ssh_discover_system_wraps_every_conn_run"` exits 0.
    - `test_probe_universal_core_extraction_parity` passes: `uv run pytest tests/test_drift_detection.py::TestPhase39Helpers::test_probe_universal_core_extraction_parity -x` exits 0.
    - mypy clean: `uv run mypy src/homelab_mcp/ssh_tools.py` exits 0.
  </acceptance_criteria>
  <done>
    `_probe_universal_core` lives in `ssh_tools.py` with full type annotations; `ssh_discover_system` calls it; Phase 35 AST guard remains green; extraction-parity test passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add five pure-function helpers to drift_detection.py and turn TestPhase39Helpers GREEN</name>
  <files>src/homelab_mcp/drift_detection.py</files>
  <read_first>
    - src/homelab_mcp/drift_detection.py (lines 27-40: existing imports; lines 58-121: existing helper templates _classify_credential_failure + _reason_message; lines 1-30: module docstring)
    - .planning/phases/39-drift-detection-cases/39-RESEARCH.md (lines 282-336: helper code examples; lines 454-482: env-var clamp + parse pattern)
    - .planning/phases/39-drift-detection-cases/39-PATTERNS.md (lines 199-240: diff helper recursive walk + env-var clamp + last-seen parse patterns)
    - tests/test_drift_detection.py::TestPhase39Helpers (the RED tests from Task 1 — implementation must turn each GREEN)
  </read_first>
  <behavior>
    - `_DEFAULT_THRESHOLD_DAYS: int = 7` module-level constant.
    - `_missing_threshold_days() -> int`: reads `os.getenv("HOMELAB_DRIFT_MISSING_THRESHOLD_DAYS", "7")`, `int()` cast inside `try/except ValueError`, returns `_DEFAULT_THRESHOLD_DAYS` on failure or non-positive value.
    - `_parse_last_seen(raw: str | None) -> datetime | None`: handles None/empty/non-parseable via `try/except (ValueError, TypeError)` → None. Naive datetimes get `replace(tzinfo=UTC)`.
    - `_classify_unreachable(row: dict, exc: BaseException, threshold_days: int, now: datetime) -> tuple[Literal["unreachable", "missing"], str]`: parses `row.get("last_seen")`. If parsed is not None AND `(now - parsed).days > threshold_days`: returns `("missing", f"Host last seen {parsed.isoformat()} (>{threshold_days}d ago). If decommissioned, run `decommission_device {row.get('hostname', '')}` or `purge_failed_discoveries` to clean up.")`. Otherwise returns `("unreachable", sanitize_error(exc))`. Pure function — NO LOOPS.
    - `_diff_fingerprints(stored: dict, current: dict) -> dict[str, dict[str, Any]]`: recursive walk via inner `_walk(s, c, path)` function. For dicts: recurse on `s.keys() & c.keys()` only (D-09a leaf-level "present in both"). For non-dict leaves: append `diffs[".".join(path)] = {"stored": s, "current": c}` if `s != c`. Returns dict-of-dicts. The inner `_walk` contains a `for` over key intersection but NO `continue` — pure recursion.
    - `_enumerate_unknown_vms(cluster_vm_map: dict[str, list[dict]], sitemap_hostnames: set[str], scan_timestamp: str) -> list[dict]`: caller pre-computes `sitemap_hostnames` (already lower-cased). Inner `_make_row(vm, hypervisor)` returns `dict | None` — None when name empty or `name.lower() in sitemap_hostnames`. Outer iteration: `for hypervisor, vms in cluster_vm_map.items(): unknown.extend(filter(None, (_make_row(vm, hypervisor) for vm in vms)))`. NO `continue` statements anywhere. Per-VM record shape per D-07: `hypervisor_hostname`, `node`, `vmid`, `vm_type` (`qemu`|`lxc`), `vm_name`, `vm_status`, `scan_timestamp`, `message`. Message: `f"VM '{name}' (vmid={vm.get('vmid')}) on node '{vm.get('node')}' not in sitemap; run `discover_and_map <ip-or-hostname>` to adopt."`.
    - All helpers have full type annotations passing strict mypy.
    - All `TestPhase39Helpers` tests turn GREEN.
  </behavior>
  <action>
    1. In `src/homelab_mcp/drift_detection.py`, add to the import block at the top: `import os` (if not already present), `from datetime import UTC, datetime, timedelta` (extend existing datetime import — confirm present), `from typing import Any, Literal`. Add module-level constant: `_DEFAULT_THRESHOLD_DAYS: int = 7`.
    2. Insert the five helpers AFTER `_reason_message` (around line 121) and BEFORE `async def scan_drift`:
       a. `def _missing_threshold_days() -> int:` per behavior spec.
       b. `def _parse_last_seen(raw: str | None) -> datetime | None:` per behavior spec.
       c. `def _classify_unreachable(row: dict[str, Any], exc: BaseException, threshold_days: int, now: datetime) -> tuple[Literal["unreachable", "missing"], str]:` per behavior spec. The "missing" branch message uses backtick-wrapped commands (per Phase 37 D-08 conventions).
       d. `def _diff_fingerprints(stored: dict[str, Any], current: dict[str, Any]) -> dict[str, dict[str, Any]]:` per behavior spec. Inner `_walk` recursion only — no `continue` statement anywhere in the helper body.
       e. `def _enumerate_unknown_vms(cluster_vm_map: dict[str, list[dict[str, Any]]], sitemap_hostnames: set[str], scan_timestamp: str) -> list[dict[str, Any]]:` per behavior spec. Use `filter(None, generator)` over `_make_row(vm, hypervisor)`. NO `continue` in body. The outer `for hypervisor, vms in cluster_vm_map.items():` is the only loop — it calls `unknown.extend(...)` and that's it.
    3. Run `uv run pytest tests/test_drift_detection.py::TestPhase39Helpers -x -v`. ALL 15 tests pass.
    4. Run `uv run mypy src/homelab_mcp/drift_detection.py`. Strict typing clean.
    5. Run `uv run ruff check src/homelab_mcp/drift_detection.py tests/test_drift_detection.py tests/conftest.py`. Lint clean.
    6. Run a quick AST sanity check via the `python -c` snippet in <verify> to confirm zero `ast.Continue` nodes inside the four NEW non-async drift helpers.
    7. Commit: `feat(39-01): add diff/enumerate/classify/threshold helpers for drift cases`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_drift_detection.py::TestPhase39Helpers -x -v 2>&1 | tail -25 && uv run python -c "import ast; src = open('src/homelab_mcp/drift_detection.py', encoding='utf-8').read(); tree = ast.parse(src); [print(n.name, '=', sum(1 for x in ast.walk(n) if isinstance(x, ast.Continue))) for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef)) and n.name in ('_diff_fingerprints','_enumerate_unknown_vms','_classify_unreachable','_missing_threshold_days','_parse_last_seen')]"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 helpers exist in `drift_detection.py`. Verify: `grep -E "^def (_missing_threshold_days|_parse_last_seen|_classify_unreachable|_diff_fingerprints|_enumerate_unknown_vms)" src/homelab_mcp/drift_detection.py | wc -l` returns 5.
    - Each new helper has zero `ast.Continue` nodes (D-11(b) preserved). Verify via the python AST inline check in <verify>: each line prints `<name> = 0`.
    - All 15 `TestPhase39Helpers` tests pass: `uv run pytest tests/test_drift_detection.py::TestPhase39Helpers -x` exits 0.
    - mypy strict clean: `uv run mypy src/homelab_mcp/drift_detection.py` exits 0.
    - ruff clean: `uv run ruff check src/homelab_mcp/drift_detection.py tests/test_drift_detection.py tests/conftest.py` exits 0.
    - The Phase 38.1 D-15 AST guard (`test_scan_drift_no_continue_in_row_loop_phase38_1`) remains GREEN — since this task does not edit `scan_drift`, the guard is unaffected. Verify: `uv run pytest tests/test_ast_regression.py::TestPhase381CredBinding::test_scan_drift_no_continue_in_row_loop_phase38_1 -x` exits 0.
    - `_classify_unreachable` message contains literal substrings `decommission_device` AND `purge_failed_discoveries`. Verify: `grep -A 20 "def _classify_unreachable" src/homelab_mcp/drift_detection.py | grep -E "decommission_device|purge_failed_discoveries" | wc -l` returns 2.
  </acceptance_criteria>
  <done>
    All 5 helpers landed; 15 unit tests GREEN; mypy/ruff clean; AST guards (Phase 35 + Phase 38.1) all green; helpers loop-free per D-11(b).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | `HOMELAB_DRIFT_MISSING_THRESHOLD_DAYS` is read from environment and used in arithmetic. Untrusted operator value crosses here. |
| DB → process | `row.get("last_seen")` may contain malformed strings (corrupted DB or manual edit). |
| SSH stdout → process | `_probe_universal_core` parses `/etc/os-release` and dpkg sha output. Hostile-host output crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | DoS (V14) | `_missing_threshold_days` | mitigate | `try/except ValueError` + non-positive clamp falls back to `_DEFAULT_THRESHOLD_DAYS`. Negative/zero/garbage env var cannot crash drift. |
| T-39-02 | DoS (V5) | `_parse_last_seen` | mitigate | `try/except (ValueError, TypeError)` returns `None`; caller defaults to `unreachable` (not `missing`) on parse failure. Malformed last_seen never crashes. |
| T-39-03 | Information Disclosure (V7) | `_classify_unreachable` message | mitigate | Error path uses `sanitize_error(exc)` from `log_filter.py`; never echoes raw exception args. The "missing" message echoes hostname, which is non-secret (already in sitemap). |
| T-39-04 | Tampering (V5) | `_diff_fingerprints` | accept | Recursion depth bounded by fingerprint shape (~3 levels: top → capabilities → leaf). No external recursion limit risk. Hostile fingerprint blob max-depth bounded by Python's default recursion limit (1000). |
| T-39-05 | DoS via slow probe (V7) | `_probe_universal_core` | mitigate | Each `conn.run` wrapped in `_run_with_timeout(10s)`. Worst-case per host: 4×10s = 40s. Outer per-host wait_for(45s) added in Plan 03. |
| T-39-06 | Information Disclosure (V7) | `_enumerate_unknown_vms` message | accept | Message echoes `vm.name` and `node` from Proxmox API. These are non-secret operator-visible labels. No credential or token interpolation. |
</threat_model>

<verification>
- All 15 `TestPhase39Helpers` unit tests GREEN.
- `_probe_universal_core` extraction parity test GREEN.
- Phase 35 AST guard (`ssh_discover_system_wraps_every_conn_run_phase35`) GREEN — unchanged scope.
- Phase 38.1 AST guard (`test_scan_drift_no_continue_in_row_loop_phase38_1`) GREEN — `scan_drift` unmodified this plan.
- Strict mypy clean on `ssh_tools.py` and `drift_detection.py`.
- Ruff lint clean on all touched files.
- AST inline check confirms zero `ast.Continue` nodes in every new helper.
</verification>

<success_criteria>
- 5 new pure-function helpers in `drift_detection.py` with full type annotations and comprehensive unit coverage.
- 1 extracted async helper `_probe_universal_core` shared between `ssh_discover_system` and (next plan) `scan_drift`.
- 1 new `tests/conftest.py` providing 9 shared fixtures.
- 1 new test class `TestPhase39Helpers` with ≥ 15 GREEN tests.
- All quality gates (mypy / ruff / pytest unit suite) pass.
- D-11(b) preserved: every new helper is loop-free w.r.t. bucket-list appends; existing AST guards remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/39-drift-detection-cases/39-01-SUMMARY.md` with:
- Helpers added (file + line ranges, signature, return type)
- Extraction summary (lines moved from `ssh_tools.py`)
- Test counts (15 unit tests, all green)
- Deviations from plan (if any)
- Commit hashes (3 expected: RED tests, extraction, helpers GREEN)
- Files modified list matching frontmatter
</output>
