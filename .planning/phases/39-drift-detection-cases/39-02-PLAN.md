---
phase: 39-drift-detection-cases
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - src/homelab_mcp/drift_detection.py
  - tests/test_drift_detection.py
autonomous: true
requirements: [DRFT-17]
requirements_addressed: [DRFT-17]
tags: [drift, unknown-vm, proxmox-api, cluster-resources]

must_haves:
  truths:
    - "D-06: When the Proxmox /cluster/resources endpoint reports a VM whose name does not match any sitemap hostname (case-insensitive), scan_drift surfaces a per-VM record in unknown[]"
    - "D-05: A 5-node Proxmox cluster with 5 sitemap rows results in exactly ONE /cluster/resources call per scan (de-dupe via _HOST_CLUSTER_CACHE cluster_name)"
    - "D-05: Standalone (single-node) Proxmox hosts hit /cluster/resources successfully and contribute their VMs (per-node fallback only on standalone-Proxmox error)"
    - "D-07: Per-VM records in unknown[] carry hypervisor_hostname, node, vmid, vm_type (qemu|lxc), vm_name, vm_status, scan_timestamp, and a message pointing the user at `discover_and_map <ip>` for adoption"
    - "VM enumeration runs only against hosts that landed in probed_ok (not unreachable / not_eligible)"
    - "VM enumeration failure on a probed_ok host does NOT change that host's bucket placement — host stays in probed_ok; just contributes no unknown[] entries"
    - "D-10: unknown[] is a parallel per-VM surface, independent of host buckets — a host in changed[] (next plan) can still emit unknown VMs"
    - "D-10: counts['unknown'] == len(unknown) and the locked envelope key order is preserved (Phase 38.1 D-08); scanned == sum(counts.values()) invariant holds"
    - "D-11, D-12: _enumerate_proxmox_vms is OUTSIDE the AST guard scope (it builds enumeration targets, does not iterate sitemap rows feeding bucket appends); Phase 38.1 D-15 guard on scan_drift's row loop remains green"
  artifacts:
    - path: "src/homelab_mcp/drift_detection.py"
      provides: "_enumerate_proxmox_vms async helper + integration into scan_drift pre-pass"
      contains: "async def _enumerate_proxmox_vms"
    - path: "tests/test_drift_detection.py"
      provides: "TestPhase39Unknown class (DRFT-17 functional tests)"
      contains: "class TestPhase39Unknown"
  key_links:
    - from: "src/homelab_mcp/drift_detection.py::scan_drift"
      to: "_enumerate_proxmox_vms"
      via: "async call after main row loop"
      pattern: "_enumerate_proxmox_vms\\("
    - from: "src/homelab_mcp/drift_detection.py::_enumerate_proxmox_vms"
      to: "_HOST_CLUSTER_CACHE"
      via: "import + read for cluster_name de-dupe"
      pattern: "_HOST_CLUSTER_CACHE"
    - from: "src/homelab_mcp/drift_detection.py"
      to: "src/homelab_mcp/drift_detection.py::_enumerate_unknown_vms"
      via: "helper from Plan 01"
      pattern: "_enumerate_unknown_vms\\("
---

<objective>
Wire DRFT-17 unknown-VM detection into `scan_drift`. Add `_enumerate_proxmox_vms` async helper that calls `/cluster/resources` once per cluster (de-duped by `cluster_name` via `_HOST_CLUSTER_CACHE`) for every host that landed in `probed_ok`, then feed the per-cluster VM list through Plan 01's `_enumerate_unknown_vms` helper to populate the `unknown[]` bucket.

Purpose: Close the unknown-infrastructure visibility gap. A user creating a VM directly in the Proxmox UI now sees it surfaced in their next drift scan with a `discover_and_map` adoption pointer.

Output: One new async helper `_enumerate_proxmox_vms` in `drift_detection.py`; `scan_drift` body grows by ~15 lines for the post-loop VM-enumeration pre-pass; new test class `TestPhase39Unknown` with ≥ 2 functional tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/39-drift-detection-cases/39-CONTEXT.md
@.planning/phases/39-drift-detection-cases/39-RESEARCH.md
@.planning/phases/39-drift-detection-cases/39-PATTERNS.md
@.planning/phases/39-drift-detection-cases/39-VALIDATION.md

@.planning/phases/39-drift-detection-cases/39-01-PLAN.md

<interfaces>
<!-- Existing contracts the executor must respect. -->

From src/homelab_mcp/proxmox_api.py:22 (cluster cache):
```python
_HOST_CLUSTER_CACHE: dict[str, tuple[str, str | None]] = {}  # hostname → (scope, cluster_name)
```

From src/homelab_mcp/proxmox_api.py:540-574 (existing /cluster/resources call shape):
```python
client = await get_proxmox_client(host=host, session=session)
resources = await client.get("/cluster/resources")
# returns list of dicts; type ∈ {"qemu", "lxc", "node", "storage", ...}
# VM/LXC entries have keys: vmid, node, name, type, status
```

From src/homelab_mcp/drift_detection.py (Plan 01 helpers, now landed):
```python
def _enumerate_unknown_vms(
    cluster_vm_map: dict[str, list[dict[str, Any]]],
    sitemap_hostnames: set[str],
    scan_timestamp: str,
) -> list[dict[str, Any]]: ...
```

From src/homelab_mcp/drift_detection.py (existing):
```python
async def scan_drift(
    session: aiohttp.ClientSession | None,
    db_adapter: DatabaseAdapter,
    node: str | None = None,
    vm_type: str = "all",
) -> dict[str, Any]:
    # ... existing single for-row loop populates probed_ok / unreachable / not_eligible ...
    # unknown: list = []  # currently always empty per Phase 38.1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED — TestPhase39Unknown functional tests</name>
  <files>tests/test_drift_detection.py</files>
  <read_first>
    - tests/test_drift_detection.py (lines 12-110: TestScanDrift4Bucket pattern; lines 821-870: TestScanDriftNotEligible pattern for `fake_get_client` shape)
    - .planning/phases/39-drift-detection-cases/39-PATTERNS.md (lines 396-444: mock pattern for /cluster/resources)
    - .planning/phases/39-drift-detection-cases/39-RESEARCH.md (lines 484-524: _enumerate_proxmox_vms code example)
    - .planning/phases/39-drift-detection-cases/39-CONTEXT.md (D-05 cluster de-dupe; D-06 case-insensitive match; D-07 per-VM record shape)
  </read_first>
  <behavior>
    Test class `TestPhase39Unknown` (DRFT-17 functional tests):

    - `test_unmatched_vm_in_unknown_bucket(mock_cluster_resources_response)`: 1 sitemap row hostname="ubuntu-prod" with proxmox_credential_id; mock `/cluster/status` returns success → host in probed_ok; mock `/cluster/resources` returns 4-element fixture (qemu vmid=100 name="ubuntu-prod" matches; qemu vmid=110 name="ubuntu-test" UNMATCHED; lxc vmid=200 name="pi-hole" UNMATCHED; node-type filtered). Asserts `result["counts"]["unknown"] == 2`, `result["unknown"][0]["vmid"] in (110, 200)`, `result["unknown"][0]["message"]` contains `"discover_and_map"`, `result["unknown"][0]["hypervisor_hostname"] == "ubuntu-prod"`. Asserts host stayed in `probed_ok` (counts["probed_ok"] == 1).

    - `test_cluster_dedup_single_enumeration`: 5 sitemap rows for the same cluster (hostnames pve1..pve5, all bound to the same `proxmox_credential_id`). Pre-populate `_HOST_CLUSTER_CACHE` (via patch) so each host resolves to `(scope="cluster", cluster_name="homelab-prod")`. Mock `client.get("/cluster/resources")` to return one mocked VM list. Assert `client.get` is called with `"/cluster/resources"` exactly ONCE total across the scan (not 5×). Assert `result["counts"]["unknown"] == 0` (all VMs in mock matched something) OR a documented count.

    - `test_case_insensitive_match`: sitemap row hostname="plex-server"; cluster_resources returns one VM name="Plex-Server" (capital P/S). Assert that VM does NOT appear in unknown[] (`counts["unknown"] == 0`).

    - `test_enumeration_failure_keeps_host_in_probed_ok`: 1 sitemap row pve1; `/cluster/status` succeeds → probed_ok. `/cluster/resources` raises `aiohttp.ClientError("api 500")`. Assert host stays in `probed_ok` (counts["probed_ok"] == 1, counts["unreachable"] == 0); `unknown[]` is empty for this hypervisor.

    - `test_unknown_independent_of_host_bucket`: 1 sitemap row pve1 with stored fingerprint; mock probe returns probed_ok (no diff yet — Plan 02 doesn't wire diff); cluster_resources returns 1 unmatched VM. Assert host in probed_ok AND unknown[] has 1 entry. (Validates D-10: unknown[] is parallel surface, not host bucket.)

    All tests use the existing `fake_get_client` / `fake_resolve` patch shape from `tests/test_drift_detection.py:43-67`. Patch `homelab_mcp.drift_detection.get_proxmox_client`, `homelab_mcp.drift_detection.resolve_proxmox_credentials`, and `homelab_mcp.drift_detection._HOST_CLUSTER_CACHE` (or equivalent — verify import path from Task 2).

    Tests MUST FAIL initially (helper does not exist; scan_drift does not yet enumerate VMs).
  </behavior>
  <action>
    1. Append a new test class `TestPhase39Unknown` to `tests/test_drift_detection.py` after `TestPhase39Helpers`. Class docstring: `"""Phase 39 D-05/D-06/D-07 + DRFT-17: unknown VM detection via /cluster/resources."""`.
    2. Each test method is `@pytest.mark.asyncio async def test_*(self, mock_cluster_resources_response)` (or other relevant fixtures from `tests/conftest.py`).
    3. Each test builds a `db_adapter = MagicMock()` with `db_adapter.get_all_devices.return_value = [...]`.
    4. Define inline `async def fake_get_client(host, *, session=None, credential_id=None)` and `async def fake_resolve(host, session=None, *, credential_id=None)` per the existing pattern at `test_drift_detection.py:43-67`. The `fake_get_client` returns a `MagicMock()` whose `.get` is `AsyncMock` configured per-test:
       - For `/cluster/status`: returns a node list (success).
       - For `/cluster/resources`: returns the appropriate mock per test.
    5. For `test_cluster_dedup_single_enumeration`, instrument `client.get` with a counter (e.g., `call_count_resources = 0` incremented when `path == "/cluster/resources"`); assert `== 1` after `await scan_drift(...)`.
    6. For `test_enumeration_failure_keeps_host_in_probed_ok`, the `/cluster/resources` AsyncMock raises `aiohttp.ClientError("api 500")` while `/cluster/status` succeeds.
    7. Patch `homelab_mcp.drift_detection.get_proxmox_client` and `homelab_mcp.drift_detection.resolve_proxmox_credentials`. For the dedup test, also patch `homelab_mcp.drift_detection._HOST_CLUSTER_CACHE` to seed `{"pve1": ("cluster", "homelab-prod"), "pve2": ("cluster", "homelab-prod"), ...}`.
    8. Run `uv run pytest tests/test_drift_detection.py::TestPhase39Unknown -x`. All tests FAIL (helper not yet implemented; scan_drift unknown[] still empty). RED stage.
    9. Commit: `test(39-02): wave 0 RED tests for unknown VM detection (DRFT-17)`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_drift_detection.py::TestPhase39Unknown -x 2>&1 | tail -20 && (uv run pytest tests/test_drift_detection.py::TestPhase39Unknown -x 2>&1 | grep -E "(FAILED|AssertionError|ImportError)" >/dev/null || (echo "tests should be RED" && exit 1))</automated>
  </verify>
  <acceptance_criteria>
    - Class `TestPhase39Unknown` exists with ≥ 5 test methods. Verify: `grep -A 1 "class TestPhase39Unknown" tests/test_drift_detection.py | head -1` matches; `grep -c "    async def test_" tests/test_drift_detection.py | head` grows by ≥ 5.
    - Each test uses `@pytest.mark.asyncio`. Verify: `grep -B 1 "async def test_unmatched_vm_in_unknown_bucket" tests/test_drift_detection.py | grep "@pytest.mark.asyncio"` matches.
    - Tests reference fixtures from `tests/conftest.py`. Verify: `grep "mock_cluster_resources_response" tests/test_drift_detection.py | head -1` matches.
    - Tests fail RED: `uv run pytest tests/test_drift_detection.py::TestPhase39Unknown -x` exits non-zero with `AssertionError` or `KeyError` (unknown[] still empty).
    - Test collection passes: `uv run pytest tests/test_drift_detection.py::TestPhase39Unknown --collect-only` exits 0.
  </acceptance_criteria>
  <done>
    `TestPhase39Unknown` test class committed; all tests RED (failing on empty unknown[] bucket).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _enumerate_proxmox_vms + wire DRFT-17 unknown enumeration into scan_drift</name>
  <files>src/homelab_mcp/drift_detection.py</files>
  <read_first>
    - src/homelab_mcp/drift_detection.py (lines 27-40: imports — add `_HOST_CLUSTER_CACHE`; lines 124-372: scan_drift body; line 217: get_all_devices call; lines 341-372: response envelope assembly)
    - src/homelab_mcp/proxmox_api.py:22 (`_HOST_CLUSTER_CACHE` dict shape) and lines 540-574 (existing /cluster/resources call signature)
    - .planning/phases/39-drift-detection-cases/39-PATTERNS.md (lines 178-196: cluster /cluster/resources enumeration mirror)
    - .planning/phases/39-drift-detection-cases/39-RESEARCH.md (lines 484-531: _enumerate_proxmox_vms code example with comprehension-based de-dupe; lines 526-531 explicitly recommends loop-free dict comprehension to keep AST guard scope clean)
  </read_first>
  <behavior>
    - New module-level helper: `async def _enumerate_proxmox_vms(probed_ok_records: list[dict[str, Any]], session: aiohttp.ClientSession | None) -> dict[str, list[dict[str, Any]]]`. Returns `{representative_hostname: [vm_record, ...]}`. De-dupes by `cluster_name` from `_HOST_CLUSTER_CACHE` so each cluster gets ONE call.
    - De-dupe uses the loop-free dict comprehension: `targets = list({(c or h): (h, c) for h, c in pairs}.values())` where `pairs` is `[(record["hostname"], cluster_name_lookup) for record in probed_ok_records]`. NO `for` loop with `continue` for de-dupe — the comprehension is sufficient.
    - Each enumeration call is wrapped in `try/except (aiohttp.ClientError, TimeoutError, ValueError)` → log debug + return `(hostname, [])`. Enumeration failure does NOT raise; affected host contributes no unknown[] entries but stays in `probed_ok`.
    - Filter `/cluster/resources` response to `r["type"] in ("qemu", "lxc")` only — drop nodes/storage/etc. (D-07 implicit; node-type entries are not VMs).
    - Concurrent enumeration via `asyncio.gather(*[_enum_one(h, c) for h, c in targets])`. No `Semaphore` — clusters are typically 1-3 in homelab; over-engineered to bound here.
    - In `scan_drift`: AFTER the `for row in rows:` loop completes (i.e., AFTER all 5 buckets are populated except `unknown`), but BEFORE the response envelope assembly:
      1. Compute `sitemap_hostnames = {row["hostname"].lower() for row in rows if row.get("hostname")}`.
      2. Call `cluster_vm_map = await _enumerate_proxmox_vms(probed_ok, session)`.
      3. Call `unknown = _enumerate_unknown_vms(cluster_vm_map, sitemap_hostnames, scan_timestamp)` (Plan 01 helper).
    - The existing `unknown: list[dict[str, Any]] = []` initialization at the top of `scan_drift` (line ~214) is REPLACED by the post-loop call. Do NOT modify the row-loop body — the AST guard's "no continue in scan_drift row loop" invariant must remain green.
    - The locked envelope key order (D-08 from Phase 38.1) is preserved: `status, scan_timestamp, scanned, counts, [guidance,] probed_ok, unreachable, not_eligible, unknown, changed`. The `counts["unknown"] = len(unknown)` line at the existing counts-build section now reflects real counts instead of 0.
  </behavior>
  <action>
    1. In `src/homelab_mcp/drift_detection.py`, extend the existing `from .proxmox_api import (...)` block to also import `_HOST_CLUSTER_CACHE`. Verify no circular import (proxmox_api.py does not import drift_detection.py).
    2. Add `async def _enumerate_proxmox_vms(probed_ok_records, session)` AFTER `_enumerate_unknown_vms` (Plan 01) and BEFORE `scan_drift`. Body per behavior spec:
       ```python
       async def _enumerate_proxmox_vms(
           probed_ok_records: list[dict[str, Any]],
           session: "aiohttp.ClientSession | None",
       ) -> dict[str, list[dict[str, Any]]]:
           """D-05: one /cluster/resources call per cluster_name; standalone hosts keyed by themselves."""
           pairs: list[tuple[str, str | None]] = []
           for record in probed_ok_records:
               hostname = record.get("hostname", "")
               if not hostname:
                   continue  # NOTE: this `continue` is OUTSIDE scan_drift's row loop and OUTSIDE the AST-guarded helpers from Plan 01
               cached = _HOST_CLUSTER_CACHE.get(hostname)
               cluster_name = cached[1] if cached else None
               pairs.append((hostname, cluster_name))
           # Loop-free de-dupe (D-11(b) clean): one entry per (cluster_name OR hostname)
           targets = list({(c or h): (h, c) for h, c in pairs}.values())

           async def _enum_one(h: str, c: str | None) -> tuple[str, list[dict[str, Any]]]:
               try:
                   client = await get_proxmox_client(host=h, session=session)
                   resources = await client.get("/cluster/resources")
                   vms = [r for r in resources if r.get("type") in ("qemu", "lxc")]
                   return (h, vms)
               except (aiohttp.ClientError, TimeoutError, ValueError) as exc:
                   logger.debug("VM enum failed for %s: %s", h, sanitize_error(exc))
                   return (h, [])

           results = await asyncio.gather(*[_enum_one(h, c) for h, c in targets])
           return dict(results)
       ```
       NOTE: the inner `for record in probed_ok_records:` loop has a `continue` for empty-hostname guard. This helper IS NOT in `_FORBIDDEN_CONTINUE_FUNCTIONS` and is OUTSIDE the AST guards (D-12 keeps scope targeted). Plan 03's Task 4 explicitly enumerates Phase 39 helpers under the new `TestPhase39DriftCases` AST guard — `_enumerate_proxmox_vms` is OUT of that guard's scope (it builds enumeration targets, it does NOT iterate sitemap rows feeding bucket appends).
    3. In `scan_drift`, AFTER the existing `for row in rows:` loop closes and BEFORE the `counts` dict is built, insert the unknown-enumeration block:
       ```python
       # Phase 39 DRFT-17: enumerate unknown VMs across probed_ok hosts (cluster-deduped).
       sitemap_hostnames: set[str] = {
           (row.get("hostname") or "").lower()
           for row in rows
           if row.get("hostname")
       }
       cluster_vm_map = await _enumerate_proxmox_vms(probed_ok, session)
       unknown = _enumerate_unknown_vms(cluster_vm_map, sitemap_hostnames, scan_timestamp)
       ```
       Where `unknown` is the already-existing local list at line ~214; replace its initial `unknown: list[dict[str, Any]] = []` with this assignment (or rename to keep `unknown = []` at the top and reassign here).
    4. Confirm the existing `counts["unknown"] = len(unknown)` line and `response["unknown"] = unknown` envelope-assembly line (already present from Phase 38.1 reservation) now produce non-zero values when VMs are unknown.
    5. Update the `scan_drift` docstring's `Returns:` block (line ~178-188) to remove "always 0 in Phase 38.1" comments next to `unknown`, and add: `Per-row record shape (unknown — D-07): {hypervisor_hostname, node, vmid, vm_type, vm_name, vm_status, scan_timestamp, message}`.
    6. Run `uv run pytest tests/test_drift_detection.py::TestPhase39Unknown -x -v`. All 5 tests pass GREEN.
    7. Run `uv run pytest tests/test_drift_detection.py::TestScanDrift4Bucket tests/test_drift_detection.py::TestScanDriftNotEligible tests/test_ast_regression.py::TestPhase381CredBinding -x` to confirm Phase 36/37/38.1 regression tests stay green.
    8. Run `uv run mypy src/homelab_mcp/drift_detection.py` and `uv run ruff check src/homelab_mcp/drift_detection.py`.
    9. Commit: `feat(39-02): wire DRFT-17 unknown VM detection via /cluster/resources de-dupe`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_drift_detection.py::TestPhase39Unknown tests/test_drift_detection.py::TestScanDrift4Bucket tests/test_drift_detection.py::TestScanDriftNotEligible tests/test_ast_regression.py::TestPhase381CredBinding -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `_enumerate_proxmox_vms` exists. Verify: `grep -c "^async def _enumerate_proxmox_vms" src/homelab_mcp/drift_detection.py` returns 1.
    - `_HOST_CLUSTER_CACHE` is imported. Verify: `grep "from .proxmox_api import" src/homelab_mcp/drift_detection.py | grep "_HOST_CLUSTER_CACHE"` matches.
    - `scan_drift` calls `_enumerate_proxmox_vms` and `_enumerate_unknown_vms`. Verify: `grep -E "_enumerate_(proxmox_vms|unknown_vms)" src/homelab_mcp/drift_detection.py | wc -l` returns ≥ 3 (1 def for proxmox_vms + 1 def for unknown_vms + ≥1 call inside scan_drift).
    - The Phase 38.1 D-15 AST guard remains GREEN — `scan_drift`'s row loop has zero `continue` (the new code is OUTSIDE the row loop). Verify: `uv run pytest tests/test_ast_regression.py::TestPhase381CredBinding::test_scan_drift_no_continue_in_row_loop_phase38_1 -x` exits 0.
    - All 5 `TestPhase39Unknown` tests pass: `uv run pytest tests/test_drift_detection.py::TestPhase39Unknown -x` exits 0.
    - Phase 36/37/38.1 regression tests stay green: `uv run pytest tests/test_drift_detection.py::TestScanDrift4Bucket tests/test_drift_detection.py::TestScanDriftNotEligible -x` exits 0.
    - mypy + ruff clean.
    - The locked envelope key order is preserved. Verify: `grep -A 30 "response\[.status.\]" src/homelab_mcp/drift_detection.py | grep -E "response\[.(probed_ok|unreachable|not_eligible|unknown|changed)" | head -5` shows the correct order.
  </acceptance_criteria>
  <done>
    DRFT-17 wired end-to-end. Cluster de-dupe verified. Enumeration failures degrade gracefully. AST guards + prior-phase regression tests all green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Proxmox API → process | `/cluster/resources` response from a remote Proxmox host. Contents (vmid, name, node, status) used to populate the unknown[] bucket and rendered in MCP client. |
| `_HOST_CLUSTER_CACHE` → process | In-process cache populated by prior `resolve_proxmox_credentials` calls. Trusted (we wrote it). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07 | Information Disclosure (V7) | `_enumerate_proxmox_vms` error path | mitigate | Enumeration failures → `sanitize_error(exc)` in debug log only; no raw error leaks into the response. Failed hypervisor stays in `probed_ok` with no unknown contribution. |
| T-39-08 | Tampering (V5) | `/cluster/resources` response | accept | Hostile Proxmox API could return crafted `name` / `node` strings rendered in `unknown[].message`. MCP clients are responsible for escaping their own rendering layer. Field values are echoed in backtick-wrapped command syntax — clients render as code, not HTML. |
| T-39-09 | DoS (V14) | `_enumerate_proxmox_vms` cardinality | accept | Pitfall 6: a 50-LXC cluster with only 5 in sitemap → 45 unknown[] rows. Honest cardinality of untracked infra; not a bug. v1.7.1 LIFE-* hooks reduce future load. |
| T-39-10 | Spoofing (V2) | Cluster authentication | mitigate | `_enumerate_proxmox_vms` calls `get_proxmox_client` which threads through `resolve_proxmox_credentials` (Phase 38.1 R6) — no env var bypass. Cluster-scope token authenticates the call. |
| T-39-11 | DoS (V5) | `_enumerate_proxmox_vms` per-call timeout | accept | aiohttp default ClientSession timeout (~5min) bounds each call. Outer scan timeout from Plan 03 (asyncio.wait_for(120s)) bounds total runtime. |
</threat_model>

<verification>
- All 5 `TestPhase39Unknown` tests GREEN.
- Phase 36/37/38.1 regression tests stay GREEN.
- Phase 38.1 D-15 AST guard stays GREEN — `scan_drift` row loop unmodified.
- mypy + ruff clean.
- One `/cluster/resources` call per cluster_name verified by call-counter test.
- Enumeration failure does NOT degrade host bucket placement.
</verification>

<success_criteria>
- `unknown[]` bucket populated for any sitemap-Proxmox host whose `/cluster/resources` reports VMs missing from sitemap.
- Cluster de-dupe verified — single API call per cluster regardless of how many sitemap rows share that cluster.
- Case-insensitive name match prevents false positives on capitalization mismatches.
- Enumeration failure isolated — host stays in `probed_ok`; only contribution to unknown[] is empty.
- Locked envelope key order preserved.
- `scanned == sum(counts.values())` invariant unchanged (unknown[] does NOT count toward `scanned` since it's a per-VM surface, not a host bucket — counts["unknown"] is just the per-VM record count, and scanned still equals the sum of host buckets only). NOTE: per D-10 the scanned-equals-counts-sum invariant DOES include unknown count in `counts` but unknown is the only non-host-bucket; the existing line `scanned = sum(counts.values())` continues to work because counts is built from the same buckets as the response. Plan 03 verifies the invariant explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/39-drift-detection-cases/39-02-SUMMARY.md` with:
- New helper signature + line range
- scan_drift edits (line ranges added/modified, NO row-loop edits)
- TestPhase39Unknown test count + pass status
- Cluster de-dupe verification (call-counter assertion approach)
- Regression status (Phase 36/37/38.1 tests green)
- Files modified list matching frontmatter
- Commit hashes (2 expected: RED tests, GREEN implementation)
</output>
