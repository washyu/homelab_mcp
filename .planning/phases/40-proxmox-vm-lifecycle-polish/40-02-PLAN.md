---
phase: 40-proxmox-vm-lifecycle-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/homelab_mcp/tool_schemas/proxmox_tools_schema.py
  - src/homelab_mcp/openapi_app.py
autonomous: true
requirements:
  - POL-02
tags:
  - proxmox
  - schema
  - error-hygiene

must_haves:
  truths:
    - "create_proxmox_vm schema declares 'host' in its required list"
    - "create_proxmox_vm 'host' property description points at `homelab-mcp credentials add --type proxmox` and `--scope cluster:`"
    - "D-05: Zero `PROXMOX_HOST` substrings remain in proxmox_tools_schema.py — sweep across all tool description text (lines 48, 54, 76 + any other matches), rewriting to point at `homelab-mcp credentials add --type proxmox` or omitting redundant env-var sentences; description-text only, no schema shape change for tools other than create_proxmox_vm"
    - "openapi_app.py INFRA_REQUIREMENTS['Proxmox'] entry points at the credentials CLI, not PROXMOX_HOST"
  artifacts:
    - path: src/homelab_mcp/tool_schemas/proxmox_tools_schema.py
      provides: "create_proxmox_vm schema with host required + description sweep across all tool entries"
      contains: "homelab-mcp credentials add --type proxmox"
    - path: src/homelab_mcp/openapi_app.py
      provides: "INFRA_REQUIREMENTS['Proxmox'] rewritten to mirror 'Drift Detection' phrasing (Phase 37 D-08)"
      contains: "homelab-mcp credentials add --type proxmox"
  key_links:
    - from: "create_proxmox_vm tool schema"
      to: "MCP client agents (which key off `required` list to know which arguments to supply)"
      via: "JSON Schema `required` array on inputSchema"
      pattern: "\"required\":.*\"host\""
    - from: "openapi_app INFRA_REQUIREMENTS['Proxmox']"
      to: "OpenAPI documentation surface for HTTP REST consumers"
      via: "dict literal value embedded in error messages and docs"
      pattern: "homelab-mcp credentials add --type proxmox"
---

<objective>
Land POL-02 (D-03 schema change for `create_proxmox_vm`), the D-05 description sweep across all PROXMOX_HOST mentions in `proxmox_tools_schema.py`, and the matching `openapi_app.py` INFRA_REQUIREMENTS rewrite. After this plan, the schema layer makes `host` mandatory for `create_proxmox_vm`, every description string in the schema file points at the credentials CLI rather than the deprecated env var, and the OpenAPI surface mirrors the Phase 37 D-08 "Drift Detection" entry phrasing.

Purpose: Closes Bug G schema-half by making the `create_proxmox_vm` schema honest about its runtime requirements (now coupled with Plan 01's POL-03 env-var hard-removal — without this plan landing in the same wave, the schema would still claim `host` is optional while the runtime ValueError rejects None). Closes the D-05 ripple so users hitting any proxmox-tool description text never see PROXMOX_HOST language.

Output: Modified schema file with `host` in `create_proxmox_vm`'s `required` array, rewritten host descriptions on `create_proxmox_vm` / `list_proxmox_resources` / `get_proxmox_node_status` / `get_proxmox_vm_status`, and a rewritten `openapi_app.py` line 59 entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-proxmox-vm-lifecycle-polish/40-CONTEXT.md
@.planning/phases/40-proxmox-vm-lifecycle-polish/40-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing PROXMOX_TOOLS dict shape (uniform across the file) -->
```python
PROXMOX_TOOLS = {
    "<tool_name>": {
        "description": str,
        "inputSchema": {
            "type": "object",
            "properties": {
                "<param>": {"type": str, "description": str, ...},
                ...
            },
            "required": list[str],
        },
    },
    ...
}
```

<!-- Existing PROXMOX_HOST mention sites (verbatim from current file) -->
- Line 48 (`list_proxmox_resources` tool description): `"List all Proxmox cluster resources (VMs, containers, nodes, storage). Uses PROXMOX_HOST from environment if host not provided."`
- Line 54 (`list_proxmox_resources.host` description): `"Proxmox host (optional, uses PROXMOX_HOST env var if not provided)"`
- Line 76 (`get_proxmox_node_status.host` description): `"Proxmox host (optional, uses PROXMOX_HOST env var)"`

<!-- Existing create_proxmox_vm host property (lines 287-290) -->
```python
"host": {
    "type": "string",
    "description": "Proxmox host (optional)",
},
```

<!-- Existing create_proxmox_vm required (line 292) -->
```python
"required": ["node", "vmid", "name"],
```

<!-- Sibling-entry phrasing already in tree (openapi_app.py:60 — Phase 37 D-08) -->
```python
"Drift Detection": "a Proxmox VE host registered in the sitemap. Populate via 'discover_and_map' and configure credentials with 'homelab-mcp credentials add --type proxmox' (per-node) or '--scope cluster:<name>' (cluster-wide)",
```

<!-- Current openapi_app.py:59 (target for rewrite) -->
```python
"Proxmox": "a Proxmox VE host. Set PROXMOX_HOST and credentials via environment or POST /api/tools/register_server",
```

<!-- get_proxmox_vm_status.host description (line ~103, may need sweep — verify in task) -->
```python
"host": {
    "type": "string",
    "description": "Proxmox host (optional)",
},
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mark `host` required on `create_proxmox_vm` + sweep all PROXMOX_HOST mentions in proxmox_tools_schema.py (POL-02 D-03 + D-05)</name>
  <files>src/homelab_mcp/tool_schemas/proxmox_tools_schema.py</files>
  <read_first>
    - src/homelab_mcp/tool_schemas/proxmox_tools_schema.py (whole file — verify the current line numbers; the file has been edited since CONTEXT.md was written so positions may have drifted; locate by content not by line number)
    - .planning/phases/40-proxmox-vm-lifecycle-polish/40-CONTEXT.md (D-03 description template, D-05 sweep targets)
    - .planning/phases/40-proxmox-vm-lifecycle-polish/40-PATTERNS.md (lines 175-218 — schema sweep section with verbatim before/after)
  </read_first>
  <behavior>
    - `create_proxmox_vm` schema's `required` list contains `"host"` as its 4th element. Order: `["node", "vmid", "name", "host"]`.
    - `create_proxmox_vm.properties.host.description` is rewritten per the D-03 template; contains literal `homelab-mcp credentials add --type proxmox` and `--scope cluster:`.
    - `list_proxmox_resources.description` (the tool-level description string) no longer mentions `PROXMOX_HOST`. The sentence "Uses PROXMOX_HOST from environment if host not provided." is removed entirely.
    - `list_proxmox_resources.properties.host.description` no longer mentions `PROXMOX_HOST`. New text: `"Proxmox host. Register via `homelab-mcp credentials add --type proxmox`."` (or polished equivalent — must lack the env-var name).
    - `get_proxmox_node_status.properties.host.description` no longer mentions `PROXMOX_HOST`. Same replacement style.
    - `get_proxmox_vm_status.properties.host.description` (currently `"Proxmox host (optional)"`) is updated for consistency with the rest of the sweep — no PROXMOX_HOST is currently present here, but the "(optional)" wording becomes misleading given POL-03 + the runtime now hard-requires host. Replace with the same credentials-CLI pointer shape.
    - Any other tool entry in the file with a `host` property follows the same pattern (manage_proxmox_vm, clone_proxmox_vm, delete_proxmox_vm, get_proxmox_vm_config, create_proxmox_lxc — sweep all `host` description strings).
    - Total `PROXMOX_HOST` literal count in the file is 0 after edit.
  </behavior>
  <action>
1. **First, audit the file.** Use `grep -n "host\|PROXMOX_HOST" src/homelab_mcp/tool_schemas/proxmox_tools_schema.py` to locate every `host` property and every `PROXMOX_HOST` mention. The CONTEXT-listed lines (48, 54, 76, 287-292) are starting points — confirm them and find any others.

2. **`create_proxmox_vm` schema edit.** Locate the entry by searching for the literal `"create_proxmox_vm":`. In its `inputSchema`:
   - Change the `required` list from `["node", "vmid", "name"]` to `["node", "vmid", "name", "host"]`.
   - Rewrite the `host` property block from:
     ```python
     "host": {
         "type": "string",
         "description": "Proxmox host (optional)",
     },
     ```
     to:
     ```python
     "host": {
         "type": "string",
         "description": (
             "Proxmox host. Any node hostname covered by your registered "
             "credential (per-node) or cluster-scope token. Register with "
             "`homelab-mcp credentials add --type proxmox <host> <username>`, "
             "or `... --scope cluster:<name> <token_id>` for cluster tokens."
         ),
     },
     ```

3. **`list_proxmox_resources` tool description sweep (line 48 area).** Locate the entry by searching `"list_proxmox_resources":`. Rewrite its top-level `"description"` from:
   ```
   "List all Proxmox cluster resources (VMs, containers, nodes, storage). Uses PROXMOX_HOST from environment if host not provided."
   ```
   to:
   ```
   "List all Proxmox cluster resources (VMs, containers, nodes, storage)."
   ```
   (Drop the env-var sentence entirely — the host property's own description carries the credentials-add pointer.)

4. **`list_proxmox_resources.host` property description (line 54 area).** Rewrite from:
   ```
   "Proxmox host (optional, uses PROXMOX_HOST env var if not provided)"
   ```
   to:
   ```
   "Proxmox host. Register via `homelab-mcp credentials add --type proxmox <host> <username>` (per-node) or `... --scope cluster:<name> <token_id>` (cluster scope)."
   ```

5. **`get_proxmox_node_status.host` property description (line 76 area).** Rewrite from:
   ```
   "Proxmox host (optional, uses PROXMOX_HOST env var)"
   ```
   to the same text used in step 4 (verbatim).

6. **`get_proxmox_vm_status.host` property description.** Currently `"Proxmox host (optional)"` (no PROXMOX_HOST mention but stale "(optional)" wording). Rewrite to the same text used in step 4 (verbatim).

7. **Any remaining tool entries with a `host` property.** Sweep `manage_proxmox_vm`, `clone_proxmox_vm`, `delete_proxmox_vm`, `get_proxmox_vm_config`, `create_proxmox_lxc`. For each, if the `host` property's description mentions PROXMOX_HOST or "(optional, uses ...)" wording, rewrite to the step-4 text verbatim. If it already lacks PROXMOX_HOST and is succinct, leave it — but if the count gate at the end shows non-zero matches, return here.

8. **Do NOT change** the `required` lists of any tool other than `create_proxmox_vm`. Per CONTEXT D-05: "no schema shape change for tools other than `create_proxmox_vm`." The other 6 tools' optional-host claims are now schema-runtime-misaligned, but that is captured as deferred ripple (v1.7.1 LIFE-* phases or v1.8 mechanical sweep — out of scope here).

After all edits, re-run the audit grep. Target: zero `PROXMOX_HOST` matches in the file.
  </action>
  <verify>
    <automated>uv run python -c "from src.homelab_mcp.tool_schemas.proxmox_tools_schema import PROXMOX_TOOLS; assert 'host' in PROXMOX_TOOLS['create_proxmox_vm']['inputSchema']['required'], 'host not required'; print('OK: host required on create_proxmox_vm')"</automated>
    Additional grep gates (run after the python check passes):
    - `grep -c PROXMOX_HOST src/homelab_mcp/tool_schemas/proxmox_tools_schema.py` returns 0.
    - `grep -c "homelab-mcp credentials add --type proxmox" src/homelab_mcp/tool_schemas/proxmox_tools_schema.py` returns ≥ 4 (create_proxmox_vm.host + 3 swept descriptions, possibly more).
    - `grep -c '"host"' src/homelab_mcp/tool_schemas/proxmox_tools_schema.py` shows the same count as before the edit (no host properties accidentally deleted).
    Then run module-level checks:
    - `uv run ruff check src/homelab_mcp/tool_schemas/proxmox_tools_schema.py` is clean.
    - `uv run mypy src/homelab_mcp/tool_schemas/proxmox_tools_schema.py` reports no new errors.
  </verify>
  <acceptance_criteria>
    - `PROXMOX_TOOLS["create_proxmox_vm"]["inputSchema"]["required"]` equals `["node", "vmid", "name", "host"]` (order: node, vmid, name, host).
    - `PROXMOX_TOOLS["create_proxmox_vm"]["inputSchema"]["properties"]["host"]["description"]` contains literal substring `homelab-mcp credentials add --type proxmox`.
    - `PROXMOX_TOOLS["create_proxmox_vm"]["inputSchema"]["properties"]["host"]["description"]` contains literal substring `--scope cluster:`.
    - File contains zero literal `PROXMOX_HOST` substrings.
    - `PROXMOX_TOOLS["list_proxmox_resources"]["description"]` does NOT contain `PROXMOX_HOST` and does NOT contain "Uses ... env var" or similar env-var fallback phrasing.
    - For each tool in `["list_proxmox_resources", "get_proxmox_node_status", "get_proxmox_vm_status"]`, the `properties.host.description` value does NOT contain `PROXMOX_HOST` and DOES contain `homelab-mcp credentials add --type proxmox`.
    - The Python module imports cleanly and `PROXMOX_TOOLS` is a valid dict (the python -c smoke check above passes).
    - `uv run ruff check` and `uv run mypy` on the file are clean.
  </acceptance_criteria>
  <done>
    - All edits applied; grep gates pass; module imports.
    - `host` is required on `create_proxmox_vm` ONLY (other tools' `required` lists unchanged per D-05 scope).
    - File is PROXMOX_HOST-free.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite `INFRA_REQUIREMENTS["Proxmox"]` in openapi_app.py to mirror the Phase 37 D-08 "Drift Detection" entry (D-05 ripple)</name>
  <files>src/homelab_mcp/openapi_app.py</files>
  <read_first>
    - src/homelab_mcp/openapi_app.py (lines 50-65 — `INFRA_REQUIREMENTS` dict; line 60 is the verbatim phrasing template)
    - .planning/phases/40-proxmox-vm-lifecycle-polish/40-CONTEXT.md (Claude's Discretion line 76 — openapi_app verification; line 112 schema-duplication audit)
    - .planning/phases/40-proxmox-vm-lifecycle-polish/40-PATTERNS.md (lines 221-235 — openapi_app sibling-entry analog with verbatim before/after)
  </read_first>
  <behavior>
    - Line 59 `INFRA_REQUIREMENTS["Proxmox"]` value is rewritten. New text mirrors the structure of line 60 ("Drift Detection") — references `homelab-mcp credentials add --type proxmox` (per-node) and `--scope cluster:<name>` (cluster-wide). Drops `PROXMOX_HOST` and drops the `POST /api/tools/register_server` reference (deprecated env-var-era hint).
    - Line 60 ("Drift Detection") is UNCHANGED — it was rewritten in Phase 37 D-08 and is the canonical phrasing template.
    - No other dict entries are modified.
    - File still imports `PROXMOX_TOOLS` (or whatever existing import shape) — no new imports needed.
  </behavior>
  <action>
1. **Verify openapi_app.py does NOT duplicate the proxmox tool schemas.** Run `grep -n "create_proxmox_vm\|inputSchema" src/homelab_mcp/openapi_app.py`. Expected: only references in `_PROXMOX_TOOLS_WITH_HOST` and other helper tuples — NO duplicated `inputSchema` definition. If a duplicated schema is found (defying the prior audit), STOP and escalate; CONTEXT line 76 says the planner verifies and acts accordingly. Default expectation per the prior audit: only the dict-value rewrite below is needed.

2. **Rewrite line 59.** Locate `"Proxmox":` in `INFRA_REQUIREMENTS`. Change from:
   ```python
   "Proxmox": "a Proxmox VE host. Set PROXMOX_HOST and credentials via environment or POST /api/tools/register_server",
   ```
   to:
   ```python
   "Proxmox": "a Proxmox VE host. Configure credentials with 'homelab-mcp credentials add --type proxmox' (per-node) or '--scope cluster:<name>' (cluster-wide).",
   ```

   Notes:
   - Mirrors the structure of the line-60 "Drift Detection" entry: same `(per-node)` / `(cluster-wide)` parenthetical pattern, same single-quoted CLI literal embedding, same trailing period.
   - Uses single quotes inside the string for the CLI fragments (matching line 60's style: `'homelab-mcp credentials add --type proxmox'`).
   - Drops both `PROXMOX_HOST` (deprecated) and `POST /api/tools/register_server` (the latter was the v1.0 OpenAPI registration path; superseded by the credentials CLI in v1.3).

3. **Do NOT touch line 60 ("Drift Detection")** — it is the verbatim phrasing source.

4. **Do NOT add or remove other entries** from `INFRA_REQUIREMENTS`. The other entries' updates (SSH, Network Discovery, etc.) are out of scope for Phase 40.
  </action>
  <verify>
    <automated>uv run python -c "from src.homelab_mcp.openapi_app import INFRA_REQUIREMENTS; v = INFRA_REQUIREMENTS['Proxmox']; assert 'PROXMOX_HOST' not in v, f'PROXMOX_HOST still present: {v!r}'; assert 'homelab-mcp credentials add --type proxmox' in v, f'credentials-add CLI missing: {v!r}'; assert '--scope cluster:' in v, f'cluster-scope hint missing: {v!r}'; print('OK:', v)"</automated>
    Additional grep gates (run after the python check passes):
    - `grep -c "PROXMOX_HOST" src/homelab_mcp/openapi_app.py` returns 0.
    - `grep -c '"Proxmox":' src/homelab_mcp/openapi_app.py` returns ≥ 1 (entry not accidentally deleted).
    - `grep -c '"Drift Detection":' src/homelab_mcp/openapi_app.py` returns ≥ 1 (sibling entry preserved).
    - `uv run ruff check src/homelab_mcp/openapi_app.py` is clean.
    - `uv run mypy src/homelab_mcp/openapi_app.py` reports no new errors.
  </verify>
  <acceptance_criteria>
    - `INFRA_REQUIREMENTS["Proxmox"]` value contains the literal substring `homelab-mcp credentials add --type proxmox`.
    - `INFRA_REQUIREMENTS["Proxmox"]` value contains the literal substring `--scope cluster:`.
    - `INFRA_REQUIREMENTS["Proxmox"]` value does NOT contain `PROXMOX_HOST`.
    - `INFRA_REQUIREMENTS["Proxmox"]` value does NOT contain `POST /api/tools/register_server`.
    - `INFRA_REQUIREMENTS["Drift Detection"]` value is byte-for-byte unchanged from pre-edit (verify via git diff).
    - File contains zero literal `PROXMOX_HOST` substrings overall.
    - The python -c smoke check above succeeds.
    - `uv run ruff check` and `uv run mypy` on the file are clean.
  </acceptance_criteria>
  <done>
    - Line 59 rewritten with credentials-CLI phrasing mirroring line 60's structure.
    - File is PROXMOX_HOST-free.
    - "Drift Detection" entry untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → MCP server | Untrusted tool arguments cross here; agents key off the schema's `required` list to know which arguments must be supplied. |
| OpenAPI consumer → MCP server (HTTP REST surface) | INFRA_REQUIREMENTS strings are surfaced in error messages and HTTP docs; consumers act on the suggested CLI commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07 | T (Tampering) — schema/runtime divergence (auth bypass risk) | `create_proxmox_vm` schema's `host` field | mitigate | Marking `host` required closes the schema lie. Without this change, agents could omit `host` and the runtime would silently fall back to the (now-removed) PROXMOX_HOST env var; with Plan 01's POL-03 in the same wave, omitting `host` raises an actionable ValueError instead of misrouting to an unintended host. |
| T-40-08 | I (Information Disclosure) — credential-pointer phishing | INFRA_REQUIREMENTS error messages and schema descriptions | mitigate | All rewritten descriptions point at the canonical public CLI form (`homelab-mcp credentials add --type proxmox`) — the same literal grep-able string used in `proxmox_api.py:431-440` and the new ValueError in Plan 01. A user copy-pasting the suggestion lands on the documented credentials flow, not on attacker-craftable wording. The AST guard in Plan 03 D-06 prevents future PROXMOX_HOST reintroduction. |
| T-40-09 | T (Tampering) — silent description drift | Multiple tool entries with `host` properties not all swept | mitigate | Acceptance criteria require zero PROXMOX_HOST literal in the schema file post-edit; Plan 03 (D-06) adds the AST guard locking this in. Sweep gate enforces the outcome rather than relying on the planner enumerating every site. |
| T-40-10 | I (Information Disclosure) — over-aggressive sweep | The 6 non-create_proxmox_vm tools whose `host` is still optional in their `required` lists but advertised here as required-via-credentials | accept | Per CONTEXT D-05: "no schema shape change for tools other than `create_proxmox_vm`." The other 6 tools now have schema descriptions that point at the credentials CLI but still declare host optional — runtime accepts None (and Plan 01's ValueError fires if no env-var auth bypass is set). This is captured as deferred ripple (v1.7.1 LIFE-* / v1.8 mechanical sweep). Accepted: documented, scoped out, tracked. |
| T-40-11 | D (Denial of Service) — schema-rejection backwards-incompatibility | Existing MCP clients pinned to v1.6 schema may have cached `create_proxmox_vm` schema and not realize `host` is now required | accept | Per CONTEXT D-03: "Breaking schema change. Acceptable: v1.7 milestone-open already announced the env-var deprecation direction; users registering via CLI per CRED-04 are unaffected. Document in milestone close notes." Documented in milestone close notes. Accepted as deliberate breaking change. |
</threat_model>

<verification>
1. `uv run python -c "from src.homelab_mcp.tool_schemas.proxmox_tools_schema import PROXMOX_TOOLS; assert 'host' in PROXMOX_TOOLS['create_proxmox_vm']['inputSchema']['required']"` exits 0.
2. `uv run python -c "from src.homelab_mcp.openapi_app import INFRA_REQUIREMENTS; assert 'PROXMOX_HOST' not in INFRA_REQUIREMENTS['Proxmox']"` exits 0.
3. `grep -c PROXMOX_HOST src/homelab_mcp/tool_schemas/proxmox_tools_schema.py src/homelab_mcp/openapi_app.py` returns 0 for both files.
4. `grep -c "homelab-mcp credentials add --type proxmox" src/homelab_mcp/tool_schemas/proxmox_tools_schema.py` returns ≥ 4.
5. `uv run ruff check src/homelab_mcp/tool_schemas/proxmox_tools_schema.py src/homelab_mcp/openapi_app.py && uv run mypy src/homelab_mcp/tool_schemas/proxmox_tools_schema.py src/homelab_mcp/openapi_app.py` is clean.
</verification>

<success_criteria>
- POL-02: `host` is in `create_proxmox_vm`'s `required` array; the property description points at the credentials-add CLI and `--scope cluster:`.
- D-05 sweep: zero PROXMOX_HOST mentions remain in `proxmox_tools_schema.py`; every host-bearing tool description points at the credentials-add CLI.
- D-05 ripple: `openapi_app.py` line 59 mirrors the line-60 "Drift Detection" phrasing structure; PROXMOX_HOST gone.
- All changed modules import cleanly, ruff/mypy clean.
</success_criteria>

<output>
After completion, create `.planning/phases/40-proxmox-vm-lifecycle-polish/40-02-SUMMARY.md` per the standard summary template.
</output>
