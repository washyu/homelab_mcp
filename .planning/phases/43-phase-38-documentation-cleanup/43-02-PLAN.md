---
phase: 43-phase-38-documentation-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - docs/tool-reference.md
autonomous: true
requirements:
  - IN-04

must_haves:
  truths:
    - "docs/tool-reference.md MCP Prompts section documents all FIVE registered prompts: connect_to_device, decommission_device_workflow, deploy_service_workflow, homelab_health_check, configure_host_fingerprint"
    - "Each new prompt entry has the same structure as the existing configure_host_fingerprint entry: heading, description, argument table, related-tools cross-link"
    - "Prompt names + descriptions + argument schemas are copied verbatim from prompt_registry.py HOMELAB_PROMPTS dict (no invented wording)"
    - "The update_device_fingerprint tool entry (line 358-360) uses the corrected one-level overwrite wording matching the schema description rewritten in Plan 01 Task 2 (IN-03 docs surface)"
    - "The update_device_fingerprint_preview tool entry (if present) reflects the WR-03 preview-vs-persist contrast"
    - "Cross-link breadcrumbs from related tool entries (e.g., decommission_device → decommission_device_workflow prompt) are added where natural"
    - "uv run pytest -m 'not integration' -q stays green (no source change in Plan 02 — pure docs)"
  artifacts:
    - path: "docs/tool-reference.md"
      provides: "Complete MCP Prompts section + corrected fingerprint tool wording"
      contains: "### connect_to_device + 4 other prompt headings (decommission_device_workflow, deploy_service_workflow, homelab_health_check, configure_host_fingerprint) + 'one level deep' wording"
  key_links:
    - from: "docs/tool-reference.md MCP Prompts entries"
      to: "src/homelab_mcp/prompt_registry.py HOMELAB_PROMPTS"
      via: "Names + descriptions + argument schemas copied verbatim from the registry"
      pattern: "name == HOMELAB_PROMPTS[name].name and description == HOMELAB_PROMPTS[name].description"
    - from: "docs/tool-reference.md update_device_fingerprint entry"
      to: "src/homelab_mcp/tool_schemas/network_tools_schema.py update_device_fingerprint description (rewritten in Plan 01 Task 2)"
      via: "Wording must agree — both surfaces use 'one level deep' / 'REPLACE' phrasing"
---

<objective>
Close the final Phase 43 documentation gap: docs/tool-reference.md MCP Prompts section
currently documents only `configure_host_fingerprint` (Phase 38's new prompt). The other
four registered prompts (`connect_to_device`, `decommission_device_workflow`,
`deploy_service_workflow`, `homelab_health_check`) have shipped since v1.2/v1.3 but are
absent from the docs. Plus, fold in the docs-surface portion of IN-03 + WR-03 (the
update_device_fingerprint tool entry currently says "deep-merge" — must match Plan 01's
corrected schema wording).

Purpose: A user reading docs/tool-reference.md gets a complete inventory of MCP prompts
the server registers, with names + descriptions + arguments matching what
`prompts/list` returns at runtime. No more "Phase 38 added the only documented prompt"
misimpression flagged in 38-REVIEW.md IN-04.

Output: Single file (docs/tool-reference.md) with four new prompt entries inserted
above the existing `### configure_host_fingerprint` heading + the IN-03/WR-03 wording
fix on the update_device_fingerprint tool reference entry.

Wave 2 (depends on Plan 01) because Plan 01 rewrites the schema description text that
Plan 02's tool-reference.md update_device_fingerprint entry must mirror — Plan 02 reads
the post-Plan-01 schema description text as ground truth for the docs wording.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/38-sitemap-fingerprint-schema/38-REVIEW.md
@.planning/phases/43-phase-38-documentation-cleanup/43-01-SUMMARY.md

<interfaces>
<!-- Ground truth for the four new prompt entries — copy VERBATIM from prompt_registry.py. -->
<!-- Read prompt_registry.py:19-78 (HOMELAB_PROMPTS dict) for the canonical names, -->
<!-- descriptions, and argument schemas. Do NOT invent or paraphrase. -->

Existing entry shape to mirror (docs/tool-reference.md:1636-1651, configure_host_fingerprint):

```markdown
### configure_host_fingerprint

**Description:** [verbatim from HOMELAB_PROMPTS["configure_host_fingerprint"].description, possibly with extra prose elaborating workflow steps]

1. [Step 1 paraphrase]
2. [Step 2 paraphrase]
...

**Argument:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| <arg name> | <type> | Yes/No | <verbatim from PromptArgument.description> |

**Related tools:** [`tool_a`](#tool_a), [`tool_b`](#tool_b).
```

Authoritative HOMELAB_PROMPTS contents (from prompt_registry.py:19-78):

```python
"decommission_device_workflow":
    description="Safe guided workflow for decommissioning a homelab device"
    arguments=[hostname (string, required, "Hostname or IP of the device to decommission")]

"deploy_service_workflow":
    description="Pre-flight checked service deployment workflow"
    arguments=[
        service_name (string, required, "Name of the service to deploy"),
        target_host (string, required, "Target host for deployment"),
    ]

"homelab_health_check":
    description="Read all infrastructure resources and summarize homelab state"
    arguments=[]

"connect_to_device":
    description="Step-by-step onboarding workflow for connecting a new device to the homelab"
    arguments=[hostname (string, required, "Hostname or IP address of the new device to onboard")]

"configure_host_fingerprint":  # already documented at line 1636-1651
    description="Conversational workflow for capturing per-host capability fingerprints (GPU passthrough state, Vulkan/CUDA versions, ZFS pool config, etc.) to enable Phase 39 changed-infrastructure drift detection."
    arguments=[hostname (string, required, "Hostname or IP of the device to configure fingerprint tracking for")]
```

Workflow narrative for each prompt builder (read these to write the step list):
- `_build_decommission_result` (prompt_registry.py:99-118): 5-step workflow
- `_build_deploy_service_result` (prompt_registry.py:121-137): pre-flight + install + report
- `_build_connect_to_device_result` (prompt_registry.py:140-169): 6-step onboarding
- `_build_health_check_result` (prompt_registry.py:222-236): 3-resource read + summary

Insertion point: docs/tool-reference.md:1636 (immediately above the existing
`### configure_host_fingerprint` heading). New prompts go in this order to match docs
conventions (alphabetical-ish, ordered by maturity):
1. connect_to_device
2. decommission_device_workflow
3. deploy_service_workflow
4. homelab_health_check
5. configure_host_fingerprint (already there)

IN-03/WR-03 tool entry update site (docs/tool-reference.md:358-369):
Current line 360 description says "deep-merges" — must be replaced with the same
"one level deep" / "REPLACE" wording Plan 01 Task 2 used in network_tools_schema.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add four missing MCP prompt entries to docs/tool-reference.md (IN-04)</name>
  <files>docs/tool-reference.md</files>

  <read_first>
    - docs/tool-reference.md (lines 1632-1651 — existing MCP Prompts section + configure_host_fingerprint entry as the structural template)
    - src/homelab_mcp/prompt_registry.py (lines 19-78 — HOMELAB_PROMPTS dict for verbatim names + descriptions + argument metadata)
    - src/homelab_mcp/prompt_registry.py (lines 99-236 — the four prompt builder functions for workflow narrative)
    - .planning/phases/38-sitemap-fingerprint-schema/38-REVIEW.md (IN-04 section, lines ~301-314 — the original finding)
  </read_first>

  <action>
**Edit docs/tool-reference.md** — Insert the following four new sub-sections IMMEDIATELY ABOVE the existing `### configure_host_fingerprint` heading at line 1636 (i.e., between line 1635 blank and line 1636 `### configure_host_fingerprint`). Preserve the existing `## MCP Prompts` heading at line 1632 + intro paragraph at line 1634 unchanged. Append the four new entries verbatim:

```markdown
<!-- Executor note: the " ---" horizontal rules below carry a single leading space ONLY because plain "---" at column 0 inside this PLAN file confuses the YAML frontmatter parser. When you paste these blocks into docs/tool-reference.md, you MAY remove the leading space (Markdown renders both identically — leading-space-up-to-3 is still a valid horizontal rule). -->
### connect_to_device

**Description:** Step-by-step onboarding workflow for connecting a new device to the homelab. The prompt instructs the agent to:

1. Confirm the target host has an SSH-accessible user with sudo privileges.
2. Direct the user to run `homelab-mcp credentials add <hostname> <username>` (or `--key-path <path>` for key auth) to store the SSH credential in the OS keyring.
3. Call `register_server` to verify the stored credential end-to-end.
4. Call `ssh_discover` to collect hardware and system info into the database.
5. Call `discover_and_map` to add the device to the network sitemap.
6. Call `ssh_execute_command` with `sudo -n true` to confirm passwordless sudo for the registered user.

**Argument:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| hostname | string | Yes | Hostname or IP address of the new device to onboard |

**Related tools:** [`ssh_discover`](#ssh_discover), [`register_server`](#register_server), [`discover_and_map`](#discover_and_map), [`ssh_execute_command`](#ssh_execute_command).

 ---

### decommission_device_workflow

**Description:** Safe guided workflow for decommissioning a homelab device. The prompt instructs the agent to:

1. Call `get_network_sitemap` to find the target device's `device_id` by hostname match.
2. Call `decommission_device_preview` with the resolved `device_id` to preview the operation.
3. Present the preview result to the user and require explicit confirmation.
4. Only on confirmation: call `decommission_device` with the same `device_id`.
5. Report the result to the user.

The workflow refuses to proceed past step 3 without explicit user confirmation.

**Argument:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| hostname | string | Yes | Hostname or IP of the device to decommission |

**Related tools:** [`get_network_sitemap`](#get_network_sitemap), [`decommission_device_preview`](#decommission_device_preview), [`decommission_device`](#decommission_device).

 ---

### deploy_service_workflow

**Description:** Pre-flight checked service deployment workflow. The prompt instructs the agent to:

1. Call `ssh_discover` against the target host to verify SSH connectivity.
2. Call `get_service_status` to check whether the service is already installed on the target.
3. If pre-flight checks pass, call `install_service` to deploy.
4. Report the installation result to the user.

**Arguments:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| service_name | string | Yes | Name of the service to deploy |
| target_host | string | Yes | Target host for deployment |

**Related tools:** [`ssh_discover`](#ssh_discover), [`get_service_status`](#get_service_status), [`install_service`](#install_service).

 ---

### homelab_health_check

**Description:** Read all infrastructure resources and summarize homelab state. The prompt instructs the agent to read three MCP resources and produce a consolidated summary:

1. `homelab://vms` — list all VMs and containers.
2. `homelab://devices` — list all tracked network devices.
3. `homelab://drift/latest` — check for infrastructure drift.

The summary surfaces total VM count, total device count, drift status, and prominently flags any drifted items when `drift_detected` is true.

**Arguments:** None.

**Related tools / resources:** [`get_network_sitemap`](#get_network_sitemap), [`scan_infrastructure_drift`](#scan_infrastructure_drift), and the MCP resources `homelab://vms`, `homelab://devices`, `homelab://drift/latest`.

 ---

```

(Note: The trailing `---\n` separator above is REQUIRED — it visually separates `homelab_health_check` from the existing `configure_host_fingerprint` entry. The existing `### configure_host_fingerprint` heading at line 1636 is NOT modified; it remains the FIFTH and final entry in the section.)

Use Edit tool with the existing line `### configure_host_fingerprint` as the unique anchor for `old_string` so Insert lands at the correct line. Do NOT regenerate or rewrite the file. Per D-XX traceability: this implements **IN-04**.
  </action>

  <verify>
    <automated>cd C:/Users/washy/projects/mcp_python_server &amp;&amp; bash -c "set -e; for p in connect_to_device decommission_device_workflow deploy_service_workflow homelab_health_check configure_host_fingerprint; do count=\$(grep -c \"^### \$p\$\" docs/tool-reference.md); test \$count -eq 1 || { echo \"FAIL: ### \$p heading count=\$count (expected 1)\"; exit 1; }; done; echo 'All 5 prompt headings present exactly once'"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^### connect_to_device$" docs/tool-reference.md` returns `1`
    - `grep -c "^### decommission_device_workflow$" docs/tool-reference.md` returns `1`
    - `grep -c "^### deploy_service_workflow$" docs/tool-reference.md` returns `1`
    - `grep -c "^### homelab_health_check$" docs/tool-reference.md` returns `1`
    - `grep -c "^### configure_host_fingerprint$" docs/tool-reference.md` returns `1` (was already 1; must remain 1, not duplicated)
    - `grep -c "^## MCP Prompts$" docs/tool-reference.md` returns `1` (the section heading was not duplicated)
    - `grep -A1 "^### homelab_health_check$" docs/tool-reference.md | grep -c "summarize homelab state"` returns at least 1 (proves the verbatim description from prompt_registry.py landed)
    - `grep -A1 "^### connect_to_device$" docs/tool-reference.md | grep -c "Step-by-step onboarding workflow"` returns at least 1
    - `grep -A1 "^### deploy_service_workflow$" docs/tool-reference.md | grep -c "Pre-flight checked service deployment workflow"` returns at least 1
    - `grep -A1 "^### decommission_device_workflow$" docs/tool-reference.md | grep -c "Safe guided workflow for decommissioning"` returns at least 1
  </acceptance_criteria>

  <done>
    All five MCP prompt sub-sections exist in tool-reference.md under the `## MCP Prompts` heading, in the documented order: connect_to_device → decommission_device_workflow → deploy_service_workflow → homelab_health_check → configure_host_fingerprint. Each carries the verbatim `prompt_registry.py` description, the argument table, the workflow step list, and a related-tools cross-link.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync update_device_fingerprint tool entry wording with Plan 01 schema rewrites (IN-03 + WR-03 docs surface)</name>
  <files>docs/tool-reference.md</files>

  <read_first>
    - docs/tool-reference.md (lines 358-380 — current `### update_device_fingerprint` tool entry; check whether `### update_device_fingerprint_preview` exists in the file too via grep)
    - src/homelab_mcp/tool_schemas/network_tools_schema.py (lines 105-145 AFTER Plan 01 Task 2 has rewritten them — these are the source of truth for docs wording)
    - .planning/phases/43-phase-38-documentation-cleanup/43-01-SUMMARY.md (confirm Plan 01 completed and Task 2 actually rewrote the schema descriptions)
  </read_first>

  <action>
**Edit 1 — docs/tool-reference.md:360** — Replace the `### update_device_fingerprint` description paragraph (currently ends with "...for the conversational workflow that drives this tool.") with EXACTLY:

```markdown
**Description:** Merge fingerprint data (kernel, OS, package digest, capabilities) into a device's sitemap row. Top-level keys (`kernel_name`, `kernel_version`, `os_name`, `os_version`, `package_fingerprint`) overwrite (last-write-wins). The `capabilities` sub-dict updates **one level deep** — incoming top-level capability keys REPLACE the stored entry entirely (NOT a recursive merge), so callers updating any field within a capability must pass the full capability dict. Run `discover_and_map` first to add the device to the sitemap. See the [`configure_host_fingerprint`](#configure_host_fingerprint) MCP prompt for the conversational workflow that drives this tool. Persists to DB and bumps `updated_at`; `last_seen` is preserved (Phase 38 REVIEW-FIX WR-03).
```

**Edit 2 — docs/tool-reference.md** — Search for `### update_device_fingerprint_preview`. If it exists, replace its `**Description:**` paragraph EXACTLY with:

```markdown
**Description:** Preview the merge result of `update_device_fingerprint` without persisting. Returns the would-be merged fingerprint dict using the same merge rules: top-level overwrite + `capabilities` one-level overwrite (incoming capability keys replace stored entries entirely; not recursive). Read-only — no DB write, no `last_seen` or `updated_at` mutation. Phase 38 D-05c.
```

If `### update_device_fingerprint_preview` does NOT exist as a heading in tool-reference.md (search via `grep -n "### update_device_fingerprint_preview" docs/tool-reference.md` first), do NOT add it — that's a separate gap and out of scope for this plan. Document the absence in the SUMMARY.

Per D-XX traceability: this implements **IN-03 docs surface** + **WR-03 docs surface** — completes the cross-surface consistency invariant whose source-side half landed in Plan 01.
  </action>

  <verify>
    <automated>cd C:/Users/washy/projects/mcp_python_server &amp;&amp; bash -c "set -e; deep=\$(grep -c 'capabilities sub-dict deep-merges' docs/tool-reference.md); test \$deep -eq 0 || { echo \"FAIL: old 'deep-merges' wording still in docs/tool-reference.md (count=\$deep)\"; exit 1; }; new=\$(grep -c 'one level deep' docs/tool-reference.md); test \$new -ge 1 || { echo \"FAIL: new 'one level deep' wording missing from docs/tool-reference.md (count=\$new)\"; exit 1; }; persist=\$(grep -c 'bumps .updated_at.; .last_seen. is preserved' docs/tool-reference.md); test \$persist -ge 1 || { echo \"FAIL: WR-03 persist-side note missing\"; exit 1; }; echo 'IN-03/WR-03 docs wording synced'"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "capabilities sub-dict deep-merges" docs/tool-reference.md` returns `0` (old IN-03 wording removed)
    - `grep -c "one level deep" docs/tool-reference.md` returns at least `1` (new IN-03 wording present)
    - `grep -c "bumps .updated_at.; .last_seen. is preserved" docs/tool-reference.md` returns at least `1` (WR-03 persist-side note present; the regex uses `.` to match the backticks portably)
    - The `### update_device_fingerprint` heading at line 358 is unchanged (only the description paragraph below it is touched)
    - The Arguments table + Example block + Returns line below the description paragraph are unchanged
  </acceptance_criteria>

  <done>
    docs/tool-reference.md update_device_fingerprint entry uses the corrected one-level-overwrite wording matching Plan 01 Task 2's schema description rewrite. WR-03 persist-side note about updated_at/last_seen contract is present. If the preview tool entry exists, it carries the matching read-only/no-mutation wording.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final phase gate — pytest stays green, no source diff regression vs Plan 01</name>
  <files>(none — verification task only)</files>

  <read_first>
    - git log --oneline (last 5 commits — confirm Plan 01's commits landed before Plan 02's tool-reference.md edits)
    - docs/tool-reference.md (final state — visually scan the MCP Prompts section to confirm all 5 entries render as expected markdown)
  </read_first>

  <action>
Run the phase-final gate:

1. `uv run pytest -m "not integration" -q` — must exit 0 (no behavior change in either Plan 01 or Plan 02; test count matches Phase 42 baseline ≥907 passing).
2. `git diff --stat src/ docs/` — confirm only the 6 source files from Plan 01 + the single docs file from Plan 02 appear in the diff.
3. `grep -c "^### " docs/tool-reference.md` — capture the new total tool/prompt entry count (informational only, no assertion).
4. Visually open docs/tool-reference.md MCP Prompts section (lines ~1632 onward) and confirm the 5 entries render as a coherent section, separator-divided, with no orphaned `---` rules or missing blank lines.

If any gate fails, fix the underlying edit and re-run. Per D-XX traceability: this asserts the phase-level **no-behavior-change** invariant + the IN-04 **5 prompts documented** invariant.
  </action>

  <verify>
    <automated>cd C:/Users/washy/projects/mcp_python_server &amp;&amp; uv run pytest -m "not integration" -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -m "not integration" -q` exit code 0
    - `git diff --stat src/ docs/` shows exactly 7 files modified (6 from Plan 01 — but tool_annotations.py was NOT modified per Plan 01 Task 4, so actually 5 source files + 1 docs file = 6 total)
    - All 5 grep gates from Task 1 pass post-Plan-02
    - All 3 grep gates from Task 2 pass post-Plan-02
    - `git status` is clean (no untracked artifacts left in working tree besides the SUMMARY.md the executor will produce)
  </acceptance_criteria>

  <done>
    Phase 43 closes all 5 success criteria from ROADMAP. pytest stays green. The combined Plan 01 + Plan 02 diff is exclusively docstrings, comments, schema-description strings, the v1.8 sentinel comment, and docs prose — zero functional code change. Ready for `/gsd-verify-work 43`.
  </done>
</task>

</tasks>

<verification>
End-of-Plan-02 invariants:
- `grep -c "^### " docs/tool-reference.md` is 4 higher than pre-Plan-02 (4 new prompt sub-sections added)
- `grep -c "^## MCP Prompts$" docs/tool-reference.md` returns 1 (section heading not duplicated)
- All 5 prompt names appear exactly once as `^### <name>$`
- Old "deep-merges" wording purged from docs/tool-reference.md
- New "one level deep" + "bumps `updated_at`; `last_seen` is preserved" wording present
- pytest -m "not integration" exits 0
</verification>

<success_criteria>
Combined Plan 01 + Plan 02 closes all 5 ROADMAP success criteria:
- IN-03: merge_fingerprint docstring + 3 surface-text mirrors corrected (Plan 01 Tasks 1-2 + Plan 02 Task 2)
- WR-03: preview handler docstring + tool description note updated_at/last_seen contract (Plan 01 Task 3 + Plan 02 Task 2)
- IN-04: 4 missing MCP prompts documented in tool-reference.md (Plan 02 Task 1)
- WR-02: invariant asserted clean (Plan 01 Task 4)
- Dead-code: _resolve_ssh_credentials_with_binding annotated with v1.8 sentinel (Plan 01 Task 4)

Plus the no-behavior-change gate (Plan 01 Task 5 + Plan 02 Task 3): pytest green, ruff clean, mypy clean, only docstring/comment/string/markdown diffs.
</success_criteria>

<output>
After completion, create `.planning/phases/43-phase-38-documentation-cleanup/43-02-SUMMARY.md`. Document:
1. Confirmation that all 5 MCP prompt sub-sections exist in docs/tool-reference.md.
2. Whether `### update_device_fingerprint_preview` was found and updated, or noted-absent.
3. Final test count (must match Phase 42 baseline).
4. Cross-reference to 43-01-SUMMARY.md for the source-side half of IN-03/WR-03.
5. Phase-close note: ready for `/gsd-verify-work 43`.
</output>
