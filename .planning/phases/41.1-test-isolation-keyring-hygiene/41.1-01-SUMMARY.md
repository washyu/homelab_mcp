---
phase: 41.1
plan: 01
subsystem: testing
tags: [testing, keyring, forensic, red-tests]
requires: []
provides:
  - tests/test_keyring_isolation_forensic.py (Wave-0 forensic harness, deleted in Plan 02)
  - tests/test_keyring_isolation_phase41_1.py (permanent SC-1/SC-3/SC-4 unit tests, xfail-strict in Wave 0)
affects: []
tech-stack:
  added: []
  patterns:
    - mocker.patch side_effect for runtime traceback capture
    - pytest.mark.xfail(strict=True) for RED→GREEN flip discipline
key-files:
  created:
    - tests/test_keyring_isolation_forensic.py
    - tests/test_keyring_isolation_phase41_1.py
  modified: []
key-decisions:
  - "Gate the SC-1 Layer 2 round-trip behind a backend-identity check so the test does not briefly write to a working OS keyring during Wave 0 — keeps the xfail signal honest on Windows Credential Manager / macOS keychain."
requirements-completed: [SC-1, SC-2, SC-3, SC-4]
duration: ~12 min
completed: 2026-04-29
---

# Phase 41.1 Plan 01: Forensic + RED scaffolds Summary

Two-file Wave-0 deliverable. The forensic harness (`tests/test_keyring_isolation_forensic.py`) is a temporary diagnostic that uses `mocker.patch(..., side_effect=...)` plus `traceback.format_stack()` to log every `keyring.set_password` and `register_credential` call site to a file under `tmp_path`. The permanent scaffold file (`tests/test_keyring_isolation_phase41_1.py`) ships four `xfail(strict=True)` tests covering SC-1 Layer 1 / Layer 2, SC-3 (test-host/test-user leak), and SC-4 (snapshot helper).

- Duration: ~12 min
- Started: 2026-04-29T18:35:22Z
- Completed: 2026-04-29 (this run)
- Tasks: 2
- Files created: 2
- Files modified: 0

## Tasks completed

| # | Task | Commit |
|---|------|--------|
| 1 | Forensic instrumentation harness | 9b91e4c |
| 2 | RED scaffolds for SC-1/SC-3/SC-4 | d84dfcf |

## Forensic log paths (for Plan 02 to read)

The Wave-0 forensic harness ran successfully and printed two log file paths during the verification run:

- `keyring.set_password` log: `C:\Users\washy\AppData\Local\Temp\pytest-of-washy\pytest-211\test_keyring_set_password_trac0\keyring_set_password_calls.log`
- `register_credential` log: `C:\Users\washy\AppData\Local\Temp\pytest-of-washy\pytest-211\test_register_credential_trace0\register_credential_calls.log`

These are `tmp_path`-rooted, so they are gone now. **Plan 02 must re-run the forensic harness** (`uv run pytest tests/test_keyring_isolation_forensic.py -v -s --no-cov`) to read fresh tracebacks and confirm the `tests/test_sitemap.py:947` hypothesis (or identify any second leak source). The harness writes only `service:username` and tracebacks (no passwords).

The forensic harness only logs the seeded calls it drives explicitly — it does not yet hook into a full-suite run. To capture cross-suite leak sources, Plan 02 may need to extend the harness with a session-scope `autouse` instrumentation pass before the in-memory backend goes live, or rely on the `tests/test_sitemap.py:947` static hypothesis (PATTERNS.md confirmed).

## Wave-0 test posture

```
tests/test_keyring_isolation_forensic.py:    2 PASSED (instrumentation works)
tests/test_keyring_isolation_phase41_1.py:   4 XFAILED (strict, awaiting Wave 1)
Full unit suite:                             876 passed, 4 xfailed, 15 skipped
```

`test_set_password_routes_to_in_memory` was originally going to do an unguarded keyring round-trip; on Windows that XPASSes (Credential Manager works) and would briefly write `secret-A` to the real keychain. The deviation note in the task-2 commit message records the gate that prevents this.

## Deviations from Plan

**[Rule 1 - bug] test_set_password_routes_to_in_memory backend gate** — Found during: Task 2 verification. Issue: on Windows the test XPASS-strict-failed and would have written a real credential to Credential Manager (the very leak the phase prevents). Fix: prepend a `keyring.get_keyring()` backend-identity assertion so the round-trip is gated on the in-memory backend being active. Wave 0: gate fails → test fails RED as required. Wave 1: gate passes → round-trip executes → test PASSED. Files modified: `tests/test_keyring_isolation_phase41_1.py`. Verification: `uv run pytest tests/test_keyring_isolation_phase41_1.py -v --no-cov` reports `4 xfailed, 0 failed`. Commit: d84dfcf.

**Total deviations:** 1 auto-fixed (Rule 1). **Impact:** prevents a real OS keyring write during Wave-0 test runs on platforms where the backend is functional. Plan 02 still flips this test GREEN once the fixture installs the in-memory backend.

## Issues Encountered

None.

## Next Phase Readiness

Ready for Plan 02 (Wave 1).
