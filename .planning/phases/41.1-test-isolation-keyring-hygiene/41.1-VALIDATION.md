---
phase: 41.1
slug: test-isolation-keyring-hygiene
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-29
---

# Phase 41.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x (with pytest-asyncio, pytest-mock) |
| **Config file** | pyproject.toml (`[tool.pytest.ini_options]`) + tests/conftest.py |
| **Quick run command** | `uv run pytest tests/ -m "not integration" -x` |
| **Full suite command** | `uv run pytest tests/` |
| **Estimated runtime** | ~30s unit / ~3-5min full (integration adds Docker) |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/ -m "not integration" -x`
- **After every plan wave:** Run `uv run pytest tests/`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds (quick run target)

---

## Per-Task Verification Map

> Filled by gsd-planner during planning. Tasks must reference REQ-IDs from ROADMAP success criteria (SC-1..SC-4).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | 0 | SC-3 (leak source) | — | Forensic instrumentation logs traceback for `keyring.set_password` calls during full suite run | unit | `uv run pytest tests/test_keyring_isolation_forensic.py -v` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | SC-1 (session fixture) | — | `keyring.set_password` raises or routes to in-memory backend during any test run | unit | `uv run pytest tests/test_keyring_isolation_fixture.py -v` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | SC-4 (idempotent) | — | Pre/post snapshot of `~/.homelab_mcp/credential_registry.json` and keyring backend identity matches | unit | `uv run pytest tests/test_keyring_pre_post_snapshot.py -v` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | SC-2 (AST guard) | — | AST guard fails build when a `tests/` file calls `store_credential`/`register_credential` outside the allowlist | unit | `uv run pytest tests/test_ast_regression.py::TestPhase41_1KeyringHygiene -v` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | SC-3 (regression test) | — | Regression test asserts the `test-host`/`test-user` SSH entry never reaches the real keyring | unit | `uv run pytest tests/test_ssh_*.py -k test_host -v` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_keyring_isolation_forensic.py` — temporary instrumentation harness for SC-3 leak triage
- [ ] `tests/test_keyring_isolation_fixture.py` — assertions that the new session-autouse fixture is active and short-circuits `keyring.set_password`
- [ ] `tests/test_keyring_pre_post_snapshot.py` — pre/post snapshot helper used to validate SC-4
- [ ] `tests/test_ast_regression.py::TestPhase41_1KeyringHygiene` — new test class scaffold (skeleton only — filled in Wave 2)
- [ ] `tests/conftest.py` augmentation — session-autouse `_isolate_keyring` fixture (preserves existing Phase 39 drift fixtures)

*Existing pytest framework + conftest.py provide the infrastructure; new files above are the Wave 0 stubs the planner must include before the per-task verifications can run green.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| OS keyring entries unchanged after `uv run pytest tests/` on a clean dev machine | SC-4 | Cross-platform behavior (Windows Credential Manager, macOS keychain, Linux secret-service) cannot be uniformly enumerated in CI | 1) Snapshot keyring state via OS-native tooling. 2) Run full suite. 3) Diff snapshot. Should be empty. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
