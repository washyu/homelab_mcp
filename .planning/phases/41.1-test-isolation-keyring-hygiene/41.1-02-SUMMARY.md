---
phase: 41.1
plan: 02
subsystem: testing
tags: [testing, keyring, fixture, isolation]
requires: [41.1-01]
provides:
  - tests/conftest.py::_isolate_keyring (session-autouse, three-layer defence)
  - tests/conftest.py::_capture_real_state (snapshot helper)
  - SC-3 leak fix at tests/test_sitemap.py:947 (dual-alias _REGISTRY_PATH)
affects:
  - tests/test_keyring_isolation_phase41_1.py (xfail markers removed → GREEN)
  - tests/test_keyring_isolation_forensic.py (DELETED — diagnostic purpose served)
tech-stack:
  added: []
  patterns:
    - keyring.set_keyring(_InMemoryKeyring()) — backend swap
    - pytest.MonkeyPatch().setattr on keyring.{set,get,delete}_password — function-level monkeypatch
    - Dual-alias _REGISTRY_PATH redirect (homelab_mcp.* AND src.homelab_mcp.*)
    - sha256 file content snapshot pre/post for state-leak detection
key-files:
  created: []
  modified:
    - tests/conftest.py
    - tests/test_sitemap.py
    - tests/test_keyring_isolation_phase41_1.py
  deleted:
    - tests/test_keyring_isolation_forensic.py
key-decisions:
  - "Snapshot uses sha256 of registry file content — not mtime. Mtime is timezone-dependent on Windows and would create false positives across DST transitions."
  - "Snapshot tracks backend class NAME (not identity). The original backend is restored by reference in teardown, so identity could differ if `keyring.get_keyring()` re-initializes; class name is the stable invariant."
  - "Per-test dual-alias fix at test_sitemap.py:947 kept even though the session fixture covers it — explicit per-test patch lets the AST guard's allowlist citation reference a concrete pattern."
requirements-completed: [SC-1, SC-3, SC-4]
duration: ~10 min
completed: 2026-04-29
---

# Phase 41.1 Plan 02: Session-autouse fixture + leak fix Summary

Three-layer defence in `tests/conftest.py` shipped as a session-autouse fixture. Layer 1 swaps the keyring backend to a per-session `_InMemoryKeyring` via `keyring.set_keyring(...)`. Layer 2 monkeypatches `keyring.{set,get,delete}_password` so production code's lazy `import keyring` (credential_store.py:58) still resolves to the in-memory shim. Layer 3 redirects `_REGISTRY_PATH` on both `homelab_mcp.credential_store` AND `src.homelab_mcp.credential_store` aliases to a session-scoped `tmp_path_factory.mktemp(...)` directory.

Plus: the SC-3 leak source fix at `tests/test_sitemap.py:947` (dual-alias monkeypatch); removal of the four `xfail(strict=True)` markers from the Wave-0 scaffolds (now GREEN); deletion of the temporary forensic harness `tests/test_keyring_isolation_forensic.py`.

- Duration: ~10 min
- Tasks: 3
- Files modified: 3
- Files deleted: 1

## Tasks completed

| # | Task | Commit |
|---|------|--------|
| 1 | Add `_isolate_keyring` fixture + `_capture_real_state` helper | 1f9b387 |
| 2 | Dual-alias `_REGISTRY_PATH` fix at `test_sitemap.py:947` | 325f0c5 |
| 3 | Flip RED→GREEN + delete forensic harness | c836ef5 |

## Wave-0 leak source confirmation

The Wave-0 forensic logs (Plan 01 SUMMARY.md) ran but used `tmp_path` for storage — those tracebacks are gone. The static analysis hypothesis from PATTERNS.md held: `tests/test_sitemap.py:947` is the only leak site that needed a dual-alias fix. No second leaker was found in the test suite during Wave 1 verification (all ten tests of the changed test_sitemap module pass under the session fixture, and the SC-4 pre/post hash assertion is GREEN at session teardown).

## SC-4 clean-machine invariant verified live

Pre-run / post-run sha256 of `~/.homelab_mcp/credential_registry.json`:

```
pre-run sha256:  ca7cda74a1f9e4d1a0a1e4df75a0f4a37f4955126690785efe3f2c4a130b9e77
post-run sha256: ca7cda74a1f9e4d1a0a1e4df75a0f4a37f4955126690785efe3f2c4a130b9e77
```

Identical. The full unit suite (878 passed, 15 skipped) leaves the registry byte-identical. The `_isolate_keyring` teardown's `assert pre == post` did not trip during the run, providing in-process confirmation alongside the external sha256 capture.

## Files where dual-alias `_REGISTRY_PATH` monkeypatch is applied

For Plan 03's allowlist construction, the canonical dual-alias pattern is in:

- `tests/conftest.py::_isolate_keyring` (Layer 3, session scope)
- `tests/integration/test_credential_binding_round_trip.py:60-90` (existing pre-Phase-41.1 pattern)
- `tests/test_sitemap.py:947` (Phase 41.1 fix)

No other test files needed dual-alias fixes — the forensic harness was the only other suspected location and it is now deleted.

## Wave-1 test posture

```
tests/test_keyring_isolation_phase41_1.py: 4 PASSED (was 4 xfailed in Wave 0)
tests/test_sitemap.py:                     33 PASSED (sitemap suite unchanged)
Full unit suite:                           878 passed, 15 skipped, 0 xfailed
Lint:                                      uv run ruff check tests/ → clean
SC-4 invariant:                            sha256 pre == post (verified live)
```

## Deviations from Plan

**[Rule 1 - bug] Auxiliary ruff format normalization in tests/test_sitemap.py** — Found during: Task 2 commit. Issue: pre-commit's `ruff format` hook normalized two unrelated blocks (removed unused `import asyncio` from `test_eligibility_field_phase381`, reformatted a dict literal and a function signature) when the SC-3 fix was committed. Fix: accepted the reformat as it is non-behavioral and pre-commit's standard pipeline. Files modified: `tests/test_sitemap.py` (incidental). Verification: 33 sitemap tests still pass. Commit: 325f0c5. Out of strict task scope but cleanup is harmless and the pre-commit hook owns this normalization.

**Total deviations:** 1 auto-fixed (Rule 1). **Impact:** none on behavior; tests still green; minor noise in the diff that the project's pre-commit pipeline owns by design.

## Issues Encountered

None.

## Next Phase Readiness

Ready for Plan 03 (Wave 2) — AST regression guard for SC-2.
