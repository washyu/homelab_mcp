---
phase: 41.1
phase_name: test-isolation-keyring-hygiene
status: clean
files_reviewed: 4
findings:
  critical: 0
  warning: 0
  info: 1
  total: 1
review_depth: standard
review_date: 2026-04-29
review_method: inline-orchestrator (gsd-code-reviewer subagent skipped per Smart App Control caveat on Win11)
---

# Phase 41.1 Code Review

## Scope

Phase 41.1 modified test infrastructure only. Zero `src/` files changed. Files reviewed:

- `tests/conftest.py` — added session-autouse `_isolate_keyring` fixture and `_capture_real_state` helper (additive, all Phase 39 fixtures byte-preserved)
- `tests/test_sitemap.py` — single-line dual-alias `_REGISTRY_PATH` fix at line 947 (plus pre-commit ruff-format normalization of unrelated blocks)
- `tests/test_keyring_isolation_phase41_1.py` — created in Plan 01 with `xfail-strict`, flipped GREEN in Plan 02
- `tests/test_ast_regression.py` — added `TestPhase41_1KeyringHygiene` class (4 sub-tests + frozenset allowlist)

## Findings

### Critical

None.

### Warning

None.

### Info

**[I-1] Optional polish — redundant per-test keyring monkeypatch**

The session-autouse `_isolate_keyring` fixture in `tests/conftest.py` now covers the entire keyring + registry surface for unit AND integration tests. The per-test setup inside `tests/integration/test_credential_binding_round_trip.py::_setup_isolated_environment` (lines 48-90) is a redundant overlay — its dual-alias `_REGISTRY_PATH` patch and per-test keyring shim are functional duplicates of the session fixture's Layers 2 + 3.

This is **not** a bug — belt-and-braces is harmless and mirrors RESEARCH.md's "Optional polish" note. Trimming would tighten the diff but is out of stated Phase 41.1 scope. No action recommended within this milestone; could be revisited as a tidy-up PR if the diff churn is acceptable.

## Quality Observations

- **Type hints**: All new code uses complete type annotations (`tuple[str, str]`, `Iterator[None]`, `frozenset[str]`, `pathlib.Path`).
- **Docstrings**: Each new function/class has a docstring citing the SC / pitfall it addresses, with cross-references to RESEARCH.md sections.
- **Allowlist hygiene**: The `TestPhase41_1KeyringHygiene._ALLOWLIST` frozenset has one citation comment per entry, matching the `DEFERRED_MCP_ADMIN_DEFAULT_FILES` precedent set in v1.6 Phase 33.1.
- **Stress-tested**: The AST guard's deny-by-default property was empirically verified (synthetic violation fired RED, cleanup restored GREEN — Plan 03 SUMMARY documents the run).
- **No production code modified**: `git diff --stat src/` returned 0 changed files. SC-1/SC-2/SC-3/SC-4 are entirely test-side infrastructure.
- **Pre-commit hooks**: All commits passed ruff/format/AST checks. Ruff format applied incidental normalization to `tests/test_sitemap.py` (unused-import removal + dict-literal/function-signature reformat) — recorded as Plan 02 Rule 1 deviation.
- **Tests**: Full unit suite (882 passed, 15 skipped, 0 xfailed); SC-4 invariant verified live (sha256 pre == post on real `~/.homelab_mcp/credential_registry.json`).

## Threat-Model Cross-Check

| Threat | Disposition | Pinned by |
|--------|-------------|-----------|
| T-41.1-01 (forensic file lingers as unprotected caller) | mitigated | Plan 02 deletes file; Plan 03 guard does not allowlist it |
| T-41.1-02 (allowlist becomes stale escape hatch) | mitigated | Each entry carries a one-line citation comment; deny-by-default + non-vacuity floor test |
| T-41.1-03 (in-memory shim leaks across sessions) | mitigated | Fixture state is per-session; teardown restores original backend; pre/post sha256 catches escapes |
| T-41.1-04 (tmp_path_factory accumulating dirs) | accepted | pytest-managed; one extra session dir per run is negligible |
| T-41.1-05 (`_InMemoryKeyring` exposes `_store` to production) | accepted | Backend's public API is the three `KeyringBackend` ABC methods; production code does not inspect `_store` |
| T-41.1-06 (aliased imports defeat AST guard) | mitigated | `test_guarded_symbols_not_aliased_in_tests` blocks `as <foo>` rebinding |
| T-41.1-07 (eager `from keyring import set_password`) | mitigated | `test_no_eager_keyring_function_imports` blocks function-level eager binding |
| T-41.1-08 (`getattr(...)`-indirected calls) | accepted | Out of scope per Phase 39.1 precedent (T-39.1-02); code-smell caught at review |

## Verdict

`clean` — no critical or warning findings. One non-blocking info note (optional polish that is explicitly out of scope per the plan).
