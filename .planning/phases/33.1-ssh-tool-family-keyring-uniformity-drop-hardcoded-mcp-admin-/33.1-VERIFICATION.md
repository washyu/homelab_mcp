---
phase: 33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-
verified: 2026-04-23T04:05:26Z
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
---

# Phase 33.1: SSH Tool Family Keyring Uniformity Verification Report

**Phase Goal:** Finish the Phase 33 keyring-only migration across the remaining SSH tool surface — no function or schema advertises a hardcoded mcp_admin default, no MCP tool schema exposes a password property, `update_mcp_admin_groups` and `verify_mcp_admin_access` are removed, and a caller providing only hostname resolves both username and password from the keyring registry (with an actionable error on ambiguous multi-user registration).

**Verified:** 2026-04-23T04:05:26Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (mapped to CONTEXT decisions D-01..D-13)

| # | Decision | Truth | Status | Evidence |
|---|----------|-------|--------|----------|
| 1 | D-01 | `discover_and_map` schema has no `password` property | VERIFIED | `network_tools_schema.py` — `NETWORK_TOOLS['discover_and_map']['inputSchema']['properties']` has keys `hostname, username, key_path, port` only (lines 11–24). `password` absent. |
| 2 | D-01 | `bulk_discover_and_map.targets[]` schema has no `password` property | VERIFIED | `network_tools_schema.py` — `targets.items.properties` has keys `hostname, username, key_path, port` only (lines 40–44). `password` absent. |
| 3 | D-02 | `update_mcp_admin_groups` schema has no `password` property (superseded by D-05) | VERIFIED | Entire `update_mcp_admin_groups` entry absent from `SSH_TOOLS`. Remaining keys: `ssh_discover, ssh_execute_command, start_interactive_shell`. |
| 4 | D-03 | `discover_and_map.username` has no `default: 'mcp_admin'` | VERIFIED | `network_tools_schema.py` line 12–15 — no `default` key on `username`. |
| 5 | D-03 | `bulk_discover_and_map.targets[].username` has no `default: 'mcp_admin'` | VERIFIED | `network_tools_schema.py` line 42 — `"username": {"type": "string"}` with no default. |
| 6 | D-04, D-04a | Resolver registry-scan when `username=None` — single-match injects from keyring; multi-match raises actionable error naming users and `list_keyring_credentials`; zero-match raises `CredentialNotFoundError` | VERIFIED | Live behavioral spot-check: single-match returned `alice`/`secret`; multi-match raised `"Multiple credentials registered for h: alice, bob. Specify username explicitly, or call list_keyring_credentials..."`; zero-match raised `"No credentials found for h. Run `homelab-mcp credentials add h <username>`..."`. Tier-1 (explicit password) branch also performs registry-scan (no `or "mcp_admin"` fallback). See `ssh_tools.py:40–189` (helper + two tiers) and `tests/test_ssh_credentials.py::TestResolveSSHCredentials` (14/14 green). |
| 7 | D-05, D-05a | `update_mcp_admin_groups` and `verify_mcp_admin_access` removed from MCP tool surface (schema + handler + dispatch + annotation list + annotation dict + openapi allow-list + openapi category + ssh_tools module) | VERIFIED | 7-way import-time parity assertion passes; `grep update_mcp_admin_groups\|verify_mcp_admin` across `src/homelab_mcp/` returns empty. `len(get_available_tools()) == 51` (down from 53). |
| 8 | D-05b | `connect_to_device` prompt Step 6 no longer names a removed tool | VERIFIED | `prompt_registry.py:146` — `"6. Call ssh_execute_command with hostname=... and command=\"sudo -n true\" to confirm the registered user has passwordless sudo."`. No `verify_mcp_admin` reference. |
| 9 | D-05c | Tool count decreased (53 → 51) | VERIFIED | `len(get_available_tools()) == 51`. Schema/annotation parity preserved (`tests/test_tools.py::test_get_available_tools` passes with hardcoded expectation 51). |
| 10 | D-06 | `sitemap.discover_and_store` no longer has `username="mcp_admin"` default | VERIFIED | `sitemap.py:315` — `username: str \| None = None`. `inspect.signature` confirms `parameters["username"].default is None`. |
| 11 | D-07, D-07a | `sitemap.bulk_discover_and_store` uses `target.get("username")` with no `"mcp_admin"` fallback | VERIFIED | `sitemap.py:366` — `target.get("username")` (no second positional arg). `grep -c mcp_admin src/homelab_mcp/sitemap.py` returns 0. |
| 12 | D-08, D-09, D-10 | AST meta-tests installed: function-signature scan, TOOLS-dict recursive scan, forbidden-strings extension | VERIFIED | `tests/test_ast_regression.py` contains `DEFERRED_MCP_ADMIN_DEFAULT_FILES` (2 entries), `ALLOWED_PASSWORD_TOOLS` (13 entries), `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS` (9 entries), `FORBIDDEN_SOURCE_STRINGS` includes both removed identifiers. All 5 tests in file pass under `uv run pytest`. |
| 13 | D-12, D-13 | Docstring sweep / sanity grep clean of misleading mcp_admin copy in Phase-33.1-scope files | VERIFIED | `discover_and_map.username.description` no longer claims "Defaults to 'mcp_admin'". `sitemap.py` has zero `mcp_admin` references. Phase-33-legitimate residuals (`mcp_admin_key` file path, `mcp_admin@homelab` SSH key comment, `ssh_execute_command` passwordless-sudo branch) and Phase-33.2-deferred sites (`ssh_connection.py`, `service_installer.py`, `service_tools_schema.py`, `infrastructure_crud.py`, `vm_operations.py`) are documented in Plan 03 SUMMARY and covered by allowlists. |

**Score:** 13/13 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/homelab_mcp/ssh_tools.py` | `_resolve_username_from_registry` helper; `resolve_ssh_credentials` registry-scan in both tiers; no `or "mcp_admin"` fallback; no `update_mcp_admin_groups`/`verify_mcp_admin_access` functions | VERIFIED | Helper at lines 40–74; Tier-1 branch calls helper on lines 109–112; Tier-2 branch calls helper on line 126. `grep 'or "mcp_admin"'` empty. `hasattr(module, 'update_mcp_admin_groups')` = False. |
| `src/homelab_mcp/sitemap.py` | `username: str \| None = None` default; `target.get("username")` no fallback; no `mcp_admin` references anywhere | VERIFIED | Line 315 signature, line 366 bulk extraction; `grep mcp_admin` returns 0 hits. |
| `src/homelab_mcp/tool_schemas/network_tools_schema.py` | no `password` property, no `default: "mcp_admin"` anywhere | VERIFIED | Both schemas cleaned; description copy sanitized. |
| `src/homelab_mcp/tool_schemas/ssh_tools_schema.py` | `update_mcp_admin_groups` entry removed; `verify_mcp_admin` entry removed | VERIFIED | Remaining keys: `ssh_discover, ssh_execute_command, start_interactive_shell`. |
| `src/homelab_mcp/tool_handlers/__init__.py` | dispatch entries for removed tools absent | VERIFIED | `grep` returns empty. |
| `src/homelab_mcp/tool_handlers/ssh_handlers.py` | `handle_update_mcp_admin_groups` + `handle_verify_mcp_admin` + their imports absent | VERIFIED | `grep` returns empty. |
| `src/homelab_mcp/tool_annotations.py` | `verify_mcp_admin` removed from `_READ_ONLY_TOOLS` list AND `update_mcp_admin_groups` removed from `_MUTATING_ANNOTATIONS` dict (two distinct shapes, BLOCKER 2) | VERIFIED | `_READ_ONLY_TOOLS` lines 23–52 omit `verify_mcp_admin`; `_MUTATING_ANNOTATIONS` lines 78–... omit `update_mcp_admin_groups` (goes from `bulk_discover_and_map` straight to `ssh_execute_command`). |
| `src/homelab_mcp/openapi_app.py` | allow-list entries for removed tools absent | VERIFIED | `grep` returns empty. |
| `src/homelab_mcp/prompt_registry.py` | `connect_to_device` Step 6 rewritten to `ssh_execute_command ... sudo -n true` | VERIFIED | Line 146–148 matches plan-specified text verbatim. |
| `tests/test_ast_regression.py` | D-08/D-09/D-10 guards with narrow-scope allowlists (`DEFERRED_MCP_ADMIN_DEFAULT_FILES`, `ALLOWED_PASSWORD_TOOLS`, `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS`); module docstring documents 33.2 scope | VERIFIED | File 386 lines; 5 tests collect; all pass; allowlists match plan shape (+1 `create_proxmox_lxc` documented inline as semantic exception). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `sitemap.discover_and_store` | `ssh_tools.ssh_discover_system` | pass-through `username` (None when omitted) | WIRED | `sitemap.py:330` — `await ssh_discover_system(hostname, username, password, key_path, port)`; `username` flows unchanged. |
| `sitemap.bulk_discover_and_store` | `sitemap.discover_and_store` | `target.get("username")` (None when key missing) | WIRED | `sitemap.py:366`. |
| `ssh_tools.resolve_ssh_credentials` | `credential_store.list_credentials` | module-level import `from .credential_store import get_credential, list_credentials` | WIRED | `ssh_tools.py:11` (combined-line import); called at `_resolve_username_from_registry` line 59 and resolver line 130. Pytest monkeypatch on `homelab_mcp.ssh_tools.list_credentials` works (14 resolver tests pass). |
| `ssh_tools.resolve_ssh_credentials` Tier-1 (explicit password + username=None) | `_resolve_username_from_registry` | direct call | WIRED | Lines 109–112. Spot-check confirmed: explicit `password="explicit-pw"` + `username=None` returns `alice` (keyring-registered) with the explicit password honored. |
| `ssh_tools.resolve_ssh_credentials` Tier-2 | `_resolve_username_from_registry` | direct call when `username is None` | WIRED | Line 126. Spot-check confirmed: single-match returns alice/secret; ambiguous/zero match raise actionable errors. |
| `tool_handlers.TOOL_HANDLERS` | `SSH_TOOLS` | parity by tool name | WIRED | 7-way parity assertion: every tool in `SSH_TOOLS` has a handler, annotation, openapi entry, and ssh_tools function. `update_mcp_admin_groups` and `verify_mcp_admin` absent from all registries simultaneously. |
| `prompt_registry._build_connect_to_device_result` Step 6 | `SSH_TOOLS["ssh_execute_command"]` | prompt text names a live tool | WIRED | Prompt references `ssh_execute_command` which exists in SSH_TOOLS. `test_connect_to_device_prompt` passes with positive assertions for `"ssh_execute_command"` and `"sudo -n true"`. |
| `tests/test_ast_regression.py::test_no_forbidden_strings_in_source` | `src/homelab_mcp/**/*.py` | AST walk over all .py files; intersect with `FORBIDDEN_SOURCE_STRINGS` | WIRED | Test passes (GREEN) — confirms no `update_mcp_admin_groups` / `verify_mcp_admin_access` identifiers remain in `src/`. |
| `tests/test_ast_regression.py::test_no_username_mcp_admin_default_in_function_signatures` | `src/homelab_mcp/**/*.py` minus `DEFERRED_MCP_ADMIN_DEFAULT_FILES` | AST walk over FunctionDef/AsyncFunctionDef | WIRED | Test passes (GREEN) — confirms no `username='mcp_admin'` defaults in scope. |
| `tests/test_ast_regression.py::test_no_password_or_mcp_admin_default_in_tool_registries` | all `*_TOOLS` dicts minus allowlists | `pkgutil.iter_modules` + recursive traversal | WIRED | Test passes (GREEN) — confirms no password property / mcp_admin default in Phase-33.1-scope tools. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `resolve_ssh_credentials` | `SSHCredentials.username` (when caller omits) | `list_credentials(credential_type="ssh")` → registry entries → `_resolve_username_from_registry` | Yes — live spot-check patched registry returned alice and credential flowed through; end-to-end test `test_discover_and_store_resolves_username_from_keyring` verifies full stack `discover_and_store → ssh_discover_system → resolve_ssh_credentials → ssh_connect` receives keyring-registered user | FLOWING |
| `resolve_ssh_credentials` | `SSHCredentials.password` (Tier-2) | `get_credential(hostname, username, credential_type="ssh")` → real keyring value | Yes — monkeypatched return flows through resolver to SSHCredentials dataclass | FLOWING |
| `sitemap.discover_and_store` | `username` parameter (None when omitted) | Caller (MCP tool layer); flows to `ssh_discover_system` → `resolve_ssh_credentials` | Yes — end-to-end test proves full stack | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Tool count reduced to 51 | `len(get_available_tools())` | `51` | PASS |
| 7-way parity (removed tools absent everywhere) | Python assertion across `SSH_TOOLS`, `TOOL_HANDLERS`, `TOOL_ANNOTATIONS`, `_SSH_TOOLS_WITH_HOSTNAME`, `TOOL_CATEGORIES['SSH']`, `homelab_mcp.ssh_tools` module attrs | "7-way parity OK" | PASS |
| Resolver registry-scan single-match (D-04) | Patched `list_credentials` + `get_credential`; call `resolve_ssh_credentials(hostname='h', username=None)` | `SSHCredentials(username='alice', password='secret', ...)` | PASS |
| Resolver ambiguous-match (D-04a) | Patched `list_credentials` with two users; call resolver with `username=None` | `CredentialNotFoundError`: `"Multiple credentials registered for h: alice, bob. Specify username explicitly, or call list_keyring_credentials to inspect registered entries."` | PASS |
| Resolver Tier-1 no silent fallback (BLOCKER 1) | Patched empty registry; call with `password='p', username=None` | `CredentialNotFoundError` raised (no silent `mcp_admin` injection) | PASS |
| `sitemap.discover_and_store` signature default | `inspect.signature(discover_and_store).parameters['username'].default` | `None` | PASS |
| AST regression suite | `uv run pytest tests/test_ast_regression.py -v` | 5 passed in 1.06s | PASS |
| Resolver test suite | `uv run pytest tests/test_ssh_credentials.py::TestResolveSSHCredentials -v` | 14 passed in 0.17s | PASS |
| MCP prompts test suite | `uv run pytest tests/test_mcp_prompts.py -v` | 12 passed in 0.48s | PASS |
| Sitemap keyring end-to-end | `uv run pytest tests/test_sitemap.py::TestAsyncFunctions::test_discover_and_store_resolves_username_from_keyring -v` | 1 passed | PASS |
| Full unit test sweep | `uv run pytest tests/ -m "not integration" -q` | 673 passed, 2 pre-existing unrelated failures, 9 skipped | PASS (with documented pre-existing failures) |

### Requirements Coverage

Phase 33.1 declares no REQ-IDs (driven by CONTEXT decisions D-01..D-13). REQUIREMENTS.md does not map this phase. Coverage measured against the 13 decisions is 13/13 (see Observable Truths table above).

### Anti-Patterns Found

None in Phase-33.1-scope files.

Residual `mcp_admin` hits documented and deferred to Phase 33.2 (not blockers):

| File | Lines / Hits | Category | Disposition |
|------|--------------|----------|-------------|
| `src/homelab_mcp/ssh_connection.py` | line 162: `username: str = "mcp_admin"` | Phase-33.2-deferred | Covered by `DEFERRED_MCP_ADMIN_DEFAULT_FILES` allowlist |
| `src/homelab_mcp/service_installer.py` | 8 function-signature defaults | Phase-33.2-deferred | Covered by `DEFERRED_MCP_ADMIN_DEFAULT_FILES` allowlist |
| `src/homelab_mcp/tool_schemas/service_tools_schema.py` | 9 tool schemas with username default + password | Phase-33.2-deferred | Covered by `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS` + `ALLOWED_PASSWORD_TOOLS` |
| `src/homelab_mcp/infrastructure_crud.py:30` | dict literal `"username": "mcp_admin"` | Phase-33.2-adjacent (documented by Plan 03 SUMMARY) | Documented for Phase 33.2 orchestrator — dict-literal form bypasses AST guard |
| `src/homelab_mcp/vm_operations.py:25` | dict literal `"username": "mcp_admin"` | Phase-33.2-adjacent (documented by Plan 03 SUMMARY) | Documented for Phase 33.2 orchestrator |
| `src/homelab_mcp/ssh_tools.py:479–501` | `ssh_execute_command` passwordless-sudo branch keyed on `creds.username == "mcp_admin"` | Phase-33-legitimate (intentional) | Out of 33.1 scope; harmless no-op when no mcp_admin user exists |
| `src/homelab_mcp/ssh_tools.py` | `mcp_admin_key` file path, `mcp_admin@homelab` SSH key comment, docstring historical references | Phase-33-legitimate (D-09a) | Expected residuals per Phase 33 D-09a / D-13 |

Info severity only. None of these affect Phase 33.1 goal achievement — all are explicitly deferred with allowlist coverage.

### Human Verification Required

None. All 13 decisions verified programmatically through a combination of:
- Static grep/AST assertions (D-01, D-02, D-03, D-05, D-12)
- Python import-time parity checks (D-05a, D-05c)
- Live behavioral spot-checks of the resolver (D-04, D-04a, BLOCKER 1)
- Prompt-text inspection (D-05b)
- End-to-end unit test traversing the full stack (D-06, D-07, D-07a)
- Meta-test installation and green state (D-08, D-09, D-10)
- Residual grep sweep (D-13)

### Gaps Summary

No gaps. Phase 33.1 goal fully achieved:

1. **No function or schema advertises a hardcoded `mcp_admin` default** (in 33.1 scope) — Plan 02 cleaned the 5 schema sites enumerated by D-01/D-02/D-03; Plan 04 removed both sitemap.py defaults (D-06/D-07); Plan 01 removed the runtime `or "mcp_admin"` fallback from the resolver; Plan 05 AST guard enforces this going forward within the documented narrow scope (Phase 33.2 allowlists preserve out-of-scope residuals).

2. **No MCP tool schema exposes a `password` property** (in 33.1 scope) — Plan 02 removed it from `discover_and_map`, `bulk_discover_and_map.targets[]`, and `update_mcp_admin_groups`. Three out-of-scope SSH tools and nine service tools retain `password` per the documented "Narrow + defer" decision, covered by `ALLOWED_PASSWORD_TOOLS` allowlist and tracked for Phase 33.2.

3. **`update_mcp_admin_groups` and `verify_mcp_admin_access` are removed** — Plan 03 lock-step deletion across 7 surfaces (schema + handler + dispatch + annotation list + annotation dict + openapi allow-list + openapi category + ssh_tools module). `tools/list` now returns 51 tools (down from 53). `connect_to_device` prompt Step 6 rewritten to call the generic `ssh_execute_command(command="sudo -n true")` instead of naming a removed tool.

4. **A caller providing only `hostname` resolves both `username` and `password` from the keyring registry** — Plan 01 added `_resolve_username_from_registry` used by both Tier-1 (explicit password) and Tier-2 (pure keyring) branches of `resolve_ssh_credentials`. Verified end-to-end by `test_discover_and_store_resolves_username_from_keyring` which monkeypatches only the keyring and SSH boundaries and proves the registered user flows all the way through `discover_and_store → ssh_discover_system → resolve_ssh_credentials → _resolve_username_from_registry → ssh_connect`.

5. **Actionable error on ambiguous multi-user registration** — The resolver raises `CredentialNotFoundError` with a message that names every registered user alphabetically sorted and references `list_keyring_credentials` so the agent can self-disambiguate. Verified by `test_resolve_with_no_username_ambiguous_match` and live spot-check.

The two pre-existing unit-test failures (`test_ssh_credentials_table_dropped_postgres`, `test_client_missing_host`) are out of Phase 33.1 scope; documented in Plan 01, Plan 02, Plan 03, and Plan 04 SUMMARYs as pre-existing on the branch baseline.

---

_Verified: 2026-04-23T04:05:26Z_
_Verifier: Claude (gsd-verifier)_
