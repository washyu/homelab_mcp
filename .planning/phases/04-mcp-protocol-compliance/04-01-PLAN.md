---
phase: 04-mcp-protocol-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/homelab_mcp/server.py
  - src/homelab_mcp/sitemap.py
  - src/homelab_mcp/infrastructure_crud.py
  - src/homelab_mcp/service_installer.py
  - tests/test_logging_notifications.py
autonomous: true
requirements:
  - MCP-03

must_haves:
  truths:
    - "Long-running operations (bulk discover, deploy, scale, install) emit MCP logging notifications with per-item progress"
    - "Client can set minimum log level via logging/setLevel and server respects it"
    - "emit_progress gracefully degrades outside request context (no crash in tests or startup)"
  artifacts:
    - path: "src/homelab_mcp/server.py"
      provides: "set_logging_level handler, emit_progress helper, LOG_LEVEL_ORDER, should_emit"
      contains: "set_logging_level"
    - path: "src/homelab_mcp/sitemap.py"
      provides: "Progress notifications in bulk_discover_and_store"
      contains: "emit_progress"
    - path: "src/homelab_mcp/infrastructure_crud.py"
      provides: "Progress notifications in deploy_infrastructure_plan and scale_infrastructure_services"
      contains: "emit_progress"
    - path: "src/homelab_mcp/service_installer.py"
      provides: "Progress notifications in install_service"
      contains: "emit_progress"
    - path: "tests/test_logging_notifications.py"
      provides: "Tests for logging capability, level filtering, emit_progress, handler integration"
      min_lines: 50
  key_links:
    - from: "src/homelab_mcp/server.py"
      to: "mcp.server.lowlevel.server.request_ctx"
      via: "emit_progress reads session from request_ctx contextvar"
      pattern: "request_ctx\\.get\\(\\)"
    - from: "src/homelab_mcp/sitemap.py"
      to: "src/homelab_mcp/server.py"
      via: "import emit_progress"
      pattern: "from.*server import.*emit_progress"
---

<objective>
Add MCP logging notification capability so long-running tool operations emit per-item progress messages to connected clients.

Purpose: MCP clients (Claude Desktop, OpenWebUI) can display progress during subnet scans, bulk deployments, and service installations instead of showing a spinner with no feedback.
Output: `set_logging_level` handler registered on server, `emit_progress` helper function, progress calls wired into 4 long-running handlers, full test coverage.
</objective>

<execution_context>
@/home/shaun/.claude/get-shit-done/workflows/execute-plan.md
@/home/shaun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mcp-protocol-compliance/04-RESEARCH.md

@src/homelab_mcp/server.py
@src/homelab_mcp/sitemap.py
@src/homelab_mcp/infrastructure_crud.py
@src/homelab_mcp/service_installer.py

<interfaces>
<!-- Key types and contracts the executor needs from the MCP SDK -->

From mcp.server.lowlevel.server:
```python
from mcp.server.lowlevel.server import request_ctx  # ContextVar for request context
# request_ctx.get() returns RequestContext with .session attribute (ServerSession)
```

From mcp.server.session (ServerSession):
```python
async def send_log_message(
    self,
    level: types.LoggingLevel,  # "debug"|"info"|"notice"|"warning"|"error"|"critical"|"alert"|"emergency"
    data: Any,                  # String, dict, or JSON-serializable value
    logger: str | None = None,
    related_request_id: types.RequestId | None = None,
) -> None: ...
```

From mcp.server.lowlevel.server (Server):
```python
@server.set_logging_level()
async def handler(level: types.LoggingLevel) -> None: ...
# Registering this handler enables logging capability in ServerCapabilities
```

Existing server.py exports:
```python
server = Server("homelab-mcp", version="0.2.0", lifespan=app_lifespan)
class ToolError(Exception): ...
def get_resource_manager() -> ResourceManager: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add set_logging_level handler, emit_progress helper, and tests</name>
  <files>src/homelab_mcp/server.py, tests/test_logging_notifications.py</files>
  <behavior>
    - Test: set_logging_level handler is registered (server capabilities include logging)
    - Test: emit_progress sends log message via session.send_log_message when in request context
    - Test: emit_progress does NOT raise when called outside request context (LookupError caught)
    - Test: should_emit returns True when level >= min_level, False when below
    - Test: emit_progress respects minimum log level (debug message skipped when min_level is "warning")
  </behavior>
  <action>
    1. In `src/homelab_mcp/server.py`, add after the existing imports:
       - `from mcp.server.lowlevel.server import request_ctx`
       - Add `LOG_LEVEL_ORDER` dict mapping syslog level names to integers (debug=0 through emergency=7)
       - Add module-level `_min_log_level: types.LoggingLevel = "info"`
       - Add `should_emit(level, min_level) -> bool` function
    2. Register `@server.set_logging_level()` handler that stores level in `_min_log_level`
    3. Add `emit_progress(level, message, data=None)` async helper:
       - Check `should_emit(level, _min_log_level)` first -- return early if below threshold
       - Try `request_ctx.get()` to get session
       - Call `session.send_log_message(level=level, data=data or message, logger="homelab-mcp")`
       - Catch `LookupError` silently (not in request context)
       - Catch `Exception` and log at debug level (don't crash tool handler for notification failure)
    4. Create `tests/test_logging_notifications.py` with tests for all behaviors above. Mock `request_ctx.get()` to return a mock session with async `send_log_message`. Test the LookupError path by having `request_ctx.get()` raise LookupError.
  </action>
  <verify>
    <automated>cd /home/shaun/projects/mcp_python_server && uv run pytest tests/test_logging_notifications.py -x -v</automated>
  </verify>
  <done>set_logging_level handler registered, emit_progress helper works with level filtering and graceful degradation, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire emit_progress into long-running tool handlers</name>
  <files>src/homelab_mcp/sitemap.py, src/homelab_mcp/infrastructure_crud.py, src/homelab_mcp/service_installer.py</files>
  <action>
    Add `from .server import emit_progress` to each file and insert `await emit_progress(...)` calls at these points:

    1. **sitemap.py** `bulk_discover_and_store`: At the start of each iteration over `targets`, emit `"info"` level: `f"Discovering {target['hostname']} ({i+1}/{len(targets)})"`. Add a completion message after the loop.

    2. **infrastructure_crud.py** `deploy_infrastructure_plan`: At the start of each service in the services loop, emit `"info"` level: `f"Deploying service {service['name']} ({i+1}/{n})"`. Same for the `network_changes` loop if present.

    3. **infrastructure_crud.py** `scale_infrastructure_services`: At the start of each scaling operation, emit `"info"` level: `f"Scaling {op['service_name']} ({i+1}/{n})"`.

    4. **service_installer.py** `install_service`: At the start of each installation step, emit `"info"` level: `f"Step {i+1}: {step_description}"`. Find the iteration pattern in the method and add notifications at meaningful milestones (not every micro-step).

    Use `enumerate()` for indices. Keep notifications to 1 per major iteration item -- do NOT add notifications inside nested loops or per-line output.
  </action>
  <verify>
    <automated>cd /home/shaun/projects/mcp_python_server && uv run pytest tests/ -m "not integration" -x --timeout=60</automated>
  </verify>
  <done>All 4 long-running handlers emit per-item progress notifications. Existing test suite passes (emit_progress is a no-op outside request context so no test breakage).</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_logging_notifications.py -x -v` -- all logging notification tests pass
2. `uv run pytest tests/ -m "not integration" -x` -- full unit suite passes (no regressions from wiring)
3. `uv run ruff check src/homelab_mcp/server.py src/homelab_mcp/sitemap.py src/homelab_mcp/infrastructure_crud.py src/homelab_mcp/service_installer.py` -- no lint errors
4. `grep -r "emit_progress" src/homelab_mcp/` confirms emit_progress present in server.py, sitemap.py, infrastructure_crud.py, service_installer.py
5. `grep "set_logging_level" src/homelab_mcp/server.py` confirms handler registered
</verification>

<success_criteria>
- set_logging_level handler registered on server (logging capability advertised)
- emit_progress helper with level filtering and graceful degradation
- 4 long-running handlers emit per-item progress notifications
- All unit tests pass including new test_logging_notifications.py
</success_criteria>

<output>
After completion, create `.planning/phases/04-mcp-protocol-compliance/04-01-SUMMARY.md`
</output>
