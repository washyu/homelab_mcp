---
phase: 36-drift-sitemap-foundation
plan: 06
type: execute
wave: 2
depends_on:
  - 04
files_modified:
  - docs/tool-reference.md
autonomous: true
requirements:
  - DRFT-11
must_haves:
  truths:
    - "docs/tool-reference.md contains a scan_infrastructure_drift entry that mentions iterating the sitemap"
    - "No speculative register_drift_baseline / list_drift_baselines / delete_drift_baseline references in docs/"
    - "The scan_infrastructure_drift doc entry does NOT mention PROXMOX_HOST"
  artifacts:
    - path: "docs/tool-reference.md"
      provides: "scan_infrastructure_drift documentation reflecting the 2-bucket sitemap-iteration model"
      contains: "scan_infrastructure_drift"
  key_links:
    - from: "docs/tool-reference.md scan_infrastructure_drift entry"
      to: "src/homelab_mcp/drift_detection.py + tool_schemas/drift_tools_schema.py"
      via: "Description text alignment with the 2-bucket interim shape and inert filter passthrough"
      pattern: "iterates the sitemap"
---

<objective>
Update `docs/tool-reference.md` to add (or update) a `scan_infrastructure_drift` entry that accurately describes the Phase 36 2-bucket sitemap-iteration model. Remove any speculative `register_drift_baseline` / `list_drift_baselines` / `delete_drift_baseline` mentions in `docs/` (Bug C — none expected per CONTEXT D-19, but verify and remove if present).

Purpose: D-19 narrow docs sweep. The wider PROXMOX_HOST sweep across `docs/configuration.md` / `docs/setup-guide.md` is explicitly Phase 37 territory (DRFT-15) — this plan touches only the drift tool entry.

Output: A `docs/tool-reference.md` with an accurate `scan_infrastructure_drift` description and zero speculative drift-baseline-tool references in any doc file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-drift-sitemap-foundation/36-CONTEXT.md
@.planning/phases/36-drift-sitemap-foundation/36-PATTERNS.md
@.planning/phases/36-drift-sitemap-foundation/36-04-SUMMARY.md
@docs/tool-reference.md

<interfaces>
<!-- The new scan_infrastructure_drift doc entry should align with the schema description -->
<!-- post-Plan 04 (Task 3 in Plan 04): -->
<!-- "Scan for infrastructure drift against the sitemap. Returns 2-bucket coverage report -->
<!--  (probed_ok, unreachable) per resolved Proxmox host. -->
<!--  Filter semantics under Phase 37 redesign — node/vm_type currently inert." -->

The sibling tool entry shape (analog from PATTERNS § docs/tool-reference.md — `### refresh_terraform_service` at ~line 956):

```markdown
### refresh_terraform_service

**Description:** Refresh Terraform state and detect configuration drift.

**Annotations:** `[Idempotent]`

**Arguments:**

| Name | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| ... | ... | ... | ... | ... |

**Example:**

```json
{
  ...
}
```

**Returns:** A dict with the refreshed state and any detected drift.
```

The new scan_infrastructure_drift entry to add (or update if present):

```markdown
### scan_infrastructure_drift

**Description:** Scan for infrastructure drift against the sitemap. Iterates registered devices in the network sitemap, resolves Proxmox credentials per row through the keyring (per-node → cluster → error), and probes each resolved host's `/cluster/status` endpoint. Returns a 2-bucket coverage report (`probed_ok`, `unreachable`) per host. Filter semantics for `node` and `vm_type` are under Phase 37 redesign and are currently inert — both arguments are accepted for back-compat but not yet acted upon.

**Annotations:** `[Read-only]`

**Arguments:**

| Name | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| node | string | No | (none) | Filter scope (Phase 37 will activate; currently inert) |
| vm_type | string | No | "all" | Filter scope: "qemu", "lxc", or "all" (Phase 37 will activate; currently inert) |

**Example:**

```json
{}
```

**Returns:**

```json
{
  "status": "success",
  "scan_timestamp": "2026-04-25T12:34:56+00:00",
  "scanned": 2,
  "probed_ok": [
    {
      "hostname": "pve1",
      "connection_ip": "10.0.0.10",
      "scope": "node",
      "cluster_name": null,
      "status": "probed-ok",
      "error": null,
      "scan_timestamp": "2026-04-25T12:34:56+00:00"
    }
  ],
  "unreachable": [
    {
      "hostname": "pi-lab",
      "connection_ip": "10.0.0.12",
      "scope": "cluster",
      "cluster_name": "homelab-prod",
      "status": "unreachable",
      "error": "Cannot connect to host pi-lab",
      "scan_timestamp": "2026-04-25T12:34:56+00:00"
    }
  ]
}
```

**Notes:**
- Baselines are not registered separately. The sitemap (populated by `discover_and_map`) serves as the baseline for drift detection. To add a host to drift coverage, register it via `discover_and_map`.
- Hosts without Proxmox credentials in the keyring are silently excluded (they are not Proxmox hosts).
- Empty sitemap returns a successful empty result (`scanned: 0`), not an error.
- Phase 37 will expand the response to a 4-bucket shape (probed-OK / unreachable / unknown / changed).
```

The grep-verifiable rule: NO `PROXMOX_HOST`, NO `register_drift_baseline`, NO `list_drift_baselines`, NO `delete_drift_baseline` strings appear in this doc entry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add or update scan_infrastructure_drift doc entry; sweep speculative drift-baseline tool mentions</name>
  <files>docs/tool-reference.md</files>
  <read_first>
    - docs/tool-reference.md (entire file — search for existing `scan_infrastructure_drift` section, search for `### refresh_terraform_service` pattern for sibling-entry style; identify which heading section drift-related tools live under)
    - .planning/phases/36-drift-sitemap-foundation/36-PATTERNS.md (§ docs/tool-reference.md — analog and phrasing guidance)
    - .planning/phases/36-drift-sitemap-foundation/36-CONTEXT.md (§D-19)
    - src/homelab_mcp/tool_schemas/drift_tools_schema.py (post-Plan 04 — verify the new schema description text the doc entry should align with)
  </read_first>
  <action>
**Step A — Determine entry status.**

Search `docs/tool-reference.md` for existing `scan_infrastructure_drift` content:

```bash
grep -n "scan_infrastructure_drift" docs/tool-reference.md
```

If the entry exists, **update** it. If it does not exist, **add** a new entry under an appropriate heading (e.g., `## Drift Tools` if such heading already exists, or under `## Infrastructure Tools` near `refresh_terraform_service`).

**Step B — Write the entry text.**

Use the entry template provided in `<interfaces>` above. Adapt the table arguments and example to match the actual MCP schema (post-Plan 04). The annotation should match what's set in `tool_annotations.py` for `scan_infrastructure_drift` (read that file briefly to confirm — likely `[Read-only]` or `[Idempotent]`).

**Critical phrasing requirements per D-19:**
- Description MUST mention "iterates the sitemap" (or equivalent — "iterates registered devices in the network sitemap" is acceptable)
- Description MUST NOT mention `PROXMOX_HOST`
- Description MUST NOT use "register a drift baseline" language (Bug C dissolution)
- Description MUST NOT reference `register_drift_baseline`, `list_drift_baselines`, or `delete_drift_baseline` tools
- Notes section SHOULD point users to `discover_and_map` for adding hosts to drift coverage (sitemap CRUD as baseline lifecycle, per CONTEXT D-17)

**Step C — Sweep for speculative drift-baseline tool mentions.**

```bash
grep -rn "register_drift_baseline\|list_drift_baselines\|delete_drift_baseline" docs/
```

If any matches appear, REMOVE them. Per CONTEXT D-19, none are expected — but Bug C says these tools were assumed but never built, so docs may have been written speculatively. Verify the result is ZERO matches across all of `docs/` after the sweep.

**Step D — Verify no PROXMOX_HOST mentions in the new entry.**

The wider `docs/configuration.md` / `docs/setup-guide.md` PROXMOX_HOST sweep is Phase 37 (DRFT-15) — out of scope here. Only verify that the NEW scan_infrastructure_drift entry contains zero PROXMOX_HOST mentions.

```bash
# Find the scan_infrastructure_drift section in tool-reference.md and verify clean:
grep -A 30 "### scan_infrastructure_drift" docs/tool-reference.md | grep -i "PROXMOX_HOST"
# Expected: ZERO matches
```

**Step E — Validation pass.**

Read the entry once more to ensure it accurately reflects:
- The 2-bucket return shape (probed_ok / unreachable)
- The inert filter passthrough (node / vm_type accepted but unused)
- The sitemap-as-baseline architectural decision
- Pointers to existing sitemap CRUD tools (discover_and_map) for baseline lifecycle
  </action>
  <verify>
    <automated>grep -n "### scan_infrastructure_drift" docs/tool-reference.md &amp;&amp; ! grep -rn "register_drift_baseline\|list_drift_baselines\|delete_drift_baseline" docs/</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "### scan_infrastructure_drift" docs/tool-reference.md` returns EXACTLY ONE match (the heading exists)
- `grep -A 50 "### scan_infrastructure_drift" docs/tool-reference.md | grep -i "iterates the sitemap\|iterates registered devices in the network sitemap"` returns AT LEAST ONE match (the description mentions sitemap iteration)
- `grep -A 50 "### scan_infrastructure_drift" docs/tool-reference.md | grep -i "PROXMOX_HOST"` returns ZERO matches (no env var pointer)
- `grep -A 50 "### scan_infrastructure_drift" docs/tool-reference.md | grep -i "register a drift baseline\|register_drift_baseline\|list_drift_baselines\|delete_drift_baseline"` returns ZERO matches
- `grep -A 50 "### scan_infrastructure_drift" docs/tool-reference.md | grep -E "probed_ok|unreachable"` returns AT LEAST 2 matches (the 2-bucket shape is documented)
- `grep -rn "register_drift_baseline\|list_drift_baselines\|delete_drift_baseline" docs/` returns ZERO matches across all of docs/
- The doc file is well-formed Markdown (no broken table rows, no unclosed code blocks)
  </acceptance_criteria>
  <done>
docs/tool-reference.md contains an accurate scan_infrastructure_drift entry. Zero speculative drift-baseline-tool mentions across docs/. Description aligns with the post-Plan 04 schema description.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → user expectations | Outdated documentation can mislead users into expecting tools that don't exist (Bug C class) or behaviors that no longer hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06-01 | Information Disclosure | Documentation could leak internal architectural details (e.g., the Phase 37 plan content) | accept | Mentioning "Phase 37 will expand" is intentional and helpful — it sets user expectations. No security-sensitive disclosure |
| T-36-06-02 | Tampering | Documentation drift from actual schema → user confusion → support burden | mitigate | The Phase 36 D-19 sweep is narrow; Phase 37 (DRFT-15) handles the wider docs/configuration.md / setup-guide.md PROXMOX_HOST sweep. Plan 06 ensures the drift tool entry specifically is accurate post-refactor |
| T-36-06-03 | Spoofing | None — documentation has no authentication surface | accept | No code paths affected |
</threat_model>

<verification>
After this plan:

1. `grep -n "### scan_infrastructure_drift" docs/tool-reference.md` returns exactly one heading.
2. `grep -rn "register_drift_baseline\|list_drift_baselines\|delete_drift_baseline" docs/` returns zero matches across the entire docs/ directory.
3. The doc entry's described return shape matches the actual `scan_drift` return shape from Plan 04.
4. Markdown lint (if configured in pre-commit) passes — no broken tables / unclosed code fences.
</verification>

<success_criteria>
- `docs/tool-reference.md` contains a `scan_infrastructure_drift` entry that accurately describes the 2-bucket sitemap-iteration model.
- The entry mentions "iterates the sitemap" (or equivalent).
- The entry does NOT mention `PROXMOX_HOST`.
- The entry does NOT reference any of the speculative `*_drift_baseline` MCP tools.
- The Notes section points users to `discover_and_map` for baseline lifecycle.
- Zero speculative drift-baseline-tool references across all of `docs/`.
</success_criteria>

<output>
After completion, create `.planning/phases/36-drift-sitemap-foundation/36-06-SUMMARY.md`
</output>
