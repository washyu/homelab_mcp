---
phase: 36-drift-sitemap-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/homelab_mcp/database.py
autonomous: true
requirements:
  - DRFT-21
must_haves:
  truths:
    - "DatabaseAdapter ABC has no upsert_drift_baseline / get_drift_baseline / get_all_drift_baselines methods"
    - "SQLiteAdapter has no implementations of those three methods"
    - "PostgreSQLAdapter has no NotImplementedError stubs of those three methods"
    - "SQLiteAdapter.init_schema does not create the drift_baselines table"
  artifacts:
    - path: "src/homelab_mcp/database.py"
      provides: "Database adapter ABC + SQLite + Postgres implementations, with all drift_baseline methods removed"
      contains: "class DatabaseAdapter"
  key_links:
    - from: "src/homelab_mcp/database.py"
      to: "(no consumers)"
      via: "All references to upsert_drift_baseline / get_drift_baseline / get_all_drift_baselines outside migration.py must be removed in dependent plans"
      pattern: "drift_baseline"
---

<objective>
Delete the parallel `drift_baselines` data layer from `database.py`: remove the three ABC method declarations, the three SQLite implementations, the three Postgres NotImplementedError stubs, and the `CREATE TABLE drift_baselines` block from `SQLiteAdapter.init_schema`.

Purpose: After Phase 36, the sitemap (`get_all_devices()`) IS the single source of truth for drift detection. The parallel baseline data layer has zero remaining consumers in production code (the only writer `update_baseline_after_mutation` is removed in Plan 04; the only reader `scan_drift` is rewritten in Plan 04). This plan dissolves the layer at the storage tier — DRFT-21.

Output: A `database.py` with zero references to `drift_baseline` (singular or plural) and zero adapter methods related to baselines. All SQLite + Postgres adapter operations for the rest of the codebase remain intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-drift-sitemap-foundation/36-CONTEXT.md
@.planning/phases/36-drift-sitemap-foundation/36-RESEARCH.md
@.planning/phases/36-drift-sitemap-foundation/36-PATTERNS.md
@src/homelab_mcp/database.py

<interfaces>
<!-- The DatabaseAdapter ABC and impls keep ALL non-drift methods. -->
<!-- Only the three drift_baseline methods are deleted. -->
<!-- The CREATE TABLE block at SQLiteAdapter.init_schema is deleted. -->
<!-- Postgres init_schema does NOT create drift_baselines (per CONTEXT D-06 + RESEARCH verification). -->

The deletion targets, by SYMBOL NAME (not line number — RESEARCH Pitfall 3 notes ±2-3 line drift):

ABC declarations (under # Drift baseline CRUD methods comment block, ~line 67-94):
  - DatabaseAdapter.upsert_drift_baseline
  - DatabaseAdapter.get_drift_baseline
  - DatabaseAdapter.get_all_drift_baselines

SQLite impls (~lines 449-527):
  - SQLiteAdapter.upsert_drift_baseline
  - SQLiteAdapter.get_drift_baseline
  - SQLiteAdapter.get_all_drift_baselines

Postgres NotImplementedError stubs (~lines 916-939):
  - PostgreSQLAdapter.upsert_drift_baseline
  - PostgreSQLAdapter.get_drift_baseline
  - PostgreSQLAdapter.get_all_drift_baselines

CREATE TABLE block (~lines 205-222) inside SQLiteAdapter.init_schema:
  - The entire `CREATE TABLE drift_baselines` statement
  - The `CREATE INDEX idx_drift_baselines_node_vmid` statement
  - The `# Create drift_baselines table for VM configuration baseline storage` comment
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove drift_baseline methods + ABC declarations + CREATE block from database.py</name>
  <files>src/homelab_mcp/database.py</files>
  <read_first>
    - src/homelab_mcp/database.py (entire file — verify symbols by name before editing; line numbers may have drifted ±3 lines per RESEARCH Pitfall 3)
    - .planning/phases/36-drift-sitemap-foundation/36-PATTERNS.md (§ src/homelab_mcp/database.py — exact symbol-resolution deletion list)
    - .planning/phases/36-drift-sitemap-foundation/36-CONTEXT.md (§D-06, §D-07 — locked decisions)
  </read_first>
  <action>
Delete the following from `src/homelab_mcp/database.py`. Resolve each by symbol name (not line number) — line numbers may have drifted slightly:

**A. ABC method declarations** (around lines 67-94, under a `# Drift baseline CRUD methods` comment):
  - Delete the entire `def upsert_drift_baseline(self, ...)` abstract method on `DatabaseAdapter`
  - Delete the entire `def get_drift_baseline(self, ...)` abstract method on `DatabaseAdapter`
  - Delete the entire `def get_all_drift_baselines(self)` abstract method on `DatabaseAdapter`
  - Delete the `# Drift baseline CRUD methods` comment block above them

**B. SQLiteAdapter implementations** (around lines 449-527, under a `# Drift baseline CRUD methods` comment):
  - Delete the entire `def upsert_drift_baseline(self, ...)` method on `SQLiteAdapter`
  - Delete the entire `def get_drift_baseline(self, ...)` method on `SQLiteAdapter`
  - Delete the entire `def get_all_drift_baselines(self)` method on `SQLiteAdapter`
  - Delete the `# Drift baseline CRUD methods` comment block above them

**C. PostgreSQLAdapter NotImplementedError stubs** (around lines 916-939, under a `# Drift baseline CRUD methods (Phase 11 scope: SQLite only — stubs for ABC compliance)` comment):
  - Delete the entire `def upsert_drift_baseline(self, ...)` method on `PostgreSQLAdapter` (raises `NotImplementedError("drift baseline CRUD is SQLite-only in Phase 11")`)
  - Delete the entire `def get_drift_baseline(self, ...)` method on `PostgreSQLAdapter`
  - Delete the entire `def get_all_drift_baselines(self)` method on `PostgreSQLAdapter`
  - Delete the comment block above them

**D. CREATE TABLE block in SQLiteAdapter.init_schema** (around lines 205-222) — per D-06:
  - Delete the entire `cursor.execute("""CREATE TABLE IF NOT EXISTS drift_baselines (...)""")` call
  - Delete the entire `cursor.execute("""CREATE INDEX IF NOT EXISTS idx_drift_baselines_node_vmid ...""")` call
  - Delete the `# Create drift_baselines table for VM configuration baseline storage` comment (or equivalent comment) above the block

The exact CREATE block to remove (verify match before deleting):
```python
# Create drift_baselines table for VM configuration baseline storage
cursor.execute("""
    CREATE TABLE IF NOT EXISTS drift_baselines (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        node TEXT NOT NULL,
        vmid INTEGER NOT NULL,
        vm_type TEXT NOT NULL DEFAULT 'qemu',
        baseline_config TEXT NOT NULL,
        recorded_at TEXT NOT NULL,
        recorded_by TEXT NOT NULL,
        UNIQUE(node, vmid, vm_type)
    )
""")

cursor.execute("""
    CREATE INDEX IF NOT EXISTS idx_drift_baselines_node_vmid
    ON drift_baselines (node, vmid, vm_type)
""")
```

**E. Postgres `init_schema` (~lines 595-665):** Verify by reading that there is NO `CREATE TABLE drift_baselines` statement here (CONTEXT D-06 + RESEARCH A1 confirm this is already absent). Make zero changes to Postgres init_schema.

**Do not touch:**
  - Any other `DatabaseAdapter`, `SQLiteAdapter`, `PostgreSQLAdapter` method (`get_all_devices`, `register_device`, etc.)
  - The `# Phase 33` / `# Phase 35` cleanup blocks in init_schema
  - Any imports at the top of the file (none of them reference drift_baselines)

After editing, run quality gates locally:
```bash
uv run ruff check src/homelab_mcp/database.py
uv run mypy src/homelab_mcp/database.py
```
Both must be clean.
  </action>
  <verify>
    <automated>uv run ruff check src/homelab_mcp/database.py &amp;&amp; uv run mypy src/homelab_mcp/database.py</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "drift_baseline" src/homelab_mcp/database.py` returns ZERO matches (singular OR plural — case sensitive substring)
- `grep -n "upsert_drift_baseline\|get_drift_baseline\|get_all_drift_baselines" src/homelab_mcp/database.py` returns ZERO matches
- `grep -n "idx_drift_baselines_node_vmid" src/homelab_mcp/database.py` returns ZERO matches
- `grep -n "CREATE TABLE IF NOT EXISTS drift_baselines" src/homelab_mcp/database.py` returns ZERO matches
- `uv run ruff check src/homelab_mcp/database.py` exits 0
- `uv run mypy src/homelab_mcp/database.py` exits 0 (no missing-method errors on the abstract class)
- `uv run python -c "from src.homelab_mcp.database import DatabaseAdapter, SQLiteAdapter, PostgreSQLAdapter; assert not hasattr(SQLiteAdapter, 'upsert_drift_baseline')"` exits 0
- File still contains `class DatabaseAdapter`, `class SQLiteAdapter`, `class PostgreSQLAdapter` (deletion did not corrupt class boundaries)
- File still contains `def get_all_devices` on both adapters (sitemap iteration entry point preserved per Pattern E)
  </acceptance_criteria>
  <done>
All three drift_baseline methods removed from ABC, SQLiteAdapter, and PostgreSQLAdapter. CREATE TABLE block removed from SQLiteAdapter.init_schema. Quality gates green. The grep-verifiable acceptance criteria above all pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user data → DB | Phase 36 removes a write path (no user input ever reaches drift_baselines after this plan); no new boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Tampering | Pre-existing `drift_baselines` table rows on user installs | accept | Per DRFT-21 architectural decision (homelab single-user scope, mirrors v1.6 CRED-04 keyring migration). Plan 02's migration drops the table cleanly; pre-existing rows are not preserved. Documented in CONTEXT D-08 banner. No security risk — baseline rows store config snapshots, not credentials |
| T-36-01-02 | Information Disclosure | None — no new code surfaces, no new logs, no new error paths | accept | Pure deletion, no new disclosure surface |
| T-36-01-03 | Denial of Service | None — no runtime code added, only removal | accept | Pure deletion, no new computation surface |
</threat_model>

<verification>
After this plan:

1. `grep -rn "drift_baseline" src/homelab_mcp/database.py` returns ZERO matches.
2. `uv run pytest tests/test_database.py -v --co -q | grep -i "TestDriftBaselines"` will still pass-collect (Plan 05 deletes the class). Don't worry about its tests failing now — they go away in Plan 05.
3. The `database.py` module imports cleanly: `uv run python -c "from src.homelab_mcp.database import SQLiteAdapter; SQLiteAdapter(':memory:').init_schema()"` exits 0.
4. Other database tests stay green for the non-drift-baseline scope: `uv run pytest tests/test_database.py -k "not Drift" -x` passes.
</verification>

<success_criteria>
- `database.py` contains zero `drift_baseline` references (verified via grep).
- All other database adapter methods (e.g., `get_all_devices`, `register_device`, init_schema's other tables) remain functional.
- `uv run ruff check` and `uv run mypy` are green for `database.py`.
- The deletion is transparent to non-drift consumers (no other module's import or call site changes).
</success_criteria>

<output>
After completion, create `.planning/phases/36-drift-sitemap-foundation/36-01-SUMMARY.md`
</output>
