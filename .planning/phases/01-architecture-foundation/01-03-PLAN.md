---
phase: 01-architecture-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - run_server.py
  - src/homelab_mcp/server.py
  - tests/test_server.py
  - tests/test_http_transport.py
autonomous: true
requirements: [ARCH-03]

must_haves:
  truths:
    - "Server shuts down cleanly on SIGTERM with all connections closed"
    - "Server shuts down cleanly on SIGINT with all connections closed"
    - "No orphaned aiohttp sessions or database connections after shutdown"
    - "Existing unit test suite passes against the fully migrated architecture"
  artifacts:
    - path: "run_server.py"
      provides: "Signal handling for graceful shutdown in stdio mode"
      contains: "signal"
    - path: "tests/test_server.py"
      provides: "Test for graceful shutdown behavior"
      contains: "test_graceful_shutdown"
  key_links:
    - from: "run_server.py"
      to: "src/homelab_mcp/resource_manager.py"
      via: "Signal triggers shutdown which triggers ResourceManager.shutdown via lifespan"
      pattern: "shutdown"
    - from: "src/homelab_mcp/server.py"
      to: "src/homelab_mcp/resource_manager.py"
      via: "Lifespan finally block calls resource_manager.shutdown()"
      pattern: "finally"
---

<objective>
Implement graceful shutdown on SIGTERM/SIGINT ensuring ResourceManager cleanup, and verify the complete architecture works end-to-end with no test regressions.

Purpose: ARCH-03 requires clean shutdown with no orphaned resources. This plan also serves as the integration verification point for the entire phase -- confirming Plans 01 and 02 work together correctly.
Output: Signal handling in run_server.py, shutdown tests, full test suite green.
</objective>

<execution_context>
@/home/shaun/.claude/get-shit-done/workflows/execute-plan.md
@/home/shaun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-architecture-foundation/01-RESEARCH.md
@.planning/phases/01-architecture-foundation/01-01-SUMMARY.md
@.planning/phases/01-architecture-foundation/01-02-SUMMARY.md

@run_server.py
@src/homelab_mcp/server.py
@src/homelab_mcp/resource_manager.py
@tests/test_server.py

<interfaces>
<!-- From RESEARCH.md Pattern 5: Signal handling -->
```python
# For stdio mode: anyio task group cancellation triggers lifespan cleanup
# For HTTP mode: uvicorn handles SIGTERM/SIGINT natively, triggers Starlette lifespan shutdown

# The key guarantee: ResourceManager.shutdown() runs because:
# 1. Signal cancels the task group (stdio) or uvicorn stops (HTTP)
# 2. This exits the transport context manager
# 3. Which exits server.run()
# 4. Which exits the lifespan context manager
# 5. Lifespan's finally block calls resource_manager.shutdown()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement signal handling and graceful shutdown</name>
  <files>run_server.py, src/homelab_mcp/server.py, tests/test_server.py</files>
  <behavior>
    - Test: SIGTERM during stdio mode triggers ResourceManager.shutdown()
    - Test: SIGINT during stdio mode triggers ResourceManager.shutdown()
    - Test: Lifespan finally block executes on cancellation (ResourceManager.shutdown called)
    - Test: HTTP mode shutdown triggers ResourceManager.shutdown() via Starlette lifespan
  </behavior>
  <action>
Update run_server.py stdio mode to handle signals:

1. For stdio mode, wrap the main async function with signal handling. Use the pattern from RESEARCH.md:
   - Register signal handlers for SIGTERM and SIGINT that set an anyio.Event
   - Run server in a task group alongside a task that waits for the shutdown event
   - When event fires, cancel the task group scope
   - This triggers the chain: task group cancel -> stdio_server exits -> server.run exits -> lifespan finally -> ResourceManager.shutdown()

2. For HTTP mode, uvicorn already handles SIGTERM/SIGINT. The Starlette lifespan (session_manager.run()) will exit, which triggers the server lifespan chain. No additional signal handling needed. Verify this path is correct.

3. In server.py, ensure the lifespan's finally block logs shutdown activity:
   - Log "Shutting down ResourceManager..." at info level
   - Call resource_manager.shutdown()
   - Log "ResourceManager shutdown complete" at info level

4. Remove any old KeyboardInterrupt handling from run_server.py that referenced HomelabMCPServer.

Add/update tests in tests/test_server.py:

5. test_graceful_shutdown_lifespan: Create a mock ResourceManager, run app_lifespan as context manager, verify initialize() called on enter, simulate cancellation (exit the context), verify shutdown() called.

6. test_signal_triggers_shutdown: Test that the stdio main function's signal handling correctly cancels the task group. Mock the server.run() call, send SIGTERM, verify the function exits cleanly. This may need to use subprocess or signal simulation.

Update tests/test_http_transport.py:
7. Remove or update any tests that reference the old MCPHTTPTransport class. If the file tests non-MCP HTTP routes (health endpoint, shell routes), keep those tests but update imports to use http_app.py. If it only tests old transport code, mark tests as skipped or remove them.
  </action>
  <verify>
    <automated>cd /home/shaun/projects/mcp_python_server && uv run pytest tests/test_server.py -x -v -k "shutdown or signal"</automated>
  </verify>
  <done>Signal handling implemented for stdio mode. Lifespan guarantees ResourceManager.shutdown() on any exit path. Shutdown tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite verification and cleanup</name>
  <files>tests/test_http_transport.py</files>
  <action>
Run the complete unit test suite and fix any failures:

1. Run `uv run pytest tests/ -m "not integration" -v` and identify all failures.

2. For each failure, determine the cause:
   - Import errors from removed HomelabMCPServer -> update imports
   - Tests referencing old JSON-RPC dict format -> update to test new interfaces
   - Tests importing from http_transport.py -> update to import from http_app.py or mark as not applicable
   - Mock targets pointing at old code paths -> update mock targets

3. Fix each failure with minimal changes. Do not rewrite tests that still pass. Focus on:
   - tests/test_http_transport.py: Most likely needs significant updates since http_transport.py is deprecated. Update to test http_app.py routes (health endpoint, etc.) or remove tests for deprecated transport code.
   - Any other test file that imports from server.py or http_transport.py

4. Run ruff check and ruff format on all modified files.

5. Run the full suite one final time to confirm zero failures.
  </action>
  <verify>
    <automated>cd /home/shaun/projects/mcp_python_server && uv run pytest tests/ -m "not integration" -v --tb=short</automated>
  </verify>
  <done>Full unit test suite passes with zero failures. No test references old HomelabMCPServer or hand-rolled JSON-RPC. Code passes ruff check.</done>
</task>

</tasks>

<verification>
```bash
cd /home/shaun/projects/mcp_python_server && uv run pytest tests/ -m "not integration" -v
```
Full unit test suite passes.

```bash
cd /home/shaun/projects/mcp_python_server && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/
```
Code quality checks pass.
</verification>

<success_criteria>
- SIGTERM/SIGINT in stdio mode triggers clean shutdown with ResourceManager.shutdown()
- HTTP mode shutdown triggers clean shutdown via Starlette lifespan chain
- Shutdown tests exist and pass
- Full unit test suite passes with zero failures
- No references to HomelabMCPServer in active code paths
- Code passes ruff check and format
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-foundation/01-03-SUMMARY.md`
</output>
