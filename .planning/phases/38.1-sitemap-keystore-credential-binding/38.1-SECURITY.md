# Phase 38.1 Security Audit — sitemap-keystore-credential-binding

**Phase:** 38.1 — sitemap-keystore-credential-binding
**Branch:** credential-cleanup
**ASVS Level:** L1
**Threats Closed:** 48 / 48
**Threats Open:** 0
**Audit date:** 2026-04-27

---

## Result: SECURED

All 48 STRIDE threats from the 9-plan threat register are verified by grep / test / structural evidence in the implemented code. Each `mitigate` disposition resolves to a concrete file:line citation; each `accept` disposition resolves to a documented rationale in plan or summary.

---

## Threat Verification

### Plan 01 — test infrastructure / AST guards

| Threat ID | Category | Disposition | Evidence |
|-----------|----------|-------------|----------|
| T-38.1-W0-01 | Tampering — fixture leak to real registry | mitigate | `tests/test_credential_store.py` monkeypatches `homelab_mcp.credential_store._REGISTRY_PATH` to `tmp_path / "registry.json"` at lines 132, 154, 170, 192, 203, 241, 256, 267, 280, 297, 312, 464, 479, 496, 515. Same pattern used in `tests/test_migration_phase381.py:402` and `tests/integration/test_credential_binding_round_trip.py:62-63`. |
| T-38.1-W0-02 | Information Disclosure — placeholder fixture strings | accept | Fixture strings are static literals like `"fake-secret-token"` (round-trip integration test); no real secrets in fixtures. Documented in 38.1-09-SUMMARY.md §Threat Flags T-38.1-09-03. |
| T-38.1-W0-03 | Spoofing — AST guard targets wrong function | mitigate | `tests/test_ast_regression.py:763` `test_scan_drift_no_continue_in_row_loop_phase38_1` walks AST for `FunctionDef.name == "scan_drift"` and asserts no `ast.Continue` nodes inside the row-iter loop. Class docstring lines 758–760 explicitly notes guard targets `scan_drift`, NOT `scan_infrastructure_drift`. |

### Plan 02 — find_credential_by_id / UUIDv4

| Threat ID | Category | Disposition | Evidence |
|-----------|----------|-------------|----------|
| T-38.1-02-01 | Spoofing — UUIDv4 collision | accept | RFC 4122 §4.4 — collision probability negligible (122-bit random). Documented in 38.1-02-PLAN.md threat_model. |
| T-38.1-02-02 | Tampering — malformed UUID match | mitigate | `credential_store.find_credential_by_id` (line 327-329) uses string equality — malformed UUIDs cannot match. Resolver entry validates via `uuid.UUID(credential_id)` at `proxmox_api.py:269` and `ssh_tools.py:141` before lookup; raises `CredentialNotFoundError("binding stale: malformed credential_id ...")`. |
| T-38.1-02-03 | Information Disclosure — UUID in errors | accept | UUIDs are non-secret identifiers per D-23. Documented in 38.1-02-PLAN.md and 38.1-08-SUMMARY.md §Threat Flags T-38.1-08-03. |
| T-38.1-02-04 | EoP — credential_store imports adapter | mitigate | `credential_store.py` has zero `from .database` or `from homelab_mcp` imports (grep confirmed empty). Module imports are limited to stdlib (`json`, `logging`, `pathlib`, `uuid`). |

### Plan 03 — adapter typed methods

| Threat ID | Category | Disposition | Evidence |
|-----------|----------|-------------|----------|
| T-38.1-03-01 | Tampering — credential_type SQL injection | mitigate | SQLite `set_device_credential_binding` validates at `database.py:426-427` (`if credential_type not in ("ssh","proxmox"): raise ValueError`) BEFORE constructing `column = f"{credential_type}_credential_id"` at line 433. Postgres path at `database.py:958-959`. Same guard repeated in `bulk_null_credential_binding` (lines 460-461 SQLite, 1009-1010 Postgres). |
| T-38.1-03-02 | Spoofing — UUID type mismatch in DB | mitigate | Both `_SQLITE_RECREATE_DEVICES` (migration.py:125-126) and `_POSTGRES_RECREATE_DEVICES` (migration.py:145-146) declare `ssh_credential_id TEXT` and `proxmox_credential_id TEXT`. Postgres ALTER also uses `TEXT` (migration.py:599-600). |
| T-38.1-03-03 | Tampering — race on Postgres binding update | mitigate | `database.py:966-978` PG path: `BEGIN` → `SELECT id FROM devices WHERE id = %s FOR UPDATE` → `UPDATE` → `commit`, with rollback on exception. |
| T-38.1-03-04 | Information Disclosure — eligibility booleans | accept | Returns `dict[str, Any]` with hostname/connection_ip/credential_id columns only — no raw secrets. Documented in 38.1-03-PLAN.md threat_model. |
| T-38.1-03-05 | Information Disclosure — explicit SELECT in get_all_devices | accept | Phase pre-existing; reviewed and documented in 38.1-03-PLAN.md. |
| T-38.1-03-06 | Tampering — hostname SQL injection | mitigate | `find_devices_by_hostname_or_ip` SQLite at `database.py:446-451`: `WHERE hostname = ? OR connection_ip = ?` with bound params. Postgres at `database.py:995-1000`: `WHERE hostname = %s OR host(connection_ip) = %s` with bound params. |
| T-38.1-03-07 | Tampering — credential_ids list injection | mitigate | `bulk_null_credential_binding` SQLite at `database.py:482` constructs placeholders `",".join("?" * len(chunk))` and binds via `tuple(chunk)` (line 486). Postgres at `database.py:1024-1025` uses `WHERE {column} = ANY(%s)` with bound list. `credential_type` validated against closed set on entry. |
| T-38.1-03-08 | Tampering — race on Postgres bulk-null | mitigate | `database.py:1019-1037`: `BEGIN` → `SELECT … FOR UPDATE` → `UPDATE … WHERE … = ANY(%s)` → `commit`. |

### Plan 04 — destructive migration

| Threat ID | Category | Disposition | Evidence |
|-----------|----------|-------------|----------|
| T-38.1-04-01 | Tampering — incomplete migration leaves stamp | mitigate | `migration.py:_archive_registry_for_phase_38_1` (lines 45-69) runs FIRST (line 231 SQLite, 567 PG); `_stamp_migration` runs LAST after both rename + drop succeed (line 251 SQLite, 590 PG). State-file check `_phase_38_1_applied()` makes re-runs idempotent (line 208 SQLite, 542 PG). |
| T-38.1-04-02 | Tampering — backup mode regression | mitigate | `Path.rename` preserves mode by design. Test `test_backup_file_mode_preserved_phase381` at `tests/test_migration_phase381.py:388-423` asserts `0o600` bits survive the rename. |
| T-38.1-04-03 | Tampering — backup overwrite collision | mitigate | `migration.py:61` and `migration.py:66` use `datetime.now(UTC).strftime("%Y%m%dT%H%M%S%fZ")` (microsecond precision); `while backup_path.exists()` loop at lines 65-67 regenerates timestamp until free slot found. |
| T-38.1-04-04 | DoS — corrupt state file blocks startup | mitigate | `_load_migration_state` (lines 20-28) catches all exceptions and returns `{}` (warning logged); migration retries cleanly. |
| T-38.1-04-05 | Tampering — partial migration mis-stamped | mitigate | `_stamp_migration(_PHASE_38_1_KEY)` only runs after `cursor.executescript(_SQLITE_RECREATE_DEVICES)` + `conn.commit()` succeed (migration.py:248-251 SQLite; 588-590 PG). `try/raise` propagation on failure leaves stamp absent. |
| T-38.1-04-06 | Tampering — Postgres FK leftover rows | mitigate | `migration.py:574-584` PG path: pre-DROP `DELETE FROM discovery_history` (guarded on table existence per WR-09), then `DROP TABLE IF EXISTS devices CASCADE`. |
| T-38.1-04-07 | Information Disclosure — banner misleads operator | mitigate | `_print_phase_38_1_banner` (migration.py:72-108) names BOTH dropped tables AND cleared FK-dependent rows literally: `"Cleared discovery_history rows (FK-dependent on devices table)"` at lines 87 and 93. |

### Plan 05 — resolver Tier-0 binding

| Threat ID | Category | Disposition | Evidence |
|-----------|----------|-------------|----------|
| T-38.1-05-01 | Tampering — malformed credential_id slips past lookup | mitigate | `proxmox_api.py:268-271` and `ssh_tools.py:140-143`: `try: uuid.UUID(credential_id) except (ValueError, AttributeError): raise CredentialNotFoundError("binding stale: malformed credential_id ...")` BEFORE `find_credential_by_id`. |
| T-38.1-05-02 | Spoofing — wrong-type UUID resolved | mitigate | `proxmox_api.py:285-292` raises `binding type mismatch` when `entry.get("credential_type") != "proxmox"`; mirrored at `ssh_tools.py:156-163` checking `!= "ssh"`. Both raise BEFORE the keyring fetch. |
| T-38.1-05-03 | Information Disclosure — UUID in resolver logs | accept | UUIDs non-secret per D-23. Documented in 38.1-05-PLAN.md threat_model. |
| T-38.1-05-04 | Tampering — fallback to hostname masks rotation bugs | mitigate | `proxmox_api.py:273-284` and `ssh_tools.py:145-155`: when `find_credential_by_id` returns None, immediately raise `CredentialNotFoundError(reason_hint="binding_stale")` with no fallback. Inline comment line 275 / 147 explicitly states "NEVER fall back to hostname-exact-match — would mask rotation cleanup bugs." |
| T-38.1-05-05 | Tampering — positional `session=` collision | mitigate | `proxmox_api.py:222` defines `session: aiohttp.ClientSession | None = None` after `*` (kwarg-only). Both callsites use kwarg form: `drift_detection.py:294` (`session=session, credential_id=binding`) and `proxmox_api.py:507-509` (recursive). |
| T-38.1-05-06 | Information Disclosure — UUID-wins design | accept | D-13 architectural decision: hostname mismatch between sitemap row and registry entry is the WHOLE POINT of the binding. Documented in 38.1-05-PLAN.md and resolver docstring lines 230-233. |

### Plan 06 — drift wiring + classification

| Threat ID | Category | Disposition | Evidence |
|-----------|----------|-------------|----------|
| T-38.1-06-01 | Tampering — silent skip via `continue` regression | mitigate | AST guard `tests/test_ast_regression.py:763 test_scan_drift_no_continue_in_row_loop_phase38_1` asserts `ast.Continue` count == 0 inside `for row in rows:` loop of `scan_drift`. |
| T-38.1-06-02 | EoP — in-process trust | accept | Single-process MCP server design; sitemap rows and registry are co-located. Documented in 38.1-06-PLAN.md threat_model. |
| T-38.1-06-03 | Information Disclosure — UUID in drift response | accept | UUIDs non-secret per D-23. Documented in 38.1-06-PLAN.md. |
| T-38.1-06-04 | Tampering — classifier mis-route on null binding | mitigate | `drift_detection.py:73-74`: `if binding is None: return "unbound"` (definitionally). Cluster-walk fall-through paths in resolver leave `.reason_hint` unset; classifier defaults to `"binding_stale"` only when binding is non-None (lines 75-93) with WR-08 warning log. |
| T-38.1-06-05 | Tampering — bucket order drift | mitigate | `drift_detection.py:366-370` envelope assigns keys in fixed order: `probed_ok` → `unreachable` → `not_eligible` → `unknown` → `changed`. Matches D-08 locked envelope contract. |

### Plan 07 — credentials CLI

| Threat ID | Category | Disposition | Evidence |
|-----------|----------|-------------|----------|
| T-38.1-07-01 | DoS — non-TTY block on input() | mitigate | `server.py:626-633` (single-match overwrite) and `server.py:651-658` (multi-match menu): `if not sys.stdin.isatty(): print(warning, file=sys.stderr); return` BEFORE any `input()` call. |
| T-38.1-07-02 | Tampering — credential_type SQL injection from CLI | mitigate | argparse `choices=["ssh","proxmox"]` (server.py:1232+ scope) PLUS adapter re-validation at `database.py:426`, `database.py:460`, `database.py:958`, `database.py:1009` (`if credential_type not in ("ssh","proxmox"): raise ValueError`). |
| T-38.1-07-03 | Tampering — credential_id injection | mitigate | CLI passes `credential_id` through to typed adapter `set_device_credential_binding` which binds via `?` / `%s` (database.py:435 SQLite, 974-977 PG). UUID validated at resolver entry (`proxmox_api.py:269`, `ssh_tools.py:141`). |
| T-38.1-07-04 | Tampering — hostname injection | mitigate | `server.py:610` calls `get_sitemap_rows_for_hostname(hostname)` → `find_devices_by_hostname_or_ip(hostname)` which uses `?` / `%s` placeholder binding (database.py:449-451 SQLite, 998-1000 PG). |
| T-38.1-07-05 | Information Disclosure — UUID in default CLI output | mitigate | `_cmd_credentials_list` (server.py:825-853) tabular path prints only `username@hostname` / `username@cluster:name`. Only `--json` (line 837-838) emits `credential_id`. Test `test_list_default_tabular_omits_credential_id_phase381` at `tests/test_credentials_cli.py:1130`. |
| T-38.1-07-06 | Tampering — race add/remove self-heal | accept | Race window self-heals via `not_eligible(binding_stale)` routing on next drift scan. Documented in 38.1-07-SUMMARY.md §Threat Flags. |
| T-38.1-07-07 | EoP — link operator-initiated | accept | `credentials link` is a manual operator command, not autoboundsamlly. Documented in 38.1-07-PLAN.md. |
| T-38.1-07-08 | Tampering — placeholder mismatch in server.py SQL | mitigate | grep `execute_query.*"[^"%]*\?'` against `src/homelab_mcp/server.py` returns no matches (verified via Grep tool). server.py never constructs raw SQL placeholders directly; all writes go through typed adapter wrappers `set_device_credential_binding`, `null_bindings_for_credential_id`, `get_sitemap_rows_for_hostname` (server.py:538-580). |

### Plan 08 — discovery auto-bind

| Threat ID | Category | Disposition | Evidence |
|-----------|----------|-------------|----------|
| T-38.1-08-01 | Tampering — race between discover+remove | mitigate | `set_device_credential_binding` uses Postgres `SELECT FOR UPDATE` (database.py:967-968) and SQLite single-writer transaction (database.py:434-438). Stale-bind self-heals on next drift scan via `not_eligible(binding_stale)`. |
| T-38.1-08-02 | Tampering — helper duplicates registry scan | accept | Registry edits during a discovery run are extraordinary in homelab single-user scope; worst-case stale UUID bind self-heals on next drift scan. Documented in 38.1-08-SUMMARY.md §Threat Flags. |
| T-38.1-08-03 | Information Disclosure — UUID in response JSON | accept | UUIDs non-secret per D-23, parallel to `credentials list --json` precedent. Documented in 38.1-08-SUMMARY.md §Threat Flags. |
| T-38.1-08-04 | Tampering — multi-match auto-bind ambiguity | mitigate | `_scan_registry_for_binding` at `ssh_tools.py:798-821`: `if len(matching) == 1: return matching[0].get("credential_id")` else returns None. Multi-match leaves `used_credential_id=None`; no binding write on ambiguity. |

### Plan 09 — round-trip integration test

| Threat ID | Category | Disposition | Evidence |
|-----------|----------|-------------|----------|
| T-38.1-09-01 | Tampering — false-positive lucky pass | mitigate | Every test asserts `result["counts"]["not_eligible"] == 0` and `rows[0]["proxmox_credential_id"] == new_uuid` invariants: `tests/integration/test_credential_binding_round_trip.py:233, 263, 311-332, 377-398, 446-470, 552`. |
| T-38.1-09-02 | Tampering — mock doesn't validate Tier-0 | mitigate | `_make_proxmox_mocks` at `tests/integration/test_credential_binding_round_trip.py:144-181` includes `assert credential_id == expected_credential_id` inside both `fake_get_proxmox_client` (line 162) and `fake_resolve` (line 176) BEFORE returning a client. |
| T-38.1-09-03 | Information Disclosure — token leak outside test process | accept | Token is hardcoded literal `"fake-secret-token"`; `tmp_path` registry isolated; monkeypatch reverses on test exit. Documented in 38.1-09-SUMMARY.md §Threat Flags. |

---

## Unregistered Flags

None. All nine SUMMARY.md `## Threat Flags` sections explicitly state "None — no new network endpoints, auth paths, file access patterns, or schema changes beyond those declared in the plan's threat model." Surface area introduced during implementation matches the plan-time threat register exactly.

---

## Accepted Risks Log

The following dispositions are accepted, with rationale documented in the cited plan:

| Threat ID | Rationale | Plan reference |
|-----------|-----------|----------------|
| T-38.1-W0-02 | Static placeholder strings in fixtures (no real secret material) | 38.1-09-SUMMARY.md |
| T-38.1-02-01 | UUIDv4 collision is negligible (RFC 4122 §4.4, 122-bit random) | 38.1-02-PLAN.md |
| T-38.1-02-03 | UUIDs are non-secret identifiers (D-23) | 38.1-02-PLAN.md |
| T-38.1-03-04 | Eligibility booleans, no raw secrets returned from adapter | 38.1-03-PLAN.md |
| T-38.1-03-05 | Pre-existing explicit SELECT pattern in `get_all_devices` | 38.1-03-PLAN.md |
| T-38.1-05-03 | UUID in resolver logs is non-secret (D-23) | 38.1-05-PLAN.md |
| T-38.1-05-06 | UUID-wins design is intentional architecture (D-13) | 38.1-05-PLAN.md |
| T-38.1-06-02 | In-process trust (single MCP server, co-located registry+sitemap) | 38.1-06-PLAN.md |
| T-38.1-06-03 | UUID in drift response is non-secret (D-23) | 38.1-06-PLAN.md |
| T-38.1-07-06 | Race self-heals on next drift scan via not_eligible(binding_stale) | 38.1-07-SUMMARY.md |
| T-38.1-07-07 | `credentials link` is operator-initiated, not autonomous | 38.1-07-PLAN.md |
| T-38.1-08-02 | Registry edits during discovery extraordinary in homelab single-user; self-heals | 38.1-08-SUMMARY.md |
| T-38.1-08-03 | UUID in discovery response JSON is non-secret (D-23 parallel) | 38.1-08-SUMMARY.md |
| T-38.1-09-03 | Hardcoded fixture token; monkeypatch reverses; tmp_path isolated | 38.1-09-SUMMARY.md |

---

## Verification artifacts

- Threat register: 9 PLAN.md `<threat_model>` blocks (38.1-01 through 38.1-09)
- AST regression guard: `tests/test_ast_regression.py:763`
- CLI tabular omit test: `tests/test_credentials_cli.py:1130`
- Backup mode preservation test: `tests/test_migration_phase381.py:388`
- Round-trip Tier-0 enforcement: `tests/integration/test_credential_binding_round_trip.py:144`
- Resolver UUID validation: `src/homelab_mcp/proxmox_api.py:268-271`, `src/homelab_mcp/ssh_tools.py:140-143`
- Adapter closed-set guards: `src/homelab_mcp/database.py:426-427, 460-461, 958-959, 1009-1010`
- Migration order (archive → drop → stamp): `src/homelab_mcp/migration.py:208-256, 542-592`

All 48 declared threat mitigations verified present. Phase 38.1 cleared for ship.
