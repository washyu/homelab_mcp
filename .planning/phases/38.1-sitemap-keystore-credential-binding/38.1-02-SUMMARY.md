---
phase: 38.1-sitemap-keystore-credential-binding
plan: "02"
subsystem: credential-store
tags: [uuid, credential-store, registry, python, stdlib]

# Dependency graph
requires:
  - phase: 38.1-01
    provides: "Wave 0 RED tests for R1 in tests/test_credential_store.py"
provides:
  - "register_credential() returns UUIDv4 credential_id string (R1)"
  - "find_credential_by_id(uuid) -> dict | None lookup helper (R1)"
  - "credential_id field present in every new registry entry"
affects:
  - "38.1-03 (database columns bind onto this credential_id)"
  - "38.1-04 (migration archives registry with credential_id entries)"
  - "38.1-05 (CLI credentials add uses returned UUID for auto-bind)"
  - "38.1-06 (resolver credential_id= kwarg resolved via find_credential_by_id)"
  - "38.1-07 (sitemap stores UUID from register_credential return value)"
  - "38.1-08 (drift routing depends on find_credential_by_id for D-11/D-12)"

# Tech tracking
tech-stack:
  added: [stdlib uuid]
  patterns:
    - "UUIDv4 generated at write time (register_credential), never at read time"
    - "find_credential_by_id as single source of truth for binding staleness"
    - "entry.get('credential_id') for backward-compatible lookup (legacy entries silently skipped)"

key-files:
  created: []
  modified:
    - "src/homelab_mcp/credential_store.py"
    - "tests/test_credential_store.py"

key-decisions:
  - "credential_id is first key in entry dict (D-23 display order — UUID visible in list --json)"
  - "find_credential_by_id uses .get() not direct key access to safely skip legacy entries without credential_id"
  - "Rotation (remove + re-add) naturally produces a fresh UUID because dedup logic discards the old entry before new UUID is generated"
  - "Callers of register_credential that previously ignored None return value continue working unchanged (str is strict superset)"

patterns-established:
  - "TDD wave pattern: RED tests committed first (3854f8e), GREEN implementation follows (8d89468, 6ea6e86)"
  - "Rule 1 auto-fix pattern: existing exact-match test dicts updated to pop dynamic UUID fields"

requirements-completed: [R1]

# Metrics
duration: 20min
completed: 2026-04-27
---

# Phase 38.1 Plan 02: UUID Generation + Lookup Helper Summary

**UUIDv4 credential_id added to every register_credential() registry entry; find_credential_by_id() lookup helper provides the single source of truth for binding staleness across all downstream waves**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-04-27T04:55:00Z
- **Completed:** 2026-04-27T05:15:56Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- `register_credential()` now generates a UUIDv4, stores it as `credential_id` in the entry dict, and returns it as `str` (change from `-> None`)
- `find_credential_by_id(uuid)` added as the lookup helper downstream waves (D-11/D-12 drift routing, Wave 4-6 resolvers) will use
- All 31 tests in `tests/test_credential_store.py` pass; CRED-04 preserved (zero `homelab_mcp.*` imports)
- Rotation produces a fresh UUID by design: dedup removes old entry before new UUID is generated
- Legacy entries without `credential_id` silently skipped by `find_credential_by_id` via `.get()` — backward-safe

## Task Commits

Each task was committed atomically (TDD cycle):

1. **RED: Failing tests for R1 UUID + lookup** - `3854f8e` (test)
2. **Task 1: UUID generation in register_credential()** - `8d89468` (feat)
3. **Task 2: find_credential_by_id() helper** - `6ea6e86` (feat)

**Plan metadata:** (committed after this summary)

_Note: TDD plan — RED commit lands first, then two GREEN feature commits_

## Files Created/Modified
- `src/homelab_mcp/credential_store.py` - Added `import uuid`; changed `register_credential()` return type to `str`; added `credential_id` field to entry; added `find_credential_by_id()` function
- `tests/test_credential_store.py` - Added 4 phase381 RED tests; updated 2 existing tests for new `credential_id` field

## Decisions Made
- `credential_id` is the first key in the dict literal for display-order alignment with D-23 (`credentials list --json` should show UUID prominently)
- `find_credential_by_id` placed after `list_credentials` as the natural "query by ID" companion to "query by type"
- No changes to `unregister_credential`, `unregister_cluster_credential`, or `list_credentials` — they work correctly with the new dict shape

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated existing tests broken by new credential_id field**
- **Found during:** Task 1 (UUID generation)
- **Issue:** `test_register_and_list` and `test_list_filters_by_type` used exact-match dict assertions. Adding `credential_id` to every entry caused them to fail with "unexpected key in dict"
- **Fix:** Updated both tests to `.pop("credential_id", None)` before the stable-field assertion, with an explicit check that `credential_id` is not None
- **Files modified:** `tests/test_credential_store.py`
- **Verification:** All 31 tests pass after fix
- **Committed in:** `8d89468` (Task 1 feat commit)

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug fix)
**Impact on plan:** Necessary correctness fix for pre-existing tests. No scope creep.

## Issues Encountered
None beyond the auto-fixed test regression.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- R1 complete: every registry entry written after this plan has a `credential_id` UUIDv4 field
- `find_credential_by_id` ready for Wave 4-6 resolver changes (D-11 binding_stale, D-12 keyring_desync)
- Wave 3 (Plan 04: migration) can archive/replace the registry with UUID-keyed entries
- Caller `server.py:_cmd_credentials_add` still ignores the return value — Wave 6 (Plan 05/06) will use it for auto-bind

## Known Stubs
None — no placeholder values or wired-but-empty data paths.

## Threat Flags
None — no new network endpoints, auth paths, file access patterns, or schema changes beyond those declared in the plan's threat model.

## Self-Check: PASSED
- `src/homelab_mcp/credential_store.py` exists and contains `def find_credential_by_id` and `import uuid`
- Commits `3854f8e`, `8d89468`, `6ea6e86` all present in git log
- All 31 tests pass: `uv run pytest tests/test_credential_store.py -q` shows "31 passed"

---
*Phase: 38.1-sitemap-keystore-credential-binding*
*Completed: 2026-04-27*
