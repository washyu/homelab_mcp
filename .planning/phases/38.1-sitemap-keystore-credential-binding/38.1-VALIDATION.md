---
phase: 38.1
slug: sitemap-keystore-credential-binding
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-26
last_audit: 2026-04-27
---

# Phase 38.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x + pytest-asyncio (already pinned in pyproject.toml) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` |
| **Quick run command** | `uv run pytest tests/ -m "not integration" -x` |
| **Full suite command** | `uv run pytest tests/ -v` |
| **AST guard run** | `uv run pytest tests/test_ast_regression.py -v` |
| **Estimated runtime** | ~30 sec (quick) · ~2-3 min (full incl. integration when Docker available) |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/ -m "not integration" -x`
- **After every plan wave:** Run `uv run pytest tests/ -v`
- **Before `/gsd-verify-work`:** Full suite must be green; AST guards explicitly green; integration round-trip explicitly green
- **Max feedback latency:** 30 seconds for unit feedback

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 38.1-W0-01 | TBD | 0 | All | — | Test scaffolds in place | unit | `uv run pytest tests/test_ast_regression.py tests/test_credential_store.py tests/test_database.py tests/test_credentials_cli.py tests/test_proxmox_api.py tests/test_ssh_tools.py tests/test_drift_detection.py tests/test_sitemap.py -k phase381 --collect-only` | ✅ | ✅ green |
| 38.1-R1 | TBD | 1 | R1 | — | UUID generated, rotation produces fresh UUID | unit | `uv run pytest tests/test_credential_store.py::test_register_credential_generates_uuid_phase381 -x` | ✅ | ✅ green |
| 38.1-R2 | TBD | 2 | R2 | T-V8 | Schema columns exist on both adapters | unit | `uv run pytest tests/test_database.py::test_devices_has_credential_id_columns_phase381 -x` | ✅ | ✅ green |
| 38.1-R3 | TBD | 6 | R3 | — | Discovery writes ssh_credential_id from resolver entry | unit | `uv run pytest tests/test_sitemap.py::test_discover_writes_credential_id_phase381 -x` | ✅ | ✅ green |
| 38.1-R4 | TBD | 5 | R4 | T-V5 / T-Spoofing | `credentials add` auto-binds 0/1/N matches per D-04..D-06 (non-TTY default-skip) | unit | `uv run pytest tests/test_credentials_cli.py::TestAutoBind -x` | ✅ | ✅ green |
| 38.1-R5 | TBD | 3 | R5 | — | Resolver short-circuits on `credential_id=`; D-11/D-12/D-13 paths | unit | `uv run pytest tests/test_proxmox_api.py::test_resolve_credential_id_short_circuit_phase381 tests/test_ssh_tools.py::test_resolve_credential_id_short_circuit_phase381 -x` | ✅ | ✅ green |
| 38.1-R6-AST | TBD | 4 | R6 | — | No `continue` in `scan_drift` row-loop body | AST | `uv run pytest tests/test_ast_regression.py::TestPhase381CredBinding::test_scan_drift_no_continue_in_row_loop_phase38_1 -x` | ✅ | ✅ green |
| 38.1-R6-Func | TBD | 4 | R6 | — | Drift never silently skips bound rows; routes via reason enum | functional | `uv run pytest tests/test_drift_detection.py::TestScanDriftNotEligible -x` | ✅ | ✅ green |
| 38.1-R7-bucket | TBD | 4 | R7 | — | `not_eligible` bucket + counts | unit | `uv run pytest tests/test_drift_detection.py::test_not_eligible_bucket_phase381 -x` | ✅ | ✅ green |
| 38.1-R7-elig | TBD | 6 | R7 | — | `eligibility` field on `get_network_sitemap` rows | unit | `uv run pytest tests/test_sitemap.py::test_eligibility_field_phase381 -x` | ✅ | ✅ green |
| 38.1-R8 | TBD | 5 | R8 | T-V5 | `link`/`unlink` CLI commands; type-mismatch errors | unit | `uv run pytest tests/test_credentials_cli.py::TestLinkUnlink -x` | ✅ | ✅ green |
| 38.1-R9 | TBD | 5 | R9 | — | `credentials remove` nulls bindings + prints affected rows | unit | `uv run pytest tests/test_credentials_cli.py::test_remove_nulls_bindings_phase381 -x` | ✅ | ✅ green |
| 38.1-R10 | TBD | 2 | R10 | T-V8 | Drop & recreate migration with version stamp; idempotent re-run; backup mode preserved | integration | `uv run pytest tests/test_migration_phase381.py -v` | ✅ | ✅ green |
| 38.1-Acceptance | TBD | 7 | SPEC §Acceptance | — | Headline round-trip succeeds across IP/short/FQDN registrations | integration | `uv run pytest tests/integration/test_credential_binding_round_trip.py -m integration -v` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Tests and scaffolds the planner MUST create in Wave 0 BEFORE any implementation wave runs:

- [ ] `tests/test_ast_regression.py` — append `class TestPhase381CredBinding`:
  - `test_scan_drift_no_continue_in_row_loop_phase38_1` (D-15: AST walker over `drift_detection.scan_drift` — fails if any `ast.Continue` node exists in its body)
  - `test_drift_loop_routes_degenerate_to_not_eligible_phase38_1` (D-17: at least one `not_eligible.append(...)` reachable from the degenerate-hostname branch)
- [ ] `tests/test_drift_detection.py` — append `class TestScanDriftNotEligible` (functional, D-18) with the 3-row fixture: NULL binding + bound UUID + degenerate row; assert correct bucket routing for each.
- [ ] `tests/test_credential_store.py` — extend with `test_register_credential_generates_uuid_phase381` + `test_get_credential_by_id_phase381` + rotation test (remove + add → fresh UUID).
- [ ] `tests/test_credentials_cli.py` — extend (or create) with `TestAutoBind` (0/1/N match handling, D-04..D-07 + non-TTY behavior), `TestLinkUnlink` (D-23..D-25), `test_remove_nulls_bindings_phase381` (D-26), `test_list_json_emits_credential_ids_phase381` (D-23).
- [ ] `tests/test_proxmox_api.py` + `tests/test_ssh_tools.py` — add `test_resolve_credential_id_short_circuit_phase381` covering D-11 stale, D-12 desync, D-13 host-mismatch UUID-wins, plus the legacy positional caller backward-compat assertion.
- [ ] `tests/test_database.py` — extend with `test_devices_has_credential_id_columns_phase381` (both adapters); `test_set_device_credential_binding_phase381` if planner lands the abstract method approach (Open Question §3).
- [ ] `tests/test_sitemap.py` — extend with `test_eligibility_field_phase381` (per-row `eligibility: {ssh: bool, proxmox: bool}`) + `test_discover_writes_credential_id_phase381`.
- [ ] `tests/test_migration_phase381.py` — NEW file: drop-and-recreate migration (registry archive + devices drop), version-stamp idempotency (re-run is no-op), `.bak.<ts>` uniqueness on retries (D-22), banner emission on stderr (D-21 three-block style), file-mode preservation on the backup (V8 control).
- [ ] `tests/integration/test_credential_binding_round_trip.py` — NEW file marked `@pytest.mark.integration`: SPEC §Acceptance "headline round-trip" (add → discover → drift) + identifier-form independence (IP / short / FQDN) + order independence (discover-first vs add-first).
- [ ] No new `conftest.py` fixtures required — existing fixtures cover sitemap and credential mocking patterns. (Planner verifies in Wave 0 task.)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Interactive overwrite prompt UX (D-04) | R4 | TTY interaction; pytest-can mock `input()` for unit but the operator-facing wording is human-judged | Run `homelab-mcp credentials add --type proxmox <hostname>` against a host whose row already has a binding; confirm prompt: `Sitemap row "<hostname>" is already bound to a different proxmox credential. Overwrite the mapping? [y/N]:`. Answer `n` → row unchanged. Answer `y` → row rebinds. |
| Multi-match prompt UX (D-06) | R4 | Numbered-list TTY interaction | Create two sitemap rows that both match a credential's hostname/IP. Run `credentials add` → expect numbered list with hostnames and `[s] skip auto-bind` option. |
| Migration banner content + recovery sequence (D-21) | R10 | Operator reads stderr and copy-pastes the recovery commands | Boot a server with pre-migration data; expect stderr to print three blocks (action / why / recovery commands); commands must be copy-paste runnable. |
| Release notes contain recovery sequence | R10 | Doc-correctness | Verify CHANGELOG/release notes section explicitly lists `credentials add` and `discover_and_map` recovery sequence before the v1.7.x tag is cut. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s for unit; < 3 min for full
- [ ] AST guard targets `scan_drift` (NOT `scan_infrastructure_drift` — pitfall 1 from RESEARCH.md)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** APPROVED — 2026-04-27

---

## Validation Audit 2026-04-27

| Metric | Count |
|--------|-------|
| Gaps found | 6 |
| Resolved | 6 |
| Escalated | 0 |

**Audit summary:**
- 50 unit tests + 5 integration tests + 6 newly-fixed migration tests = **58 GREEN, 1 platform-skipped (Windows backup-mode test)**, 0 RED
- Implementation correct; gaps were Wave 0 RED scaffolds in `tests/test_migration_phase381.py` that pre-dated Plan 04's CR-01/WR-09 defense-in-depth guard at `migration.py:208-227`
- Fix pattern: added `_install_legacy_devices_table` helper that drops the migrated-shape `devices` + `discovery_history` tables and recreates them in pre-Phase-38.1 schema (no binding columns), then seeds rows. All 6 destructive-path tests now exercise the migration as designed.
- All assertion intents (D-19 stamp idempotency, D-20 ordering, D-21 banner, D-22 microsecond timestamp, R10 drop, W5 history clear) preserved verbatim.

| Resolved Test | Requirement |
|---------------|-------------|
| `test_first_run_archives_registry_to_bak_phase381` | D-21 |
| `test_first_run_drops_devices_table_phase381` | R10 |
| `test_first_run_clears_discovery_history_phase381` | W5 |
| `test_drop_failure_leaves_stamp_unwritten_phase381` | D-20 |
| `test_bak_filename_collision_uses_microsecond_timestamp_per_d22` | D-22 |
| `test_banner_printed_to_stderr_3_blocks_per_d21` | D-21 / W5 |
