---
phase: 38.1-sitemap-keystore-credential-binding
verified: 2026-04-28T18:36:37Z
status: gaps_found
score: 9/10 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions:
    - "Phase 39 _enumerate_proxmox_vms helper introduced an unbinding regression on the Phase 38.1 R6 contract — drift_detection.py:419 calls get_proxmox_client without credential_id, breaking all 5 SPEC headline round-trip tests"
gaps:
  - truth: "Headline round-trip works: register-by-IP/short/FQDN + discover_and_map + scan_drift returns probed_ok ≥ 1 (SPEC §Acceptance Criteria headline; R6)"
    status: failed
    reason: |
      All 5 integration tests in tests/integration/test_credential_binding_round_trip.py
      that pin SPEC headline acceptance now fail on credential-cleanup HEAD. The Phase 39
      VM-enumeration helper _enumerate_proxmox_vms calls get_proxmox_client without the
      binding UUID, violating Phase 38.1 R6's "drift loop threads credential_id through
      every Proxmox-touching call" contract. The 5 fakes used by the round-trip tests
      assert credential_id is threaded; the Phase 39 helper never passes it, so every
      identifier-form / order permutation fails with the same AssertionError.

      This was verified working at the Phase 38.1 GREEN commit (46f7d59 — all 5 passed)
      and broke at Phase 39 (commit e05df24 onwards) when _enumerate_proxmox_vms was
      added inside scan_drift. From a goal-backward stance: at the current HEAD, the
      Phase 38.1 headline goal is no longer achieved.

      Phase 38.1 D-16 explicitly scoped the AST guard to scan_drift only, anticipating
      that "Phase 39's unknown/changed detection helpers will likely extend the guard's
      target list when they land" — but Phase 39 added the helper without extending the
      guard OR threading credential_id, leaving the 38.1 contract enforced only by the
      integration test (which is a real failing test, not just a missing guard).
    artifacts:
      - path: "src/homelab_mcp/drift_detection.py:419"
        issue: "get_proxmox_client(host=h, session=session) — missing credential_id= kwarg. Must be threaded from the probed_ok record (which currently does not carry the binding UUID forward, so the record shape also needs the column)."
      - path: "src/homelab_mcp/drift_detection.py:941"
        issue: "probed_ok.append({...}) record does not include proxmox_credential_id; _enumerate_proxmox_vms cannot recover the binding from the record because it was dropped at this site."
      - path: "tests/integration/test_credential_binding_round_trip.py"
        issue: "5/5 tests fail on credential-cleanup HEAD with `AssertionError: drift must pass binding UUID via credential_id=; got credential_id=None`. Tests passed at commit 46f7d59 (Phase 38.1 GREEN); regressed at commit e05df24 (Phase 39 _enumerate_proxmox_vms)."
    missing:
      - "Thread credential_id through _enumerate_proxmox_vms / _enum_one — either by carrying proxmox_credential_id in the probed_ok record shape, or by re-deriving from a hostname→binding map captured during the row loop."
      - "Extend the Phase 38.1 D-15 AST guard target list (or add a sibling guard) to cover _enumerate_proxmox_vms / _enum_one once it threads the binding, so this regression class is structurally caught next time."
human_verification: []
---

# Phase 38.1: Sitemap-keystore credential binding — Verification Report

**Phase Goal:** A user who runs the documented happy path — `credentials add --type proxmox <id> <user> <secret>`, then `discover_and_map <id>`, then `scan_infrastructure_drift` — sees their host in `probed_ok` (count ≥ 1), regardless of whether they registered the credential by IP, short hostname, or FQDN.

**Verified:** 2026-04-28T18:36:37Z
**Status:** gaps_found
**Re-verification:** No — initial retroactive verification

## Goal Achievement

### Observable Truths (10 SPEC requirements)

| #   | Truth (R-tag)                                                                  | Status      | Evidence |
| --- | ------------------------------------------------------------------------------ | ----------- | -------- |
| R1  | Registry entries carry an opaque UUIDv4 `credential_id`                         | ✓ VERIFIED  | `credential_store.py:246-249` generates `uuid.uuid4()` and persists it; `find_credential_by_id()` at line 307; rotation by remove+re-add produces fresh UUID (clean-slate semantics) |
| R2  | `devices` table has nullable `ssh_credential_id` + `proxmox_credential_id` columns on both adapters | ✓ VERIFIED  | SQLite: `database.py:220-221`, `:707-708` (Postgres). Bulk null + binding methods at 448, 543-544, 997, 1054 |
| R3  | `discover_and_map` writes `ssh_credential_id` from the credential it used        | ✓ VERIFIED  | `sitemap.py:430` calls `ssh_discover_system_with_binding`; `:460` writes `"ssh_credential_id": used_credential_id`. Helper `ssh_tools.py:_scan_registry_for_binding` (line 812) + `ssh_discover_system_with_binding` (line 838) wired |
| R4  | `credentials add` auto-binds new UUID to matching sitemap row                   | ✓ VERIFIED  | `server.py:_auto_bind_credential` (line 583); call site at `:808`. Handles 0/1/N match cases per D-04/D-05/D-06; cluster scope skip per D-07 (`server.py:619, :643, :666`) |
| R5  | Resolvers accept `credential_id` keyword-only param (Tier-0 short-circuit)      | ✓ VERIFIED  | `proxmox_api.py:223` (resolve_proxmox_credentials), `:264-322` Tier-0 path with D-11/D-12/D-13 logic; `ssh_tools.py:102, :138-186` same shape. Both keyword-only with default None per D-14 |
| R6  | Drift uses binding, never silently skips bound rows                             | ✗ **FAILED** | Row loop threads binding (`drift_detection.py:718, :751, :835`). **BUT** Phase 39 helper `_enumerate_proxmox_vms._enum_one` at `:419` calls `get_proxmox_client(host=h, session=session)` without `credential_id=`. All 5 round-trip integration tests fail on this. AST guard at `tests/test_ast_regression.py:774` covers only `scan_drift` body, not the helper |
| R7  | `not_eligible` bucket + counts in drift; `eligibility` field on sitemap rows   | ✓ VERIFIED  | Drift: `drift_detection.py:12, :607, :673` (bucket + counts). Sitemap: `database.py:542-545` (SQLite), `:1095-1097` (Postgres) — derives `eligibility = {ssh, proxmox}` from binding-column presence per D-10 |
| R8  | `homelab-mcp credentials link/unlink` CLI                                       | ✓ VERIFIED  | `server.py:_cmd_credentials_link` (line 976), `_cmd_credentials_unlink` (line 1034); subparsers at `:1272, :1292`. Type-mismatch validation per D-25 |
| R9  | `credentials remove` nulls sitemap bindings pointing at the removed UUID        | ✓ VERIFIED  | `server.py:_cmd_credentials_remove` (line 856) calls `null_bindings_for_credential_id` at `:912, :963`; bulk SQL at `database.py:bulk_null_credential_binding`. Affected-rows print on stderr per D-26 |
| R10 | Drop-and-recreate migration with backup of registry JSON                        | ✓ VERIFIED  | `migration.py:17` `_PHASE_38_1_KEY`; `:45` `_archive_registry_for_phase_38_1`; `:72` 3-block banner; `:208-255` SQLite path, `:542-592` Postgres path. Order per D-20 (backup → drop → stamp); fresh-timestamp-never-overwrite per D-22 |

**Score:** 9/10 truths verified. **R6 fails at the headline acceptance criterion.**

### Required Artifacts

| Artifact                                          | Expected                                          | Status     | Details |
| ------------------------------------------------- | ------------------------------------------------- | ---------- | ------- |
| `src/homelab_mcp/credential_store.py`             | UUID generation + `find_credential_by_id`         | ✓ VERIFIED | 331 lines; R1 wiring intact; CRED-04 import boundary preserved |
| `src/homelab_mcp/database.py` (SQLite + Postgres) | Two nullable UUID columns + binding setter/nuller | ✓ VERIFIED | Both adapters mirror; `eligibility` flatten at row-read |
| `src/homelab_mcp/proxmox_api.py`                  | `credential_id` keyword-only on resolver          | ✓ VERIFIED | `resolve_proxmox_credentials`; Tier-0 logic at D-11/12/13 lines 264-322 |
| `src/homelab_mcp/ssh_tools.py`                    | `credential_id` keyword-only + `_with_binding` helper | ✓ VERIFIED | `resolve_ssh_credentials` + `ssh_discover_system_with_binding` + `_scan_registry_for_binding` |
| `src/homelab_mcp/drift_detection.py` row loop     | Binding threaded; 5 buckets; D-15 no-continue     | ⚠️ PARTIAL | Row loop is correct (R6 contract met). _Sibling helper_ `_enumerate_proxmox_vms` violates the contract at line 419 (Phase 39 regression) |
| `src/homelab_mcp/sitemap.py`                      | `discover_and_map` writes `ssh_credential_id`     | ✓ VERIFIED | Lines 426-460 |
| `src/homelab_mcp/server.py` CLI                   | auto-bind + link/unlink + remove rotation         | ✓ VERIFIED | All four wired with module-wrapper seams |
| `src/homelab_mcp/migration.py`                    | Drop & recreate with backup + banner              | ✓ VERIFIED | Both adapters; idempotent stamp; defense-in-depth gate (CR-01 fix) |
| `tests/test_ast_regression.py::TestPhase381CredBinding` | D-15 no-continue + D-17 degenerate routing  | ✓ VERIFIED | 2 AST tests pass on HEAD (`scan_drift` only — D-16 scope) |
| `tests/integration/test_credential_binding_round_trip.py` | 5 SPEC headline acceptance tests          | ✗ FAILING  | **5/5 fail on credential-cleanup HEAD.** Passed at 46f7d59. Regressed by Phase 39 commit e05df24 |

### Key Link Verification

| From                                       | To                                          | Via                                              | Status     | Details |
| ------------------------------------------ | ------------------------------------------- | ------------------------------------------------ | ---------- | ------- |
| `register_credential` → registry JSON      | UUIDv4 persisted as `credential_id`         | `_save_registry`                                 | ✓ WIRED    | `credential_store.py:246-258` |
| `_cmd_credentials_add` → sitemap binding   | `_auto_bind_credential` → `set_device_credential_binding` | server-module wrappers → `db.set_device_credential_binding` | ✓ WIRED    | `server.py:808 → :583 → :538` |
| `discover_and_map` → sitemap row UUID      | `ssh_discover_system_with_binding` → upsert | `_scan_registry_for_binding` → `ssh_credential_id` field on upsert | ✓ WIRED    | `sitemap.py:430-460` |
| Resolver `credential_id=` → registry entry | Tier-0 short-circuit                        | `find_credential_by_id`                          | ✓ WIRED    | `proxmox_api.py:264-322`, `ssh_tools.py:138-186` |
| Drift row loop → resolver                  | `proxmox_credential_id` threaded            | `get_proxmox_client(credential_id=binding)`      | ✓ WIRED    | `drift_detection.py:718, :751, :835` |
| **Drift VM-enum helper → resolver**        | **`proxmox_credential_id` threaded**        | **`get_proxmox_client(credential_id=...)`**      | ✗ **NOT_WIRED** | **`drift_detection.py:419` passes only `host=h, session=session`. Phase 39 introduced this; Phase 38.1 contract violated.** |
| `_cmd_credentials_remove` → sitemap nulls  | `null_bindings_for_credential_id`           | `bulk_null_credential_binding` SQL UPDATE        | ✓ WIRED    | `server.py:912, :963` → `database.py` |
| `credentials link/unlink` CLI → DB         | `set_device_credential_binding(... | None)` | wrapper → adapter                                | ✓ WIRED    | `server.py:976, :1034` |
| Migration → backup file                    | `.bak.<microsecond_ts>` rename              | `_archive_registry_for_phase_38_1`               | ✓ WIRED    | `migration.py:45-69` |
| Migration → `devices` drop                 | `DROP TABLE devices`                        | guarded SQLite + Postgres branches               | ✓ WIRED    | `migration.py:208-255, :542-592` |

### Behavioral Spot-Checks

| Behavior                                                            | Command                                                                                  | Result    | Status |
| ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | --------- | ------ |
| Phase 38.1 AST guards pass                                          | `pytest tests/test_ast_regression.py::TestPhase381CredBinding --no-cov`                  | 2 passed  | ✓ PASS |
| All Phase 38.1-tagged tests (full footprint)                        | `pytest tests/ -k "phase38_1 or phase381 or Phase381 or Phase38_1" --no-cov`             | 49 passed, 5 failed, 1 skipped | ✗ FAIL — 5 round-trip fails |
| Round-trip integration suite (SPEC headline acceptance)             | `pytest tests/integration/test_credential_binding_round_trip.py --no-cov`                | 0 passed, 5 failed | ✗ FAIL |
| Round-trip suite at Phase 38.1 GREEN commit (regression-bisect)     | `git checkout 46f7d59 -- src/ tests/; pytest .../round_trip --no-cov`                    | 5 passed  | ✓ PASS — confirms regression occurred AFTER 38.1 |

### Requirements Coverage

| Requirement | Source Plan(s)            | Description                                                  | Status      | Evidence |
| ----------- | ------------------------- | ------------------------------------------------------------ | ----------- | -------- |
| R1          | 38.1-02                   | Registry UUID identifier on every entry                      | ✓ SATISFIED | `credential_store.py:246-258, :307-330` |
| R2          | 38.1-03                   | Sitemap binding columns on both adapters                     | ✓ SATISFIED | `database.py:220-221, :707-708` |
| R3          | 38.1-08                   | Auto-bind ssh_credential_id at discovery                     | ✓ SATISFIED | `sitemap.py:426-460` |
| R4          | 38.1-07                   | Auto-bind on `credentials add`                               | ✓ SATISFIED | `server.py:583-680, :808` |
| R5          | 38.1-05                   | Resolver `credential_id=` keyword-only param                 | ✓ SATISFIED | `proxmox_api.py:223`, `ssh_tools.py:102` |
| R6          | 38.1-06                   | Drift threads binding; never silently skips bound rows       | ✗ **BLOCKED** | Row-loop OK; `_enumerate_proxmox_vms:419` does NOT thread binding (Phase 39 regression) — round-trip integration tests fail at HEAD |
| R7          | 38.1-03 (sitemap), 38.1-06 (drift) | not_eligible bucket + sitemap eligibility field            | ✓ SATISFIED | `drift_detection.py:673`, `database.py:542-545` |
| R8          | 38.1-07                   | link/unlink CLI                                              | ✓ SATISFIED | `server.py:976, :1034, :1272, :1292` |
| R9          | 38.1-07                   | Rotation nulls bindings                                      | ✓ SATISFIED | `server.py:912, :963` → `database.bulk_null_credential_binding` |
| R10         | 38.1-04                   | Drop & recreate migration                                    | ✓ SATISFIED | `migration.py:17, :45-69, :72-105, :208-255, :542-592` |

### Anti-Patterns Found

| File                                          | Line  | Pattern                                                            | Severity   | Impact |
| --------------------------------------------- | ----- | ------------------------------------------------------------------ | ---------- | ------ |
| `src/homelab_mcp/drift_detection.py`          | 419   | `get_proxmox_client(host=h, session=session)` — missing `credential_id=`; identical bug-shape to original Bug O ("Tier-1 hostname-exact-match silently fails") | 🛑 Blocker  | Breaks SPEC headline round-trip; 5 integration tests RED |
| `src/homelab_mcp/ssh_tools.py`                | 312-401 | `_resolve_ssh_credentials_with_binding` is dead code retained "solely so the Phase 38.1-08 plan-acceptance grep finds the symbol" | ℹ️ Info    | Documented as deprecated in REVIEW-FIX WR-01; not blocker but worth removing in a follow-up |

### Human Verification Required

None mid-milestone — UAT explicitly deferred to milestone close per task instructions.

### Gaps Summary

**The phase goal is ALMOST achieved on HEAD: 9/10 SPEC requirements VERIFIED in code.** All wiring on the registry/keyring/sitemap/CLI/migration surfaces is intact and reaches its acceptance evidence. The one gap is real and observable:

**R6 — "drift uses binding, never silently skips bound rows" — is broken at HEAD.** Phase 38.1's headline integration test (5 permutations of identifier-form × order in `tests/integration/test_credential_binding_round_trip.py`) all fail with the same root cause: `drift_detection.py:419` (`_enumerate_proxmox_vms._enum_one`) calls `get_proxmox_client` without `credential_id=`, exactly the bug-shape Phase 38.1 was built to close.

This regression was introduced by **Phase 39** (commit `e05df24` — DRFT-17 unknown VM enumeration), not Phase 38.1 itself. At Phase 38.1's GREEN commit `46f7d59`, all 5 tests pass. Phase 38.1 D-16 explicitly anticipated this risk ("Phase 39's unknown/changed detection helpers will likely extend the guard's target list when they land") but the AST guard was not extended when `_enumerate_proxmox_vms` was added, and the helper itself was written without the keyword.

**Why this is a 38.1 verification gap, not just a Phase 39 followup:**
- The 5 failing tests are Phase 38.1's OWN acceptance-criterion tests
- The SPEC headline goal ("scan_drift sees host in probed_ok ≥ 1") is no longer verifiable end-to-end at HEAD
- Phase 40 already noted the failure as out-of-scope-for-Phase-40 but in-scope-for-someone (`40-03-SUMMARY.md`); this verifier flags it as the 38.1 owner's gap

**Recommended fix scope (out of band — verifier does not patch):**
1. Carry `proxmox_credential_id` forward into the `probed_ok` record shape at `drift_detection.py:941`
2. Pull binding from the record inside `_enum_one` and pass it as `get_proxmox_client(host=h, session=session, credential_id=binding)` at line 419
3. Extend `TestPhase381CredBinding` AST guard target list (or add `TestPhase39NoSkipInVMEnum`) so the regression class is structurally caught next time, per D-16's anticipated extension point

---

*Verified: 2026-04-28T18:36:37Z*
*Verifier: Claude (gsd-verifier)*
*Branch: credential-cleanup; work tree clean (read-only verification — no source changes)*
