---
phase: 06-tech-debt-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/homelab_mcp/vm_providers/base.py
  - src/homelab_mcp/vm_providers/docker_provider.py
  - src/homelab_mcp/vm_providers/lxd_provider.py
  - tests/test_vm_providers.py
autonomous: true
requirements: [DEBT-03]
must_haves:
  truths:
    - "All VM provider error paths return dicts with error, error_type, and detail fields"
    - "DockerProvider.list_vms exception handler returns structured error dict with error_type and detail"
    - "LXDProvider.list_vms exception handler returns structured error dict with error_type and detail"
  artifacts:
    - path: "src/homelab_mcp/vm_providers/base.py"
      provides: "Updated _format_error returning 6-field structured error dict"
      contains: "error_type"
    - path: "src/homelab_mcp/vm_providers/docker_provider.py"
      provides: "list_vms exception path with error_type and detail"
      contains: "error_type"
    - path: "src/homelab_mcp/vm_providers/lxd_provider.py"
      provides: "list_vms exception path with error_type and detail"
      contains: "error_type"
    - path: "tests/test_vm_providers.py"
      provides: "Tests verifying structured error fields in all error paths"
      contains: "test_error_result_has_required_fields"
  key_links:
    - from: "src/homelab_mcp/vm_providers/docker_provider.py"
      to: "src/homelab_mcp/vm_providers/base.py"
      via: "_format_error returns structured dict with error_type and detail"
      pattern: "error_type.*type\\(.*\\).__name__"
    - from: "src/homelab_mcp/vm_providers/lxd_provider.py"
      to: "src/homelab_mcp/vm_providers/base.py"
      via: "_format_error returns structured dict with error_type and detail"
      pattern: "error_type.*type\\(.*\\).__name__"
---

<objective>
Upgrade all VM provider error paths to return structured dicts with error, error_type, and detail fields instead of raw str(e) strings.

Purpose: DEBT-03 makes VM provider errors programmatically classifiable and loggable. Current error paths return bare {"error": str(e)} without type information, making automated error handling impossible.

Output: Updated _format_error in base.py, fixed bare exception handlers in docker_provider.py and lxd_provider.py, tests proving all error paths include required fields.
</objective>

<execution_context>
@/home/shaun/.claude/get-shit-done/workflows/execute-plan.md
@/home/shaun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tech-debt-cleanup/06-RESEARCH.md

@src/homelab_mcp/vm_providers/base.py
@src/homelab_mcp/vm_providers/docker_provider.py
@src/homelab_mcp/vm_providers/lxd_provider.py
@src/homelab_mcp/log_filter.py
@tests/test_vm_providers.py

<interfaces>
<!-- From src/homelab_mcp/vm_providers/base.py - current signature -->
```python
class VMProvider(ABC):
    def _format_error(self, operation: str, vm_name: str, error: str) -> dict[str, Any]:
        """Format error response."""
        return {
            "status": "error",
            "operation": operation,
            "vm_name": vm_name,
            "error": error,
        }
```

<!-- From src/homelab_mcp/log_filter.py -->
```python
def sanitize_error(error: BaseException | str) -> str:
    """Sanitize error messages to remove credentials and sensitive data."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade _format_error and fix bare exception handlers with structured error fields</name>
  <files>src/homelab_mcp/vm_providers/base.py, src/homelab_mcp/vm_providers/docker_provider.py, src/homelab_mcp/vm_providers/lxd_provider.py, tests/test_vm_providers.py</files>
  <behavior>
    - Test: _format_error with Exception arg returns dict with error, error_type, and detail fields
    - Test: _format_error with string arg returns dict with error, error_type ("Error"), and detail fields
    - Test: _format_error detail field uses sanitize_error (not raw repr)
    - Test: DockerProvider.deploy_vm error path includes error_type and detail fields
    - Test: DockerProvider.list_vms exception path includes error_type and detail fields
    - Test: LXDProvider.list_vms exception path includes error_type and detail fields
  </behavior>
  <action>
1. Add tests to tests/test_vm_providers.py (RED phase):
   - Test _format_error directly on a concrete provider (DockerProvider) with both Exception and string inputs
   - Verify error dicts contain all 3 required fields: error, error_type, detail
   - Test that error_type is the exception class name (e.g., "TimeoutError" not "Exception" for TimeoutError)
   - Test DockerProvider.list_vms exception path by mocking conn.run to raise Exception, then assert result has error_type and detail
   - Test LXDProvider.list_vms exception path similarly
   - Run tests, confirm they FAIL

2. Modify src/homelab_mcp/vm_providers/base.py (GREEN phase):
   - Change `_format_error` signature to accept `error: str | Exception` (not just str)
   - Import `sanitize_error` from `..log_filter`
   - When error is an Exception:
     ```python
     return {
         "status": "error",
         "operation": operation,
         "vm_name": vm_name,
         "error": str(error),
         "error_type": type(error).__name__,
         "detail": sanitize_error(error),
     }
     ```
   - When error is a string (backward compat for existing callers):
     ```python
     return {
         "status": "error",
         "operation": operation,
         "vm_name": vm_name,
         "error": error,
         "error_type": "Error",
         "detail": error,
     }
     ```
   - Update callers within base.py: wherever `_format_error(op, name, str(e))` is called, change to `_format_error(op, name, e)` to pass the Exception directly so error_type is derived from the actual exception class

3. Modify src/homelab_mcp/vm_providers/docker_provider.py (GREEN phase):
   - Find the bare exception handler in list_vms (~line 207) that returns `{"status": "error", "platform": "docker", "error": str(e)}`
   - Add `error_type` and `detail` fields:
     ```python
     except Exception as e:
         return {
             "status": "error",
             "platform": "docker",
             "error": str(e),
             "error_type": type(e).__name__,
             "detail": sanitize_error(e),
         }
     ```
   - Import `sanitize_error` from `..log_filter`
   - Search for any other bare `str(e)` error returns in docker_provider.py and fix them similarly
   - Update any callers of `self._format_error(op, name, str(e))` to pass `e` directly instead of `str(e)`

4. Modify src/homelab_mcp/vm_providers/lxd_provider.py (GREEN phase):
   - Same fixes as docker_provider.py for list_vms (~line 209) bare exception handler
   - Import `sanitize_error` from `..log_filter`
   - Update any callers of `self._format_error(op, name, str(e))` to pass `e` directly

5. Run tests to confirm GREEN.
  </action>
  <verify>
    <automated>cd /home/shaun/projects/mcp_python_server && uv run pytest tests/test_vm_providers.py -x -k "error" -v</automated>
  </verify>
  <done>All error paths return dicts with error, error_type, and detail; _format_error handles both Exception and string inputs; detail uses sanitize_error; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite and quality checks pass</name>
  <files>src/homelab_mcp/vm_providers/base.py, src/homelab_mcp/vm_providers/docker_provider.py, src/homelab_mcp/vm_providers/lxd_provider.py, tests/test_vm_providers.py</files>
  <action>
1. Run the full unit test suite to confirm no regressions from changing _format_error signature and error dict structure.
2. Run mypy on the modified vm_providers files.
3. Run ruff check and format on all modified files.
4. Run bandit on vm_providers to confirm no security flags from the error handling changes.
5. Fix any issues found.
  </action>
  <verify>
    <automated>cd /home/shaun/projects/mcp_python_server && uv run pytest tests/ -m "not integration" -x -q && uv run mypy src/homelab_mcp/vm_providers/ && uv run ruff check src/homelab_mcp/vm_providers/ tests/test_vm_providers.py</automated>
  </verify>
  <done>Full unit test suite passes, mypy clean, ruff clean, no bandit issues on all modified files.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_vm_providers.py -x -k "error" -v` — structured error tests pass
- `uv run pytest tests/ -m "not integration" -x -q` — full unit suite green
- `uv run mypy src/homelab_mcp/vm_providers/` — type checks clean
- `grep -r "error_type" src/homelab_mcp/vm_providers/` — confirms all providers include error_type field
</verification>

<success_criteria>
- _format_error in base.py accepts str | Exception and returns dict with error, error_type, and detail
- detail field uses sanitize_error() not raw repr() (see Research pitfall 3)
- DockerProvider.list_vms and LXDProvider.list_vms bare exception handlers include error_type and detail
- Existing callers passing string errors still work (backward compatible)
- Zero test regressions, mypy clean, ruff clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-tech-debt-cleanup/06-03-SUMMARY.md`
</output>
