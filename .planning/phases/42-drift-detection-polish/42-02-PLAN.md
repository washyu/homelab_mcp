---
phase: 42-drift-detection-polish
plan: 02
type: tdd
wave: 2
depends_on:
  - 42-01
files_modified:
  - tests/test_drift_detection_polish.py
autonomous: true
requirements:
  - 39-REVIEW-B1
  - 39-REVIEW-B2
  - 39-REVIEW-B3
  - 39-REVIEW-B4
  - 39-REVIEW-W1
  - 39-REVIEW-W2
  - 39-REVIEW-W3
  - 39-REVIEW-W4
  - 39-REVIEW-W5
  - 39-REVIEW-W6
  - 39-REVIEW-W7
  - 39-REVIEW-W8

must_haves:
  truths:
    - "A regression test for B1 constructs two sitemap rows with the SAME hostname but DIFFERENT ssh_credential_ids and DIFFERENT stored fingerprints, runs scan_drift, and asserts no phantom changed[] entry from cross-attribution."
    - "A regression test for B2 includes a /cluster/resources mock that returns one VM with vmid='abc' and asserts the scan completes without exception and the malformed VM is skipped."
    - "A regression test for B3 starts with empty _HOST_CLUSTER_CACHE and asserts that across N hosts in one cluster, /cluster/resources is called exactly once (not N times). Note: the post-revision-1 plan does NOT add a warm-up loop; the test asserts the existing dedupe behavior delivered by `_enumerate_proxmox_vms` line ~428 + `_make_row` line ~357."
    - "A regression test for B4 forces an enumeration failure and asserts logger.warning was called with the expected substring."
    - "A regression test for W1 mocks the DB adapter to return a `datetime` object for last_seen and asserts the JSON-serializable response has an ISO-8601 string for that field."
    - "A regression test for W2 (post writer-side fix) asserts sitemap.py's writer produces an aware UTC ISO-8601 string. The assertion uses the prefixed pattern `last_seen=datetime.now(UTC).isoformat()` (NOT the bare substring) to disambiguate from the unrelated `stored_at=` and `completed_at=` writer sites at sitemap.py lines 544 and 624 — those are out of scope for W2 and may legitimately remain naive."
    - "A regression test for W7 sets HOMELAB_DRIFT_MISSING_THRESHOLD_DAYS=1, simulates a host with last_seen exactly 24h+5min in the past, and asserts the host is promoted to status='missing'."
    - "A regression test for W4 imports `_probe_one` (or its parent) and AST-checks that the function body does NOT contain the string 'probe_one_unreachable_fallthrough'."
    - "A regression test for W6 exercises the inner /cluster/status `except` branch (drift_detection.py:1004 — `record_inner[\"scope\"] = scope` reads the loop-local). The fixture is shaped so Row A reaches the resolver-success branch (binds scope='cluster' for that iteration) and Row B falls to the inner /cluster/status except (where its emitted record's scope must be 'unknown' from B's iteration's explicit reset, not 'cluster' leaked from A's iteration). The `not_eligible` branch at lines 764-773 is NOT used as the assertion target because it hardcodes scope='unknown' at every emission site and therefore cannot detect a regression of the loop-local reset."
    - "All new tests pass (uv run pytest tests/test_drift_detection_polish.py -v exits 0)."
  artifacts:
    - path: "tests/test_drift_detection_polish.py"
      provides: "Regression tests for all 12 polish fixes — pinning the new behavior of B1-B4 and W1-W8."
      min_lines: 400
      contains: "class TestPhase42"
  key_links:
    - from: "tests/test_drift_detection_polish.py"
      to: "src/homelab_mcp/drift_detection.py"
      via: "import scan_drift, _bulk_universal_core_probes, _classify_unreachable, _parse_last_seen"
      pattern: "from homelab_mcp.drift_detection import"
    - from: "tests/test_drift_detection_polish.py::TestPhase42B1::test_dup_hostname_no_phantom"
      to: "_bulk_universal_core_probes tuple-key map"
      via: "two-row fixture with shared hostname + distinct ssh_credential_ids; assertion on changed[] is empty when stored fps match probes"
      pattern: "ssh_probe_results"
    - from: "tests/test_drift_detection_polish.py::TestPhase42W6::test_scope_does_not_leak_into_inner_except_branch"
      to: "drift_detection.py:1004 (record_inner['scope'] = scope inside /cluster/status except)"
      via: "two-row fixture: Row A reaches resolver-success and binds scope='cluster'; Row B falls to inner /cluster/status except; assert B's emitted record_inner['scope'] == 'unknown' (from B's iteration reset), NOT 'cluster' (which would prove a leak)"
      pattern: "record_inner\\[\"scope\"\\] = scope"
---

<objective>
Build the regression test harness for all 12 advisory fixes (B1-B4, W1-W8) applied in Plan 01. Each fix gets at least one test that pins the new behavior and would fail against the pre-Plan-01 implementation. The B1 phantom regression is mandatory and must construct the input shape, run scan_drift, and assert the previous bug-shape (cross-attributed phantom changed[] entry) is gone.

Purpose: Operators running scan_infrastructure_drift can rely on the polish fixes staying in place across future refactors. Any future regression that reverts a fix is caught by a named test in tests/test_drift_detection_polish.py.

Output: A new test module `tests/test_drift_detection_polish.py` organized into per-finding test classes (TestPhase42B1, TestPhase42B2, ..., TestPhase42W8), plus shared fixtures. All tests pass when run alone (`uv run pytest tests/test_drift_detection_polish.py -v`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-drift-detection-polish/42-01-PLAN.md
@.planning/phases/42-drift-detection-polish/42-01-SUMMARY.md
@.planning/phases/39-drift-detection-cases/39-REVIEW.md
@./CLAUDE.md
@src/homelab_mcp/drift_detection.py
@src/homelab_mcp/sitemap.py
@tests/test_drift_detection.py

<interfaces>
<!-- Patterns extracted from tests/test_drift_detection.py for reuse. -->

Existing test patterns:
- `class TestScanDrift4Bucket:` (line 21) — async tests using @pytest.mark.asyncio
- `async def test_three_row_classification(self):` (line 38) — fixture pattern: mock db_adapter.get_all_devices() returning a list of dicts; mock get_proxmox_client; assert envelope shape.
- `class TestScanDriftNotEligible:` (line 1038) — degenerate row + binding tests
- `class TestPhase39Helpers:` (line 1245) — pure-function tests for _diff_fingerprints, _classify_unreachable, etc.

Reuse patterns:
- pytest.mark.asyncio decorator
- pytest_mock's `mocker` for monkeypatching get_proxmox_client / resolve_ssh_for_sitemap_row / _HOST_CLUSTER_CACHE
- conftest.py session-autouse `_isolate_keyring` fixture (Phase 41.1) — already in scope; no re-import needed.
- Construct sitemap rows as dicts matching `db_adapter.get_all_devices()` row shape: keys hostname, connection_ip, ssh_credential_id, proxmox_credential_id, last_seen, status, fingerprint.

Imports the test file will need:
```python
import asyncio
import logging
from datetime import UTC, datetime, timedelta
from unittest.mock import AsyncMock, MagicMock, patch
import pytest
from homelab_mcp.drift_detection import (
    _bulk_universal_core_probes,
    _classify_unreachable,
    _diff_fingerprints,
    _enumerate_unknown_vms,
    _missing_threshold_days,
    _parse_last_seen,
    scan_drift,
)
from homelab_mcp.proxmox_api import _HOST_CLUSTER_CACHE
```
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → drift module | Tests construct adversarial inputs (malformed VMID, datetime objects in last_seen, duplicate hostnames) to exercise hardened paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-T1 | Tampering | Test fixtures | mitigate | Tests use `_isolate_keyring` (Phase 41.1) so no real keyring writes happen |
| T-42-T2 | Repudiation | Test outcomes | mitigate | Each test names its finding (TestPhase42B1::test_dup_hostname_no_phantom) so future regressions trace to the original bug |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BLOCKER regression tests (B1, B2, B3, B4)</name>
  <files>tests/test_drift_detection_polish.py</files>
  <read_first>
    - tests/test_drift_detection.py:1-50 (imports, fixture patterns)
    - tests/test_drift_detection.py:38-200 (TestScanDrift4Bucket — fixture construction examples)
    - tests/test_drift_detection.py:1245-1400 (TestPhase39Helpers — pure-function test patterns)
    - src/homelab_mcp/drift_detection.py:296-380 (_enumerate_unknown_vms — for B2/B3 fixture targets)
    - src/homelab_mcp/drift_detection.py:382-468 (_enumerate_proxmox_vms — for B3/B4 mocking)
    - src/homelab_mcp/drift_detection.py:470-582 (_bulk_universal_core_probes — for B1 fixture targets; tuple-key map post Plan 01)
    - tests/conftest.py (in full — to discover the _isolate_keyring fixture and any other shared fixtures)
  </read_first>
  <behavior>
    Test classes:

    `TestPhase42B1` — duplicate hostname phantom regression
    - test_dup_hostname_distinct_credentials_both_probed: two rows hostname='pve' with ssh_credential_id={'A','B'} and stored fingerprint={kernel: '6.5'}, {kernel: '6.8'}. Mock _probe_universal_core to return matching fingerprints per credential. Run scan_drift. Assert changed[] is empty (each row's stored matches its own probe).
    - test_dup_hostname_phantom_pre_polish_shape_is_gone: same fixture; assert ssh_probe_results map has TUPLE keys (hostname, ssh_credential_id) — not bare hostname. AST check on _bulk_universal_core_probes signature OR runtime introspection of the dict's key shape on first probe.

    `TestPhase42B2` — malformed vmid coercion
    - test_malformed_vmid_string_skipped_no_crash: /cluster/resources returns [{name: 'good', vmid: 100, type: 'qemu', node: 'pve1'}, {name: 'bad', vmid: 'abc', type: 'qemu', node: 'pve1'}]. scan_drift runs to completion. unknown[] has the 'good' VM only.
    - test_malformed_vmid_none_skipped_no_crash: vmid=None.
    - test_malformed_vmid_logs_warning: caplog captures a logger.warning with the host context (pve1) and raw vmid value.

    `TestPhase42B3` — _HOST_CLUSTER_CACHE cold-cache dedupe (existing-truth assertion)
    - test_cold_cache_single_enumeration_per_cluster: clear _HOST_CLUSTER_CACHE; mock /cluster/resources to count calls; construct three rows pointing at the same Proxmox cluster (cluster_name='cl1', distinct hostnames ['pve1','pve2','pve3']); run scan_drift. Assert /cluster/resources was called exactly ONCE (not 3 times). Per Plan 01 revision 1, this property is satisfied by the existing `(cluster_name OR hostname)` dedupe at `_enumerate_proxmox_vms` line ~428 — NOT by a warm-up loop. The test passes against the post-Plan-01 source as-is.
    - test_cold_cache_unknown_emits_one_per_vm_not_n_copies: same fixture; assert unknown[] has exactly len(unmatched_vms) entries (no N-copy duplication). Backed by the `(hypervisor, name.lower(), vmid)` consumer dedupe at `_make_row` line ~357.

    `TestPhase42B4` — enumeration failure visibility
    - test_enumeration_failure_logs_warning: mock get_proxmox_client (or client.get) to raise aiohttp.ClientError on /cluster/resources; assert caplog has a logger.warning with the host name.
    - test_enumeration_failure_does_not_abort_scan: mock failure on host A; assert host B's enumeration still runs and unknown[] has B's VMs.

    Use the existing test_drift_detection.py fixture patterns: mock db_adapter as MagicMock with get_all_devices returning the list of dicts; patch get_proxmox_client with AsyncMock returning a fake client whose .get(path) is AsyncMock returning the per-path payload.
  </behavior>
  <action>
Create `tests/test_drift_detection_polish.py`. Module-level docstring:

```python
"""Phase 42 regression tests — pin the polish fixes from 39-REVIEW.md.

Each test class covers one finding (B1-B4, W1-W8) so a future regression
trace from a failing test name lands directly on the bug it re-opens.

Test conventions:
  - All async tests use @pytest.mark.asyncio
  - Fixtures construct sitemap rows as plain dicts matching the
    db_adapter.get_all_devices() shape; no NetworkDevice instantiation
    needed (drift_detection reads dict-shaped rows).
  - _HOST_CLUSTER_CACHE.clear() is called at the start of each test that
    asserts cold-cache behavior; the cache is module-level state so
    parallel test runs MUST run with --forked or be opted out via the
    pytest.mark.forked marker if collisions surface.
  - caplog is used to assert logger.warning fires; the drift_detection
    logger name is `homelab_mcp.drift_detection`.
"""
```

Implement four test classes (TestPhase42B1, TestPhase42B2, TestPhase42B3, TestPhase42B4) per the behavior block above. Each class has 1-3 tests. Total ~10-12 test methods.

Shared fixture at module level:
```python
@pytest.fixture
def fake_db_adapter():
    """Returns a MagicMock db_adapter with a configurable get_all_devices()."""
    adapter = MagicMock()
    adapter.get_all_devices.return_value = []
    return adapter

@pytest.fixture(autouse=True)
def _clear_host_cluster_cache():
    """Each Phase 42 test starts with an empty _HOST_CLUSTER_CACHE so cold-cache
    semantics are deterministic. The cache is module-level state in proxmox_api,
    so any other test that pre-populated it would corrupt our assertions."""
    _HOST_CLUSTER_CACHE.clear()
    yield
    _HOST_CLUSTER_CACHE.clear()
```

For B1 specifically: import `_bulk_universal_core_probes` directly and call it with two-row input + mocked dependencies, then assert the returned dict's key shape is `tuple[str, str | None]` (post-Plan-01 form). This is the "the previous bug-shape is gone" assertion required by the phase must_haves.

Run after writing:
- `uv run pytest tests/test_drift_detection_polish.py::TestPhase42B1 -v`
- `uv run pytest tests/test_drift_detection_polish.py::TestPhase42B2 -v`
- `uv run pytest tests/test_drift_detection_polish.py::TestPhase42B3 -v`
- `uv run pytest tests/test_drift_detection_polish.py::TestPhase42B4 -v`

All must pass (counts may vary based on parameterization).
  </action>
  <verify>
    <automated>uv run pytest tests/test_drift_detection_polish.py::TestPhase42B1 tests/test_drift_detection_polish.py::TestPhase42B2 tests/test_drift_detection_polish.py::TestPhase42B3 tests/test_drift_detection_polish.py::TestPhase42B4 -v</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_drift_detection_polish.py` exists.
    - `grep -c "class TestPhase42B[1-4]" tests/test_drift_detection_polish.py` returns 4.
    - `grep -c "@pytest.mark.asyncio" tests/test_drift_detection_polish.py` returns ≥ 6 (at least one async test per BLOCKER class).
    - `grep -n "_HOST_CLUSTER_CACHE.clear" tests/test_drift_detection_polish.py` returns ≥ 1 hit (B3 cold-cache fixture).
    - `grep -n "test_dup_hostname" tests/test_drift_detection_polish.py` returns ≥ 1 hit (B1 phantom regression mandatory).
    - `grep -n "caplog" tests/test_drift_detection_polish.py` returns ≥ 2 hits (B2 + B4 use it).
    - `uv run pytest tests/test_drift_detection_polish.py -k "TestPhase42B" -v` exits 0.
  </acceptance_criteria>
  <done>
    All four BLOCKER regression test classes (TestPhase42B1, B2, B3, B4) exist with ≥ 1 named test each. The B1 phantom regression test is present and passing. Test methods together exercise the new (post-Plan-01) tuple-keyed probe map, malformed-vmid skip, cold-cache single-call invariant, and warning-level enumeration-failure logging.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WARNING regression tests (W1, W2, W3, W4, W5, W6, W7, W8)</name>
  <files>tests/test_drift_detection_polish.py</files>
  <read_first>
    - tests/test_drift_detection_polish.py (the file from Task 1; extend, do not overwrite)
    - src/homelab_mcp/drift_detection.py:139-194 (_DEFAULT_THRESHOLD_DAYS, _missing_threshold_days, _parse_last_seen — for W1/W2/W7 imports)
    - src/homelab_mcp/drift_detection.py:197-230 (_classify_unreachable — for W7 fixture)
    - src/homelab_mcp/drift_detection.py:236-293 (_diff_fingerprints — for W5 semantic test)
    - src/homelab_mcp/drift_detection.py:505-561 (_probe_one — for W4 AST check)
    - src/homelab_mcp/drift_detection.py:584-700 (scan_drift docstring — for W3 docstring assertion)
    - src/homelab_mcp/drift_detection.py:730-746 (W8 ssh_eligible_rows filter)
    - src/homelab_mcp/drift_detection.py:747-758 (W6 per-row reset — top of `for row in rows:`)
    - src/homelab_mcp/drift_detection.py:995-1027 (W6 inner /cluster/status except branch — `record_inner["scope"] = scope` reads the loop-local at line 1004; THIS is the W6 emission site Plan 02's W6 test must exercise)
    - src/homelab_mcp/sitemap.py:90-100, 160-175 (W2 writer-side assertion target — the two `last_seen=datetime.now(UTC).isoformat()` writers)
    - src/homelab_mcp/sitemap.py:540-550, 620-630 (W2 NEGATIVE-space — `stored_at=` and `completed_at=` writers; these may legitimately remain `datetime.now()` and the W2 test must NOT pick them up)
  </read_first>
  <behavior>
    Test classes:

    `TestPhase42W1` — last_seen ISO-8601 normalization at boundary
    - test_postgres_datetime_last_seen_serializes_as_iso_string: mock get_all_devices() to return a row with last_seen = datetime(2026, 4, 1, tzinfo=UTC); force the row into the missing branch (old date + threshold=1); run scan_drift; assert the unreachable[0]['last_seen'] is a string (not datetime) and json.dumps() does not raise.
    - test_integer_epoch_last_seen_stringified_when_parse_fails: row with last_seen=1735689600 (int); assert record['last_seen'] is the string '1735689600'.
    - test_date_object_last_seen_stringified_when_parse_fails: row with last_seen=date(2026,4,1) (date, not datetime); assert record['last_seen'] is a string and JSON-serializable.

    `TestPhase42W2` — canonical UTC writer at sitemap.py (PREFIXED-PATTERN ASSERTION)
    - test_sitemap_last_seen_writer_uses_utc_prefixed_pattern: source-inspection test using `inspect.getsource(sitemap)`. Use re.findall (or a line-scan) for the EXACT pattern `last_seen=datetime.now(UTC).isoformat()` and assert it appears at LEAST 2 times. This disambiguates from the unrelated `stored_at=datetime.now().isoformat()` and `completed_at=datetime.now().isoformat()` writer sites at sitemap.py lines ~544 / ~624 — those are out of scope for W2 and may legitimately remain naive. The bare substring `datetime.now(UTC).isoformat() in inspect.getsource(sitemap)` is INSUFFICIENT (Plan revision 1 fix) because the W2 fix is scoped only to the two `last_seen=` writers.
    - test_sitemap_last_seen_writer_no_naive_pattern_remains: assert the EXACT pattern `last_seen=datetime.now().isoformat()` (no UTC) appears 0 times in `inspect.getsource(sitemap)`.
    - (Optional, runtime confirmation) test_sitemap_writer_emits_aware_iso_string: call `sitemap.discover_and_map(...)` (or whichever path triggers a row write) with a mocked SSH/JSON layer, capture the resulting NetworkDevice's `last_seen` string, and assert it ends with `+00:00` or `Z`. Skip if the call graph is too heavy to mock cheaply; the prefixed-pattern source test above is the primary assertion.

    `TestPhase42W3` — 9-key missing-shape docstring matches runtime
    - test_missing_substatus_record_keys_match_docstring: trigger a missing-status emission via fixture; assert `set(record.keys()) == {hostname, connection_ip, scope, cluster_name, status, error, scan_timestamp, last_seen, message}` (exactly 9 keys).
    - test_unreachable_substatus_record_keys_match_7_key_base: trigger a non-missing unreachable emission; assert exactly 7 keys.

    `TestPhase42W4` — _probe_one dead fallthrough removed
    - test_probe_one_no_unreachable_sentinel_string: `import inspect; src = inspect.getsource(homelab_mcp.drift_detection._bulk_universal_core_probes); assert 'probe_one_unreachable_fallthrough' not in src`.
    - test_probe_one_exception_branch_returns_sanitized_error: mock _probe_universal_core to raise a custom exception; assert the returned dict has '_error' and not the unreachable sentinel string.

    `TestPhase42W5` — _diff_fingerprints asymmetric semantic
    - test_diff_emits_current_only_top_level: stored={kernel: '6.5'}, current={kernel: '6.5', package_fingerprint: 'sha256:...'}; assert `package_fingerprint` is in diff with stored=None.
    - test_diff_suppresses_stored_only: stored={kernel: '6.5', capabilities: {vulkan: True}}, current={kernel: '6.5'}; assert `capabilities.vulkan` is NOT in diff (D-09a suppression).

    `TestPhase42W6` — per-row scope reset (RESHAPED FIXTURE — Plan revision 1)
    - test_scope_does_not_leak_into_inner_except_branch: construct two rows in this order so they iterate sequentially in scan_drift's for-loop:
      * Row A — fully-bound row that REACHES the resolver-success branch and sets the loop-local `scope = "cluster"` and `cluster_name = "cl1"`. To get there, Row A must have valid hostname, ssh_credential_id, proxmox_credential_id; mock `resolve_proxmox_credentials` and `get_proxmox_client` so Row A's resolver succeeds; mock the SSH probe so Row A lands in `probed_ok` (or `changed`). After Row A's iteration, the loop-local variables hold scope='cluster', cluster_name='cl1'.
      * Row B — fully-bound row that ALSO reaches the resolver-success branch but whose `client.get("/cluster/status")` raises `aiohttp.ClientError` (or similar) so the inner /cluster/status `except` branch at drift_detection.py:1001 fires and emits `record_inner` whose `record_inner["scope"] = scope` (line 1004) reads the loop-local. To make the assertion meaningful, Row B's resolver-success branch must compute its OWN scope/cluster_name — mock the resolver so Row B's branch ASSIGNS scope='cluster' / cluster_name='cl2' explicitly during its iteration before the /cluster/status exception. (If Row B's resolver-success path never reassigns the loop-locals, the test devolves to "B inherits A's scope" and that's the leak we're catching — but the explicit reset at lines 757-758 means B starts each iteration with scope='unknown' / cluster_name=None and the resolver-success branch then ASSIGNS new values for B; if a future regression DROPS the explicit reset AND any code path after the reset fails to reassign before reaching line 1004, this test catches it.)
      * Run scan_drift. Locate B's `unreachable` record (status='unreachable' or 'missing'). Assert `record["scope"] != "cluster"` ESPECIALLY assert `record["scope"] != "cl1-derived-scope"`. Better: assert `record["scope"]` equals whatever B's iteration legitimately bound (e.g., 'cluster' from B's own resolver-success branch with cluster_name='cl2', NOT 'cl1' from A).
      * The KEY assertion is on `cluster_name`: Row B's emitted `record_inner["cluster_name"]` must equal 'cl2' (B's own) or None (B's iteration reset), NEVER 'cl1' (A's). If a regression drops the explicit `cluster_name: str | None = None` reset at line 758 AND B's resolver path doesn't reassign cluster_name before line 1005 (`record_inner["cluster_name"] = cluster_name`), B's record will leak A's 'cl1' — and the test catches that.
    - rationale-comment in test body: explicitly note that the `not_eligible` records at lines 764-773 hardcode `scope="unknown"` and CANNOT detect this regression; that is why the fixture targets the inner /cluster/status except branch instead.

    `TestPhase42W7` — timedelta second-resolution threshold
    - test_threshold_1_day_promotes_at_24h_plus_5min: monkeypatch HOMELAB_DRIFT_MISSING_THRESHOLD_DAYS=1; row last_seen = now - 24h - 5min; force probe failure; assert status='missing' (not 'unreachable').
    - test_threshold_1_day_does_not_promote_at_23h_55min: last_seen = now - 23h55min; force probe failure; assert status='unreachable' (not 'missing').

    `TestPhase42W8` — SSH pre-pass degenerate-row gate
    - test_degenerate_row_with_ssh_credential_id_not_probed: row with hostname='unknown' AND ssh_credential_id='abc' (degenerate but bound). Mock _bulk_universal_core_probes to track call inputs. Assert the row is NOT in the eligible set passed to the probe call.
    - test_status_error_row_with_ssh_credential_id_not_probed: row with hostname='pve', status='error', ssh_credential_id='abc'. Assert NOT probed.

    Append all eight test classes to the file from Task 1. Total file ends with ~20-25 test methods across all twelve test classes.
  </behavior>
  <action>
Extend `tests/test_drift_detection_polish.py` with eight more test classes per the behavior block. Each class has 1-3 tests; total ~12-15 new methods.

Style requirements:
- Use AAA (Arrange / Act / Assert) comments inside each test for readability.
- For W4's source-inspection test, use `inspect.getsource(_bulk_universal_core_probes)` rather than reading the file directly — survives file refactors.
- For W2's writer test (Plan revision 1 fix):
  * Use `inspect.getsource(sitemap)` to get the full module source.
  * Search for the PREFIXED pattern `last_seen=datetime.now(UTC).isoformat()` — count occurrences with `re.findall(r"last_seen=datetime\.now\(UTC\)\.isoformat\(\)", src)` and assert `len(matches) >= 2`.
  * Independently search for the PREFIXED naive pattern `last_seen=datetime.now().isoformat()` and assert it appears 0 times.
  * The bare substring assertion `"datetime.now(UTC).isoformat()" in inspect.getsource(sitemap)` is FORBIDDEN because it would be satisfied by an unrelated `stored_at=` or `completed_at=` writer happening to use UTC even though the W2 fix only covers the `last_seen=` writers.
  * Optional: also write a runtime test that calls a sitemap writer path and asserts the resulting `last_seen` string contains `+00:00` or `Z`. This is the strongest possible W2 assertion but requires mocking the SSH/JSON layer; treat as nice-to-have, not mandatory.
- For W7's threshold test, use `monkeypatch.setenv("HOMELAB_DRIFT_MISSING_THRESHOLD_DAYS", "1")` — NOT global os.environ mutation.
- For W6's scope-leak test (Plan revision 1 fix):
  * The fixture MUST exercise the inner /cluster/status except branch at drift_detection.py:1004 where `record_inner["scope"] = scope` reads the loop-local. The `not_eligible` records at lines 764-773 hardcode `scope="unknown"` so they CANNOT detect a regression of the explicit reset — using them as the assertion target is a false guarantee.
  * Construct two rows (Row A then Row B in the iteration order). Row A reaches resolver-success and binds scope='cluster' / cluster_name='cl1'. Row B reaches resolver-success and binds scope='cluster' / cluster_name='cl2' BUT then `client.get("/cluster/status")` raises so the inner except at line 995 fires and emits a record_inner.
  * Primary assertion: `record_inner["cluster_name"]` for B equals 'cl2' (B's own) or None (B's iteration reset), NEVER 'cl1' (A's leaked value). If the explicit `cluster_name: str | None = None` reset at line 758 is dropped AND B's path fails to reassign cluster_name before line 1005, B's emitted record will leak A's 'cl1' value and the assertion will fire.
  * Add a comment in the test body explaining why the not_eligible branch is NOT used: "The not_eligible records at lines 764-773 hardcode scope='unknown' at the emission site, so they cannot detect a regression of the loop-local reset. This test targets the inner /cluster/status except branch (line 1004) where `record_inner['scope'] = scope` actually consumes the loop-local."

Run after writing:
- `uv run pytest tests/test_drift_detection_polish.py -v`

All tests (Task 1's 4 classes + Task 2's 8 classes = 12 classes total) must pass. Total method count ≥ 22.
  </action>
  <verify>
    <automated>uv run pytest tests/test_drift_detection_polish.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TestPhase42W[1-8]" tests/test_drift_detection_polish.py` returns 8.
    - `grep -c "class TestPhase42B[1-4]\|class TestPhase42W[1-8]" tests/test_drift_detection_polish.py` returns 12 (all twelve finding-named test classes present).
    - `grep -n "monkeypatch.setenv.*HOMELAB_DRIFT_MISSING_THRESHOLD_DAYS" tests/test_drift_detection_polish.py` returns ≥ 1 hit (W7).
    - `grep -n "inspect.getsource" tests/test_drift_detection_polish.py` returns ≥ 1 hit (W4 OR W2).
    - `grep -n "probe_one_unreachable_fallthrough" tests/test_drift_detection_polish.py` returns ≥ 1 hit (W4 negative assertion).
    - `grep -n "datetime(2026" tests/test_drift_detection_polish.py` returns ≥ 1 hit (W1 datetime fixture) — OR equivalent datetime construction.
    - W2 prefixed-pattern assertion: `grep -nE "last_seen=datetime\\\\\\.now\\\\\\(UTC\\\\\\)\\\\\\.isoformat\\\\\\(\\\\\\)" tests/test_drift_detection_polish.py` returns ≥ 1 hit (the regex literal inside the test). Equivalently, `grep -n "last_seen=datetime" tests/test_drift_detection_polish.py` returns ≥ 1 hit. The W2 test MUST NOT use the bare substring `"datetime.now(UTC).isoformat()" in inspect.getsource(sitemap)` as its sole assertion.
    - W6 inner-except fixture: `grep -nE "cl1|cluster_name.*cl2" tests/test_drift_detection_polish.py` returns ≥ 1 hit (the two-row fixture distinguishing A's cluster_name from B's). The W6 test must reference `record_inner` OR target the inner /cluster/status except path explicitly (e.g., mock `client.get` to raise on /cluster/status only).
    - `uv run pytest tests/test_drift_detection_polish.py -v` exits 0 with ALL tests passing (no skips, no xfails).
    - Test method count: `uv run pytest tests/test_drift_detection_polish.py --collect-only -q | tail -1` reports ≥ 22 collected tests.
  </acceptance_criteria>
  <done>
    All eight WARNING regression test classes exist. Combined with Task 1's BLOCKER tests, the file pins behavior for all 12 polish findings. The W2 test uses a prefixed-pattern regex (not a bare substring) to disambiguate from out-of-scope writer sites. The W6 test exercises the inner /cluster/status except branch (line 1004) where the loop-local scope is actually consumed, NOT the not_eligible branch which hardcodes scope='unknown'. `uv run pytest tests/test_drift_detection_polish.py -v` exits 0. The full-suite quality gate (≥ 907 passing) is deferred to Plan 03.
  </done>
</task>

</tasks>

<verification>
At plan-end:

1. `uv run pytest tests/test_drift_detection_polish.py -v` exits 0 with 0 failures, 0 errors, 0 skips.
2. `uv run pytest tests/test_drift_detection_polish.py --collect-only -q | tail -1` reports ≥ 22 collected tests.
3. `git diff --stat tests/test_drift_detection_polish.py` shows the file was created (and nothing else in tests/ was modified).
4. Manual readback: each of the 12 findings (B1-B4, W1-W8) has at least one named test in TestPhase42<finding-id>. W2 uses prefixed-pattern regex assertion. W6 targets the inner /cluster/status except branch.
</verification>

<success_criteria>
- `tests/test_drift_detection_polish.py` exists with 12 test classes (TestPhase42B1..B4, TestPhase42W1..W8).
- Total ≥ 22 test methods, all passing.
- B1 phantom regression test is present, named, and explicitly asserts the previous bug-shape (cross-attributed phantom changed[] entry) is gone.
- W2 test uses the prefixed pattern `last_seen=datetime.now(UTC).isoformat()` (NOT the bare substring) to disambiguate from out-of-scope writer sites at sitemap.py lines 544 / 624.
- W6 test exercises the inner /cluster/status except branch at drift_detection.py:1004 (where `record_inner["scope"] = scope` actually consumes the loop-local) — NOT the `not_eligible` branch which hardcodes scope='unknown' and cannot detect a regression of the loop-local reset.
- No existing tests in `tests/test_drift_detection.py` are modified — Phase 42 polish lives in its own file.
- ruff + mypy clean on the new test file.
</success_criteria>

<output>
After completion, create `.planning/phases/42-drift-detection-polish/42-02-SUMMARY.md` documenting:
- Test class → finding ID mapping (table form).
- Number of tests per class.
- Any tests that required source-inspection (inspect.getsource) vs runtime fixtures, with rationale.
- For W2: confirm the prefixed-pattern regex assertion is in use (not a bare substring).
- For W6: confirm the test targets the inner /cluster/status except branch (line 1004), with grep evidence.
- Pytest output snippet showing the green run.
</output>
