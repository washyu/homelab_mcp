---
phase: 42-drift-detection-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/homelab_mcp/drift_detection.py
  - src/homelab_mcp/sitemap.py
autonomous: true
requirements:
  - 39-REVIEW-B1
  - 39-REVIEW-B2
  - 39-REVIEW-B3
  - 39-REVIEW-B4
  - 39-REVIEW-W1
  - 39-REVIEW-W2
  - 39-REVIEW-W3
  - 39-REVIEW-W4
  - 39-REVIEW-W5
  - 39-REVIEW-W6
  - 39-REVIEW-W7
  - 39-REVIEW-W8

must_haves:
  truths:
    - "Two sitemap rows sharing the same hostname both contribute SSH probe results to the drift comparison (no phantom changed[] entry from one row's stored_fp diffed against another row's probe)."
    - "A `/cluster/resources` payload that includes a single VM with vmid='abc' (or None, list, dict) does NOT abort the scan; the malformed VM is skipped with a warning and the rest of the scan proceeds."
    - "After a server restart, the first scan_drift call against an N-node cluster issues exactly ONE /cluster/resources call per cluster (not N), and the unknown[] bucket has exactly one entry per unmatched VM (not N copies). NOTE (Phase 42 revision): this property is satisfied UPSTREAM by `_enumerate_proxmox_vms`'s `(cluster_name OR hostname)` dedupe at line 428 and the `_make_row` consumer dedupe by `(hypervisor, name.lower(), vmid)` at line 357. The cache `_HOST_CLUSTER_CACHE` is side-effect-populated by `get_proxmox_client(host=hostname, ...)` during the resolver-success branch for every record that reaches `probed_ok + changed`. No explicit warm-up loop is added — the dedupe layers already deliver the truth. Verified by audit, not by new code."
    - "When a VM enumeration call raises any exception, the failure surfaces in logs at WARNING level (not DEBUG/silent) with the host context."
    - "A Postgres adapter that returns datetime objects for last_seen produces ISO-8601 string values in the JSON-serialized drift response (not raw datetime, not raw int, not raw date)."
    - "drift_detection.py and sitemap.py agree on the canonical timestamp convention: sitemap writes `datetime.now(UTC).isoformat()` and drift parses naive timestamps as UTC."
    - "With HOMELAB_DRIFT_MISSING_THRESHOLD_DAYS=1, a host whose last_seen is exactly 24h+1s in the past is promoted to status='missing' (not held back to 47h59m by .days floor)."
    - "_probe_one has no unreachable code paths; the dead `probe_one_unreachable_fallthrough` sentinel return is gone."
    - "_diff_fingerprints' asymmetric current-only-emit semantics are documented in code via inline comments referencing D-09a."
    - "scope and cluster_name are bound per-iteration of scan_drift's row loop with no possibility of leaking from a prior iteration."
    - "The SSH pre-pass call to _bulk_universal_core_probes never receives degenerate rows (hostname None/'' /'unknown' or status='error')."
  artifacts:
    - path: "src/homelab_mcp/drift_detection.py"
      provides: "All 12 polish fixes (B1-B4, W1-W8) applied; no dead code; canonical TZ; per-row scope reset"
      contains: "datetime.now(UTC)"
    - path: "src/homelab_mcp/sitemap.py"
      provides: "last_seen writer uses datetime.now(UTC).isoformat() at both call sites (lines 95, 168)"
      contains: "datetime.now(UTC).isoformat()"
  key_links:
    - from: "src/homelab_mcp/drift_detection.py:_bulk_universal_core_probes"
      to: "scan_drift row loop"
      via: "ssh_probe_results dict keyed by (hostname, ssh_credential_id) tuple; duplicate hostnames with distinct credential_ids both preserved (B1 fix)"
      pattern: "ssh_probe_results"
    - from: "src/homelab_mcp/sitemap.py:datetime.now(UTC).isoformat()"
      to: "src/homelab_mcp/drift_detection.py:_parse_last_seen"
      via: "ISO-8601 with UTC offset suffix; parse path returns aware UTC datetime"
      pattern: "datetime.now\\(UTC\\)"
    - from: "src/homelab_mcp/drift_detection.py:_classify_unreachable"
      to: "_parse_last_seen"
      via: "timedelta(days=threshold_days) comparison (W7 — second-resolution, not .days floor)"
      pattern: "timedelta\\(days=threshold_days\\)"
    - from: "src/homelab_mcp/drift_detection.py:_enumerate_proxmox_vms"
      to: "_make_row consumer (B3 dedupe layers)"
      via: "(cluster_name OR hostname) dedupe at line 428 + (hypervisor, name.lower(), vmid) dedupe at line 357 deliver the 'one /cluster/resources call per cluster, one unknown[] entry per VM' truth without an explicit cache warm-up"
      pattern: "seen_keys"
---

<objective>
Apply all 12 advisory fixes (B1-B4, W1-W8) surfaced by `.planning/phases/39-drift-detection-cases/39-REVIEW.md` to `src/homelab_mcp/drift_detection.py` and `src/homelab_mcp/sitemap.py`. Several findings (B2, B3, B4, W1, W3, W5, W6, W7, W8) were already partially addressed during Phase 39's iterative review or are satisfied by existing dedupe layers; this plan AUDITS each in turn against the current source, tightens any partial fix, and lands the still-outstanding ones (B1 multi-result attribution, W2 canonical UTC convention, W4 dead-code removal).

Purpose: A future operator running `scan_infrastructure_drift` against a real homelab — duplicate hostnames in the sitemap, malformed VMID payload, cold-cache server restart, Postgres adapter writing datetime objects, non-UTC clock, integer threshold of 1 day — never sees a phantom changed entry, a whole-scan abort, a duplicated unknown VM emission, a JSON serialization break, or a ±machine-TZ-offset threshold drift.

Output: Two source files (`drift_detection.py`, `sitemap.py`) updated with the still-outstanding fixes (B1, B2 log-level upgrade, W2, W4) plus audit notes for the verified-already-done findings (B3, B4, W1, W3, W5, W6, W7, W8). No new modules, no new helpers beyond what is needed for the W2 canonical convention. Tests are written in Plan 02; quality gate runs in Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-drift-detection-cases/39-REVIEW.md
@.planning/phases/39-drift-detection-cases/39-REVIEW-FIX.md
@.planning/phases/39-drift-detection-cases/39-CONTEXT.md
@./CLAUDE.md
@src/homelab_mcp/drift_detection.py
@src/homelab_mcp/sitemap.py

<interfaces>
<!-- Key contracts the executor MUST preserve. Extracted from drift_detection.py current source. -->

Module-level helpers in drift_detection.py:
- `_DEFAULT_THRESHOLD_DAYS: int = 7` (line 139)
- `_missing_threshold_days() -> int` (line 142)
- `_parse_last_seen(raw: str | datetime | None) -> datetime | None` (line 155)
- `_classify_unreachable(row, exc, threshold_days, now) -> tuple[Literal["unreachable", "missing"], str]` (line 197)
- `_diff_fingerprints(stored, current) -> dict[str, dict[str, Any]]` (line 236)
- `_enumerate_unknown_vms(cluster_vm_map, sitemap_hostnames, scan_timestamp) -> list[dict[str, Any]]` (line 296)
- `_enumerate_proxmox_vms(probed_ok_records, session, host_to_binding) -> dict[str, list[dict[str, Any]]]` (async, line 382)
- `_bulk_universal_core_probes(rows, *, db_adapter) -> dict[str, dict[str, Any]]` (async, line 470)
- `scan_drift(session, db_adapter, node, vm_type) -> dict[str, Any]` (async, line 584)

The 5-bucket envelope shape and the 7/9-key per-row record shapes are LOCKED by AST guards in tests/test_ast_regression.py — do NOT add fields to records, do NOT add buckets. Per-record warnings go to logger.warning, not envelope fields.

Cross-phase invariants to preserve:
- Phase 36 D-12/D-13: scan_drift reads only via db_adapter.get_all_devices()
- Phase 38.1 D-15: NO `continue` inside scan_drift's row loop body
- Phase 39 D-11(b): Phase 39 helpers stay loop-free w.r.t. bucket appends
- Phase 39.1 D-16: credential_id= threading through _enumerate_proxmox_vms._enum_one
- Phase 41-04/06: host=hostname (resolver/cache key) + dial_host=connection_ip (TCP target) split
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sitemap row → drift parse | Adapter-layer types (str / datetime / int / date) cross into drift's parse path |
| Proxmox API → drift | /cluster/resources payload may contain malformed records (vmid='abc', None, list, dict) |
| SSH host → drift probe | Two sitemap rows may share a hostname (migration overlap, race, degenerate); probe results must attribute correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | _enumerate_unknown_vms._make_row | mitigate | Tolerant int() coercion at vmid; on (ValueError, TypeError) emit `logger.warning` with host context (B2 audit) |
| T-42-02 | DoS / Tampering | _bulk_universal_core_probes hostname dedupe | mitigate | Preserve all SSH probe results when two rows legitimately share a hostname; no phantom diffs (B1 fix) |
| T-42-03 | Information Disclosure | _classify_unreachable.error message | mitigate | sanitize_error() already wraps the unreachable path — verify in audit (existing pattern, no regression) |
| T-42-04 | Denial of Service | _enumerate_proxmox_vms cold-cache dedupe | accept | Cold-cache 1st scan correctness preserved by `(cluster_name OR hostname)` dedupe at line 428 + `(hypervisor, name.lower(), vmid)` dedupe at line 357. The original B3 finding (N-copy unknown emission) is satisfied by these existing layers — no warm-up needed. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enumeration robustness (B1, B2, B3 audit, B4, W8)</name>
  <files>src/homelab_mcp/drift_detection.py</files>
  <read_first>
    - src/homelab_mcp/drift_detection.py:1-100 (module imports, _classify_credential_failure, _reason_message)
    - src/homelab_mcp/drift_detection.py:296-380 (_enumerate_unknown_vms — verify B2/B4 already done; tighten B2 warning level if currently debug; CONFIRM `seen_keys` dedupe at line 357-360 — this is part of B3's existing-truth)
    - src/homelab_mcp/drift_detection.py:382-468 (_enumerate_proxmox_vms — verify B4 logger.warning, CONFIRM `(cluster_name OR hostname)` dedupe at line 428 — this is the other half of B3's existing-truth)
    - src/homelab_mcp/drift_detection.py:470-582 (_bulk_universal_core_probes — B1 dedupe site at lines 569-576, audit & fix)
    - src/homelab_mcp/drift_detection.py:730-746 (scan_drift's ssh_eligible_rows filter — verify W8 already done at lines 730-737)
    - src/homelab_mcp/drift_detection.py:935-945 (the SINGLE B1 consumer site at line 939: `ssh_probe_results.get(hostname or "", {})`. NOTE: line 961-962 is `probe.get("_error")` — that's a lookup on the already-fetched probe dict, NOT a second `ssh_probe_results.get((` call site. Plan revision 1 corrected this misidentification.)
    - src/homelab_mcp/proxmox_api.py:243-280 (_HOST_CLUSTER_CACHE — read to confirm cache shape; cache is side-effect-populated by `get_proxmox_client(host=hostname, ...)` during resolver-success — no warm-up needed)
  </read_first>
  <behavior>
    - B1: Two rows with hostname='pve' (different ssh_credential_ids, different fingerprints) BOTH contribute probe results. The drift loop attributes each row's probe correctly so no row sees another row's probe as its current_fp.
    - B2: A /cluster/resources payload with vmid='abc' is skipped without raising; logger.warning fires with host + raw vmid context.
    - B3 (verified-already-done — NO source change): After a clean process restart with empty `_HOST_CLUSTER_CACHE`, the scan still emits exactly one unknown[] entry per unmatched VM. This is delivered by:
      (a) `_enumerate_proxmox_vms` line 428 dedupes targets by `(cluster_name OR hostname)` — BEFORE any /cluster/resources call. When two probed_ok rows in the same cluster both reach this function, the dedupe collapses them to ONE enumeration target per cluster regardless of cache state.
      (b) `_make_row` line 357-360 dedupes consumed VMs by `(hypervisor, name.lower(), vmid)`, so even if N enumeration calls returned the same VM list, only one unknown[] entry survives.
      (c) `_HOST_CLUSTER_CACHE` is populated as a side-effect of every `get_proxmox_client(host=hostname, ...)` call inside scan_drift's resolver-success branch. By the time `_enumerate_proxmox_vms` runs, every host in `probed_ok + changed` has already touched the cache.
      The audit task confirms (a), (b), (c) are present with grep snippets in the SUMMARY. No new code. Plan 02's B3 test asserts the existing dedupe behavior (one /cluster/resources call per cluster on cold cache).
    - B4: Any exception inside _enum_one's try/except logs at WARNING level (not DEBUG) with sanitize_error(exc) and host context.
    - W8: ssh_eligible_rows filter excludes rows with hostname None/''/'unknown' or status='error' BEFORE the bulk probe call.
  </behavior>
  <action>
**B1 — `_bulk_universal_core_probes` duplicate-hostname phantom (drift_detection.py:569-576):**

CURRENT (lines 569-581):
```python
seen_hostnames: set[str] = set()
eligible: list[dict[str, Any]] = []
for r in rows:
    h = r.get("hostname") or ""
    if not h or not r.get("ssh_credential_id") or h in seen_hostnames:
        continue
    seen_hostnames.add(h)
    eligible.append(r)
pairs = await asyncio.gather(...)
return dict(pairs)
```

The current dedupe DROPS the second row entirely. The B1 finding states the original bug is `dict(pairs)` collapsing two probes into one entry — but the current "dedupe + first-write-wins" approach silently drops the duplicate row's probe attempt. For Phase 42, REPLACE the silent-drop with: probe BOTH rows (or all N duplicates), keyed by `(hostname, ssh_credential_id)` so the row loop in scan_drift can look up each row's specific probe result.

Implementation:
1. Change the result map signature from `dict[str, dict[str, Any]]` to `dict[tuple[str, str | None], dict[str, Any]]` keyed on `(hostname, ssh_credential_id)`.
2. Remove the `seen_hostnames` filter (still skip empty hostname / None ssh_credential_id, but allow duplicates with distinct ssh_credential_ids).
3. Update the docstring (lines 470-501) to describe the (hostname, ssh_credential_id) key instead of the hostname-only key.
4. In `scan_drift` — there is exactly ONE consumer site at line 939: `probe = ssh_probe_results.get(hostname or "", {})`. Change to: `probe = ssh_probe_results.get((hostname, row.get("ssh_credential_id")), {})`. (Line 961-962 references `probe.get("_error")` — that operates on the already-fetched local `probe` dict and does NOT need to change.)
5. Two rows with the SAME (hostname, ssh_credential_id) ARE legitimately the same logical row — keep the dedupe ONLY for that exact tuple match (use a set of seen tuples).
6. Empty hostname '' rows are still filtered out at the W8 level (degenerate-row gate); we never reach _bulk_universal_core_probes with hostname=''.

**B2 — Verify malformed-vmid handling at drift_detection.py:337-345.** Current state has try/except with `logger.debug`. UPGRADE the log to `logger.warning` (the BL-04 finding upgraded enumeration-level errors to warning; per-VM malformed records deserve the same visibility — operators want to see "VM with vmid='abc' on host pve1 was skipped"):
```python
try:
    vmid = int(vm.get("vmid", 0))
except (ValueError, TypeError):
    logger.warning(
        "Skipping malformed vmid in /cluster/resources for %s: %r (VM name=%r)",
        hypervisor,
        vm.get("vmid"),
        vm.get("name"),
    )
    return None
```

**B3 — Verify-already-done (NO source change). Audit only.** The original B3 advice ("warm `_HOST_CLUSTER_CACHE` before enumeration") would have added a warm-up loop iterating `probed_ok + changed`. After re-reading the source, that loop is a no-op:

- `_HOST_CLUSTER_CACHE` is populated as a side-effect of `get_proxmox_client(host=hostname, ...)` inside scan_drift's resolver-success branch.
- Every record that lands in `probed_ok` or `changed` has reached the resolver-success branch, which means `get_proxmox_client` was called with that hostname, which means the cache has an entry for that hostname.
- A warm-up loop that iterates `probed_ok + changed` and calls `_HOST_CLUSTER_CACHE.setdefault(...)` would write nothing new — the cache is already warm.
- Independently, `_enumerate_proxmox_vms` line 428 already dedupes targets by `(cluster_name OR hostname)`, and `_make_row` line 357-360 dedupes consumed VMs by `(hypervisor, name.lower(), vmid)`. These two dedupe layers, not the cache, are what actually deliver the "one unknown entry per VM" truth.

ACTIONS for B3:
1. Do NOT add a warm-up loop. The original draft of this plan included one; the revision dropped it as a no-op.
2. In the SUMMARY, document B3 as "verified-already-done" with grep snippets showing:
   - `_HOST_CLUSTER_CACHE[hostname or h] = ...` in proxmox_api.py inside `get_proxmox_client` (the side-effect populator).
   - `targets = list({(c or h): (h, ci, c) for h, ci, c in pairs}.values())` at drift_detection.py:428 (the cluster-level dedupe).
   - `key = (hypervisor, name.lower(), vmid)` at drift_detection.py:357 + the `seen_keys` check (the consumer-level dedupe).
3. (Optional, IF audit reveals a transient_resolver_exc branch that bypasses get_proxmox_client and still reaches enumeration) — re-target a warm-up to that specific cold path. Otherwise skip.

**B4 — Verify enumeration-failure log level.** Current state at line 457 already uses `logger.warning`. AUDIT the message — confirm it includes (a) the host context, (b) `sanitize_error(exc)`, (c) reason this means VM-level drift detection silently broke. Line 457-461 already meets this; no change needed unless audit reveals regression.

**W8 — Verify SSH pre-pass degenerate-row gate.** Current state at drift_detection.py:730-737 already filters by hostname truthiness, hostname not in ('', 'unknown'), status != 'error', and ssh_credential_id present. AUDIT the filter — confirm parity with the row-loop degenerate-row routing at lines 764-773 (D-17). If parity is broken (e.g., row loop adds a new degenerate condition), align here.

After all five sub-fixes (B1 + B2 log upgrade are real edits; B3 + B4 + W8 are audits):
- Run `uv run ruff check src/homelab_mcp/drift_detection.py`
- Run `uv run mypy src/homelab_mcp/drift_detection.py`
Both must be clean.
  </action>
  <verify>
    <automated>uv run ruff check src/homelab_mcp/drift_detection.py && uv run mypy src/homelab_mcp/drift_detection.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "logger.warning" src/homelab_mcp/drift_detection.py` shows a hit inside `_make_row` (B2 upgrade) AND inside `_enum_one` (B4 already done).
    - `grep -nc "ssh_probe_results.get((" src/homelab_mcp/drift_detection.py` returns ≥ 1 (the single tuple-key consumer site at the post-fix replacement of line 939). Plan revision 1 correction: original draft asserted "two call sites" — there is ONE call site (line 939). Line 961-962 is `probe.get("_error")` which operates on the already-fetched `probe` local dict, not on `ssh_probe_results`.
    - `grep -nc "ssh_probe_results.get(hostname" src/homelab_mcp/drift_detection.py` returns 0 (the pre-fix bare-hostname lookup at line 939 is gone).
    - `grep -n "seen_hostnames" src/homelab_mcp/drift_detection.py` returns 0 hits (replaced by tuple-keyed approach) OR the seen-set name is renamed and the comment confirms multi-credential-per-hostname semantics.
    - `grep -nE "ssh_eligible_rows = \[" src/homelab_mcp/drift_detection.py` shows the W8 filter at the documented site.
    - B3 audit evidence (grep, no source change required):
      * `grep -nE "key = \(hypervisor, name\.lower\(\), vmid\)" src/homelab_mcp/drift_detection.py` returns ≥ 1 (the `_make_row` consumer dedupe at line ~357).
      * `grep -nE "\(c or h\): \(h, ci, c\)" src/homelab_mcp/drift_detection.py` returns ≥ 1 (the `_enumerate_proxmox_vms` cluster dedupe at line ~428).
      * `grep -nE "_HOST_CLUSTER_CACHE\[" src/homelab_mcp/proxmox_api.py` returns ≥ 1 (the side-effect cache populator inside `get_proxmox_client`).
    - `grep -n "Phase 42 B3" src/homelab_mcp/drift_detection.py` returns 0 hits (no warm-up comment block — B3 is audit-only per revision 1).
    - `uv run ruff check src/homelab_mcp/drift_detection.py` exits 0.
    - `uv run mypy src/homelab_mcp/drift_detection.py` exits 0.
  </acceptance_criteria>
  <done>
    All five enumeration-robustness findings (B1 fix, B2 log-upgrade, B3 verified-already-done, B4 verified, W8 verified) addressed. drift_detection.py is ruff + mypy clean. Test creation deferred to Plan 02.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Threshold/TZ + JSON serialization hygiene (W1, W2, W7)</name>
  <files>src/homelab_mcp/drift_detection.py, src/homelab_mcp/sitemap.py</files>
  <read_first>
    - src/homelab_mcp/drift_detection.py:139-194 (_DEFAULT_THRESHOLD_DAYS, _missing_threshold_days, _parse_last_seen — W1, W2, W7 sites)
    - src/homelab_mcp/drift_detection.py:197-230 (_classify_unreachable — W7 timedelta comparison at line 222; verify already done)
    - src/homelab_mcp/drift_detection.py:820-846, 916-928, 1010-1027 (the THREE last_seen normalization sites in scan_drift — W1 audit; ensure all three use the same 3-branch coercion pattern)
    - src/homelab_mcp/sitemap.py:1-50, 90-100, 160-175 (NetworkDevice.last_seen field at line 40; the two writer sites at lines 95, 168 — `datetime.now().isoformat()` to be replaced)
    - src/homelab_mcp/sitemap.py:540-550, 620-630 (the OTHER datetime.now().isoformat() sites at 544 and 624 — these are stored_at / completed_at; out of scope for W2 but verify they don't feed last_seen)
  </read_first>
  <behavior>
    - W2 canonical: `sitemap.py` writes `datetime.now(UTC).isoformat()` for the last_seen field. `_parse_last_seen` continues to treat naive timestamps as UTC for backward-compat with existing rows; the docstring is updated to mark the former imprecision as resolved at the writer side.
    - W1: All three last_seen-normalization sites in scan_drift use the EXACT same 3-branch coercion (parsed → isoformat; raw is not None → str(raw); else None). Any drift between the three sites is corrected.
    - W7: `_classify_unreachable` uses `(now - parsed) > timedelta(days=threshold_days)` (already at line 222). Verify in audit; no change needed unless regression.
    - A Postgres adapter returning `datetime` objects for last_seen produces ISO-8601 strings in the JSON-serialized response. A vendor-format string returns the raw string. An integer epoch returns its `str(int)` form. A `date` object returns `str(date)` form. JSON serialization never fails.
  </behavior>
  <action>
**W2 — sitemap.py canonical UTC writer (lines 95, 168 — the `last_seen=` writer sites):**

```python
# BEFORE (sitemap.py:95):
last_seen=datetime.now().isoformat(),
# AFTER:
last_seen=datetime.now(UTC).isoformat(),

# BEFORE (sitemap.py:168):
last_seen=datetime.now().isoformat(),
# AFTER:
last_seen=datetime.now(UTC).isoformat(),
```

Update the import at sitemap.py:7 from `from datetime import datetime` to `from datetime import UTC, datetime`.

Do NOT change the OTHER `datetime.now().isoformat()` sites at lines 544 and 624 — those are `stored_at` / `completed_at` for separate tables, not `last_seen`, and changing them is out of scope for W2 (would touch records the polish phase isn't auditing). The W2 fix is scoped EXCLUSIVELY to the two `last_seen=` writer sites; the verification in Plan 02 must disambiguate accordingly (see plan 02 W2 test — it asserts the prefix `last_seen=datetime.now(UTC).isoformat()` rather than the bare substring `datetime.now(UTC).isoformat()`).

After the writer is canonical, update `_parse_last_seen` docstring in drift_detection.py:155-194:
- The WR-03 paragraph (lines 168-183) describes the prior imprecision. UPDATE this paragraph to note that as of Phase 42, sitemap.py writes `datetime.now(UTC).isoformat()` so the unconditional `replace(tzinfo=UTC)` only applies to legacy rows from before the writer change. New rows already carry an explicit UTC offset (`+00:00`).
- Keep the parse code itself unchanged (lines 184-194) — it already handles both aware and naive datetimes correctly.

**W1 — Audit and align the THREE last_seen-normalization sites in scan_drift:**

Sites are:
- `drift_detection.py:820-844` (cluster-walk / outer except: lines following `except (aiohttp.ClientError, TimeoutError, ValueError) as exc:`)
- `drift_detection.py:916-927` (transient_resolver_exc fallback)
- `drift_detection.py:1010-1026` (inner /cluster/status except)

Each uses the 3-branch pattern:
```python
parsed = _parse_last_seen(row.get("last_seen"))
raw_last_seen = row.get("last_seen")
if parsed is not None:
    record["last_seen"] = parsed.isoformat()
elif raw_last_seen is not None:
    record["last_seen"] = str(raw_last_seen)
else:
    record["last_seen"] = None
```

AUDIT each site for byte-identical structure (modulo variable naming). If any site uses a different pattern (e.g., shorter coercion, missing branch), align it. Currently all three look identical — confirm via diff and leave untouched if so.

**W7 — Verify `_classify_unreachable` uses timedelta comparison:**

drift_detection.py:222 currently reads:
```python
if parsed is not None and (now - parsed) > timedelta(days=threshold_days):
```

This is correct (second-resolution). Confirm via grep; no change needed.

After all three sub-fixes:
- Run `uv run ruff check src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py`
- Run `uv run mypy src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py`
Both must be clean.
  </action>
  <verify>
    <automated>uv run ruff check src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py && uv run mypy src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "last_seen=datetime\.now\(UTC\)\.isoformat\(\)" src/homelab_mcp/sitemap.py` returns ≥ 2 hits (W2 fix prefixed-pattern; both writer sites near lines 95 and 168). The `last_seen=` prefix is REQUIRED to disambiguate from the unrelated `stored_at` / `completed_at` writers at lines 544 / 624.
    - `grep -n "last_seen=datetime.now().isoformat()" src/homelab_mcp/sitemap.py` returns 0 hits.
    - `grep -nE "from datetime import (UTC, datetime|datetime, UTC)" src/homelab_mcp/sitemap.py` returns 1 hit.
    - `grep -c "raw_last_seen" src/homelab_mcp/drift_detection.py` returns ≥ 3 (one per normalization site).
    - `grep -nE "\(now - parsed\) > timedelta\(days=threshold_days\)" src/homelab_mcp/drift_detection.py` returns 1 hit (W7 verified).
    - `grep -nE "\.days >\s*threshold_days" src/homelab_mcp/drift_detection.py` returns 0 hits.
    - `uv run ruff check src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py` exits 0.
    - `uv run mypy src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py` exits 0.
  </acceptance_criteria>
  <done>
    All three TZ/JSON-hygiene fixes (W1 alignment, W2 canonical UTC writer, W7 verified) applied. drift_detection.py and sitemap.py both ruff + mypy clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dead code + docstring sweep (W3, W4, W5, W6)</name>
  <files>src/homelab_mcp/drift_detection.py</files>
  <read_first>
    - src/homelab_mcp/drift_detection.py:236-293 (_diff_fingerprints — W5 documentation site)
    - src/homelab_mcp/drift_detection.py:505-561 (_probe_one inner — W4 dead fallthrough at line 553-561)
    - src/homelab_mcp/drift_detection.py:584-700 (scan_drift docstring — W3 9-key shape doc; verify lines 670-690 match the actual `unreachable` record shape)
    - src/homelab_mcp/drift_detection.py:747-758 (W6 per-row scope/cluster_name reset — verify already done)
    - src/homelab_mcp/drift_detection.py:1001-1027 (the inner /cluster/status `except` branch where `record_inner["scope"] = scope` reads the loop-local at line 1004 — this is the W6 emission site Plan 02's W6 test must exercise; the `not_eligible` records at lines 764-773 hardcode `scope="unknown"` so they CANNOT detect a regression of the explicit reset)
  </read_first>
  <behavior>
    - W4: `_probe_one` has NO unreachable `return (hostname, {"_error": "probe_one_unreachable_fallthrough"})` line. The `except Exception` catch-all already provides exhaustiveness.
    - W3: scan_drift's docstring documents the 9-key missing-substatus extension shape EXACTLY matching the runtime emission (the 7 base keys + last_seen + message). No drift between docstring and emission.
    - W5: `_diff_fingerprints` has an inline comment block referencing D-09a that documents the asymmetric current-only-emit semantic (current-only → emit, stored-only → suppress). Already documented; verify and tighten if needed.
    - W6: scan_drift's row loop has explicit `scope: str = "unknown"` and `cluster_name: str | None = None` reset at the top of each iteration (currently lines 757-758). Verify already done. The corresponding regression test (Plan 02 W6) must exercise the inner-except branch at line 1004 (where `record_inner["scope"] = scope` reads the loop-local) — NOT the `not_eligible` branch, which hardcodes scope='unknown' and cannot detect leakage.
  </behavior>
  <action>
**W4 — Remove the dead fallthrough in `_probe_one` (drift_detection.py:553-561):**

CURRENT (lines 553-561):
```python
        # Phase 41-06 WR-04: removed the unreachable belt-and-braces raise.
        # The bare ``except Exception`` above catches every exception type;
        # any future refactor that narrows it must add its own logged
        # sentinel-return path here, not rely on a structurally-unreachable
        # raise (would crash inside asyncio.gather(return_exceptions=False)
        # and abort the entire scan, defeating D-10). Sentinel return below
        # is required only for mypy's exhaustiveness check; runtime is
        # unreachable per the comment above.
        return (hostname, {"_error": "probe_one_unreachable_fallthrough"})
```

REPLACE with: DELETE lines 553-561 entirely. The bare `except Exception` at line 548-552 ALREADY satisfies mypy's exhaustiveness check (it catches everything). Mypy will not complain about a missing terminal return when every branch of the inner try/except returns. Verify with `uv run mypy src/homelab_mcp/drift_detection.py` after deletion.

If mypy DOES complain (unlikely but possible due to async-with control flow analysis), use this defensive form instead:
```python
        # mypy exhaustiveness: the bare `except Exception` above catches
        # all exception types; this raise is structurally unreachable but
        # makes the intent explicit. If a future refactor narrows the
        # except clause, this raise crashes loudly rather than silently
        # emitting a sentinel into the probe map.
        raise AssertionError(f"_probe_one reached unreachable fallthrough for hostname={hostname!r}")
```

This raises (loud failure) rather than returns a sentinel (silent failure mode). The 39-REVIEW WR-B finding explicitly recommends this form.

**W3 — Audit scan_drift docstring against runtime emission (drift_detection.py:584-700):**

The docstring at lines 662-690 documents:
- 7-key probed_ok / unreachable.status="unreachable" base
- 9-key unreachable.status="missing" extension (adds `last_seen` + `message`)

Compare against the runtime emission sites:
- 7-key: lines 984-993 (probed_ok), 1001-1009 (unreachable base before the if-substatus block)
- 9-key extension: lines 1010-1026 conditionally adds `last_seen` and `message` when `substatus == "missing"`

Verify:
1. The docstring's 7 keys match exactly: `hostname`, `connection_ip`, `scope`, `cluster_name`, `status`, `error`, `scan_timestamp`.
2. The docstring's 9-key extension keys: 7 base + `last_seen` + `message`.
3. Runtime emits the same 7 + 2 keys.

If any drift exists (e.g., a key was renamed in a prior phase but the docstring wasn't updated), correct the docstring. Currently the docstring at lines 662-690 looks correct — confirm and leave untouched.

ALSO — there are now THREE missing-substatus emission sites (the WR-C addition at lines 916-928 introduced a new one). Confirm that the docstring's 9-key shape applies to ALL three sites identically (lines 820-844, 916-927, 1010-1026 all attach `last_seen` and `message` only when `substatus == "missing"`). If any site emits a different key set in its missing branch, either align it or extend the docstring to document the variant.

**W5 — Audit `_diff_fingerprints` semantic comment (drift_detection.py:236-293):**

Current docstring at lines 240-263 already documents the asymmetric current-only-emit (WR-05). Verify the implementation at lines 264-292 matches. The docstring correctly says:
- stored-only → SUPPRESSED (D-09a capability drop)
- current-only → EMITTED with stored=None
- both with diff → EMITTED

Confirm the comprehension at line 276 (`keys_to_walk = [k for k in s.keys() | c.keys() if k in c]`) implements this. It does.

If the audit reveals the docstring is stale (e.g., describes a behavior the code no longer matches), align. Currently no drift — leave untouched.

**W6 — Verify per-row scope/cluster_name reset (drift_detection.py:757-758):**

Current state:
```python
scope: str = "unknown"
cluster_name: str | None = None
```

Located at the top of each `for row in rows:` iteration. Confirm via grep — currently at line 757-758. No change needed.

NOTE for the W6 regression test (lives in Plan 02): the test fixture must exercise the inner /cluster/status except branch at line 1004 (`record_inner["scope"] = scope` reads the loop-local) — NOT the `not_eligible` records at lines 764-773 which hardcode `scope="unknown"`. See Plan 02 W6 task for the corrected fixture shape (Row A reaches resolver-success and binds scope='cluster'; Row B falls to the inner /cluster/status except branch and emits a record_inner whose scope MUST be 'unknown' from B's loop iteration's reset, not 'cluster' leaked from A).

After all four sub-fixes (most are verifications):
- Run `uv run ruff check src/homelab_mcp/drift_detection.py`
- Run `uv run mypy src/homelab_mcp/drift_detection.py`
Both must be clean.
  </action>
  <verify>
    <automated>uv run ruff check src/homelab_mcp/drift_detection.py && uv run mypy src/homelab_mcp/drift_detection.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "probe_one_unreachable_fallthrough" src/homelab_mcp/drift_detection.py` returns 0 hits (the dead sentinel string is gone).
    - `grep -nE "raise AssertionError.*_probe_one|return.*hostname,.*_error" src/homelab_mcp/drift_detection.py | grep -i unreachable` returns ≤ 1 hit (either fully deleted, or replaced by AssertionError raise as documented in the action).
    - `grep -c "scope: str = \"unknown\"" src/homelab_mcp/drift_detection.py` returns ≥ 1 (W6 verified).
    - `grep -c "cluster_name: str | None = None" src/homelab_mcp/drift_detection.py` returns ≥ 1 (W6 verified).
    - `grep -n "last_seen.*scan_timestamp.*missing.*message" src/homelab_mcp/drift_detection.py` shows the docstring 9-key shape block (W3).
    - `grep -nE "current-only|WR-05" src/homelab_mcp/drift_detection.py` returns ≥ 1 hit inside _diff_fingerprints (W5).
    - `uv run ruff check src/homelab_mcp/drift_detection.py` exits 0.
    - `uv run mypy src/homelab_mcp/drift_detection.py` exits 0.
  </acceptance_criteria>
  <done>
    All four dead-code-and-docstring fixes (W3 verified, W4 dead return removed, W5 verified, W6 verified) applied. drift_detection.py is ruff + mypy clean. The full unit suite is NOT yet re-run — that's Plan 03's job.
  </done>
</task>

</tasks>

<verification>
At plan-end, before the SUMMARY:

1. `uv run ruff check src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py` exits 0.
2. `uv run mypy src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py` exits 0.
3. `git diff --stat src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py` shows changes ONLY in those two files.
4. Manual readback: every one of B1, B2, B3, B4, W1, W2, W3, W4, W5, W6, W7, W8 has a corresponding code change OR verified-already-done audit note in the per-task SUMMARY. Specifically B3 must be flagged "verified-already-done; no source change" with the three dedupe-layer grep snippets attached.
</verification>

<success_criteria>
- All 12 findings from `.planning/phases/39-drift-detection-cases/39-REVIEW.md` (B1-B4, W1-W8) are addressed in `drift_detection.py` and `sitemap.py`.
- ruff + mypy clean on both modified files.
- No tests have been added in this plan (Plan 02 owns regression tests; Plan 03 owns the full quality gate).
- Phase 36 D-12/D-13, Phase 38.1 D-15, Phase 39.1 D-16, Phase 41 host/dial_host invariants preserved (no `continue` added to scan_drift's row loop; no parallel-table reads added; credential_id= still threaded through `_enumerate_proxmox_vms._enum_one`; host=hostname / dial_host=connection_ip split preserved at every API call site).
</success_criteria>

<output>
After completion, create `.planning/phases/42-drift-detection-polish/42-01-SUMMARY.md` documenting:
- For each of the 12 findings: "fixed" (code change), "log-upgrade" (B2), or "verified already done" (audit pass — applies to B3, B4, W1, W3, W5, W6, W7, W8) with line numbers.
- For B3 specifically: include grep snippets showing the three layers that satisfy the truth without a warm-up loop:
  * `_HOST_CLUSTER_CACHE[...]` populator inside `get_proxmox_client` (proxmox_api.py).
  * `(c or h)` cluster dedupe in `_enumerate_proxmox_vms` (drift_detection.py:~428).
  * `(hypervisor, name.lower(), vmid)` consumer dedupe in `_make_row` (drift_detection.py:~357).
- Cross-phase invariant preservation evidence (grep snippets confirming no continue in scan_drift, credential_id= still passed, host=hostname pattern preserved).
- Any deviations from the plan (none expected) with rationale.
</output>
