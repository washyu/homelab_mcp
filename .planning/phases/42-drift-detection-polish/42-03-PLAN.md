---
phase: 42-drift-detection-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 42-01
  - 42-02
files_modified: []
autonomous: true
requirements:
  - 39-REVIEW-B1
  - 39-REVIEW-B2
  - 39-REVIEW-B3
  - 39-REVIEW-B4
  - 39-REVIEW-W1
  - 39-REVIEW-W2
  - 39-REVIEW-W3
  - 39-REVIEW-W4
  - 39-REVIEW-W5
  - 39-REVIEW-W6
  - 39-REVIEW-W7
  - 39-REVIEW-W8

must_haves:
  truths:
    - "The full unit test suite passes with ≥ 907 tests (no skips introduced beyond the existing 15)."
    - "ruff is clean on src/homelab_mcp/drift_detection.py and src/homelab_mcp/sitemap.py."
    - "mypy is clean on src/homelab_mcp/drift_detection.py and src/homelab_mcp/sitemap.py."
    - "Every existing AST guard still passes: Phase 36 D-12/D-13 (no parallel-table reads on drift-scan call chain), Phase 38.1 D-15 (no `continue` inside scan_drift body), Phase 39.1 D-16 (credential_id= threading), Phase 41 host/dial_host hygiene."
    - "tests/test_drift_detection.py (existing) still passes — Phase 42 changes did not regress any of the 25+ scan_drift tests there."
    - "tests/test_drift_detection_polish.py (new) passes."
    - "tests/test_ast_regression.py passes — no AST guard is broken by Plan 01's source edits."
  artifacts:
    - path: "tests/test_drift_detection_polish.py"
      provides: "Already-written; this plan only RUNS the suite, not edits it"
      min_lines: 1
    - path: ".planning/phases/42-drift-detection-polish/42-03-SUMMARY.md"
      provides: "Quality-gate evidence: pytest output snippet (passed/failed/skipped counts), ruff+mypy clean output, AST guard pass output"
      contains: "passed"
  key_links:
    - from: "uv run pytest tests/"
      to: "test_drift_detection_polish.py + test_drift_detection.py + test_ast_regression.py + all other unit tests"
      via: "pytest collection of all tests/test_*.py modules"
      pattern: "passed"
    - from: "uv run mypy src/homelab_mcp/drift_detection.py"
      to: "drift_detection.py post-Plan-01 source"
      via: "static type check"
      pattern: "Success: no issues"
---

<objective>
Run the full quality gate against the post-Plan-01 source and post-Plan-02 test suite. This plan does not edit code or tests; it executes the gate commands, captures evidence, and produces the per-phase quality SUMMARY. If any gate fails, this plan reports the failure and the phase is NOT marked complete until the underlying plan (01 or 02) is revised and this plan re-runs.

Purpose: A clean gate is the final ship-blocker for Phase 42. The gate verifies that the 12 polish fixes (Plan 01) and their regression tests (Plan 02) integrate cleanly with the existing 907+ test suite, the AST guards from prior phases (36 D-12/D-13, 38.1 D-15, 39.1 D-16, 41 host/dial_host hygiene) all still pass, and ruff+mypy are clean on the two source files modified by Plan 01.

Output: A 42-03-SUMMARY.md with raw pytest/ruff/mypy command output (or equivalent evidence). No source files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-drift-detection-polish/42-01-PLAN.md
@.planning/phases/42-drift-detection-polish/42-01-SUMMARY.md
@.planning/phases/42-drift-detection-polish/42-02-PLAN.md
@.planning/phases/42-drift-detection-polish/42-02-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- Commands and their expected output shapes. -->

Quality gate commands (in execution order):

1. Lint:
   `uv run ruff check src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py`
   Expected: `All checks passed!` and exit 0.

2. Type check:
   `uv run mypy src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py`
   Expected: `Success: no issues found in 2 source files` and exit 0.

3. AST guards:
   `uv run pytest tests/test_ast_regression.py -v`
   Expected: all guards pass (Phase 36 D-12/D-13, Phase 37 D-11, Phase 38.1 D-15/D-16, Phase 39.1 D-16, Phase 41 hygiene, Phase 41.1 keyring hygiene).

4. Phase 42 new tests in isolation:
   `uv run pytest tests/test_drift_detection_polish.py -v`
   Expected: ≥ 22 collected, 0 failed, 0 errored, 0 skipped.

5. Phase 39 existing drift tests still green:
   `uv run pytest tests/test_drift_detection.py -v`
   Expected: ~30+ tests, 0 failed.

6. Full unit suite:
   `uv run pytest tests/ -m "not integration"`
   Expected: ≥ 907 passed, ≤ 15 skipped, 0 failed, 0 errored.
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Quality-gate plan only runs commands; no input crosses any trust boundary |

## STRIDE Threat Register

This plan introduces no new code, no new attack surface. Threat model inherited from Plans 01 and 02.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-G1 | n/a | n/a | accept | Plan 03 is read-only; no mitigations needed |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run quality gate and capture evidence</name>
  <files>.planning/phases/42-drift-detection-polish/42-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/42-drift-detection-polish/42-01-SUMMARY.md (verify Plan 01 actually completed and what it changed)
    - .planning/phases/42-drift-detection-polish/42-02-SUMMARY.md (verify Plan 02 test counts)
  </read_first>
  <behavior>
    Execute the six gate commands in order. For each command:
    - Capture stdout + stderr + exit code.
    - If exit code != 0 (or expected pattern absent from output), STOP and write a FAILURE section in the SUMMARY with the offending command's full output, then exit Plan 03 with a clear error message — Plan 01 or Plan 02 must be revised before retrying.
    - If exit code == 0, capture the headline counts (e.g., "907 passed, 15 skipped") and continue.

    After all six pass, write the SUMMARY with a per-command evidence block. The SUMMARY is the deliverable.
  </behavior>
  <action>
**Step 1 — Lint:**
```bash
uv run ruff check src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py
```
Capture output. Expected: `All checks passed!` exit 0.

**Step 2 — Type check:**
```bash
uv run mypy src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py
```
Capture output. Expected: `Success: no issues found in 2 source files` exit 0.

**Step 3 — AST guards:**
```bash
uv run pytest tests/test_ast_regression.py -v
```
Capture output. Expected: all tests pass. CRITICAL: this catches any regression of Phase 36 D-12/D-13, Phase 37 D-11, Phase 38.1 D-15/D-16, Phase 39.1 D-16, Phase 41 host/dial_host hygiene, Phase 41.1 keyring hygiene.

**Step 4 — Phase 42 new tests:**
```bash
uv run pytest tests/test_drift_detection_polish.py -v
```
Capture output. Expected: ≥ 22 collected, 0 failed, 0 skipped.

**Step 5 — Phase 39 existing drift tests:**
```bash
uv run pytest tests/test_drift_detection.py -v
```
Capture output. Expected: 0 failed.

**Step 6 — Full unit suite:**
```bash
uv run pytest tests/ -m "not integration"
```
Capture output. Expected: ≥ 907 passed, 0 failed, 0 errored. (15 skipped is the existing baseline; do not regress.)

**Step 7 — Write `.planning/phases/42-drift-detection-polish/42-03-SUMMARY.md`** with the following structure:

  - YAML frontmatter with: phase=42-drift-detection-polish, plan=03, status=complete-or-failed, quality_gate_run_at=ISO-8601-UTC.
  - H1 heading: "Plan 03 — Quality Gate Summary".
  - Six H2 sections (one per gate step) following this shape: command run, exit code, output (in a fenced block).
    1. Step 1: Lint (ruff) — command "uv run ruff check src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py".
    2. Step 2: Type check (mypy) — command "uv run mypy src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py".
    3. Step 3: AST guards — command "uv run pytest tests/test_ast_regression.py -v". List named guards that passed.
    4. Step 4: Phase 42 new tests — command "uv run pytest tests/test_drift_detection_polish.py -v". Show count of tests and list test classes that passed.
    5. Step 5: Phase 39 existing drift tests — command "uv run pytest tests/test_drift_detection.py -v". Show passed count.
    6. Step 6: Full unit suite — command "uv run pytest tests/ -m not-integration". Show the "X passed, Y skipped, 0 failed" line.
  - H2 "Verdict" section with one of: all_passed (then a sentence: "Phase 42 quality gate is GREEN. All 12 polish findings (B1-B4, W1-W8) are landed, tested, and integrate cleanly with the existing test suite and AST guards.") OR failed_at_step_N (then quote the offending output).

If any step fails:
- Write the SUMMARY with `status: failed` and the offending step's full output.
- Stop. Do NOT proceed. The phase is NOT complete until the underlying plan is revised and Plan 03 re-runs cleanly.
- Return a FAILED status to the orchestrator.
  </action>
  <verify>
    <automated>uv run ruff check src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py && uv run mypy src/homelab_mcp/drift_detection.py src/homelab_mcp/sitemap.py && uv run pytest tests/test_ast_regression.py tests/test_drift_detection_polish.py tests/test_drift_detection.py -q && uv run pytest tests/ -m "not integration" -q</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/42-drift-detection-polish/42-03-SUMMARY.md` exists.
    - The SUMMARY has `status: complete` (not `status: failed`).
    - The SUMMARY's Step 1 section shows `All checks passed!` (or equivalent ruff success indicator).
    - The SUMMARY's Step 2 section shows `Success: no issues found`.
    - The SUMMARY's Step 6 section shows a `passed` count ≥ 907.
    - `uv run pytest tests/ -m "not integration" -q` exits 0 (verified by running it after the SUMMARY is written).
    - `git diff --stat src/ tests/` shows zero changes from this plan (Plan 03 is read-only against source/tests; only adds the SUMMARY under .planning/).
  </acceptance_criteria>
  <done>
    All six quality-gate commands pass. The SUMMARY is written with `status: complete` and per-step evidence. Phase 42 is ready to mark complete in the ROADMAP. Any failure aborts the phase and routes back to revise Plan 01 or Plan 02.
  </done>
</task>

</tasks>

<verification>
- All six gate commands exit 0.
- The 42-03-SUMMARY.md exists and has `status: complete`.
- No source or test files modified by this plan (read-only quality gate).
</verification>

<success_criteria>
- ruff clean on drift_detection.py + sitemap.py.
- mypy clean on drift_detection.py + sitemap.py.
- AST guards (test_ast_regression.py) all pass — no regression of Phase 36/37/38.1/39.1/41/41.1 invariants.
- Phase 42 new tests (test_drift_detection_polish.py) pass with ≥ 22 collected, 0 failed.
- Phase 39 existing tests (test_drift_detection.py) still pass.
- Full unit suite passes with ≥ 907 tests, 0 failed.
- Phase 42 ROADMAP entry can be flipped to `Complete` after this plan SUMMARY is committed.
</success_criteria>

<output>
After completion, the SUMMARY at `.planning/phases/42-drift-detection-polish/42-03-SUMMARY.md` IS the output. No additional artifacts.

The orchestrator will use this SUMMARY's `status: complete` field as the signal to mark Phase 42 done in the ROADMAP and advance to Phase 43.
</output>
