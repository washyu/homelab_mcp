---
phase: 20-release-automation-prmt-02
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_mcp_prompts.py
autonomous: true
requirements:
  - CLI-02

must_haves:
  truths:
    - "test_decommission_workflow_prompt asserts that prompt text contains 'get_network_sitemap'"
    - "test_decommission_workflow_prompt asserts that prompt text contains 'device_id'"
    - "Both new assertions FAIL (RED) because prompt_registry.py still uses the old hostname-based text"
  artifacts:
    - path: "tests/test_mcp_prompts.py"
      provides: "Extended test for decommission prompt with device_id resolution assertions"
      contains: "get_network_sitemap"
  key_links:
    - from: "tests/test_mcp_prompts.py::test_decommission_workflow_prompt"
      to: "src/homelab_mcp/prompt_registry.py::_build_decommission_result"
      via: "get_prompt_result call"
      pattern: "get_network_sitemap.*device_id"
---

<objective>
Wave 0: Add failing assertions to test_decommission_workflow_prompt for CLI-02.

Purpose: Establish the test contract for the PRMT-02 fix before implementing it (TDD RED phase). The current test only checks for `decommission_device_preview` and `confirm` — it does not verify that the AI is instructed to call `get_network_sitemap` first and use `device_id`.

Output: Extended test with two new assertions that are intentionally RED (current prompt text still uses `hostname=` not `device_id`).
</objective>

<execution_context>
@/home/shaun/.claude/get-shit-done/workflows/execute-plan.md
@/home/shaun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-release-automation-prmt-02/20-RESEARCH.md
@src/homelab_mcp/prompt_registry.py
@tests/test_mcp_prompts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend test_decommission_workflow_prompt with device_id assertions (RED)</name>
  <files>tests/test_mcp_prompts.py</files>
  <behavior>
    - Existing assertions (`decommission_device_preview` in combined_text, `confirm` in combined_text) MUST remain — they guard against regression
    - New assertion 1: `get_network_sitemap` appears in combined_text — verifies prompt instructs AI to call the sitemap tool first
    - New assertion 2: `device_id` appears in combined_text — verifies prompt uses device_id (integer) not hostname (string)
    - Both new assertions will FAIL (RED) immediately after commit because current prompt text contains neither string
    - `pytest --collect-only` must succeed — no import or collection errors
  </behavior>
  <action>
    Edit `tests/test_mcp_prompts.py` inside `test_decommission_workflow_prompt`. After the existing two assertions, add:

    ```python
    assert "get_network_sitemap" in combined_text, (
        "CLI-02: prompt must instruct AI to call get_network_sitemap to resolve hostname to device_id"
    )
    assert "device_id" in combined_text, (
        "CLI-02: prompt must use device_id (integer) not hostname — decommission_device schema requires device_id"
    )
    ```

    Do NOT modify any other test or any other part of the file. The test function is at line 39.

    After editing, run `uv run pytest tests/test_mcp_prompts.py::test_decommission_workflow_prompt -v` and confirm:
    - The test FAILs (RED) — expected at Wave 0
    - All other tests in the file still PASS
    - `uv run pytest --collect-only tests/test_mcp_prompts.py` succeeds with no errors

    Per project pattern (STATE.md Phase 12): Wave 0 tests are intentionally RED at commit time — correctness verified by pytest --collect-only.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mcp_prompts.py -v -m "not integration" 2>&1 | tail -20</automated>
  </verify>
  <done>
    test_decommission_workflow_prompt FAILS on the new assertions (get_network_sitemap, device_id not found in current prompt text).
    All other 5 tests in test_mcp_prompts.py PASS.
    pytest --collect-only shows no collection errors.
  </done>
</task>

</tasks>

<verification>
Run after task completion:
- `uv run pytest --collect-only tests/test_mcp_prompts.py` — must succeed (no import errors)
- `uv run pytest tests/test_mcp_prompts.py -v -m "not integration"` — exactly 1 FAILED (test_decommission_workflow_prompt), 5 PASSED
</verification>

<success_criteria>
Wave 0 contract established: test_decommission_workflow_prompt has RED assertions for CLI-02 (get_network_sitemap + device_id). Plan 20-03 will implement the fix to turn these GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/20-release-automation-prmt-02/20-01-SUMMARY.md`
</output>
