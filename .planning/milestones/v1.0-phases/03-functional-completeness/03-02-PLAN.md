---
phase: 03-functional-completeness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/homelab_mcp/sitemap.py
  - src/homelab_mcp/ssh_tools.py
  - src/homelab_mcp/service_installer.py
  - src/homelab_mcp/database.py
  - tests/test_silent_exceptions.py
autonomous: true
requirements: [FUNC-04]

must_haves:
  truths:
    - "No genuine silent except:pass remains in production code"
    - "All previously-silent handlers now emit log messages at debug or warning level"
  artifacts:
    - path: "src/homelab_mcp/sitemap.py"
      provides: "Logged exception handlers at lines 189-190 and 276-277"
      contains: "logger.debug"
    - path: "src/homelab_mcp/ssh_tools.py"
      provides: "Logged exception handlers at lines 480-482 and 575-576"
      contains: "logger.debug"
    - path: "src/homelab_mcp/service_installer.py"
      provides: "Logged exception handler at line 719-720"
      contains: "logger.debug"
    - path: "src/homelab_mcp/database.py"
      provides: "Logged exception handler at line 402-403"
      contains: "logger.debug"
    - path: "tests/test_silent_exceptions.py"
      provides: "AST-based test scanning for bare except:pass patterns"
      contains: "test_no_silent_exception_handlers"
  key_links:
    - from: "tests/test_silent_exceptions.py"
      to: "src/homelab_mcp/"
      via: "AST parsing of all .py files"
      pattern: "ast.parse"
---

<objective>
Replace all 6 silent exception handlers (except:pass) with logged handlers using logger.debug() or logger.warning(), and add an AST-based regression test that prevents new silent handlers from being introduced.

Purpose: Silent exception handlers mask real errors and make debugging impossible. FUNC-04 requires that all previously-silent handlers emit log messages.
Output: 6 modified exception handlers + 1 regression test file.
</objective>

<execution_context>
@/home/shaun/.claude/get-shit-done/workflows/execute-plan.md
@/home/shaun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-functional-completeness/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AST-based regression test for silent exception handlers</name>
  <files>tests/test_silent_exceptions.py</files>
  <behavior>
    - Test test_no_silent_exception_handlers: AST-parse all .py files in src/homelab_mcp/, find `except` clauses where the body is only `pass` (no logging, no raise, no assignment), fail if any found
    - The test must EXCLUDE known-acceptable patterns: abstract methods (pass in method with no other body), import fallbacks (except ImportError: ... with alternative import), and asyncio.CancelledError handlers
    - The test must report the exact file:line for each violation
  </behavior>
  <action>
    1. Create tests/test_silent_exceptions.py with a test that:
       - Uses ast module to parse all .py files under src/homelab_mcp/
       - Walks the AST looking for ExceptHandler nodes where the body is a single ast.Pass (or ast.Expr(ast.Constant(value=Ellipsis)))
       - Excludes these acceptable patterns:
         a. `except ImportError` (import fallbacks are fine)
         b. `except asyncio.CancelledError` (cancellation cleanup)
         c. Handlers inside abstract methods (class body with `pass` only)
       - Collects violations as list of (file, line_number, exception_type)
       - Asserts violations list is empty, printing all found violations on failure

    2. Run the test -- it MUST FAIL (RED phase) because the 6 silent handlers still exist.
  </action>
  <verify>
    <automated>uv run pytest tests/test_silent_exceptions.py -x -v 2>&1 | grep -E "FAILED|PASSED|violations"</automated>
  </verify>
  <done>Test file exists and correctly identifies the 6 known silent exception handlers. Test fails (RED) before fixes are applied.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 6 silent exception handlers with logged handlers</name>
  <files>src/homelab_mcp/sitemap.py, src/homelab_mcp/ssh_tools.py, src/homelab_mcp/service_installer.py, src/homelab_mcp/database.py</files>
  <action>
    Replace each `pass` with a logger.debug() call. Use debug level (not warning) because these are expected fallback paths, not unexpected errors. Each file already has a logger configured.

    **sitemap.py line 190** (in analyze_network_topology, topology analysis):
    ```
    except (ValueError, AttributeError):
        pass
    ```
    Replace `pass` with: `logger.debug("Skipping device %s in topology analysis: unable to parse disk usage", device.get("hostname", "unknown"))`

    **sitemap.py line 277** (in suggest_deployments, deployment suggestions):
    ```
    except (ValueError, AttributeError):
        pass
    ```
    Replace `pass` with: `logger.debug("Skipping device %s for deployment suggestion: unable to parse disk usage", hostname)`

    **ssh_tools.py line 482** (in ssh_discover_system, network interface JSON parsing fallback):
    ```
    except json.JSONDecodeError:
        pass
    ```
    Replace `pass` with: `logger.debug("JSON parsing failed for network interface data on %s, falling back to basic parsing", hostname)`

    **ssh_tools.py line 576** (in ssh_discover_system, block device JSON parsing):
    ```
    except json.JSONDecodeError:
        pass
    ```
    Replace `pass` with: `logger.debug("JSON parsing failed for block device data on %s", hostname)`

    **service_installer.py line 720** (in terraform service, output parsing):
    ```
    except json.JSONDecodeError:
        pass
    ```
    Replace `pass` with: `logger.debug("Failed to parse terraform output JSON for service %s", service_name)`

    **database.py line 403** (in discovery history, JSON parsing):
    ```
    except json.JSONDecodeError:
        pass
    ```
    Replace `pass` with: `logger.debug("Failed to parse discovery history JSON for record at %s", row[1] if len(row) > 1 else "unknown")`

    After all replacements, run the AST test -- it MUST PASS (GREEN phase).
  </action>
  <verify>
    <automated>uv run pytest tests/test_silent_exceptions.py -x -v</automated>
  </verify>
  <done>All 6 silent exception handlers replaced with logger.debug() calls. AST regression test passes. No bare except:pass remains in production code.</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_silent_exceptions.py -x -v
uv run pytest tests/ -m "not integration" -x -q
</verification>

<success_criteria>
- All 6 identified silent exception handlers now have logger.debug() calls
- AST-based test confirms zero silent handlers remain in src/homelab_mcp/
- Existing test suite remains green
- Test serves as regression guard against future silent handlers
</success_criteria>

<output>
After completion, create `.planning/phases/03-functional-completeness/03-02-SUMMARY.md`
</output>
