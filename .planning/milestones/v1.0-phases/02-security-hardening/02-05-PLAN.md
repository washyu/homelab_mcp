---
phase: 02-security-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/homelab_mcp/proxmox_api.py
  - src/homelab_mcp/vm_operations.py
  - src/homelab_mcp/infrastructure_crud.py
  - src/homelab_mcp/ssh_tools.py
  - src/homelab_mcp/service_installer.py
  - src/homelab_mcp/sitemap.py
  - src/homelab_mcp/http_transport.py
  - src/homelab_mcp/proxmox_scripts.py
  - tests/test_sanitize_wiring.py
autonomous: true
requirements:
  - SEC-04
gap_closure: true

must_haves:
  truths:
    - "No str(e) calls remain in error response dicts returned to MCP clients across proxmox_api.py, vm_operations.py, infrastructure_crud.py, ssh_tools.py, and service_installer.py"
    - "All exception messages in error responses use sanitize_error(e) instead of str(e)"
    - "Existing test suite passes after the replacements"
  artifacts:
    - path: "src/homelab_mcp/proxmox_api.py"
      provides: "Sanitized error responses in all 16 except blocks"
      contains: "sanitize_error"
    - path: "src/homelab_mcp/vm_operations.py"
      provides: "Sanitized error responses in all 12 except blocks"
      contains: "sanitize_error"
    - path: "src/homelab_mcp/infrastructure_crud.py"
      provides: "Sanitized error responses in all 13 except blocks"
      contains: "sanitize_error"
    - path: "tests/test_sanitize_wiring.py"
      provides: "Tests proving sanitize_error is used in error paths"
  key_links:
    - from: "src/homelab_mcp/proxmox_api.py"
      to: "src/homelab_mcp/log_filter.py"
      via: "from .log_filter import sanitize_error"
      pattern: "sanitize_error\\(e\\)"
    - from: "src/homelab_mcp/vm_operations.py"
      to: "src/homelab_mcp/log_filter.py"
      via: "from .log_filter import sanitize_error"
      pattern: "sanitize_error\\(e\\)"
    - from: "src/homelab_mcp/infrastructure_crud.py"
      to: "src/homelab_mcp/log_filter.py"
      via: "from .log_filter import sanitize_error"
      pattern: "sanitize_error\\(e\\)"
---

<objective>
Replace all raw str(e) calls in error response dicts with sanitize_error(e) across the five production modules identified in the verification gap.

Purpose: SEC-04 requires credentials never appear in error responses to MCP clients. The logging path is covered by CredentialFilter, but ~60+ str(e) calls in direct try/except blocks bypass sanitization. This plan closes that gap.
Output: All error response dicts use sanitize_error(e), with tests proving the wiring.
</objective>

<execution_context>
@/home/shaun/.claude/get-shit-done/workflows/execute-plan.md
@/home/shaun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-hardening/02-VERIFICATION.md

@src/homelab_mcp/log_filter.py

<interfaces>
<!-- From src/homelab_mcp/log_filter.py -->
```python
def sanitize_error(error: Exception) -> str:
    """Sanitize an exception message by redacting credentials.
    Use this instead of str(e) when including exception details in
    error responses returned to MCP clients."""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace str(e) with sanitize_error(e) in proxmox_api.py and vm_operations.py</name>
  <files>src/homelab_mcp/proxmox_api.py, src/homelab_mcp/vm_operations.py</files>
  <action>
    1. **src/homelab_mcp/proxmox_api.py** (16 str(e) calls):
       - Add `from .log_filter import sanitize_error` at the top imports.
       - Find every `str(e)` in error response dicts (lines ~268-676 per verification report). Replace each `str(e)` with `sanitize_error(e)`.
       - Be careful NOT to replace `str(e)` that is used for control flow (like checking exception type). Only replace those in f-strings or dict values that become part of the response to MCP clients.
       - If any `str(e)` appears in a `logger.*` call, leave it -- the CredentialFilter on the root logger already handles logging redaction.

    2. **src/homelab_mcp/vm_operations.py** (12 str(e) calls):
       - Add `from .log_filter import sanitize_error` at the top imports.
       - Same pattern: replace `str(e)` in error response dicts with `sanitize_error(e)`.
       - Leave `str(e)` in logger calls unchanged (covered by CredentialFilter).

    3. Run test suite to verify no regressions.
  </action>
  <verify>
    <automated>uv run pytest tests/ -m "not integration" -x -q</automated>
  </verify>
  <done>proxmox_api.py has 0 raw str(e) calls in error response dicts (all use sanitize_error). vm_operations.py has 0 raw str(e) calls in error response dicts. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace str(e) in infrastructure_crud.py, ssh_tools.py, service_installer.py, and remaining files</name>
  <files>src/homelab_mcp/infrastructure_crud.py, src/homelab_mcp/ssh_tools.py, src/homelab_mcp/service_installer.py, src/homelab_mcp/sitemap.py, src/homelab_mcp/http_transport.py, src/homelab_mcp/proxmox_scripts.py, tests/test_sanitize_wiring.py</files>
  <action>
    1. **src/homelab_mcp/infrastructure_crud.py** (13 str(e) calls):
       - Add `from .log_filter import sanitize_error` import.
       - Replace all `str(e)` in error response dicts with `sanitize_error(e)`.

    2. **src/homelab_mcp/ssh_tools.py** (6 str(e) calls):
       - Add `from .log_filter import sanitize_error` import.
       - Replace all `str(e)` in error response dicts with `sanitize_error(e)`.

    3. **src/homelab_mcp/service_installer.py** (3 str(e) calls):
       - Add `from .log_filter import sanitize_error` import.
       - Replace all `str(e)` in error response dicts with `sanitize_error(e)`.

    4. **Remaining files** -- also check and fix:
       - `src/homelab_mcp/sitemap.py` (2 str(e) calls)
       - `src/homelab_mcp/http_transport.py` (4 str(e) calls)
       - `src/homelab_mcp/proxmox_scripts.py` (5 str(e) calls)
       For each: add `from .log_filter import sanitize_error` and replace str(e) in error response dicts.

    5. **tests/test_sanitize_wiring.py**: Create a test file that verifies sanitize_error is imported in the key modules:
       - `test_proxmox_api_uses_sanitize_error`: Import proxmox_api module, read its source with `inspect.getsource()`, assert "sanitize_error" appears and count of raw `str(e)` in error response patterns (f-string or dict value context) is 0.
       - `test_vm_operations_uses_sanitize_error`: Same for vm_operations.
       - `test_infrastructure_crud_uses_sanitize_error`: Same for infrastructure_crud.
       - `test_ssh_tools_uses_sanitize_error`: Same for ssh_tools.
       Alternative simpler approach: just use `grep -c "str(e)" src/homelab_mcp/{file}.py` in a subprocess and assert the count is within expected range (only logger calls should remain).

    6. Run full test suite.
  </action>
  <verify>
    <automated>uv run pytest tests/ -m "not integration" -x -q</automated>
  </verify>
  <done>All five production modules (proxmox_api, vm_operations, infrastructure_crud, ssh_tools, service_installer) plus sitemap, http_transport, and proxmox_scripts use sanitize_error(e) in error responses. No raw str(e) remains in error response dicts returned to MCP clients. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -m "not integration" -x -q` -- full unit test suite passes
- `grep -rn "sanitize_error" src/homelab_mcp/ --include="*.py"` shows sanitize_error imported in all files that return error responses
- `grep -c "str(e)" src/homelab_mcp/proxmox_api.py src/homelab_mcp/vm_operations.py src/homelab_mcp/infrastructure_crud.py src/homelab_mcp/ssh_tools.py src/homelab_mcp/service_installer.py` -- any remaining str(e) should only be in logger calls, not error response dicts
</verification>

<success_criteria>
- sanitize_error imported from .log_filter in proxmox_api.py, vm_operations.py, infrastructure_crud.py, ssh_tools.py, service_installer.py, sitemap.py, http_transport.py, proxmox_scripts.py
- Zero raw str(e) calls remain in error response dicts returned to MCP clients
- Only str(e) calls in logger.* statements remain (covered by CredentialFilter)
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-hardening/02-05-SUMMARY.md`
</output>
