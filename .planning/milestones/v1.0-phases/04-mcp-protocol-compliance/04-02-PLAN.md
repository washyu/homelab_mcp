---
phase: 04-mcp-protocol-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/homelab_mcp/http_app.py
  - run_server.py
  - tests/test_http_app.py
autonomous: true
requirements:
  - MCP-04

must_haves:
  truths:
    - "HTTP requests to /mcp with disallowed Origin header receive 403 Forbidden"
    - "HTTP requests to /mcp with no Origin header are allowed (non-browser clients)"
    - "HTTP requests to /mcp with localhost Origin are allowed by default"
    - "Allowed origins are configurable via MCP_ALLOWED_ORIGINS env var"
    - "Default HTTP bind address is 127.0.0.1 (not 0.0.0.0) per MCP spec recommendation"
  artifacts:
    - path: "src/homelab_mcp/http_app.py"
      provides: "OriginValidationMiddleware class, wired into Starlette middleware stack"
      contains: "OriginValidationMiddleware"
    - path: "run_server.py"
      provides: "Default host changed to 127.0.0.1"
      contains: '127.0.0.1'
    - path: "tests/test_http_app.py"
      provides: "Origin validation tests"
      min_lines: 40
  key_links:
    - from: "src/homelab_mcp/http_app.py"
      to: "Starlette middleware stack"
      via: "OriginValidationMiddleware added to middleware list"
      pattern: "Middleware.*OriginValidationMiddleware"
    - from: "src/homelab_mcp/http_app.py"
      to: "os.getenv"
      via: "MCP_ALLOWED_ORIGINS env var parsed in create_http_app"
      pattern: "MCP_ALLOWED_ORIGINS"
---

<objective>
Add Origin header validation middleware for DNS rebinding protection and change the default HTTP bind address to localhost per MCP spec.

Purpose: Prevent DNS rebinding attacks on the Streamable HTTP transport and follow the MCP spec security recommendation for local-only binding by default.
Output: OriginValidationMiddleware in http_app.py, configurable via MCP_ALLOWED_ORIGINS, default bind 127.0.0.1, full test coverage.
</objective>

<execution_context>
@/home/shaun/.claude/get-shit-done/workflows/execute-plan.md
@/home/shaun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mcp-protocol-compliance/04-RESEARCH.md

@src/homelab_mcp/http_app.py
@run_server.py

<interfaces>
<!-- Starlette middleware pattern already in use -->

From src/homelab_mcp/http_app.py (current middleware):
```python
from starlette.middleware import Middleware
from starlette.middleware.base import BaseHTTPMiddleware  # available via starlette
from starlette.middleware.cors import CORSMiddleware

middleware = [
    Middleware(
        CORSMiddleware,
        allow_origins=origins,
        allow_credentials=True,
        allow_methods=["GET", "POST", "DELETE", "OPTIONS"],
        allow_headers=["*"],
    )
]
```

From run_server.py (current defaults):
```python
parser.add_argument("--host", default=os.getenv("MCP_HTTP_HOST", "0.0.0.0"))
# MCP spec: "Servers SHOULD bind only to localhost (127.0.0.1)"
```

create_http_app signature:
```python
def create_http_app(cors_origins: list[str] | None = None) -> Starlette:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OriginValidationMiddleware and update default bind address</name>
  <files>src/homelab_mcp/http_app.py, run_server.py, tests/test_http_app.py</files>
  <behavior>
    - Test: POST to /mcp with Origin "http://evil.com" returns 403
    - Test: POST to /mcp with Origin "http://localhost" returns non-403 (allowed)
    - Test: POST to /mcp with Origin "http://localhost:3000" returns non-403 (port variant allowed)
    - Test: POST to /mcp with Origin "http://127.0.0.1" returns non-403 (allowed)
    - Test: POST to /mcp with no Origin header returns non-403 (non-browser allowed)
    - Test: Custom allowed origins via constructor parameter work
    - Test: MCP_ALLOWED_ORIGINS env var is parsed (comma-separated) and passed to middleware
  </behavior>
  <action>
    1. In `src/homelab_mcp/http_app.py`:
       - Add `import os` and `from starlette.middleware.base import BaseHTTPMiddleware`
       - Create `OriginValidationMiddleware(BaseHTTPMiddleware)` class:
         - `__init__(self, app, allowed_origins: list[str] | None = None)` -- defaults to localhost variants: `["http://localhost", "https://localhost", "http://127.0.0.1", "https://127.0.0.1"]`
         - `async def dispatch(self, request, call_next)` -- if no Origin header, allow. If Origin present, check `_is_allowed`. Return 403 JSONResponse if disallowed.
         - `def _is_allowed(self, origin: str) -> bool` -- check if origin matches any allowed origin exactly, or starts with `allowed + ":"` (for port variants like `http://localhost:3000`).
       - Update `create_http_app` to accept `allowed_origins: list[str] | None = None` parameter. Parse `MCP_ALLOWED_ORIGINS` env var (comma-separated) as default if no parameter given. Add `OriginValidationMiddleware` to middleware list BEFORE CORSMiddleware (Origin validation runs first).
    2. In `run_server.py`:
       - Change default host from `"0.0.0.0"` to `"127.0.0.1"` in both `os.getenv("MCP_HTTP_HOST", "127.0.0.1")` and the help text.
       - Update `run_http` default parameter from `"0.0.0.0"` to `"127.0.0.1"`.
       - Pass `MCP_ALLOWED_ORIGINS` env var through to `create_http_app` if set (parse comma-separated list).
    3. Create `tests/test_http_app.py`:
       - Use Starlette `TestClient` (from `starlette.testclient`) to test the middleware.
       - Create a minimal Starlette app with OriginValidationMiddleware for isolated testing.
       - Test all behaviors listed above.
       - Test env var parsing with `monkeypatch.setenv("MCP_ALLOWED_ORIGINS", "http://myapp.local,https://openwebui.local")`.
  </action>
  <verify>
    <automated>cd /home/shaun/projects/mcp_python_server && uv run pytest tests/test_http_app.py -x -v</automated>
  </verify>
  <done>OriginValidationMiddleware rejects unknown origins with 403, allows localhost and no-origin, configurable via env var. Default bind is 127.0.0.1. All tests pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_http_app.py -x -v` -- all Origin validation tests pass
2. `uv run pytest tests/ -m "not integration" -x` -- full unit suite passes
3. `uv run ruff check src/homelab_mcp/http_app.py run_server.py` -- no lint errors
4. `grep "OriginValidationMiddleware" src/homelab_mcp/http_app.py` -- class defined and wired
5. `grep "127.0.0.1" run_server.py` -- default bind address updated
6. `grep "MCP_ALLOWED_ORIGINS" src/homelab_mcp/http_app.py` -- env var support present
</verification>

<success_criteria>
- OriginValidationMiddleware rejects disallowed origins with 403
- No-Origin requests (non-browser) pass through
- Localhost origins allowed by default
- MCP_ALLOWED_ORIGINS env var enables custom origins
- Default HTTP bind address is 127.0.0.1
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-mcp-protocol-compliance/04-02-SUMMARY.md`
</output>
