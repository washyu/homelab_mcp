---
phase: 05-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/tool-reference.md
  - README.md
autonomous: true
requirements:
  - DOCS-02

must_haves:
  truths:
    - "Every tool is documented with its arguments, return format, and at least one usage example"
    - "README links to docs/ for detailed documentation instead of duplicating content"
  artifacts:
    - path: "docs/tool-reference.md"
      provides: "Complete reference for all 49 tools"
      contains: "ssh_discover"
    - path: "README.md"
      provides: "Slim project overview linking to docs/"
      contains: "docs/setup-guide.md"
  key_links:
    - from: "README.md"
      to: "docs/tool-reference.md"
      via: "documentation link"
      pattern: "tool-reference\\.md"
    - from: "README.md"
      to: "docs/setup-guide.md"
      via: "getting started link"
      pattern: "setup-guide\\.md"
---

<objective>
Create the tool reference documenting all 49 tools (DOCS-02) and slim the README to link to the new docs/ files instead of duplicating content.

Purpose: Users and MCP clients need to know what each tool does, what arguments it takes, and what it returns. The README should be an attractive landing page, not an encyclopedia.

Output: docs/tool-reference.md, updated README.md
</objective>

<execution_context>
@/home/shaun/.claude/get-shit-done/workflows/execute-plan.md
@/home/shaun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-RESEARCH.md

@src/homelab_mcp/tool_schemas/__init__.py
@src/homelab_mcp/tool_schemas/ssh_tools_schema.py
@src/homelab_mcp/tool_schemas/network_tools_schema.py
@src/homelab_mcp/tool_schemas/infrastructure_tools_schema.py
@src/homelab_mcp/tool_schemas/vm_tools_schema.py
@src/homelab_mcp/tool_schemas/service_tools_schema.py
@src/homelab_mcp/tool_schemas/credential_tools_schema.py
@src/homelab_mcp/tool_schemas/proxmox_tools_schema.py
@src/homelab_mcp/tool_annotations.py
@README.md
</context>

<interfaces>
<!-- Tool categories from schema files (source of truth for DOCS-02) -->

Tool schema files to read (each exports a dict of tool_name -> {description, inputSchema}):
- ssh_tools_schema.py: SSH_TOOLS (6 tools)
- network_tools_schema.py: NETWORK_TOOLS (6 tools)
- infrastructure_tools_schema.py: INFRASTRUCTURE_TOOLS (7 tools)
- vm_tools_schema.py: VM_TOOLS (6 tools)
- service_tools_schema.py: SERVICE_TOOLS (10 tools)
- credential_tools_schema.py: CREDENTIAL_TOOLS (4 tools)
- proxmox_tools_schema.py: PROXMOX_TOOLS (10 tools)

Tool annotations from tool_annotations.py:
- _READ_ONLY_TOOLS: 21 tools (readOnlyHint=True)
- _DESTRUCTIVE_TOOLS: 6 tools (destructiveHint=True)
- _MUTATING_ANNOTATIONS: 22 tools (various hints)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create tool reference (DOCS-02)</name>
  <files>docs/tool-reference.md</files>
  <action>
Create docs/tool-reference.md by reading ALL 7 tool schema files and tool_annotations.py as the source of truth.

Structure:
1. **Header** - Brief intro. Note the tool count is derived from schemas (do NOT hardcode -- use "See individual categories below for the complete list").
2. **Table of Contents** - Link to each category section.
3. **Annotation Legend** - Explain readOnlyHint, destructiveHint, idempotentHint, openWorldHint badges.
4. **Per-category sections** (7 sections, one per schema file):
   - SSH Tools, Network Tools, Infrastructure Tools, VM Tools, Service Tools, Credential Tools, Proxmox Tools

For EACH tool, document:
- **Name** as a heading (### tool_name)
- **Description** from schema
- **Annotations** as inline badges: [Read-Only], [Destructive], [Idempotent], etc. from tool_annotations.py
- **Arguments** table: Name, Type, Required, Default, Description -- extracted from inputSchema.properties and inputSchema.required
- **Example** - A brief usage scenario showing the tool name and a realistic argument set as JSON. For read-only tools, show a query. For mutating tools, show a creation/modification. These are conceptual examples of what arguments to pass, NOT copy-pasteable JSON-RPC requests.
- **Returns** - Brief description of what the tool returns. For tools where the return format is not obvious from the schema, write "Returns a dict with operation results" or similar generic description. Do NOT guess specific return fields.

Read each schema file fully. Do not skip any tools. Verify the total matches 49.

IMPORTANT: Read each schema file programmatically by examining the actual dict contents. Do not rely on the research summary alone -- use the schema files as the canonical source.
  </action>
  <verify>
    <automated>test -f docs/tool-reference.md && grep -c "^### " docs/tool-reference.md | xargs test 49 -eq && echo "PASS"</automated>
  </verify>
  <done>Tool reference documents all 49 tools with arguments table, annotations, description, and example for each. Organized by 7 categories matching schema files.</done>
</task>

<task type="auto">
  <name>Task 2: Slim README to link to docs/</name>
  <files>README.md</files>
  <action>
Read the current README.md fully. Then rewrite it to be a concise project landing page:

1. **Keep:** Project title, one-paragraph description, key features as bullet list, badges (if any)
2. **Replace detailed tool listings** with a brief summary ("Tools span SSH discovery, VM management, service installation, network mapping, Proxmox operations, and credential management") and a link to docs/tool-reference.md
3. **Replace detailed setup instructions** with a Quick Start section (5-6 lines max: clone, install, configure, run) that links to docs/setup-guide.md for the full guide
4. **Replace detailed config docs** with a link to docs/configuration.md
5. **Keep:** Contributing section, license, any existing badges
6. **Add Documentation section** with links:
   - [Setup Guide](docs/setup-guide.md) - From zero to first tool call
   - [Tool Reference](docs/tool-reference.md) - All tools with arguments and examples
   - [Configuration](docs/configuration.md) - Environment variables and CLI options
7. **Do NOT hardcode tool counts** -- say "a comprehensive set of tools" or similar

The README should be attractive for GitHub visitors but not try to be the complete documentation. Target ~100-150 lines max (current is likely much longer).
  </action>
  <verify>
    <automated>test -f README.md && grep -q "setup-guide.md" README.md && grep -q "tool-reference.md" README.md && grep -q "configuration.md" README.md && echo "PASS"</automated>
  </verify>
  <done>README is a concise landing page (~100-150 lines) that links to docs/setup-guide.md, docs/tool-reference.md, and docs/configuration.md. No duplicated detailed content. No hardcoded tool counts.</done>
</task>

</tasks>

<verification>
- docs/tool-reference.md exists with exactly 49 tool entries (one ### heading per tool)
- Each tool has description, arguments table, annotations, and example
- README.md links to all 3 new docs files
- README.md does not contain detailed tool listings or setup instructions
- No hardcoded tool counts anywhere
- Existing test suite still passes: `uv run pytest tests/ -m "not integration" -x`
</verification>

<success_criteria>
- Every one of the 49 tools is documented in tool-reference.md with args, annotations, and example
- README is a concise landing page that points to docs/ for details
- Tool names and argument schemas match the actual source code exactly
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-02-SUMMARY.md`
</output>
