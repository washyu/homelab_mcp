---
milestone: v1.6
milestone_name: Credential Architecture Cleanup
audited: 2026-04-25T00:30:00Z
status: passed
scores:
  requirements: 5/5
  phases: 4/4
  integration: 7/7
  flows: 4/4
nyquist:
  compliant_phases: 0
  partial_phases: 1
  missing_phases: 3
  overall: missing
gaps: {}
tech_debt:
  - phase: 33-keyring-single-source-of-truth
    items:
      - "VERIFICATION.md missing — phase merged on plan-SUMMARY evidence (same pattern as Phase 31). Goal-backward verification supplied retroactively by milestone integration checker (all CRED-04..07 wiring PASS) and by Phase 32 revert-proof regression suite. Recommend `/gsd-validate-phase 33` post-close to also lift VALIDATION.md from draft."
      - "VALIDATION.md status=draft, nyquist_compliant=false — per-task test mapping never marked green; Wave 0 RED→green progression undocumented in this artifact."
  - phase: 33.1-ssh-tool-family-keyring-uniformity
    items:
      - "VALIDATION.md missing — Nyquist sampling-rate contract not produced for inserted phase."
  - phase: 34-cluster-scoped-proxmox-credentials
    items:
      - "VALIDATION.md missing — Nyquist contract not produced."
  - phase: 35-sitemap-discovery-reliability
    items:
      - "VALIDATION.md missing — Nyquist contract not produced."
  - phase: cross-cutting (out-of-scope for v1.6 credential contract)
    items:
      - "Hardcoded `mcp_admin` defaults remain at: infrastructure_crud.py:30 (`get_device_connection_info` dict), vm_operations.py:25 (same pattern), ssh_connection.py:162 (parameter default), ssh_tools.py:565/583 (passwordless-sudo + fallback-key branches), service_installer.py (8 parameter defaults), tool_schemas/service_tools_schema.py (8 schema `default: \"mcp_admin\"` entries). NONE are on the credential-resolution path — they are downstream consumers that never reach Phase 33's contract. Recommend follow-up phase to extend Phase 33.1 D-08 AST guard or fix sites directly."
deferred_carryforward:
  - "31-VERIFICATION.md missing (carried from v1.5 close)"
  - "31-VALIDATION.md draft (carried from v1.5)"
  - "32-VALIDATION.md missing (carried from v1.5)"
  - "SUMMARY frontmatter shape inconsistency: 32-01 flat vs 32-02..05 nested (carried from v1.5)"
v1.7_candidates:
  - "SSH-04: per-call timeout forwarded to ssh_connect() handshake"
  - "QUAL-01: Proxmox iso/cdrom mutual exclusivity via oneOf"
  - "HTTP-01: HTTP flag truthy variants (1, yes, on)"
  - "SSH-03: keyring auto-injection disambiguation error on multi-match (note: partially shipped in 33.1 D-04a — verify scope)"
  - "SSH-05: verify_mcp_admin_access uses resolved port/credentials (NOTE: tool was deleted in 33.1; revisit whether requirement is still meaningful)"
  - "ERR-02: resolve_ssh_credentials wrapped in error handler returning JSON payloads"
  - "Cross-cutting mcp_admin cleanup (see tech_debt above) — strong v1.7 candidate"
backlog_items:
  - "999.1: MCP tool CRUD for registered servers"
  - "999.2: Credential-mismatch warning on register_server"
  - "999.3: discover_and_map accepts display_name"
  - "999.4: Sitemap device tags / categories"
---

# v1.6 Milestone Audit — Credential Architecture Cleanup

**Status:** ✓ PASSED
**Audited:** 2026-04-25
**Phases in scope:** 33, 33.1, 34, 35 (4 phases, 18 plans, all complete)

## Executive Summary

Milestone v1.6 achieves its stated goal: **the OS keyring is the single source of truth for remote credentials**, with cluster-scoped Proxmox tokens layered on top. All 5 v1.6 requirements (CRED-04..08) are wired across the codebase and verified by either a phase VERIFICATION.md, the milestone integration checker, or both. All 4 end-to-end user flows are intact.

Tech debt is non-blocking and falls into two buckets: (1) documentation gaps (Phase 33 VERIFICATION.md, Nyquist VALIDATION.md across 3 of 4 phases), and (2) downstream `mcp_admin` defaults in non-resolver code paths (service_installer, vm_operations, infrastructure_crud, ssh_connection) that don't violate the v1.6 credential contract but should be cleaned up in v1.7.

**Recommendation:** proceed to `/gsd-complete-milestone v1.6`.

---

## Requirements Coverage (5/5 satisfied)

| REQ-ID | Phase | REQUIREMENTS.md | SUMMARY frontmatter | VERIFICATION evidence | Integration check | Final |
|---|---|---|---|---|---|---|
| **CRED-04** | 33 | `[ ]` (stale) | listed in 33-01 | none (no VERIFICATION.md) | PASS — table dropped on both adapters; no DB credential methods anywhere in `src/` | **SATISFIED** |
| **CRED-05** | 33 | `[ ]` (stale) | listed in 33-01 | none | PASS — `_resolve_username_from_registry` raises `CredentialNotFoundError` on zero-match (ssh_tools.py:65) and multi-match (line 72); no mcp_admin fallback in resolver | **SATISFIED** |
| **CRED-06** | 33 | `[ ]` (stale) | listed in 33-01 | none | PASS — `setup_mcp_admin` absent from every tool surface; `connect_to_device` prompt routes through CLI | **SATISFIED** |
| **CRED-07** | 33 | `[ ]` (stale) | listed in 33-01 | none | PASS — register_server schema is verify-only (no password/key_path/verify_connection); calls resolve_ssh_credentials + connect | **SATISFIED** |
| **CRED-08** | 34 | `[x]` | listed in 34-01..04 | passed (5/5 SC) | PASS — resolve_proxmox_credentials walks node→cluster; `_HOST_CLUSTER_CACHE` short-circuits; per-node precedence honored | **SATISFIED** |

**Action required at close:** flip CRED-04/05/06/07 checkboxes to `[x]` and update traceability table status to "Complete" in `REQUIREMENTS.md` before archiving (or have the milestone-archive step do it).

### Scope expansion (no REQ-ID)

Two phases inserted mid-milestone with no v1.6 requirement mapping:

| Phase | Goal | Origin | Verification |
|---|---|---|---|
| **33.1** SSH Tool Family Keyring Uniformity | drop hardcoded mcp_admin in sitemap.discover_and_store + bulk_discover_and_store; route ssh_discover, ssh_execute_command, update_mcp_admin_groups, start_interactive_shell, bulk_discover_and_map through resolve_ssh_credentials uniformly; remove update_mcp_admin_groups + verify_mcp_admin_access tools | Gap from Phase 33 live testing 2026-04-21 | passed 13/13 (.planning/phases/33.1-.../33.1-VERIFICATION.md) |
| **35** Sitemap + Discovery Reliability | fix discover_and_map field-loss; hostname-only upsert; per-subprocess SSH timeout + parallel bulk discovery; null-defensive analyzers | Gap from Phase 33 live testing 2026-04-21 | passed 32/32 (.planning/phases/35-.../35-VERIFICATION.md) |

**Recommendation for archive:** record both as "scope-expansion phases — surfaced by Phase 33 live testing, no original v1.6 REQ-ID" in the MILESTONES.md entry. Don't backfill REQ-IDs retroactively (33.1 and 35 don't map cleanly to the CRED-* requirement framing — they're reliability/uniformity work).

---

## Phase Status (4/4 complete)

| Phase | Plans | VERIFICATION.md | Score | Status |
|---|---|---|---|---|
| 33   | 5/5 | **MISSING** | n/a | complete (verified retroactively by integration checker + Phase 32 regression suite) |
| 33.1 | 5/5 | passed | 13/13 must-haves | complete |
| 34   | 4/4 | passed | 5/5 SC | complete |
| 35   | 4/4 | passed | 32/32 must-haves | complete |

---

## Cross-Phase Integration (7/7 PASS)

From integration checker (2026-04-25):

| # | Wiring Check | Status | Key Evidence |
|---|---|---|---|
| 1 | `resolve_ssh_credentials` is the sole resolver path | PASS | ssh_tools.py:238/555/630, shell_session.py:89, sitemap.py:373 (via ssh_discover_system); zero DB credential method references |
| 2 | `resolve_proxmox_credentials` precedence | PASS | proxmox_api.py:194-329 walks node→cluster; `get_proxmox_client` async; all 9 internal call sites use `await` |
| 3 | `register_server` is verify-only | PASS | credential_tools_schema.py:5-36 (hostname/username/port/display_name only); ssh_tools.py:615-677 — no DB writes |
| 4 | Removed tool surfaces gone everywhere | PASS | setup_mcp_admin / update_server_credentials / remove_server / update_mcp_admin_groups / verify_mcp_admin_access — 0 matches in `src/` |
| 5 | hostname-only upsert (both adapters) | PASS | SQLite database.py:215-282; Postgres database.py:613-722; both with degenerate-hostname fallback, connection_ip in UPDATE SET |
| 6 | Discovery field-names align producer→consumer | PASS | ssh_discover_system emits cpu.cores / memory.* / disk.* / usb_devices/pci_devices/block_devices; sitemap.parse_discovery_output reads same; bulk uses Semaphore(10) + asyncio.gather |
| 7 | CLI ↔ keyring ↔ resolver loop | PASS | server.py:512/645/665; credential_store.py:25-29 (cluster keys at `username@cluster:<name>`) — exactly what proxmox resolver walks |

---

## End-to-End Flows (4/4 WIRED)

| Flow | Status | Chain |
|---|---|---|
| **A. Fresh-host SSH onboarding** | WIRED | `credentials add` → keyring → `register_server` → `resolve_ssh_credentials` (Tier 2) → `asyncssh.connect` → JSON `{verified: true}`. Miss → `CredentialNotFoundError` with `homelab-mcp credentials add <host> <user>` pointer. No DB write. (CRED-04/05/06/07) |
| **B. Cluster Proxmox auth** | WIRED | `credentials add --type proxmox --scope cluster:home` → keyring `username@cluster:home` → `discover_and_map(node)` → `get_proxmox_client(host)` → resolver walks node tier (miss) → cluster tier probes `/cluster/status` → match populates `_HOST_CLUSTER_CACHE` → cache short-circuits second call. (CRED-08) |
| **C. Bulk discovery with parallelism + timeouts** | WIRED | `bulk_discover_and_map` → `bulk_discover_and_store` (Semaphore(10) + asyncio.gather) → `discover_and_store` per host → `ssh_discover_system` with every probe wrapped in `_run_with_timeout(10s)`. Timeouts append to `timed_out_commands`; payload tagged `partial: True`. |
| **D. Sitemap stability across IP change** | WIRED | `store_device` matches on hostname only with degenerate fallback; `connection_ip` in UPDATE SET; `dedupe_zombie_device_rows` migration is idempotent. |

---

## Anti-Patterns

| # | Pattern | Result | Detail |
|---|---|---|---|
| 1 | Hardcoded `mcp_admin` defaults on credential path | CLEAN | Resolver path is fully keyring-driven |
| 1b | Hardcoded `mcp_admin` defaults **off** credential path | **PARTIAL — out of scope** | infrastructure_crud.py:30, vm_operations.py:25, ssh_connection.py:162, ssh_tools.py:565/583 (sudo branches), service_installer.py (8 sites), tool_schemas/service_tools_schema.py (8 schema defaults). Recorded as v1.7 candidate. |
| 2 | DB credential method calls (`add_credential`, `get_credential_by_hostname`, `update_credential`, `update_last_verified`) | CLEAN | Zero matches in `src/` |
| 3 | Bare `await conn.run(...)` inside `ssh_discover_system` | CLEAN | All probes wrapped in `_run_with_timeout`; raw `conn.run` only in `_sudo_run` and `ssh_execute_command` (out of scope for Plan 35-01) |
| 4 | `device.get("cpu_cores") or 0` style coercion in analyzers | CLEAN | `_has_threshold_data` gate; zero `or 0` matches in `sitemap.py` |

---

## Nyquist Validation Coverage

| Phase | VALIDATION.md | nyquist_compliant | Status |
|---|---|---|---|
| 33 | exists (draft) | **false** | PARTIAL — never lifted from draft |
| 33.1 | missing | n/a | MISSING |
| 34 | missing | n/a | MISSING |
| 35 | missing | n/a | MISSING |

**Overall:** missing — no phase has a green VALIDATION.md. Per workflow, this is documented but does **not block close** — the regression-test pattern this project uses (Phase 32 revert-proof tests + per-phase AST meta-tests) provides equivalent coverage. Recommend `/gsd-validate-phase` retroactively for each phase post-close if Nyquist artifacts are wanted, OR document a project-level decision to skip Nyquist (per CLAUDE.md regression-test scope policy).

---

## Tech Debt Summary

**New in v1.6:**
- 4 missing/partial Nyquist VALIDATION.md files (33 draft, 33.1/34/35 missing)
- 1 missing VERIFICATION.md (Phase 33 — pattern match with Phase 31 deferred)
- Cross-cutting `mcp_admin` hardcodes in non-resolver code paths (~20 sites across 6 files)

**Carried from v1.5 close (still deferred):**
- 31-VERIFICATION.md missing
- 31-VALIDATION.md draft
- 32-VALIDATION.md missing
- SUMMARY frontmatter shape inconsistency

**v1.7 candidates surfaced during v1.6:**
- SSH-04, QUAL-01, HTTP-01, ERR-02 (carried)
- SSH-03 (partially shipped in 33.1 D-04a — verify scope)
- SSH-05 (revisit — verify_mcp_admin_access tool was deleted in 33.1; requirement may be obsolete)
- Cross-cutting mcp_admin cleanup (new — strong candidate)

---

## Gates Status

- ✓ Milestone scope identified (4 phases)
- ✓ All phase VERIFICATION.md files read (3/4 exist; Phase 33 missing-but-explained)
- ✓ SUMMARY.md `requirements-completed` frontmatter extracted
- ✓ REQUIREMENTS.md traceability parsed
- ✓ 3-source cross-reference completed (VERIFICATION + SUMMARY + traceability + integration check as 4th source supplementing missing VERIFICATION)
- ✓ Orphaned requirements: none detected
- ✓ Tech debt and deferred gaps aggregated
- ✓ Integration checker spawned with milestone REQ-IDs — passed
- ✓ FAIL gate: 0 unsatisfied requirements → status `passed`
- ⚠ Nyquist: missing for 3 phases, partial for 1 — flagged but non-blocking

---

## Routing

Audit status: **`passed`**

Next action: `/gsd-complete-milestone v1.6` (or just `/gsd-complete-milestone` — workflow auto-detects current milestone).

Pre-archive cleanup the close workflow should perform:
1. Flip CRED-04/05/06/07 checkboxes to `[x]` in REQUIREMENTS.md
2. Update REQUIREMENTS.md traceability table: CRED-04..07 status `Pending → Complete`
3. Note 33.1 and 35 as scope-expansion (no REQ-ID) in MILESTONES.md
4. Move this audit file to `.planning/milestones/v1.6-MILESTONE-AUDIT.md`
5. Carry the cross-cutting `mcp_admin` cleanup forward as a v1.7 candidate or 999.x backlog seed
