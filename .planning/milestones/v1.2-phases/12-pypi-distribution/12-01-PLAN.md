---
phase: 12-pypi-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_packaging.py
  - tests/test_service_installer.py
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03

must_haves:
  truths:
    - "tests/test_packaging.py exists and contains test_main_help, test_main_module_entry, test_version_unified, test_server_version_dynamic"
    - "tests/test_service_installer.py patch target updated to work without a module-level TEMPLATES_DIR constant"
    - "All new test functions are collected by pytest (even if they fail RED at this stage)"
  artifacts:
    - path: "tests/test_packaging.py"
      provides: "PKG-01 and PKG-02 test coverage"
      exports: ["test_main_help", "test_main_module_entry", "test_version_unified", "test_server_version_dynamic"]
    - path: "tests/test_service_installer.py"
      provides: "PKG-03 test coverage (updated patch target)"
      contains: "importlib.resources"
  key_links:
    - from: "tests/test_packaging.py"
      to: "homelab_mcp.server.main"
      via: "direct call + SystemExit capture"
      pattern: "from homelab_mcp.server import main"
    - from: "tests/test_service_installer.py"
      to: "homelab_mcp.service_installer._load_service_templates"
      via: "mock of importlib.resources.files"
      pattern: "patch.*importlib.resources"
---

<objective>
Create the Wave 0 test scaffold that implementation tasks in Plans 02 and 03 will make GREEN.

Purpose: Nyquist rule requires automated verify commands to exist before implementation. These tests define the contract for PKG-01 (entry point), PKG-02 (version unification), and PKG-03 (template loading). They will be RED at commit time — that is expected and correct for Wave 0.

Output: tests/test_packaging.py (new), tests/test_service_installer.py (updated patch target).
</objective>

<execution_context>
@/home/shaun/.claude/get-shit-done/workflows/execute-plan.md
@/home/shaun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pypi-distribution/12-CONTEXT.md
@.planning/phases/12-pypi-distribution/12-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs for writing tests. Extracted from codebase. -->

From src/homelab_mcp/__init__.py:
```python
__version__ = "0.1.0"   # will become importlib.metadata-backed after Plan 02
```

From src/homelab_mcp/server.py (line 91):
```python
server = Server("homelab-mcp", version="0.2.0", lifespan=app_lifespan)
# After Plan 02: version=_get_version() using importlib.metadata
```

From tests/test_service_installer.py (current patch target — line 45):
```python
self.patcher = patch("src.homelab_mcp.service_installer.TEMPLATES_DIR", self.template_dir)
# After Plan 03: TEMPLATES_DIR module constant is removed; patch target changes to
# "homelab_mcp.service_installer.files" (mocking importlib.resources.files)
```

From pyproject.toml:
```toml
name = "homelab-mcp-server"   # will become "homelab-mcp" in Plan 02
version = "0.2.0"             # will become "1.2.0" in Plan 02
[project.scripts]
homelab-mcp = "homelab_mcp.server:main"   # main() does not yet exist
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_packaging.py (Wave 0 scaffold)</name>
  <files>tests/test_packaging.py</files>
  <behavior>
    - test_main_help: calling main() with sys.argv = ["homelab-mcp", "--help"] raises SystemExit(0) and prints help text to stdout or stderr. Use pytest.raises(SystemExit) and capsys/capfd.
    - test_main_module_entry: importing homelab_mcp.__main__ succeeds (no ImportError); the module has a callable entry point that delegates to main(). Assert the module contains a reference to main (inspect or import check).
    - test_version_unified: importlib.metadata.version("homelab-mcp") returns a non-empty string that matches the version in pyproject.toml. Read pyproject.toml from the repo root using pathlib; parse with tomllib (stdlib Python 3.11+). Assert equality.
    - test_server_version_dynamic: the server instance in homelab_mcp.server uses a dynamically derived version, not a hardcoded string. Import `server` from homelab_mcp.server; assert server._version != "0.2.0" OR assert server._version == importlib.metadata.version("homelab-mcp"). (Both conditions confirm the hardcode is gone — check whichever attribute the MCP SDK Server exposes; inspect server.__dict__ or server.version if available.)
  </behavior>
  <action>
    Create tests/test_packaging.py with these four test functions. All tests will be RED at this stage because main() does not yet exist and the package is still named "homelab-mcp-server".

    Key implementation notes:
    - Import from "homelab_mcp.server" not "src.homelab_mcp.server" — package tests use the installed path
    - Use tomllib (Python 3.11+ stdlib) to parse pyproject.toml: `import tomllib; with open("pyproject.toml", "rb") as f: data = tomllib.load(f)`
    - For test_main_help: monkeypatch sys.argv before calling main(), wrap in pytest.raises(SystemExit) asserting code == 0
    - For test_server_version_dynamic: the MCP SDK Server class stores version as self._version or exposes it as a property — import the module-level `server` object and check it does NOT equal "0.2.0"; after Plan 02 it will equal "1.2.0" from importlib.metadata
    - Do NOT add @pytest.mark.asyncio — these are synchronous tests
    - Confirm tests are collected: `uv run pytest tests/test_packaging.py --collect-only`
  </action>
  <verify>
    <automated>uv run pytest tests/test_packaging.py --collect-only -q 2>&1 | grep "test_main_help\|test_main_module_entry\|test_version_unified\|test_server_version_dynamic"</automated>
  </verify>
  <done>All four test functions are collected by pytest. Tests may be RED (expected at Wave 0).</done>
</task>

<task type="auto">
  <name>Task 2: Update tests/test_service_installer.py patch target</name>
  <files>tests/test_service_installer.py</files>
  <action>
    The current test patches "src.homelab_mcp.service_installer.TEMPLATES_DIR" (a module-level Path constant). After Plan 03, that constant is removed and replaced with an importlib.resources call inside _load_service_templates(). The patch target must change.

    New approach — patch importlib.resources.files() to return a fake traversable:

    1. Remove the TEMPLATES_DIR patcher in setup_method/teardown_method entirely.
    2. Instead, each test that exercises template loading should use:
       ```python
       from unittest.mock import patch, MagicMock
       ```
    3. Create a helper that builds a fake Traversable: a MagicMock whose .iterdir() yields fake file items, each with .is_file() = True, .name = "test-service.yaml", and .read_text() = yaml.dump(sample_template).
    4. Patch "homelab_mcp.service_installer.files" (the importlib.resources.files function as imported in the module) to return a mock that, when .joinpath("service_templates") is called, returns the fake traversable.
    5. Apply this patch as a fixture or in setup_method so ServiceInstaller() gets the mocked templates.
    6. Keep all existing test assertions unchanged — only the setup/patching changes.

    After this change, tests/test_service_installer.py must still pass the "templates" keyword filter:
    `uv run pytest tests/test_service_installer.py -x -k "templates"` (will be RED until Plan 03 lands, but must be collected).

    Also add a new test: test_templates_loaded_from_package — instantiate ServiceInstaller() with the mock in place, assert len(installer.templates) == 1 and "test-service" in installer.templates. This is the test referenced in VALIDATION.md for PKG-03.
  </action>
  <verify>
    <automated>uv run pytest tests/test_service_installer.py --collect-only -q 2>&1 | grep "templates"</automated>
  </verify>
  <done>tests/test_service_installer.py collects all existing tests plus test_templates_loaded_from_package. TEMPLATES_DIR patch removed. New patch targets "homelab_mcp.service_installer.files".</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
uv run pytest tests/test_packaging.py --collect-only -q
uv run pytest tests/test_service_installer.py --collect-only -q
```

Both files must be collected without import errors. RED tests are expected and intentional at this stage.
</verification>

<success_criteria>
- tests/test_packaging.py exists with 4 test functions collected by pytest
- tests/test_service_installer.py updated: TEMPLATES_DIR patch removed, importlib.resources.files mock in place, test_templates_loaded_from_package added
- No SyntaxError or ImportError when pytest collects the files
- Wave 0 requirement from 12-VALIDATION.md fulfilled
</success_criteria>

<output>
After completion, create `.planning/phases/12-pypi-distribution/12-01-SUMMARY.md`
</output>
