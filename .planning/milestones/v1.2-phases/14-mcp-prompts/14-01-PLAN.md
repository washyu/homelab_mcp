---
phase: 14-mcp-prompts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_mcp_prompts.py
autonomous: true
requirements:
  - PRMT-01
  - PRMT-02
  - PRMT-03
  - PRMT-04

must_haves:
  truths:
    - "tests/test_mcp_prompts.py exists and is collected by pytest without ImportError"
    - "Six failing tests cover the four PRMT requirements"
    - "Tests use local imports inside test function bodies to avoid collection-level ImportError"
  artifacts:
    - path: "tests/test_mcp_prompts.py"
      provides: "Wave 0 RED test scaffold for PRMT-01..04"
      contains: "test_prompts_capability_advertised, test_list_prompts_returns_prompts, test_decommission_workflow_prompt, test_deploy_service_workflow_prompt, test_health_check_prompt_resources, test_get_unknown_prompt_raises_mcp_error"
  key_links:
    - from: "tests/test_mcp_prompts.py"
      to: "src/homelab_mcp/prompt_registry.py"
      via: "local import inside each test function body"
      pattern: "from homelab_mcp.prompt_registry import"
    - from: "tests/test_mcp_prompts.py"
      to: "src/homelab_mcp/server.py"
      via: "local import inside capability test"
      pattern: "from homelab_mcp.server import server"
---

<objective>
Create the Wave 0 test scaffold for Phase 14 MCP Prompts. Six failing tests covering PRMT-01 through PRMT-04 are written before any implementation exists.

Purpose: Establish RED tests that define the contract. Plan 02 turns them GREEN.
Output: tests/test_mcp_prompts.py with 6 stub tests — all expected to FAIL at commit time (pytest --collect-only must succeed, individual tests must fail).
</objective>

<execution_context>
@/home/shaun/.claude/get-shit-done/workflows/execute-plan.md
@/home/shaun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-mcp-prompts/14-RESEARCH.md
@.planning/phases/14-mcp-prompts/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Wave 0 test scaffold for MCP prompts</name>
  <files>tests/test_mcp_prompts.py</files>
  <action>
Create `tests/test_mcp_prompts.py` with six stub tests. All imports MUST be local (inside test function bodies) — established pattern from Phase 13 to avoid collection-level ImportError when `prompt_registry.py` does not yet exist.

Test functions to write:

1. `test_prompts_capability_advertised` — PRMT-01:
   Import `server` from `homelab_mcp.server` and `NotificationOptions` from `mcp.server.lowlevel`.
   Call `server.get_capabilities(NotificationOptions(), {})`.
   Assert `caps.prompts is not None`.

2. `test_list_prompts_returns_prompts` — PRMT-01:
   Import `HOMELAB_PROMPTS` from `homelab_mcp.prompt_registry`.
   Assert `len(HOMELAB_PROMPTS) >= 3`.
   Assert all three names present: `"decommission_device_workflow"`, `"deploy_service_workflow"`, `"homelab_health_check"`.

3. `test_decommission_workflow_prompt` — PRMT-02:
   Import `get_prompt_result` from `homelab_mcp.prompt_registry`.
   Call `get_prompt_result("decommission_device_workflow", {"hostname": "test-host"})`.
   Assert result is a `GetPromptResult` (import from `mcp.types`).
   Assert result has at least one message.
   Assert the combined message text contains `"decommission_device_preview"` (case-insensitive).
   Assert the combined message text contains `"confirm"` (case-insensitive).

4. `test_deploy_service_workflow_prompt` — PRMT-03:
   Import `get_prompt_result` from `homelab_mcp.prompt_registry`.
   Call `get_prompt_result("deploy_service_workflow", {"service_name": "nginx", "target_host": "test-host"})`.
   Assert result has at least one message.
   Assert the combined message text contains `"pre-flight"` or `"preflight"` or `"ssh_discover"` or `"list_installed_services"` (any of these — pre-flight guidance present).

5. `test_health_check_prompt_resources` — PRMT-04:
   Import `get_prompt_result` from `homelab_mcp.prompt_registry`.
   Call `get_prompt_result("homelab_health_check", {})`.
   Assert result has at least one message.
   Assert combined message text contains `"homelab://vms"`.
   Assert combined message text contains `"homelab://devices"`.
   Assert combined message text contains `"homelab://drift/latest"`.

6. `test_get_unknown_prompt_raises_mcp_error` — PRMT-01:
   Import `get_prompt_result` from `homelab_mcp.prompt_registry`.
   Import `McpError` from `mcp.shared.exceptions`.
   Call `get_prompt_result("nonexistent_prompt", {})` inside `pytest.raises(McpError)`.
   Assert the exception's `error.code == -32002`.

Pattern for local imports (established in Phase 13, from STATE.md):
```python
@pytest.mark.asyncio
async def test_example() -> None:
    from homelab_mcp.prompt_registry import get_prompt_result  # local import
    ...
```

For the capability test, use a plain (non-async) function since `get_capabilities` is synchronous:
```python
def test_prompts_capability_advertised() -> None:
    from homelab_mcp.server import server
    from mcp.server.lowlevel import NotificationOptions
    caps = server.get_capabilities(NotificationOptions(), {})
    assert caps.prompts is not None
```

The async tests do NOT need `@pytest.mark.asyncio` since none of them call async functions — `get_prompt_result` is synchronous. Keep them as plain `def` test functions.

Do NOT create `prompt_registry.py` in this plan. The tests will fail with ImportError when run — that is correct Wave 0 behavior. Only pytest collection (`--collect-only`) must succeed.

File header:
```python
"""Tests for MCP Prompts capability (Phase 14, PRMT-01..04).

Wave 0: Tests are intentionally RED — prompt_registry.py does not exist yet.
pytest --collect-only must succeed; individual tests will fail.
"""
from __future__ import annotations

import pytest
```
  </action>
  <verify>
    <automated>cd /home/shaun/projects/mcp_python_server && uv run pytest tests/test_mcp_prompts.py --collect-only -q 2>&1 | grep -E "test session|collected|error" | head -10</automated>
  </verify>
  <done>
    - `tests/test_mcp_prompts.py` exists with 6 test functions
    - `pytest --collect-only` collects all 6 tests without collection errors
    - Running the tests produces failures (not collection errors) — ImportError inside test body is expected
    - All six test names match the VALIDATION.md map exactly
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/test_mcp_prompts.py --collect-only -q
</verification>

<success_criteria>
- tests/test_mcp_prompts.py exists and is collected by pytest (6 tests, 0 collection errors)
- Tests import from homelab_mcp.prompt_registry locally (inside function body)
- Capability test imports server from homelab_mcp.server locally
- All 6 tests are expected to fail until Plan 02 implementation
</success_criteria>

<output>
After completion, create `.planning/phases/14-mcp-prompts/14-01-SUMMARY.md`
</output>
