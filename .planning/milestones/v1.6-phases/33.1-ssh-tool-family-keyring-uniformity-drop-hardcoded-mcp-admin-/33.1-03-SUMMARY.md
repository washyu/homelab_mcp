---
phase: 33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-
plan: 03
subsystem: auth
tags: [credential-cleanup, tool-removal, lock-step, 33.1, high-risk, mcp-surface]

# Dependency graph
requires:
  - phase: 33.1
    provides: "Plan 01 resolver registry-scan (no `or \"mcp_admin\"` fallback); Plan 04 sitemap `username=None` default"
provides:
  - "MCP tool surface with `update_mcp_admin_groups` + `verify_mcp_admin` removed (tool count 53 → 51)"
  - "connect_to_device prompt Step 6 rewritten to call `ssh_execute_command(command='sudo -n true')`"
  - "ssh_tools.py with verify_mcp_admin_access() + update_mcp_admin_groups() function bodies deleted (~200 lines net)"
  - "Plan 05 AST guards `test_no_forbidden_strings_in_source` + `test_no_removed_db_methods_in_source` flipped from RED to GREEN"
affects: [Phase 33.2 — service-tool family mcp_admin sweep; Phase 33.1 close (this is the final Phase 33.1 plan)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Lock-step MCP tool deletion template (Phase 33 D-10/D-20/D-21 pattern extended for two more tools)"
    - "`ssh_execute_command(command='sudo -n true')` as the generic sudo-verification pattern for the connect_to_device prompt (D-05b) — replaces the mcp_admin-specific verify_mcp_admin tool"

key-files:
  created: []
  modified:
    - src/homelab_mcp/tool_schemas/ssh_tools_schema.py
    - src/homelab_mcp/tool_handlers/ssh_handlers.py
    - src/homelab_mcp/tool_handlers/__init__.py
    - src/homelab_mcp/tool_annotations.py
    - src/homelab_mcp/openapi_app.py
    - src/homelab_mcp/ssh_tools.py
    - src/homelab_mcp/prompt_registry.py
    - tests/test_ssh_tools.py
    - tests/test_tools.py
    - tests/test_mcp_prompts.py

key-decisions:
  - "Handled tool_annotations.py as two distinct structural deletions (BLOCKER 2): `verify_mcp_admin` from `_READ_ONLY_TOOLS` list (line 44) AND `update_mcp_admin_groups` from `_MUTATING_ANNOTATIONS` dict (lines 90-94)."
  - "Deleted 4 orphan schema tests in test_tools.py (not skipped, not stubbed) that asserted against removed schemas — would have KeyError'd after Task 1 (Phase 33 precedent D-02)."
  - "ssh_tools.py helpers `_sudo_run`, `get_mcp_ssh_key_path`, `ensure_mcp_ssh_key` all PRESERVE: each retains at least one non-deleted caller; no dead-code cleanup required."
  - "mcp_admin-specific passwordless-sudo branch in `ssh_execute_command` (lines 479-501) intentionally left in place — not in this plan's scope and no test requires removal."

patterns-established:
  - "7-way parity verification (schema + handler + dispatch + annotation list + annotation dict + openapi allow-list + openapi category + ssh_tools module): import-time assertion expanded from the Phase 33 5-way pattern to catch both tool_annotations.py structural shapes simultaneously."

requirements-completed: []

# Metrics
duration: 21min
completed: 2026-04-23
---

# Phase 33.1 Plan 03: SSH Tool Family mcp_admin Removal Summary

**Deleted the last two mcp_admin-specific MCP tools (`update_mcp_admin_groups` + `verify_mcp_admin`) from the entire tool surface — schema, handler, dispatch, annotations, openapi registries, and underlying ssh_tools.py function bodies — dropping MCP tool count from 53 to 51 and flipping Plan 05's AST regression guards from RED to GREEN.**

## Performance

- **Duration:** ~21 min (sequential executor on main working tree)
- **Started:** 2026-04-23T03:25:10Z
- **Completed:** 2026-04-23T03:45:45Z
- **Tasks:** 2 (both `type="auto"`)
- **Files modified:** 10 (5 src surfaces lock-step in Task 1 + 3 src sites in Task 2 + 3 test files in Task 2; prompt_registry and ssh_tools were both touched in Task 2)
- **Net line delta:** ~74 deletions (Task 1, surgical registry edits) + 292 net deletions (Task 2, ~200 from ssh_tools.py function bodies + 92 net from tests) = 366 lines of tool-surface code and orphan test code removed

## Accomplishments

- **MCP tool count dropped from 53 → 51.** 7-way parity verified at import time: `update_mcp_admin_groups` and both `verify_mcp_admin` (MCP name) / `verify_mcp_admin_access` (function name) absent from SSH_TOOLS, TOOL_HANDLERS, TOOL_ANNOTATIONS, _SSH_TOOLS_WITH_HOSTNAME, TOOL_CATEGORIES["SSH"], ssh_tools module exports, and ssh_handlers module exports.
- **Plan 05 AST guards flipped from RED to GREEN.** Both `test_no_forbidden_strings_in_source` (D-10 identifier sweep across src/homelab_mcp/**/*.py) and `test_no_removed_db_methods_in_source` (same sweep, DB-methods alias) now pass. Before Task 1: 6 violations (update_mcp_admin_groups in 5 files + verify_mcp_admin_access in ssh_handlers.py). After Task 2: 0 violations.
- **connect_to_device prompt Step 6 rewritten** — no longer names a removed tool. New Step 6 instructs the agent to call `ssh_execute_command(hostname="...", command="sudo -n true")`, which is a generic passwordless-sudo check that works for any registered user (not just the literal mcp_admin account). Matches user's Phase 33.1 D-05b intent verbatim.
- **ssh_tools.py function bodies deleted:** `verify_mcp_admin_access` (~65 lines, line 224-289 pre-edit) and `update_mcp_admin_groups` (~130 lines, line 597-726 pre-edit) for a combined 200-line net deletion.
- **Final Phase 33.1 plan.** This completes the D-05/D-05a/D-05b/D-05c/D-13 decision set. Phase 33.1 close is unblocked.

## Task Commits

1. **Task 1 — lock-step 5-file surface deletion** — `c24f4b5` (feat)
2. **Task 2 — ssh_tools.py function bodies + prompt Step 6 + tests** — `67c93d2` (feat)

Both commits used `--no-verify` due to the known debug-statements pre-commit hook broken on this Windows host (WinError 4551 — Application Control).

## Files Created/Modified

### Production source (Task 1 + Task 2)

| File | Delta | Purpose |
|------|-------|---------|
| `src/homelab_mcp/tool_schemas/ssh_tools_schema.py` | −51 | Deleted `verify_mcp_admin` entry (lines 33-55) AND `update_mcp_admin_groups` entry (lines 118-144). Remaining entries: `ssh_discover`, `ssh_execute_command`, `start_interactive_shell`. |
| `src/homelab_mcp/tool_handlers/ssh_handlers.py` | −10 | Removed `update_mcp_admin_groups` + `verify_mcp_admin_access` from import block; deleted `handle_verify_mcp_admin` (4 lines) + `handle_update_mcp_admin_groups` (4 lines) functions. |
| `src/homelab_mcp/tool_handlers/__init__.py` | −5 | Removed `handle_update_mcp_admin_groups` + `handle_verify_mcp_admin` from import block; deleted 2 `TOOL_HANDLERS` dict entries. |
| `src/homelab_mcp/tool_annotations.py` | −6 | **Two distinct structural deletions (BLOCKER 2):** (a) line 44 `"verify_mcp_admin",` from `_READ_ONLY_TOOLS` list; (b) lines 90-94 `"update_mcp_admin_groups": ToolAnnotations(...)` from `_MUTATING_ANNOTATIONS` dict. |
| `src/homelab_mcp/openapi_app.py` | −4 | Deleted `verify_mcp_admin` + `update_mcp_admin_groups` from `_SSH_TOOLS_WITH_HOSTNAME` tuple AND from `TOOL_CATEGORIES["SSH"]` list. |
| `src/homelab_mcp/ssh_tools.py` | −200 | Deleted `verify_mcp_admin_access()` function body (~65 lines including `@ssh_connection_wrapper` decorator) and `update_mcp_admin_groups()` function body (~130 lines including try/except and unreachable sentinel return). All imports retained — audit confirmed `cast`, `sanitize_error`, `json`, `asyncssh`, etc. still used elsewhere. |
| `src/homelab_mcp/prompt_registry.py` | +3 / −2 | `_build_connect_to_device_result` Step 6 rewritten per D-05b. |

### Test files (Task 2)

| File | Delta | Purpose |
|------|-------|---------|
| `tests/test_ssh_tools.py` | −97 | Deleted `verify_mcp_admin_access` import; deleted 3 test functions (`test_verify_mcp_admin_access_success`, `test_verify_mcp_admin_access_no_key`, `test_verify_mcp_admin_access_auth_failure`). Replaced the deleted block with a 6-line comment pointing to the Plan 05 AST guard as the new coverage surface. |
| `tests/test_tools.py` | −15 | test_get_available_tools: count 53 → 51; inverted `assert "verify_mcp_admin" in tools` → `not in tools`; added new `assert "update_mcp_admin_groups" not in tools`. Deleted 4 orphan schema tests (`test_update_mcp_admin_groups_schema_no_password`, `test_update_mcp_admin_groups_schema_password_not_required`, `test_verify_mcp_admin_schema_has_timeout`, `test_update_mcp_admin_groups_schema_has_key_path`) — all would KeyError after Task 1. |
| `tests/test_mcp_prompts.py` | +11 | `test_connect_to_device_prompt`: inverted `"verify_mcp_admin" in combined_text` → `not in`; added positive assertions for `"ssh_execute_command"` and `"sudo -n true"`. `test_connect_to_device_prompt_parameter_names`: replaced `"verify_mcp_admin"` in tool tuple with `"ssh_execute_command"`. |

## Helpers Dead-Code Audit (WARNING 2)

Required by the plan's Task 2 Step A. Grep-verified on ssh_tools.py **before** deleting function bodies:

| Helper | Pre-deletion callers | Post-deletion callers | Disposition |
|--------|----------------------|-----------------------|-------------|
| `_sudo_run` (line 509) | 2 (both in `ssh_execute_command`: lines 570, 573) | 2 (same) | **PRESERVE** (CONTEXT D-05 explicit) |
| `get_mcp_ssh_key_path` (line 192) | 2 (line 199 in `ensure_mcp_ssh_key`; line 227 in `verify_mcp_admin_access`) | 1 (line 199 only) | **PRESERVE** (Phase 33 D-09a — SSH key generation path still used by CLI key-path flow) |
| `ensure_mcp_ssh_key` (line 197) | 1 (line 551 in `ssh_execute_command`) | 1 (same) | **PRESERVE** (Phase 33 D-09a) |

Module-wide grep (`grep -rn 'ensure_mcp_ssh_key|get_mcp_ssh_key_path' src/homelab_mcp/`) confirmed **zero external callers** — helpers stay contained within `ssh_tools.py`.

No helper became orphaned. No additional deletions required.

## tool_annotations.py Two-Shape Deletion (BLOCKER 2)

The plan's WARNING 2 flagged that `tool_annotations.py` contains the two removed tool names in **two structurally distinct shapes** — a list-entry and a dict-key. Both deletions landed:

**Shape A — list entry:**
```python
# BEFORE
_READ_ONLY_TOOLS = [
    ...
    "get_proxmox_vm_status",
    "verify_mcp_admin",       # <-- line 44 deleted
    "check_ansible_service",
    ...
]

# AFTER
_READ_ONLY_TOOLS = [
    ...
    "get_proxmox_vm_status",
    "check_ansible_service",
    ...
]
```

**Shape B — dict entry:**
```python
# BEFORE
_MUTATING_ANNOTATIONS: dict[str, ToolAnnotations] = {
    ...
    "bulk_discover_and_map": ToolAnnotations(...),
    "update_mcp_admin_groups": ToolAnnotations(      # <-- lines 90-94 deleted
        readOnlyHint=False,
        destructiveHint=False,
        idempotentHint=False,
    ),
    "ssh_execute_command": ToolAnnotations(...),
    ...
}

# AFTER
_MUTATING_ANNOTATIONS: dict[str, ToolAnnotations] = {
    ...
    "bulk_discover_and_map": ToolAnnotations(...),
    "ssh_execute_command": ToolAnnotations(...),
    ...
}
```

Import-time assertion verifies both: `assert "verify_mcp_admin" not in TOOL_ANNOTATIONS` catches Shape A; `assert "update_mcp_admin_groups" not in TOOL_ANNOTATIONS` catches Shape B. Both pass.

## Before / After: connect_to_device Prompt Step 6 (D-05b)

**BEFORE (prompt_registry.py lines 146-147):**
```
6. Call verify_mcp_admin with hostname="{hostname}" to confirm that the registered \
user has sudo access.
```

**AFTER:**
```
6. Call ssh_execute_command with hostname="{hostname}" and command="sudo -n true" to \
confirm the registered user has passwordless sudo. A non-zero exit code means sudo \
is not configured for the registered user.
```

The rest of `_build_connect_to_device_result` (Steps 1-5 and the closing "If any step fails..." sentence) is unchanged.

## Tool Count Before → After

| Phase | Count | Delta | Reason |
|-------|-------|-------|--------|
| Pre-Phase 33 (v1.4) | 57 | — | baseline |
| Phase 33 | 53 | −4 | Phase 33 D-10/D-20/D-21 removed setup_mcp_admin, update_server_credentials, remove_server, remove_server_preview |
| Phase 33.1 Plan 03 (this plan) | **51** | **−2** | D-05 removed verify_mcp_admin, update_mcp_admin_groups |

Verified at import time: `len(TOOL_HANDLERS) == 51` AND `len(get_available_tools()) == 51`.

## Plan 05 AST Guard Transition (D-10)

The two guards the plan targets for the RED → GREEN flip:

| Guard | Pre-plan | Post-plan |
|-------|----------|-----------|
| `tests/test_ast_regression.py::test_no_forbidden_strings_in_source` | FAIL (6 violations) | **PASS** |
| `tests/test_ast_regression.py::test_no_removed_db_methods_in_source` | FAIL (same 6 violations — alias test) | **PASS** |

Pre-plan violations (verified via `uv run pytest` BEFORE edits):
```
- src/homelab_mcp/openapi_app.py: contains 'update_mcp_admin_groups'
- src/homelab_mcp/tool_annotations.py: contains 'update_mcp_admin_groups'
- src/homelab_mcp/tool_handlers/__init__.py: contains 'update_mcp_admin_groups'
- src/homelab_mcp/tool_handlers/ssh_handlers.py: contains 'update_mcp_admin_groups'
- src/homelab_mcp/tool_handlers/ssh_handlers.py: contains 'verify_mcp_admin_access'
- src/homelab_mcp/tool_schemas/ssh_tools_schema.py: contains 'update_mcp_admin_groups'
```

Note: the AST guard uses `Name.id` / `Attribute.attr` / string literal collection — it does NOT walk `FunctionDef.name`. That's why the guard didn't flag the `async def verify_mcp_admin_access` / `async def update_mcp_admin_groups` definitions in ssh_tools.py itself. Since those definitions and all their callers are now deleted, the guard is stable.

## D-13 Sanity Grep (mcp_admin residual scan)

`grep -rn 'mcp_admin' src/homelab_mcp/ --include='*.py'` on post-plan source produced **49 hits**, all of which fall into one of these categories:

### Phase-33-legitimate (expected — do NOT remove)

- `src/homelab_mcp/ssh_tools.py`: SSH key file constants + comment + docstrings
  - line 46, 90, 107 — `resolve_ssh_credentials` docstring references to the removed silent fallback
  - line 194 — `get_mcp_ssh_key_path` returns `SSH_KEY_DIR / "mcp_admin_key"` (Phase 33 D-09a)
  - line 210 — `generate_private_key(..., comment="mcp_admin@homelab")` (Phase 33 D-09a)
  - lines 479-501 — `ssh_execute_command` passwordless-sudo branch tied to the literal `creds.username == "mcp_admin"` (out-of-scope for this plan; functional fallback is harmless when no mcp_admin user exists)

### Phase-33.2-deferred (per plan's acceptance criterion and test_ast_regression.py allowlists)

- `src/homelab_mcp/ssh_connection.py:162` — `username: str = "mcp_admin"` default (tracked in `DEFERRED_MCP_ADMIN_DEFAULT_FILES`)
- `src/homelab_mcp/service_installer.py` — 8 hits (function defaults and Ansible inventory templates); tracked in `DEFERRED_MCP_ADMIN_DEFAULT_FILES` + `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS`
- `src/homelab_mcp/tool_schemas/service_tools_schema.py` — 9 tool schemas with `"default": "mcp_admin"`; tracked in `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS`

### Phase-33.2-adjacent (NEW finding during D-13 sanity grep — documented here for Phase 33.2)

Two residual hits surfaced outside the plan's explicit Phase-33.2-deferred enumeration. Both are dict-literal `"username": "mcp_admin"` inside `get_device_connection_info()` return values, NOT function-signature defaults — so they bypass the Phase 33.1 D-08 AST guard:

- `src/homelab_mcp/infrastructure_crud.py:30` — InfrastructureManager.get_device_connection_info
- `src/homelab_mcp/vm_operations.py:25` — VMManager.get_device_connection_info

**Disposition:** Pre-existing, not caused by this plan (same code was present before Plan 04). Out of scope per Rule: Scope Boundary — these files are not in Plan 03's `files_modified` list and are semantically the same Phase-33.2 cleanup scope as the `ssh_connection.py` default that IS explicitly deferred. Recommend Phase 33.2 extend `DEFERRED_MCP_ADMIN_DEFAULT_FILES` OR fix both sites as part of the service-tool + downstream-infrastructure mcp_admin sweep.

The plan's acceptance criterion phrased the grep filter as:
```
grep -rn 'mcp_admin' src/homelab_mcp/ --include='*.py' | grep -v 'credential_store|ssh_tools.py|mcp_admin_key|mcp_admin@homelab|ssh_connection|service_installer|service_tools_schema'
```
Two hits surface (infrastructure_crud.py, vm_operations.py) — documenting for Phase 33.2 orchestrator visibility; no action required in this plan.

## Test Functions Deleted (not skipped)

Per Phase 33 precedent D-02 (delete, don't skip) and the plan's explicit acceptance criteria:

### From `tests/test_ssh_tools.py`:

1. `test_verify_mcp_admin_access_success` (~line 294-345)
2. `test_verify_mcp_admin_access_no_key` (~line 348-363)
3. `test_verify_mcp_admin_access_auth_failure` (~line 366-385)

Import `verify_mcp_admin_access` on line 13 was removed alongside.

### From `tests/test_tools.py` (orphan schema tests):

4. `test_update_mcp_admin_groups_schema_no_password` — asserted against `SSH_TOOLS["update_mcp_admin_groups"]` (KeyError after Task 1)
5. `test_update_mcp_admin_groups_schema_password_not_required` — same
6. `test_verify_mcp_admin_schema_has_timeout` — asserted against `tools["verify_mcp_admin"]` (KeyError after Task 1)
7. `test_update_mcp_admin_groups_schema_has_key_path` — same pattern

Each deletion replaced with a 1-3 line comment documenting the removal reason (D-05 reference) so future readers understand the gap.

No tests were **skipped** — deletion is the Phase-33 / Phase-33.1 convention.

## tests/test_tools.py Surgical Edits (WARNING 1)

Plan's WARNING 1 specified three exact changes to `test_get_available_tools` (lines 11-26). All landed:

| Line | Before | After |
|------|--------|-------|
| 16 | `len(tools) == 53` + Phase 33 comment | `len(tools) == 51` + combined Phase-33 / Phase-33.1 comment |
| 20 | `assert "verify_mcp_admin" in tools` | `assert "verify_mcp_admin" not in tools  # D-05: removed in Phase 33.1` |
| 21 (NEW) | — | `assert "update_mcp_admin_groups" not in tools  # D-05: removed in Phase 33.1` |

Line 19's `assert "setup_mcp_admin" not in tools  # D-10: removed in Phase 33` preserved as-is.

## Pattern Followed

Phase 33 D-10 / D-20 / D-21 lock-step deletion template (see `.planning/phases/33-keyring-single-source-of-truth/33-04-SUMMARY.md` for the reference implementation). Phase 33 removed 4 MCP tools in lock-step; Phase 33.1 Plan 03 removes 2 more using the same 5-file (+ 1 function-body + 1 prompt) surface template, extended for the dual-shape tool_annotations.py deletion (BLOCKER 2).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Deleted 4 orphan schema tests in tests/test_tools.py beyond the 3 tests the plan explicitly listed.**

- **Found during:** Task 2 Step D. Plan called out `test_verify_mcp_admin_access_*` tests and the `test_get_available_tools` surgery (WARNING 1). It did NOT enumerate 4 other orphan tests that also asserted against the deleted schemas (`test_update_mcp_admin_groups_schema_no_password`, `test_update_mcp_admin_groups_schema_password_not_required`, `test_verify_mcp_admin_schema_has_timeout`, `test_update_mcp_admin_groups_schema_has_key_path`).
- **Issue:** Each of these tests reads `tools["update_mcp_admin_groups"]["inputSchema"]` or `SSH_TOOLS["update_mcp_admin_groups"]` — after Task 1 deleted those schema entries, the tests would KeyError and fail the full test sweep. Left unfixed, Task 2's `uv run pytest tests/ -m "not integration" -x` acceptance criterion would fail.
- **Fix:** Deleted all 4, each replaced with a 1-3 line comment pointing to D-05 as the removal reason. Mirrors the Phase-33 convention for deleted-tool-assertions (Phase 33 D-02).
- **Files modified:** `tests/test_tools.py`
- **Commit:** `67c93d2` (bundled with Task 2 — one logical unit: "delete orphan test assertions for removed tools").

### Observations (not auto-fixes)

- **Pre-existing test failures on main confirmed unchanged.** `tests/test_database.py::test_ssh_credentials_table_dropped_postgres` and `tests/test_proxmox_api.py::TestGetProxmoxClient::test_client_missing_host` were already documented in 33.1-01-SUMMARY as unrelated to Phase 33.1 scope. They remain red post-plan with the same psycopg2-import / NoneType error signatures; not caused by this plan.
- **Pre-existing ruff import-ordering warning on ssh_tools.py (lines 9-15) reproduces.** Same warning the 33.1-01 agent documented as out-of-scope. Not introduced here.
- **mypy jsonschema stubs warning on openapi_app.py:18 reproduces.** Not introduced here.
- **mcp_admin-specific passwordless-sudo branch in `ssh_execute_command` (ssh_tools.py lines 479-501) left intact.** The branch `if creds.username == "mcp_admin"` is not in scope for this plan (not listed in `files_modified` change surface) and no test requires its removal. Semantically harmless — if no registered user is named `mcp_admin`, the branch is skipped. Phase 33.2 or a later phase can consider removing it alongside the service-tool family cleanup.
- **Scope-boundary finding: infrastructure_crud.py:30 + vm_operations.py:25 carry `"username": "mcp_admin"` dict literals** — see "D-13 Sanity Grep" section above. Pre-existing, not caused by this plan, documented for Phase 33.2 visibility.

**Total auto-fixes:** 1 (Rule 3 orphan-test deletion).
**Impact on plan:** None on success criteria — all AC still met.

## Authentication Gates

None encountered — this plan is code-only surgery with no external service touchpoints.

## Issues Encountered

- **Debug-statements pre-commit hook broken on this Windows host.** Pre-existing infra bug: Windows Application Control blocks the Python subprocess that executes the `debug-statements` hook (WinError 4551). Both commits in this plan used `git commit --no-verify` with the bypass documented in the commit body: `"hook bypass — debug-statements Python infra broken (not code issue)"`. Manual grep verification performed before each commit confirmed no `pdb.set_trace` / `breakpoint()` / `import pdb` / `import ipdb` statements were introduced.

No other issues.

## Acceptance Criteria Verification

All criteria from `<success_criteria>`:

- [x] All tasks in 33.1-03-PLAN.md executed (2/2, both `type="auto"`)
- [x] Each task committed individually with proper `--no-verify` + hook-bypass commit body annotation (`c24f4b5`, `67c93d2`)
- [x] SUMMARY.md created (this document)
- [x] MCP tools/list excludes `update_mcp_admin_groups` AND `verify_mcp_admin` — verified via `len(TOOL_HANDLERS) == 51` and explicit `not in` assertions
- [x] No source file outside tests/ contains either removed identifier — Plan 05 D-10 AST guard flipped GREEN
- [x] connect_to_device prompt Step 6 no longer references a removed tool (D-13) — grep confirms `verify_mcp_admin` absent, `ssh_execute_command` + `sudo -n true` present
- [x] Tool count = 51 (down from 53) — `get_available_tools()` returns 51 entries
- [x] tool_annotations.py has `verify_mcp_admin` removed from `_READ_ONLY_TOOLS` AND `update_mcp_admin_groups` removed from `_MUTATING_ANNOTATIONS` — both verified by import-time assertion
- [x] Schema/annotation parity CI check still passes — full `uv run pytest tests/test_ast_regression.py` green
- [x] Plan 05 AST guards `test_no_forbidden_strings_in_source` + `test_no_removed_db_methods_in_source` flipped GREEN
- [x] STATE.md and ROADMAP.md updated (done post-SUMMARY by state-update step below)

## Task Verification Commands (rerun to confirm)

```bash
# 7-way parity (post-plan MUST print lock-step 7-way parity OK; tool count = 51; TOOL_HANDLERS count = 51)
uv run python -c "
import homelab_mcp.ssh_tools
from homelab_mcp.tool_handlers import TOOL_HANDLERS
from homelab_mcp.tool_schemas.ssh_tools_schema import SSH_TOOLS
from homelab_mcp.tool_annotations import TOOL_ANNOTATIONS
from homelab_mcp.openapi_app import TOOL_CATEGORIES, _SSH_TOOLS_WITH_HOSTNAME
from homelab_mcp.tools import get_available_tools
for n in ('update_mcp_admin_groups', 'verify_mcp_admin', 'verify_mcp_admin_access'):
    assert n not in SSH_TOOLS and n not in TOOL_HANDLERS and n not in TOOL_ANNOTATIONS
    assert n not in _SSH_TOOLS_WITH_HOSTNAME and n not in TOOL_CATEGORIES['SSH']
assert not hasattr(homelab_mcp.ssh_tools, 'update_mcp_admin_groups')
assert not hasattr(homelab_mcp.ssh_tools, 'verify_mcp_admin_access')
assert len(get_available_tools()) == 51
print('lock-step 7-way parity OK')
"

# Targeted test run (MUST show 63 passed)
uv run pytest tests/test_ssh_tools.py tests/test_tools.py tests/test_mcp_prompts.py tests/test_ast_regression.py

# mypy / ruff smoke (modified files clean except pre-existing ruff I001 + mypy jsonschema-stubs warnings)
uv run mypy src/homelab_mcp/ssh_tools.py src/homelab_mcp/prompt_registry.py
uv run ruff check src/homelab_mcp/prompt_registry.py
```

## Next Phase Readiness

- **Phase 33.1 close:** unblocked. All 5 plans (01, 02 docs, 03, 04, 05) shipped; all D-decisions satisfied.
- **Phase 33.2 scope (backlog, not yet started):** two newly-documented mcp_admin residual hits in `infrastructure_crud.py` and `vm_operations.py` should be added to the Phase 33.2 scope when that phase is gathered. They're the same Phase-33.2 cleanup pattern as `ssh_connection.py`'s `username="mcp_admin"` default, just expressed as dict-literal return values instead of function-signature defaults. Plan 33.2 should either extend `DEFERRED_MCP_ADMIN_DEFAULT_FILES` to include them OR fix both sites as part of the service-tool + downstream-infrastructure sweep.
- **No new blockers introduced.**

## Self-Check: PASSED

- [x] `src/homelab_mcp/tool_schemas/ssh_tools_schema.py` exists (modified — 2 schema entries gone)
- [x] `src/homelab_mcp/tool_handlers/ssh_handlers.py` exists (modified — 2 handlers gone)
- [x] `src/homelab_mcp/tool_handlers/__init__.py` exists (modified — 2 dispatch entries gone)
- [x] `src/homelab_mcp/tool_annotations.py` exists (modified — list entry + dict entry both gone)
- [x] `src/homelab_mcp/openapi_app.py` exists (modified — 4 allowlist entries gone)
- [x] `src/homelab_mcp/ssh_tools.py` exists (modified — 2 function bodies gone, helpers preserved)
- [x] `src/homelab_mcp/prompt_registry.py` exists (modified — Step 6 rewritten)
- [x] `tests/test_ssh_tools.py` exists (modified — 3 test functions + 1 import gone)
- [x] `tests/test_tools.py` exists (modified — count + inversion + new assertion + 4 orphan tests gone)
- [x] `tests/test_mcp_prompts.py` exists (modified — inverted assertion + positive assertions added)
- [x] `.planning/phases/33.1-.../33.1-03-SUMMARY.md` exists (this document)
- [x] Commit `c24f4b5` (feat: 5-surface lock-step) in `git log`
- [x] Commit `67c93d2` (feat: ssh_tools.py + prompt + tests) in `git log`

---

*Phase: 33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-*
*Plan: 03*
*Completed: 2026-04-23*
