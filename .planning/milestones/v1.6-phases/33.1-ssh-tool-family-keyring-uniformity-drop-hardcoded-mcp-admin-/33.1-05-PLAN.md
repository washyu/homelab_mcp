---
phase: 33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_ast_regression.py
autonomous: true
requirements: []
objectives: [D-08, D-09, D-10]
tags: [credential-cleanup, regression-guards, meta-tests, 33.1]

must_haves:
  truths:
    - "AST meta-test fails if any `src/homelab_mcp/**/*.py` function parameter named `username` has a literal string default of `'mcp_admin'` — narrowed to Phase 33.1 scope via a file-level allowlist (DEFERRED_MCP_ADMIN_DEFAULT_FILES) covering `ssh_connection.py` and `service_installer.py` per user's Narrow + defer decision"
    - "TOOLS-dict meta-test fails if any schema property named `username` has `'default': 'mcp_admin'` at any nesting depth (including `items.properties` for arrays) — narrowed via ALLOWED_MCP_ADMIN_DEFAULT_TOOLS allowlist covering the 9 service tools + the 3 SSH tools retaining that default"
    - "TOOLS-dict meta-test fails if any schema has a property named `password` at any nesting depth, narrowed via ALLOWED_PASSWORD_TOOLS allowlist covering ssh_discover/ssh_execute_command/start_interactive_shell (SSH family — not removed in 33.1 per CONTEXT) + the 9 service tools (deferred to Phase 33.2)"
    - "Forbidden-strings meta-test fails if the identifiers `update_mcp_admin_groups` or `verify_mcp_admin_access` appear anywhere in `src/homelab_mcp/**/*.py`"
    - "Every allowlist entry carries a `# Phase 33.2 scope` comment AND a module-level docstring block pointing to ROADMAP Phase 33.2 and CONTEXT.md's deferred section"
    - "At end of Phase 33.1 (after Plans 01/02/03/04 land): `uv run pytest tests/test_ast_regression.py -v` is GREEN within the documented narrow scope; allowlists document deferred Phase 33.2 scope"
  artifacts:
    - path: "tests/test_ast_regression.py"
      provides: "extended meta-tests D-08 (default arg scan with file allowlist), D-09 (TOOLS dict scan with two tool allowlists), D-10 (forbidden-strings append); module docstring documents 33.2 deferred scope"
      contains: "def test_no_username_mcp_admin_default_in_function_signatures"
  key_links:
    - from: "tests/test_ast_regression.py::test_no_username_mcp_admin_default_in_function_signatures"
      to: "src/homelab_mcp/**/*.py (minus DEFERRED_MCP_ADMIN_DEFAULT_FILES allowlist)"
      via: "ast.walk over FunctionDef nodes"
      pattern: "ast.parse"
    - from: "tests/test_ast_regression.py::test_no_password_or_mcp_admin_default_in_tool_registries"
      to: "NETWORK_TOOLS, SSH_TOOLS, SERVICE_TOOLS, and any other *_TOOLS dict"
      via: "import-time recursive traversal of inputSchema.properties with per-tool allowlists"
      pattern: "inputSchema"
---

<objective>
Extend the Phase 33 AST meta-test file (`tests/test_ast_regression.py`) with three new revert-proof regression guards:
- **D-08:** AST scan — fail if any function parameter named `username` defaults to the literal `"mcp_admin"` in `src/homelab_mcp/**/*.py` (outside tests). **Narrow scope via file-level allowlist** (`DEFERRED_MCP_ADMIN_DEFAULT_FILES`) covering `ssh_connection.py` and `service_installer.py`, both deferred to Phase 33.2 per user's "Narrow + defer" decision.
- **D-09:** Import-time schema scan — fail if any tool schema `username` has `"default": "mcp_admin"` or if any schema has a property named `password` at any nesting depth. **Narrow scope via two tool allowlists** — `ALLOWED_PASSWORD_TOOLS` (SSH family + 9 service tools) and `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS` (same 9 service tools + 3 SSH tools).
- **D-10:** Append `update_mcp_admin_groups` and `verify_mcp_admin_access` to the existing `FORBIDDEN_SOURCE_STRINGS` list.

Implements D-08, D-09, D-10 from `33.1-CONTEXT.md` within the user's narrow-scope + deferred decision.

Purpose: Phase 32/33 established that meta-guards are preferred to positive-only tests where a regression would be silent. These three tests make every decision in Phase 33.1 revert-proof within its documented scope: if a future change re-introduces `mcp_admin` defaults in Phase-33.1-covered sites, password schema properties on in-scope tools, or the removed tool names, the meta-test fails at test time.

**User's "Narrow + defer" decision** (see CONTEXT.md + revision context): 18 out-of-enumerated-scope `mcp_admin` sites exist (1 in `ssh_connection.py` line 162, 8 in `service_installer.py`, 9 in `service_tools_schema.py`). The user chose NOT to add plans cleaning these up in Phase 33.1. Instead this plan's allowlists document them for Phase 33.2 — so D-08 and D-09 meta-tests can be GREEN at end of Phase 33.1 within their documented narrow scope.

This plan is **Wave 1** and parallel with Plans 01 and 02 because:
- D-08's scan of function signatures (with file allowlist) tolerates pre-Plan-04 source (it will fail initially for the sitemap.py `username="mcp_admin"` default — the ONE site in scope — flipping GREEN when Plan 04 lands — this is exactly the Wave-0 TDD pattern Phase 33 used per 33-01-SUMMARY).
- D-09's scan of TOOLS dicts (with tool allowlists) similarly flips GREEN when Plan 02 lands.
- D-10's forbidden-strings extension flips GREEN when Plan 03 lands.

The test file is pure test code — no source overlap with other plans — so it can land independently in Wave 1.

Output: `tests/test_ast_regression.py` extended by two new test functions + updated FORBIDDEN_SOURCE_STRINGS list + narrow-scope allowlists with `# Phase 33.2 scope` comments + module-level docstring block. These start RED (correct TDD state) and flip GREEN as Plans 02/03/04 land — matching Phase 33's Wave-0-TDD workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-/33.1-CONTEXT.md
@.planning/phases/33-keyring-single-source-of-truth/33-01-SUMMARY.md
@.planning/milestones/v1.5-phases/32-regression-tests/32-CONTEXT.md
@tests/test_ast_regression.py

<interfaces>
<!-- Current AST meta-test shape — extend, don't replace. -->

From tests/test_ast_regression.py (existing, 119 lines):

```
FORBIDDEN_SOURCE_STRINGS: list[str] = [
    "ssh_credentials",
    "add_credential",
    "get_credential_by_hostname",
    "get_credential_by_id",
    "update_credential",
    "update_last_verified",
    "setup_remote_mcp_admin",
    "setup_mcp_admin",
    "update_server_credentials",
    "remove_server",
    # D-10 ADDS: "update_mcp_admin_groups", "verify_mcp_admin_access"
]

ALLOWED_EXCEPTIONS: dict[str, set[str]] = {
    "ssh_credentials": {"migration.py"},
}
```

Existing functions in the file:
- `_collect_string_literals(tree)`
- `_collect_name_and_attr_ids(tree)`
- `test_no_forbidden_strings_in_source()`
- `test_no_removed_db_methods_in_source()`
- `test_register_server_handler_no_verify_connection_param()`

**Out-of-enumerated-scope `mcp_admin` sites** (per user's "Narrow + defer" — covered by allowlists in this plan; cleanup deferred to Phase 33.2):

- `src/homelab_mcp/ssh_connection.py:162` — `username: str = "mcp_admin"` default (1 site)
- `src/homelab_mcp/service_installer.py` — 8 methods with `username: str = "mcp_admin"` defaults at lines 117, 204, 531, 978, 1030, 1082, 1382, 1456
- `src/homelab_mcp/tool_schemas/service_tools_schema.py` — 9 service tool schemas each with BOTH `"default": "mcp_admin"` on `username` AND a `password` property:
  1. `check_service_requirements`
  2. `install_service`
  3. `get_service_status`
  4. `plan_terraform_service`
  5. `destroy_terraform_service`
  6. `refresh_terraform_service`
  7. `check_ansible_service`
  8. `run_ansible_playbook`
  9. `destroy_terraform_service_preview`

Tool names were extracted from `src/homelab_mcp/tool_schemas/service_tools_schema.py` during this plan's preparation — these 9 names are exact.

**Target registries for D-09** (discoverable at import time via pkgutil):
- `homelab_mcp.tool_schemas.network_tools_schema.NETWORK_TOOLS`
- `homelab_mcp.tool_schemas.ssh_tools_schema.SSH_TOOLS`
- `homelab_mcp.tool_schemas.service_tools_schema.SERVICE_TOOLS`
- Any other `*_TOOLS` dict whose values are tool-schema-shaped (have `"inputSchema"` key)

Discovery approach: `pkgutil.iter_modules(homelab_mcp.tool_schemas.__path__)` + `dir(module)` + shape-check (`isinstance(v, dict) and "inputSchema" in v for all v`).

**Schema nesting to traverse** (D-09 — recursive):
- `TOOLS["tool_name"]["inputSchema"]["properties"]["propname"]` — top level
- `TOOLS["tool_name"]["inputSchema"]["properties"]["propname"]["items"]["properties"]["subprop"]` — array-of-objects (e.g., `bulk_discover_and_map.targets[]`)
- `TOOLS["tool_name"]["inputSchema"]["properties"]["propname"]["properties"]["subprop"]` — nested object

**D-09 narrow-scope allowlists (planner resolution per user's Narrow + defer decision):**

Per the user's revision decision, 33.1 scope does NOT extend the SSH family password removal beyond the three CONTEXT-enumerated tools (`discover_and_map`, `bulk_discover_and_map.targets[]`, `update_mcp_admin_groups`). The 9 service tools and the 3 other SSH tools (`ssh_discover`, `ssh_execute_command`, `start_interactive_shell`) retain their current shape pending Phase 33.2's extension sweep.

Two allowlists are needed (one per property type):

```
# Phase 33.2 scope — service-tool + SSH-family surface deferred for separate cleanup sweep.
# Every entry here is tracked in ROADMAP Phase 33.2 and documented in
# .planning/phases/33.1-*/33.1-CONTEXT.md deferred section.
ALLOWED_PASSWORD_TOOLS = frozenset({
    # SSH family (out-of-scope for Phase 33.1 CONTEXT D-01/D-02 enumeration)
    "ssh_discover",                           # Phase 33.2 scope
    "ssh_execute_command",                    # Phase 33.2 scope
    "start_interactive_shell",                # Phase 33.2 scope
    # Service family (deferred to Phase 33.2)
    "check_service_requirements",             # Phase 33.2 scope
    "install_service",                        # Phase 33.2 scope
    "get_service_status",                     # Phase 33.2 scope
    "plan_terraform_service",                 # Phase 33.2 scope
    "destroy_terraform_service",              # Phase 33.2 scope
    "refresh_terraform_service",              # Phase 33.2 scope
    "check_ansible_service",                  # Phase 33.2 scope
    "run_ansible_playbook",                   # Phase 33.2 scope
    "destroy_terraform_service_preview",      # Phase 33.2 scope
})

# Phase 33.2 scope — same 9 service tools + the 3 SSH tools that retain `username` defaults.
ALLOWED_MCP_ADMIN_DEFAULT_TOOLS = frozenset({
    # SSH family — retain their current `default: mcp_admin` pending Phase 33.2 review
    "ssh_discover",                           # Phase 33.2 scope
    "ssh_execute_command",                    # Phase 33.2 scope
    "start_interactive_shell",                # Phase 33.2 scope
    # Service family (deferred to Phase 33.2)
    "check_service_requirements",             # Phase 33.2 scope
    "install_service",                        # Phase 33.2 scope
    "get_service_status",                     # Phase 33.2 scope
    "plan_terraform_service",                 # Phase 33.2 scope
    "destroy_terraform_service",              # Phase 33.2 scope
    "refresh_terraform_service",              # Phase 33.2 scope
    "check_ansible_service",                  # Phase 33.2 scope
    "run_ansible_playbook",                   # Phase 33.2 scope
    "destroy_terraform_service_preview",      # Phase 33.2 scope
})
```

**NOTE on SSH-family username defaults:** Confirm empirically at implementation time whether `ssh_discover`, `ssh_execute_command`, and `start_interactive_shell` schemas actually carry `"default": "mcp_admin"` on their `username` properties (read `src/homelab_mcp/tool_schemas/ssh_tools_schema.py`). If they do NOT have the default, they must still appear in `ALLOWED_PASSWORD_TOOLS` (because they have `password` properties) but can be omitted from `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS`. The exact set is whichever matches the current source — the principle is: allowlist entries mirror what's currently in source that Phase 33.1 has chosen NOT to remove.

**D-08 file-level allowlist (planner resolution per user's Narrow + defer decision):**

```
# Files deferred to Phase 33.2 — mcp_admin cleanup sweep extension.
# Every entry tracked in ROADMAP Phase 33.2 and documented in
# .planning/phases/33.1-*/33.1-CONTEXT.md deferred section.
DEFERRED_MCP_ADMIN_DEFAULT_FILES: frozenset[str] = frozenset({
    "src/homelab_mcp/ssh_connection.py",       # Phase 33.2 scope
    "src/homelab_mcp/service_installer.py",    # Phase 33.2 scope
})
```

Use forward-slash paths for cross-platform comparison (convert `Path` to POSIX when checking — `py_file.relative_to(repo_root).as_posix()`).

**D-08 AST pattern** (scan function signatures):

For each .py file under `src/homelab_mcp/`: parse to AST, iterate FunctionDef / AsyncFunctionDef nodes, inspect `args.args` (positional) with right-aligned `args.defaults`, and `args.kwonlyargs` parallel with `args.kw_defaults`. Report violations when `arg.arg == "username"` AND the paired default is `ast.Constant(value="mcp_admin")`.

Defaults right-alignment rule: if `args.args` has N entries and `args.defaults` has D entries, then `args.args[N - D + i]` defaults to `args.defaults[i]`. Handle this carefully — a naive `zip` will misalign.

**Module-level docstring augmentation (BLOCKER 5):**

The existing module docstring at the top of the file starts with "D-15 + D-25 AST meta-test: ...". Augment it with a Phase 33.1 additions block documenting the three new guards, the two allowlists, the one file-allowlist, and pointing to ROADMAP Phase 33.2 and `.planning/phases/33.1-*/33.1-CONTEXT.md` deferred section. Rough shape:

```
"""AST meta-tests: regression guards against reintroduction of removed/forbidden patterns.

Phase 33 D-15 + D-25: forbidden credential-DB method names (see FORBIDDEN_SOURCE_STRINGS).

Phase 33.1 additions:
- D-08: username='mcp_admin' defaults are forbidden in function signatures under
  src/homelab_mcp/, except for files in DEFERRED_MCP_ADMIN_DEFAULT_FILES. The
  allowlist documents Phase 33.2 scope; see ROADMAP Phase 33.2 and
  .planning/phases/33.1-*/33.1-CONTEXT.md deferred section.
- D-09: password properties and username=mcp_admin defaults are forbidden in tool
  schemas, except for tools in ALLOWED_PASSWORD_TOOLS / ALLOWED_MCP_ADMIN_DEFAULT_TOOLS.
  Both allowlists document Phase 33.2 scope (SSH family tools retaining password +
  9 service tools deferred for separate cleanup sweep).
- D-10: forbidden identifiers extended with `update_mcp_admin_groups` and
  `verify_mcp_admin_access` (removed by Plan 33.1-03).
"""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add D-10 (forbidden strings append) + D-08 (function signature scan with file-level allowlist) + module docstring update</name>
  <files>tests/test_ast_regression.py</files>
  <read_first>
    - tests/test_ast_regression.py (entire file — 119 lines; existing harness to extend)
    - .planning/phases/33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-/33.1-CONTEXT.md (D-08 + D-10 verbatim, plus the "Narrow + defer" decision in the revision context)
    - .planning/phases/33-keyring-single-source-of-truth/33-01-SUMMARY.md (Phase 33 Wave 0 TDD pattern — this plan replicates it)
    - src/homelab_mcp/ssh_connection.py line 162 — the deferred mcp_admin default (confirm file path exists + line has the default)
    - src/homelab_mcp/service_installer.py lines 117, 204, 531, 978, 1030, 1082, 1382, 1456 — the 8 deferred mcp_admin defaults (confirm file path + approximate lines)
    - Python docs reference: `ast.FunctionDef.args.defaults` right-alignment against `args.args`
  </read_first>
  <action>
    **Step 1: Update module docstring (BLOCKER 5).**

    At the top of `tests/test_ast_regression.py`, replace/augment the existing module docstring with the Phase 33.1 additions block shown in `<interfaces>` above. The docstring MUST explicitly name:
    - Phase 33.1 D-08, D-09, D-10 additions
    - `DEFERRED_MCP_ADMIN_DEFAULT_FILES`, `ALLOWED_PASSWORD_TOOLS`, `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS` as narrow-scope allowlists
    - Point to ROADMAP Phase 33.2 and `.planning/phases/33.1-*/33.1-CONTEXT.md` deferred section

    **Step 2: D-10 — forbidden strings append (single-line list extension).**

    In `tests/test_ast_regression.py`, edit the `FORBIDDEN_SOURCE_STRINGS` list (lines 13–24). Append two strings after `"remove_server"`:

    - `"update_mcp_admin_groups",      # D-10: removed by Plan 33.1-03`
    - `"verify_mcp_admin_access",      # D-10: removed by Plan 33.1-03`

    No new `ALLOWED_EXCEPTIONS` entries — these names have no legitimate residual references in `src/`.

    **Step 3: D-08 — function signature scan with file-level allowlist (BLOCKER 3).**

    Near the top of the module (alongside `FORBIDDEN_SOURCE_STRINGS` / `ALLOWED_EXCEPTIONS`), add the file-level allowlist:

    ```
    # Phase 33.2 scope — mcp_admin cleanup sweep extension. These files retain
    # username='mcp_admin' defaults pending Phase 33.2. Every entry is tracked in
    # ROADMAP Phase 33.2 and documented in
    # .planning/phases/33.1-*/33.1-CONTEXT.md deferred section.
    DEFERRED_MCP_ADMIN_DEFAULT_FILES: frozenset[str] = frozenset({
        "src/homelab_mcp/ssh_connection.py",       # Phase 33.2 scope
        "src/homelab_mcp/service_installer.py",    # Phase 33.2 scope
    })
    ```

    Then add a new test function placed after `test_register_server_handler_no_verify_connection_param`:

    ```
    def test_no_username_mcp_admin_default_in_function_signatures() -> None:
        """D-08: No function in src/homelab_mcp/ may default `username` to the literal 'mcp_admin'.

        Narrow scope per Phase 33.1 CONTEXT + user's 'Narrow + defer' decision:
        files listed in DEFERRED_MCP_ADMIN_DEFAULT_FILES are allowlisted pending
        Phase 33.2's cleanup sweep. Every allowlist entry is tracked in ROADMAP
        Phase 33.2.

        Catches regression of Phase 33.1 Plan 04 (sitemap.py default cleanup).
        Catches any future re-introduction of `username='mcp_admin'` anywhere outside
        the narrow-scope allowlist.
        """
        src_root = Path(__file__).parent.parent / "src" / "homelab_mcp"
        repo_root = src_root.parent.parent
        assert src_root.exists(), f"Source root not found: {src_root}"

        violations: list[str] = []

        for py_file in sorted(src_root.rglob("*.py")):
            # File-level allowlist: compare via POSIX path for cross-platform consistency.
            rel_posix = py_file.relative_to(repo_root).as_posix()
            if rel_posix in DEFERRED_MCP_ADMIN_DEFAULT_FILES:
                continue  # Phase 33.2 scope

            source = py_file.read_text(encoding="utf-8")
            try:
                tree = ast.parse(source, filename=str(py_file))
            except SyntaxError as e:
                violations.append(f"{py_file}: SyntaxError during AST parse: {e}")
                continue

            for node in ast.walk(tree):
                if not isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                    continue

                args = node.args
                # Positional args with defaults: defaults are right-aligned.
                all_positional = args.args
                pos_defaults = args.defaults
                defaults_start = len(all_positional) - len(pos_defaults)
                for idx, arg in enumerate(all_positional):
                    if arg.arg != "username":
                        continue
                    default_idx = idx - defaults_start
                    if default_idx < 0:
                        continue  # No default
                    default_node = pos_defaults[default_idx]
                    if isinstance(default_node, ast.Constant) and default_node.value == "mcp_admin":
                        violations.append(
                            f"{rel_posix}:{node.lineno} "
                            f"function `{node.name}` has `username='mcp_admin'` default — "
                            f"remove per Phase 33.1 D-08"
                        )

                # Keyword-only args: args.kwonlyargs parallel with args.kw_defaults (same-index).
                for kw_arg, kw_default in zip(args.kwonlyargs, args.kw_defaults, strict=False):
                    if kw_arg.arg != "username":
                        continue
                    if kw_default is None:
                        continue
                    if isinstance(kw_default, ast.Constant) and kw_default.value == "mcp_admin":
                        violations.append(
                            f"{rel_posix}:{node.lineno} "
                            f"function `{node.name}` has kw-only `username='mcp_admin'` default — "
                            f"remove per Phase 33.1 D-08"
                        )

        assert not violations, (
            "Phase 33.1 regression (D-08): found `username='mcp_admin'` defaults in source files "
            "outside DEFERRED_MCP_ADMIN_DEFAULT_FILES allowlist.\n"
            + "\n".join(f"  - {v}" for v in violations)
        )
    ```

    Run `uv run pytest tests/test_ast_regression.py -v`. Expected state (pre-Plans-02/03/04):
    - `test_no_username_mcp_admin_default_in_function_signatures` — **RED** (sitemap.py line 315 has `username="mcp_admin"` default until Plan 04 lands; sitemap.py is NOT in the allowlist — only `ssh_connection.py` and `service_installer.py` are)
    - `test_no_forbidden_strings_in_source` — **RED** (ssh_tools.py / tool_schemas/ssh_tools_schema.py etc. still contain `update_mcp_admin_groups` and `verify_mcp_admin_access` until Plan 03 lands — D-10 strings now in the list)
    - The three existing Phase 33 tests — **GREEN** (unchanged)

    RED state is correct per the Wave-0-TDD pattern — tests are designed to flip GREEN as Plans 02/03/04 land.

    Manual verification: run `uv run pytest tests/test_ast_regression.py::test_no_username_mcp_admin_default_in_function_signatures -v 2>&1 | tail -20` — the failure message MUST list `sitemap.py` among the violations AND MUST NOT list `ssh_connection.py` or `service_installer.py` (those are allowlisted). If the allowlist isn't filtering correctly, fix before committing.

    Commit:
    ```
    git commit -m "test(33.1-05): add D-08 signature scan (with 33.2 file allowlist) + D-10 forbidden strings + module docstring"
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/test_ast_regression.py --collect-only -q && uv run ruff check tests/test_ast_regression.py && uv run python -c "from pathlib import Path; import ast; src = Path('tests/test_ast_regression.py').read_text(); tree = ast.parse(src); names = {n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)}; assert 'test_no_username_mcp_admin_default_in_function_signatures' in names; assert 'update_mcp_admin_groups' in src and 'verify_mcp_admin_access' in src; assert 'DEFERRED_MCP_ADMIN_DEFAULT_FILES' in src and 'ssh_connection.py' in src and 'service_installer.py' in src; assert 'Phase 33.2 scope' in src; print('D-08 + D-10 + allowlist + 33.2 comments present')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"update_mcp_admin_groups"' tests/test_ast_regression.py` returns at least 1 match (FORBIDDEN_SOURCE_STRINGS)
    - `grep -n '"verify_mcp_admin_access"' tests/test_ast_regression.py` returns at least 1 match
    - `grep -n 'def test_no_username_mcp_admin_default_in_function_signatures' tests/test_ast_regression.py` returns exactly 1 match
    - `grep -n 'DEFERRED_MCP_ADMIN_DEFAULT_FILES' tests/test_ast_regression.py` returns at least 2 matches (definition + use in test)
    - `grep -n 'ssh_connection.py' tests/test_ast_regression.py` returns at least 1 match (allowlist entry)
    - `grep -n 'service_installer.py' tests/test_ast_regression.py` returns at least 1 match (allowlist entry)
    - `grep -cE '# Phase 33.2 scope' tests/test_ast_regression.py` returns ≥ 2 (one per DEFERRED file — the trailing comments)
    - `grep -n 'ROADMAP Phase 33.2' tests/test_ast_regression.py` returns at least 1 match (module-level docstring — BLOCKER 5)
    - `uv run pytest tests/test_ast_regression.py --collect-only -q` exits 0 (test collects cleanly)
    - `uv run ruff check tests/test_ast_regression.py` clean
    - The D-08 test IS RED pre-Plan-04 and the failure message includes `sitemap.py` in the violations list AND does NOT include `ssh_connection.py` or `service_installer.py`. Verify manually: `uv run pytest tests/test_ast_regression.py::test_no_username_mcp_admin_default_in_function_signatures` produces an AssertionError whose message contains `sitemap.py:` and does NOT contain `ssh_connection.py` or `service_installer.py`.
  </acceptance_criteria>
  <done>Single commit. D-10 strings appended. D-08 test collects. D-08 test is RED in the expected way (flags sitemap.py only — allowlist silences ssh_connection.py + service_installer.py). Module docstring updated with 33.1/33.2 block.</done>
</task>

<task type="auto">
  <name>Task 2: Add D-09 (TOOLS-dict recursive schema scan) with two narrow-scope allowlists (BLOCKER 4)</name>
  <files>tests/test_ast_regression.py</files>
  <read_first>
    - tests/test_ast_regression.py (after Task 1 edits — contains FORBIDDEN_SOURCE_STRINGS + D-08 test + module docstring block + DEFERRED_MCP_ADMIN_DEFAULT_FILES)
    - .planning/phases/33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-/33.1-CONTEXT.md (D-09 verbatim + the planner-authority-limits discussion on narrow scope + the revision context's "Narrow + defer" decision)
    - src/homelab_mcp/tool_schemas/network_tools_schema.py (pre-Plan-02 state; NETWORK_TOOLS export)
    - src/homelab_mcp/tool_schemas/ssh_tools_schema.py (pre-Plan-02 state; SSH_TOOLS export — note ssh_discover/ssh_execute_command/start_interactive_shell retain password per Plan 02 scope. Check empirically whether their `username` properties carry `"default": "mcp_admin"` — this determines whether they belong in ALLOWED_MCP_ADMIN_DEFAULT_TOOLS.)
    - src/homelab_mcp/tool_schemas/service_tools_schema.py (entire file, ~289 lines) — read the 9 service tools with username+password to verify the exact names. Confirmed from plan preparation: check_service_requirements, install_service, get_service_status, plan_terraform_service, destroy_terraform_service, refresh_terraform_service, check_ansible_service, run_ansible_playbook, destroy_terraform_service_preview.
    - src/homelab_mcp/tool_schemas/ (directory listing — find all files; use Glob)
  </read_first>
  <action>
    **Empirical confirmation step (do FIRST):**

    Read `src/homelab_mcp/tool_schemas/ssh_tools_schema.py` and inspect whether `ssh_discover`, `ssh_execute_command`, `start_interactive_shell` have `"default": "mcp_admin"` on their `username` properties. For each tool:
    - If it has BOTH `password` property AND `"default": "mcp_admin"` on username → include in BOTH allowlists.
    - If it has ONLY `password` (no mcp_admin default) → include in `ALLOWED_PASSWORD_TOOLS` only.
    - If it has ONLY `"default": "mcp_admin"` (no password property) → include in `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS` only.

    Record the empirical result as an inline comment alongside the allowlist(s) before committing. The exact set is whichever matches the current source — the principle is: allowlist entries mirror what Phase 33.1 has chosen NOT to remove.

    **Allowlist definitions (BLOCKER 4 — two allowlists, not one):**

    Add these two allowlists near the top of `tests/test_ast_regression.py` (alongside `DEFERRED_MCP_ADMIN_DEFAULT_FILES` from Task 1). The exact SSH-family membership depends on the empirical check above:

    ```
    # Phase 33.2 scope — service-tool + SSH-family surface deferred for separate cleanup sweep.
    # Every entry here is tracked in ROADMAP Phase 33.2 and documented in
    # .planning/phases/33.1-*/33.1-CONTEXT.md deferred section.
    ALLOWED_PASSWORD_TOOLS: frozenset[str] = frozenset({
        # SSH family (out-of-scope for Phase 33.1 CONTEXT D-01/D-02 enumeration)
        "ssh_discover",                           # Phase 33.2 scope
        "ssh_execute_command",                    # Phase 33.2 scope
        "start_interactive_shell",                # Phase 33.2 scope
        # Service family (deferred to Phase 33.2)
        "check_service_requirements",             # Phase 33.2 scope
        "install_service",                        # Phase 33.2 scope
        "get_service_status",                     # Phase 33.2 scope
        "plan_terraform_service",                 # Phase 33.2 scope
        "destroy_terraform_service",              # Phase 33.2 scope
        "refresh_terraform_service",              # Phase 33.2 scope
        "check_ansible_service",                  # Phase 33.2 scope
        "run_ansible_playbook",                   # Phase 33.2 scope
        "destroy_terraform_service_preview",      # Phase 33.2 scope
    })

    # Phase 33.2 scope — username=mcp_admin default retention.
    # SSH family membership confirmed empirically — include only tools whose current
    # schema carries `"default": "mcp_admin"` on the username property.
    ALLOWED_MCP_ADMIN_DEFAULT_TOOLS: frozenset[str] = frozenset({
        # SSH family — include only those whose schema currently carries mcp_admin default.
        # (Adjust per empirical check of tool_schemas/ssh_tools_schema.py.)
        "ssh_discover",                           # Phase 33.2 scope — confirm empirically
        "ssh_execute_command",                    # Phase 33.2 scope — confirm empirically
        "start_interactive_shell",                # Phase 33.2 scope — confirm empirically
        # Service family (deferred to Phase 33.2) — all 9 have the default
        "check_service_requirements",             # Phase 33.2 scope
        "install_service",                        # Phase 33.2 scope
        "get_service_status",                     # Phase 33.2 scope
        "plan_terraform_service",                 # Phase 33.2 scope
        "destroy_terraform_service",              # Phase 33.2 scope
        "refresh_terraform_service",              # Phase 33.2 scope
        "check_ansible_service",                  # Phase 33.2 scope
        "run_ansible_playbook",                   # Phase 33.2 scope
        "destroy_terraform_service_preview",      # Phase 33.2 scope
    })
    ```

    **D-09 test function:**

    Add this test function to `tests/test_ast_regression.py`, placed after `test_no_username_mcp_admin_default_in_function_signatures`:

    ```
    def test_no_password_or_mcp_admin_default_in_tool_registries() -> None:
        """D-09: No tool schema may advertise a `password` property (at any nesting depth)
        or default `username` to `mcp_admin`.

        Imports every `*_TOOLS` dict under `src/homelab_mcp/tool_schemas/` and recursively
        traverses `inputSchema.properties` including `items.properties` for array items.

        Narrow scope (BLOCKER 4 resolution): Phase 33.1 D-01/D-02 removed password only
        from `discover_and_map`, `bulk_discover_and_map.targets[]`, and
        `update_mcp_admin_groups`. Other SSH tools (`ssh_discover`, `ssh_execute_command`,
        `start_interactive_shell`) retain password per CONTEXT narrow scope. The 9 service
        tools also retain both password and mcp_admin default, deferred to Phase 33.2 per
        user's "Narrow + defer" decision. Both allowlists (ALLOWED_PASSWORD_TOOLS,
        ALLOWED_MCP_ADMIN_DEFAULT_TOOLS) mirror the current source that Phase 33.1 does
        not touch; widening requires a future phase.

        Catches regression of Phase 33.1 Plan 02 (schema cleanup D-01/D-02/D-03) within
        the narrow scope.
        """
        import importlib
        import pkgutil

        import homelab_mcp.tool_schemas as tool_schemas_pkg

        # Discover every module in tool_schemas/, import it, find every dict-valued
        # top-level attribute whose entries look like tool schema definitions.
        tool_registries: dict[str, dict] = {}
        for modinfo in pkgutil.iter_modules(tool_schemas_pkg.__path__):
            module = importlib.import_module(f"homelab_mcp.tool_schemas.{modinfo.name}")
            for attr_name in dir(module):
                if attr_name.startswith("_"):
                    continue
                attr = getattr(module, attr_name)
                if not isinstance(attr, dict) or not attr:
                    continue
                # Detect "this is a TOOLS-style dict": every value is a dict with "inputSchema"
                if all(isinstance(v, dict) and "inputSchema" in v for v in attr.values()):
                    tool_registries[f"{modinfo.name}.{attr_name}"] = attr

        assert tool_registries, "No tool registries discovered — D-09 would be a no-op (check pkgutil)"

        violations: list[str] = []

        def _recurse(
            properties: dict,
            path: str,
            *,
            check_password: bool,
            check_mcp_admin_default: bool,
        ) -> None:
            if not isinstance(properties, dict):
                return
            for prop_name, prop_schema in properties.items():
                if not isinstance(prop_schema, dict):
                    continue
                # username=mcp_admin default check — gated by per-tool allowlist
                if (
                    check_mcp_admin_default
                    and prop_name == "username"
                    and prop_schema.get("default") == "mcp_admin"
                ):
                    violations.append(
                        f"{path}.{prop_name} — D-09: username must not default to 'mcp_admin'"
                    )
                # password property check — gated by per-tool allowlist
                if check_password and prop_name == "password":
                    violations.append(f"{path}.{prop_name} — D-09: password property forbidden")
                # Recurse into nested object properties
                nested = prop_schema.get("properties")
                if isinstance(nested, dict):
                    _recurse(
                        nested,
                        f"{path}.{prop_name}.properties",
                        check_password=check_password,
                        check_mcp_admin_default=check_mcp_admin_default,
                    )
                # Recurse into array items.properties
                items = prop_schema.get("items")
                if isinstance(items, dict):
                    items_props = items.get("properties")
                    if isinstance(items_props, dict):
                        _recurse(
                            items_props,
                            f"{path}.{prop_name}.items.properties",
                            check_password=check_password,
                            check_mcp_admin_default=check_mcp_admin_default,
                        )

        for registry_name, registry in tool_registries.items():
            for tool_name, tool_def in registry.items():
                props = tool_def.get("inputSchema", {}).get("properties")
                if not isinstance(props, dict):
                    continue
                check_password = tool_name not in ALLOWED_PASSWORD_TOOLS
                check_mcp_admin_default = tool_name not in ALLOWED_MCP_ADMIN_DEFAULT_TOOLS
                _recurse(
                    props,
                    f"{registry_name}[{tool_name!r}].inputSchema.properties",
                    check_password=check_password,
                    check_mcp_admin_default=check_mcp_admin_default,
                )

        assert not violations, (
            "Phase 33.1 regression (D-09): found forbidden `password` property or "
            "`username` default of 'mcp_admin' in tool schemas (outside allowlists).\n"
            + "\n".join(f"  - {v}" for v in violations)
        )
    ```

    **Pre-commit verification.** Run `uv run pytest tests/test_ast_regression.py::test_no_password_or_mcp_admin_default_in_tool_registries -v`. Expected state:

    - **RED** (pre-Plan-02): failure message MUST list violations including:
      - `network_tools_schema.NETWORK_TOOLS['discover_and_map'].inputSchema.properties.password`
      - `network_tools_schema.NETWORK_TOOLS['discover_and_map'].inputSchema.properties.username` (mcp_admin default)
      - `network_tools_schema.NETWORK_TOOLS['bulk_discover_and_map'].inputSchema.properties.targets.items.properties.password`
      - `network_tools_schema.NETWORK_TOOLS['bulk_discover_and_map'].inputSchema.properties.targets.items.properties.username` (mcp_admin default)
      - `ssh_tools_schema.SSH_TOOLS['update_mcp_admin_groups'].inputSchema.properties.password` (Phase 33.1 D-02 target; update_mcp_admin_groups is NOT in the allowlists — it's being removed entirely by Plan 03)
    - **Must NOT list** violations for any tool in `ALLOWED_PASSWORD_TOOLS` or `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS` — confirm the 9 service tools AND ssh_discover/ssh_execute_command/start_interactive_shell do NOT appear in the failure output.

    If the violations list is empty (GREEN) pre-Plan-02, the allowlist is too wide or the recursion is wrong — fix before committing. If violations appear for ALLOWED tools, the allowlist filter is not working — fix before committing.

    Also verify discovery phase: the `assert tool_registries` assertion passes (at least `network_tools_schema.NETWORK_TOOLS`, `ssh_tools_schema.SSH_TOOLS`, and `service_tools_schema.SERVICE_TOOLS` discovered). If the assertion fails, `pkgutil.iter_modules` isn't finding the schema modules — fix before committing.

    Commit:
    ```
    git commit -m "test(33.1-05): add D-09 recursive TOOLS-dict scan with ALLOWED_PASSWORD_TOOLS + ALLOWED_MCP_ADMIN_DEFAULT_TOOLS (33.2-scope narrow)"
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/test_ast_regression.py::test_no_password_or_mcp_admin_default_in_tool_registries --collect-only -q && uv run ruff check tests/test_ast_regression.py && uv run python -c "import importlib, pkgutil; import homelab_mcp.tool_schemas as p; mods = [m.name for m in pkgutil.iter_modules(p.__path__)]; assert 'network_tools_schema' in mods and 'ssh_tools_schema' in mods and 'service_tools_schema' in mods; print('registries discoverable')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def test_no_password_or_mcp_admin_default_in_tool_registries' tests/test_ast_regression.py` returns exactly 1 match
    - `grep -n 'ALLOWED_PASSWORD_TOOLS' tests/test_ast_regression.py` returns at least 2 matches (definition + use in test body)
    - `grep -n 'ALLOWED_MCP_ADMIN_DEFAULT_TOOLS' tests/test_ast_regression.py` returns at least 2 matches (definition + use in test body)
    - `grep -n '"ssh_discover"' tests/test_ast_regression.py` returns at least 1 match (allowlist entry)
    - `grep -n '"start_interactive_shell"' tests/test_ast_regression.py` returns at least 1 match (allowlist entry)
    - `grep -n '"check_service_requirements"' tests/test_ast_regression.py` returns at least 2 matches (in both allowlists)
    - `grep -n '"install_service"' tests/test_ast_regression.py` returns at least 2 matches (in both allowlists)
    - `grep -n '"destroy_terraform_service_preview"' tests/test_ast_regression.py` returns at least 2 matches (in both allowlists)
    - `grep -cE '# Phase 33.2 scope' tests/test_ast_regression.py` returns ≥ 20 (one per allowlisted entry across both allowlists + DEFERRED files — roughly 2×12 + 2 = 26 at minimum)
    - `uv run pytest tests/test_ast_regression.py::test_no_password_or_mcp_admin_default_in_tool_registries --collect-only -q` exits 0
    - Pre-Plan-02 state: test IS RED. Failure message includes the literal string `discover_and_map` AND `items.properties.password` (proving the recursion descends into bulk_discover_and_map.targets[] correctly).
    - Pre-Plan-02 state: failure message does NOT include any of: `check_service_requirements`, `install_service`, `get_service_status`, `plan_terraform_service`, `destroy_terraform_service`, `refresh_terraform_service`, `check_ansible_service`, `run_ansible_playbook`, `destroy_terraform_service_preview`, `ssh_discover`, `ssh_execute_command`, `start_interactive_shell` — these tools are all allowlisted.
    - `uv run ruff check tests/test_ast_regression.py` clean
    - The D-09 test's `assert tool_registries` non-emptiness check passes — discovery works.
  </acceptance_criteria>
  <done>Single commit. D-09 test added with two narrow-scope allowlists (ALLOWED_PASSWORD_TOOLS + ALLOWED_MCP_ADMIN_DEFAULT_TOOLS). Test collects and fails RED in the expected way (flags network_tools_schema + update_mcp_admin_groups — will flip GREEN when Plan 02 + Plan 03 land). Service tools + 3 retained SSH tools correctly silenced by allowlists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future developer edits → source tree | Meta-tests fire if regression reintroduces removed patterns (within narrow scope) |
| Pre-commit CI → meta-tests | Tests run as part of `uv run pytest`; failures block merge |
| Allowlist-as-documentation → Phase 33.2 planning | Allowlist entries tracked in ROADMAP Phase 33.2; removing an entry in a future phase is the mechanism that extends scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33.1-17 | Tampering | Future refactor re-introduces `username="mcp_admin"` default in a file NOT on the DEFERRED_MCP_ADMIN_DEFAULT_FILES allowlist | mitigate | D-08 test walks every FunctionDef/AsyncFunctionDef in `src/homelab_mcp/`, skips files in the allowlist, fires at test time for any other violation. Allowlist entries carry `# Phase 33.2 scope` comments, making any future widening visible in code review. |
| T-33.1-18 | Tampering | Future refactor re-adds `password` property to a tool NOT on the ALLOWED_PASSWORD_TOOLS allowlist | mitigate | D-09 test imports every TOOLS-dict module and recursively traverses. Allowlist is narrow (3 SSH + 9 service = 12 named tools) and any addition requires an explicit allowlist edit, making the change visible in code review. |
| T-33.1-19 | Tampering | Future refactor restores `update_mcp_admin_groups` or `verify_mcp_admin_access` | mitigate | D-10 adds the two identifiers to existing `FORBIDDEN_SOURCE_STRINGS`; existing `test_no_forbidden_strings_in_source` scans src/ and catches them. |
| T-33.1-20 | Denial of Service | AST walker fails on malformed Python → false positive | accept | Wrapped in `try/except SyntaxError`; a parse error is recorded as a violation but the test still completes. Matches existing Phase 33 harness behavior. |
| T-33.1-21 | Information Disclosure | Test failure message contains path/function names | accept | Developer-facing test output; not a user-facing channel. Matches Phase 33 pattern. |
| T-33.1-22 | Tampering | Future developer silently widens an allowlist to silence a failing test instead of fixing the source | mitigate (weak) | Code review is the primary mitigation; `# Phase 33.2 scope` comments + module docstring explicitly document the allowlist purpose; ROADMAP Phase 33.2 tracks the intended cleanup. Not a hard guard — but the explicit scope documentation makes casual widening obviously wrong. |

Residual risk: **low-medium**. Pure test code; no source surface change; three tests scoped to existing meta-test harness patterns. Medium rather than low because the narrow allowlists depend on Phase 33.2 actually happening — if 33.2 is indefinitely deferred the allowlists may grow stale.
</threat_model>

<verification>
- Two commits (Task 1 + Task 2)
- `FORBIDDEN_SOURCE_STRINGS` contains two new entries after Task 1
- Two new test functions present after Task 2
- `DEFERRED_MCP_ADMIN_DEFAULT_FILES`, `ALLOWED_PASSWORD_TOOLS`, `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS` all present with `# Phase 33.2 scope` comments
- Module-level docstring explicitly documents Phase 33.2 deferred scope pointing to ROADMAP + CONTEXT.md (BLOCKER 5)
- Both new tests COLLECT without error under `--collect-only`
- Both new tests are RED in current repo state (pre-Plans-02/03/04) — this is the Wave-0-TDD handoff contract
- Expected flip-to-GREEN order: D-10 flips when Plan 03 lands; D-09 flips when Plan 02 lands (within narrow scope); D-08 flips when Plan 04 lands (within narrow scope — sitemap.py only)
- After all Phase 33.1 plans land, every test in `test_ast_regression.py` is GREEN **within documented narrow scope**; allowlists document deferred Phase 33.2 scope. If any test is still RED outside its allowlist, that plan's execution is incomplete.
</verification>

<success_criteria>
`tests/test_ast_regression.py`:
- Line count grew by ~150+ lines (two new test functions + FORBIDDEN list extension + three allowlist definitions + docstring block).
- `FORBIDDEN_SOURCE_STRINGS` contains `"update_mcp_admin_groups"` and `"verify_mcp_admin_access"`.
- `DEFERRED_MCP_ADMIN_DEFAULT_FILES` contains `ssh_connection.py` and `service_installer.py` with `# Phase 33.2 scope` comments (BLOCKER 3).
- `ALLOWED_PASSWORD_TOOLS` contains 12 entries (3 SSH + 9 service) with `# Phase 33.2 scope` comments (BLOCKER 4).
- `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS` contains 12 entries (empirically-confirmed SSH + 9 service) with `# Phase 33.2 scope` comments (BLOCKER 4).
- Two new test functions present: `test_no_username_mcp_admin_default_in_function_signatures`, `test_no_password_or_mcp_admin_default_in_tool_registries`.
- Module-level docstring explicitly documents the three allowlists, points to ROADMAP Phase 33.2, and points to `.planning/phases/33.1-*/33.1-CONTEXT.md` deferred section (BLOCKER 5).

`uv run pytest tests/test_ast_regression.py --collect-only -q` exits 0.

In the combined end-of-phase state (after Plans 01/02/03/04 land): `uv run pytest tests/test_ast_regression.py -v` exits 0 (all tests GREEN **within documented scope**; allowlists document deferred Phase 33.2 scope) — this is the Phase 33.1 completion criterion (BLOCKER 5 — phrased precisely, not as overclaimed "all tests GREEN at end of phase").

Additional completion criterion (BLOCKER 5): D-08 and D-09 allowlists carry `# Phase 33.2 scope` comments AND a module-level docstring pointing to ROADMAP Phase 33.2.
</success_criteria>

<output>
After completion, create `.planning/phases/33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-/33.1-05-SUMMARY.md` with:
- Two new test function names
- D-10 strings appended (exact lines)
- `DEFERRED_MCP_ADMIN_DEFAULT_FILES` contents + scope-narrowing rationale (BLOCKER 3)
- `ALLOWED_PASSWORD_TOOLS` set contents (12 entries) + scope-narrowing rationale (BLOCKER 4)
- `ALLOWED_MCP_ADMIN_DEFAULT_TOOLS` set contents (empirically-confirmed) + scope-narrowing rationale (BLOCKER 4)
- Module docstring augmentation before/after (BLOCKER 5)
- Expected RED→GREEN flip table (which test goes GREEN after which plan lands, scoped to narrow allowlists)
- Empirical confirmation result for SSH-family tools (which of ssh_discover/ssh_execute_command/start_interactive_shell actually carry the mcp_admin default — determines ALLOWED_MCP_ADMIN_DEFAULT_TOOLS membership)
- Pattern followed: Phase 33 Wave-0-TDD (33-01-SUMMARY) + narrow-scope allowlist pattern new to Phase 33.1
</output>
</output>
