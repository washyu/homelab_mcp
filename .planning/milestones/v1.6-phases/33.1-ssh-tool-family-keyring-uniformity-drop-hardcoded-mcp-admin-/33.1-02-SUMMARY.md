---
phase: 33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-
plan: 02
subsystem: api
tags: [credential-cleanup, schema, protocol-boundary, 33.1, mcp, keyring, tdd]

# Dependency graph
requires:
  - phase: 33-keyring-single-source-of-truth
    provides: keyring auto-inject via resolve_ssh_credentials; D-06 "passwords never enter chat" principle
provides:
  - MCP protocol boundary no longer advertises password property on discover_and_map
  - MCP protocol boundary no longer advertises password property on bulk_discover_and_map.targets[]
  - MCP protocol boundary no longer advertises password property on update_mcp_admin_groups
  - No schema advertises default="mcp_admin" for username anywhere in network_tools_schema.py
  - discover_and_map description copy no longer claims "Defaults to mcp_admin"
  - Two schema-level regression tests guard D-01/D-02/D-03/D-12 against revert
affects: [33.1-01, 33.1-03, 33.1-04, 33.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD RED/GREEN for schema-surface changes — assertion-per-decision mapping"
    - "Scoped awk-extraction for property-count gates — tests one tool block instead of whole file"

key-files:
  created: []
  modified:
    - src/homelab_mcp/tool_schemas/network_tools_schema.py
    - src/homelab_mcp/tool_schemas/ssh_tools_schema.py
    - tests/test_tools.py

key-decisions:
  - "D-01/D-02/D-03/D-12 surgically applied to tool schemas only — resolver contract (D-04) and downstream sitemap defaults (D-06/D-07) remain for other plans in the wave"
  - "ssh_discover / ssh_execute_command / start_interactive_shell password properties NOT touched — 33.1-CONTEXT D-01/D-02 scope limits this plan to 3 explicit entries; broader cleanup is out of scope"
  - "test_sitemap_tool_schemas assertion on username.default updated (was asserting 'mcp_admin' — had to be removed because the default is removed); structurally this is part of Task 1 GREEN, not a deviation"

patterns-established:
  - "Per-decision schema regression test — assertion text references the decision ID (D-01, D-02, D-03, D-12) so revert-proofing is tied to the CONTEXT record, not just the code"
  - "Defense-in-depth schema cleanup ahead of lock-step tool removal — even if Plan 03 slips/reverts the full update_mcp_admin_groups deletion, this plan's narrower password-property deletion keeps that surface closed"

requirements-completed: []

# Metrics
duration: ~5min
completed: 2026-04-23
---

# Phase 33.1 Plan 02: SSH Tool Family Keyring Uniformity — Schema Cleanup Summary

**Stripped `password` property from 3 MCP tool schemas and dropped `default: "mcp_admin"` from 2 username properties — closes D-06 protocol-boundary surface for discover_and_map, bulk_discover_and_map, and update_mcp_admin_groups.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-23T00:46:53Z
- **Completed:** 2026-04-23T00:51:01Z
- **Tasks:** 2 (each 1 RED + 1 GREEN = 4 commits)
- **Files modified:** 3 (2 source schemas + 1 test file)

## Accomplishments

- `discover_and_map` schema: `password` property deleted; `username.default="mcp_admin"` deleted; description copy "Defaults to 'mcp_admin' if omitted" deleted (D-01, D-03, D-12)
- `bulk_discover_and_map.targets[]` items schema: `password` property deleted; `username.default="mcp_admin"` deleted (D-01, D-03)
- `update_mcp_admin_groups` schema: `password` property deleted (D-02, defense-in-depth before Plan 03 deletes the entire tool)
- Two TDD regression tests added to `tests/test_tools.py` — `test_discover_and_map_schema_no_password_no_mcp_admin_default` and `test_update_mcp_admin_groups_schema_no_password`

## Task Commits

Each task was committed atomically (TDD RED + GREEN, 4 commits total):

1. **Task 1 RED:** add failing schema-cleanup test for discover_and_map — `60a412c` (test)
2. **Task 1 GREEN:** remove password + mcp_admin default from discover_and_map schemas — `bed172b` (feat)
3. **Task 2 RED:** add failing schema-cleanup test for update_mcp_admin_groups password — `779e341` (test)
4. **Task 2 GREEN:** remove password property from update_mcp_admin_groups schema — `5607fa8` (feat)

Plan metadata commit (SUMMARY.md) handled separately.

## Schema Edit Diffs

### Task 1, Edit A — `discover_and_map.username` (D-03, D-12)

Before:
```python
"username": {
    "type": "string",
    "description": "SSH username. Defaults to 'mcp_admin' if omitted. Omit if credentials were stored with `credentials add` — they are auto-injected.",
    "default": "mcp_admin",
},
```

After:
```python
"username": {
    "type": "string",
    "description": "SSH username. Omit if credentials were stored with `credentials add` — they are auto-injected.",
},
```

### Task 1, Edit B — `discover_and_map.password` (D-01) — DELETED

Before:
```python
"password": {
    "type": "string",
    "description": "SSH password (not needed for mcp_admin after setup)",
},
```

After: (property removed entirely)

### Task 1, Edit C — `bulk_discover_and_map.targets[].items.properties.username` (D-03)

Before:
```python
"username": {"type": "string", "default": "mcp_admin"},
```

After:
```python
"username": {"type": "string"},
```

### Task 1, Edit D — `bulk_discover_and_map.targets[].items.properties.password` (D-01) — DELETED

Before:
```python
"password": {"type": "string"},
```

After: (line removed entirely)

### Task 2 — `update_mcp_admin_groups.inputSchema.properties.password` (D-02) — DELETED

Before:
```python
"password": {
    "type": "string",
    "description": "Password for the admin user. Omit if credentials were stored with `credentials add` — they are auto-injected.",
},
```

After: (property removed entirely)

## Out-of-Scope Observations (NOT Touched — per CONTEXT)

`ssh_tools_schema.py` still has `password` properties on three other tools that were explicitly **out of scope** for this plan per `33.1-CONTEXT.md` D-01/D-02:

| Tool | Line Range | Status |
|------|-----------|--------|
| `ssh_discover` | 16–19 | Still has `password` property — intentional (no CONTEXT decision covers it) |
| `ssh_execute_command` | 66–69 | Still has `password` property — intentional |
| `start_interactive_shell` | 101–104 | Still has `password` property — intentional |

These three are flagged as **observations only**; removing them would be scope creep. The scoped awk-grep verification (`awk '/update_mcp_admin_groups/,/^    \},/'`) correctly isolates only the target tool block, so the count of 0 is precise regardless of what other tools in the file contain.

## Test Functions Added to `tests/test_tools.py`

1. `test_discover_and_map_schema_no_password_no_mcp_admin_default` — imports `NETWORK_TOOLS` directly; asserts no `password` on discover/bulk-target properties; asserts `username` has no `default` key; asserts description string does not contain `"Defaults to 'mcp_admin'"`.
2. `test_update_mcp_admin_groups_schema_no_password` — imports `SSH_TOOLS` directly; asserts no `password` key on `update_mcp_admin_groups` properties.

## Decisions Made

- **Applied CONTEXT D-01/D-02 scope strictly:** did NOT extend password-property removal to `ssh_discover`, `ssh_execute_command`, or `start_interactive_shell` even though they have the same surface; the CONTEXT scoped this plan narrowly and broader removal would be scope creep per `scope_reduction_prohibition` (reverse direction).
- **Updated existing `test_sitemap_tool_schemas`** to drop its assertion on `username.default == "mcp_admin"` as part of Task 1 GREEN — the assertion would have become a false-positive failure otherwise, so the edit is structurally required, not a deviation.

## Deviations from Plan

None - plan executed exactly as written.

The minor test-assertion update to `test_sitemap_tool_schemas` (removing the obsolete `username.default` assertion) was a structurally-required part of Task 1 GREEN, explicitly anticipated by the plan's requirement that the full test sweep stays green. No auto-fix rules (1-4) invoked.

## Issues Encountered

**Pre-existing test failures (NOT introduced by this plan):** Two tests fail against the base commit `e6cf7a1` before any Plan 02 changes — they're logged in `deferred-items.md` for follow-up:

- `tests/test_database.py::test_ssh_credentials_table_dropped_postgres` — `monkeypatch.setattr("src.homelab_mcp.database.psycopg2", ...)` raises ImportError because `database` is a module, not a package.
- `tests/test_proxmox_api.py::TestGetProxmoxClient::test_client_missing_host` — expected `ValueError` not raised; likely env-leak or changed client behavior.

Verified pre-existing by `git stash && pytest` on the stashed base — both failed without my changes.

Excluding these two pre-existing failures, the full sweep passes: **668 passed, 9 skipped, 17 deselected**.

## Verification

- ✓ `grep -c '"password"' src/homelab_mcp/tool_schemas/network_tools_schema.py` → 0
- ✓ `grep -cE '"default":\s*"mcp_admin"' src/homelab_mcp/tool_schemas/network_tools_schema.py` → 0
- ✓ `awk '/"update_mcp_admin_groups"/,/^    \},/' src/homelab_mcp/tool_schemas/ssh_tools_schema.py | grep -c '"password"'` → 0 (scoped precision)
- ✓ `grep -c '"password"' src/homelab_mcp/tool_schemas/ssh_tools_schema.py` → 3 (out-of-scope tools intentionally unchanged)
- ✓ `uv run ruff check src/homelab_mcp/tool_schemas/network_tools_schema.py` → clean
- ✓ `uv run ruff check src/homelab_mcp/tool_schemas/ssh_tools_schema.py` → clean
- ✓ `uv run pytest tests/test_tools.py::test_discover_and_map_schema_no_password_no_mcp_admin_default` → passes
- ✓ `uv run pytest tests/test_tools.py::test_update_mcp_admin_groups_schema_no_password` → passes
- ✓ Full non-integration sweep (minus 2 pre-existing failures): 668 passed, 9 skipped

## TDD Gate Compliance

Plan 33.1-02 is a `type: execute` plan with two TDD-marked tasks (`tdd="true"`). Both tasks produced a RED commit (`test(...)`) followed by a GREEN commit (`feat(...)`). All four commits visible in `git log --oneline -5`:

```
5607fa8 feat(33.1-02): remove password property from update_mcp_admin_groups schema (D-02)
779e341 test(33.1-02): add failing schema-cleanup test for update_mcp_admin_groups password (D-02)
bed172b feat(33.1-02): remove password + mcp_admin default from discover_and_map schemas (D-01, D-03, D-12)
60a412c test(33.1-02): add failing schema-cleanup test for discover_and_map (D-01, D-03, D-12)
```

Each RED commit verified failing before the corresponding GREEN commit was authored. No REFACTOR phase was needed (no cleanup targets after the GREEN edits).

## User Setup Required

None - schema-only changes; no external service configuration required.

## Next Phase Readiness

- MCP protocol boundary for discover_and_map / bulk_discover_and_map / update_mcp_admin_groups now closed against password leakage and mcp_admin default injection.
- Plan 33.1-03 (lock-step tool removal per D-05) can proceed safely — this plan's narrower deletion is a strict subset of Plan 03's whole-tool deletion; there's no conflict.
- Plan 33.1-04 (D-09 TOOLS-dict meta-test for password/default=mcp_admin at any nesting depth) will find the schemas already clean — its test will pass immediately on these three tools.
- Blockers: none.

## Self-Check: PASSED

**Files verified present:**
- `.planning/phases/33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-/33.1-02-SUMMARY.md` — FOUND
- `src/homelab_mcp/tool_schemas/network_tools_schema.py` — FOUND
- `src/homelab_mcp/tool_schemas/ssh_tools_schema.py` — FOUND
- `tests/test_tools.py` — FOUND
- `.planning/phases/33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-/deferred-items.md` — FOUND

**Commits verified present in git log:**
- `60a412c` — RED test for discover_and_map — FOUND
- `bed172b` — GREEN for discover_and_map schema — FOUND
- `779e341` — RED test for update_mcp_admin_groups — FOUND
- `5607fa8` — GREEN for update_mcp_admin_groups schema — FOUND

---
*Phase: 33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-*
*Completed: 2026-04-23*
