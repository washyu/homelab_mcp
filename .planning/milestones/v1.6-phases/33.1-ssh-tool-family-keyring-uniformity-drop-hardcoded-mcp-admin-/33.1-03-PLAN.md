---
phase: 33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-
plan: 03
type: execute
wave: 2
depends_on: ["33.1-01"]
files_modified:
  - src/homelab_mcp/tool_schemas/ssh_tools_schema.py
  - src/homelab_mcp/tool_handlers/ssh_handlers.py
  - src/homelab_mcp/tool_handlers/__init__.py
  - src/homelab_mcp/tool_annotations.py
  - src/homelab_mcp/openapi_app.py
  - src/homelab_mcp/ssh_tools.py
  - src/homelab_mcp/prompt_registry.py
  - tests/test_ssh_tools.py
  - tests/test_tools.py
  - tests/test_mcp_prompts.py
autonomous: true
requirements: []
objectives: [D-05, D-05a, D-05b, D-05c, D-13]
tags: [credential-cleanup, tool-removal, lock-step, 33.1, high-risk]

must_haves:
  truths:
    - "MCP clients calling tools/list do not see `update_mcp_admin_groups` or `verify_mcp_admin_access`"
    - "The `connect_to_device` prompt's Step 6 does not name a removed tool"
    - "No source file outside `tests/` contains the identifier `update_mcp_admin_groups` or `verify_mcp_admin_access`"
    - "Schema/annotation parity CI check still passes (no partial removal)"
    - "Tool count = 51 after this plan (down from 53)"
  artifacts:
    - path: "src/homelab_mcp/tool_schemas/ssh_tools_schema.py"
      provides: "SSH_TOOLS dict with update_mcp_admin_groups entry removed"
    - path: "src/homelab_mcp/tool_handlers/ssh_handlers.py"
      provides: "ssh_handlers module with handle_update_mcp_admin_groups + handle_verify_mcp_admin removed"
    - path: "src/homelab_mcp/tool_handlers/__init__.py"
      provides: "TOOL_HANDLERS dict with 2 entries removed, imports cleaned"
    - path: "src/homelab_mcp/tool_annotations.py"
      provides: "tool_annotations.py with `verify_mcp_admin` removed from _READ_ONLY_TOOLS (line 44) AND `update_mcp_admin_groups` removed from _MUTATING_ANNOTATIONS (lines 90-94) — two distinct structural shapes, two distinct deletions"
    - path: "src/homelab_mcp/openapi_app.py"
      provides: "_SSH_TOOLS_WITH_HOSTNAME + TOOL_CATEGORIES without update_mcp_admin_groups + verify_mcp_admin"
    - path: "src/homelab_mcp/ssh_tools.py"
      provides: "Module without update_mcp_admin_groups() or verify_mcp_admin_access() functions"
    - path: "src/homelab_mcp/prompt_registry.py"
      provides: "_build_connect_to_device_result with Step 6 rewritten (no verify_mcp_admin reference)"
  key_links:
    - from: "tests/test_ast_regression.py::test_no_forbidden_strings_in_source"
      to: "src/homelab_mcp/** (scanned)"
      via: "AST meta-test proves removed identifiers absent"
      pattern: "update_mcp_admin_groups|verify_mcp_admin_access"
---

<objective>
Lock-step delete `update_mcp_admin_groups` and `verify_mcp_admin_access` from the entire tool surface (schema + handler + dispatch + annotation + openapi + underlying `ssh_tools.py` functions) and rewrite `_build_connect_to_device_result` Step 6 so it no longer names a removed tool. Implements D-05, D-05a, D-05b, D-05c from `33.1-CONTEXT.md`.

Purpose: Phase 33 D-18 made `mcp_admin` non-privileged. Tools whose name and implementation are hardcoded to that literal user are vestigial — they're the last two. Removing them completes the keyring-only migration and closes the final `mcp_admin` reference in the prompt surface. This is the highest-risk edit in Phase 33.1 because a partial removal fails CI schema/annotation parity.

Depends on Plan 01 so the resolver's registry-scan branch is in place before any remaining caller of the removed tools is left searching for them.

Output: 2 fewer MCP tools at `tools/list`; ~195-line deletion from `ssh_tools.py` (verify_mcp_admin_access ~65 lines + update_mcp_admin_groups ~130 lines); rewritten prompt Step 6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-/33.1-CONTEXT.md
@.planning/phases/33-keyring-single-source-of-truth/33-04-SUMMARY.md
@.planning/phases/33-keyring-single-source-of-truth/33-05-SUMMARY.md
@src/homelab_mcp/tool_schemas/ssh_tools_schema.py
@src/homelab_mcp/tool_handlers/ssh_handlers.py
@src/homelab_mcp/tool_handlers/__init__.py
@src/homelab_mcp/tool_annotations.py
@src/homelab_mcp/openapi_app.py
@src/homelab_mcp/ssh_tools.py
@src/homelab_mcp/prompt_registry.py

<interfaces>
<!-- Exact line numbers and strings to remove — this is a lock-step deletion. Match every site or CI parity fails. -->

**Six file sites (lock-step):**

1. `src/homelab_mcp/tool_schemas/ssh_tools_schema.py` — line 33 `"verify_mcp_admin"` entry (lines 33–55); line 118 `"update_mcp_admin_groups"` entry (lines 118–144)
2. `src/homelab_mcp/tool_handlers/ssh_handlers.py` — lines 11–12 imports (`update_mcp_admin_groups`, `verify_mcp_admin_access`); lines 23–26 `handle_verify_mcp_admin`; lines 97–100 `handle_update_mcp_admin_groups`
3. `src/homelab_mcp/tool_handlers/__init__.py` — lines 57–63 import block (remove `handle_update_mcp_admin_groups` and `handle_verify_mcp_admin`); lines 81, 84 TOOL_HANDLERS dict entries
4. `src/homelab_mcp/tool_annotations.py` — **two distinct structural shapes, two distinct deletions** (BLOCKER 2):
   - Line 44: `    "verify_mcp_admin",` — inside the `_READ_ONLY_TOOLS` list (lines 23-53). This is a list entry.
   - Lines 90-94: the `"update_mcp_admin_groups"` entry in the `_MUTATING_ANNOTATIONS` dict. This is a dict key with a `ToolAnnotations(...)` value.
5. `src/homelab_mcp/openapi_app.py` — line 70 `"verify_mcp_admin"` in `_SSH_TOOLS_WITH_HOSTNAME`; line 73 `"update_mcp_admin_groups"` in `_SSH_TOOLS_WITH_HOSTNAME`; line 145 `"verify_mcp_admin"` in `TOOL_CATEGORIES["SSH"]`; line 148 `"update_mcp_admin_groups"` in `TOOL_CATEGORIES["SSH"]`
6. `src/homelab_mcp/ssh_tools.py` — function body `update_mcp_admin_groups` at line 529 (ends around line 658); function body `verify_mcp_admin_access` at line 157 (ends around line 222); any helpers used exclusively by them (audit after deletion — `_sudo_run` is shared, keep it)

**Seventh site — prompt:**

7. `src/homelab_mcp/prompt_registry.py` lines 146–147 (`_build_connect_to_device_result` Step 6) — current text references `verify_mcp_admin`, must change. D-05b:

Current Step 6 (verbatim from source):
```
6. Call verify_mcp_admin with hostname="{hostname}" to confirm that the registered \
user has sudo access.
```

New Step 6 (D-05b — per CONTEXT, call `ssh_execute_command` or equivalent generic sudo check):
```
6. Call ssh_execute_command with hostname="{hostname}" and command="sudo -n true" to \
confirm the registered user has passwordless sudo. A non-zero exit code means sudo \
is not configured for the registered user.
```

**Test file sites to update:**

- `tests/test_tools.py` — line 16 `len(tools) == 53` → `== 51`. Line 20 currently reads `assert "verify_mcp_admin" in tools` — invert (WARNING 1). Also add a new negative assertion for `update_mcp_admin_groups`.
- `tests/test_ssh_tools.py` — any remaining tests for `update_mcp_admin_groups` or `verify_mcp_admin_access` must be deleted (not skipped, per Phase 33 precedent D-02 of 33-01-SUMMARY).
- `tests/test_mcp_prompts.py` — any assertion referencing the string `verify_mcp_admin` in the prompt text must be inverted (assert NOT present) + add a positive assertion for `"ssh_execute_command"` + `"sudo -n true"` substring.
- `tests/test_ast_regression.py` — the `FORBIDDEN_SOURCE_STRINGS` list does NOT yet include the two names being removed here. Plan 05 owns that extension (D-10). This plan does not edit test_ast_regression.py — Plan 05 does.

**What stays (do NOT delete):**
- `_sudo_run` helper in `ssh_tools.py` — still used by other paths (Phase 33 33-04-SUMMARY confirms, CONTEXT D-05 explicit "Keep `_sudo_run` (still used by other paths)").
- `get_mcp_ssh_key_path()` in `ssh_tools.py` — used by `ensure_mcp_ssh_key` (Phase 33 33-03-SUMMARY).
- `~/.ssh/mcp/mcp_admin_key` directory constant / key file generation logic — Phase 33 D-09a keeps this (users attach it via `credentials add --key-path`).
- The `_SERVICE_NAMES` keyring namespace `homelab-mcp-ssh-<user>` — Phase 33 D-19 invariant.
- Any `"mcp_admin"` string references INSIDE test files (negative assertions).

**Helper dead-code audit (WARNING 2) — do BEFORE deleting function bodies:**

`verify_mcp_admin_access` uses: `ensure_mcp_ssh_key` + `get_mcp_ssh_key_path`.
`update_mcp_admin_groups` uses: `_sudo_run` + `asyncssh.connect`.

Before Task 2's deletion, grep `ssh_tools.py` for OTHER callers of each helper:
- `grep -n 'ensure_mcp_ssh_key' src/homelab_mcp/ssh_tools.py` — enumerate callers.
- `grep -n 'get_mcp_ssh_key_path' src/homelab_mcp/ssh_tools.py` — enumerate callers.
- `grep -n '_sudo_run' src/homelab_mcp/ssh_tools.py` — enumerate callers.
- `grep -rn 'ensure_mcp_ssh_key\|get_mcp_ssh_key_path\|_sudo_run' src/homelab_mcp/` — enumerate module-wide callers.

For each helper: mark **PRESERVE** (used elsewhere) or **DELETE** (only used by removed function).

Pre-determined per CONTEXT:
- `_sudo_run` → **PRESERVE** (CONTEXT D-05 explicit: "Keep `_sudo_run` (still used by other paths)").
- `get_mcp_ssh_key_path` → **PRESERVE** (used by `ensure_mcp_ssh_key` per 33-03-SUMMARY).
- `ensure_mcp_ssh_key` → PRESERVE by default (generated SSH key is still used by `credentials add --key-path` flow per Phase 33 D-09a); confirm with the grep before deleting if it turns out to be orphan. Document the audit result in Task 2's action text.

Task 2 must RECORD the audit result (e.g., "Helpers audit: `_sudo_run` PRESERVE (3 other callers at lines X/Y/Z); `ensure_mcp_ssh_key` PRESERVE (called by CLI path at line A); `get_mcp_ssh_key_path` PRESERVE (called by `ensure_mcp_ssh_key` line B)") in its action text and in the 33.1-03-SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete schema + handler + dispatch + annotation + openapi entries (lock-step 5-file surgical edit)</name>
  <files>src/homelab_mcp/tool_schemas/ssh_tools_schema.py, src/homelab_mcp/tool_handlers/ssh_handlers.py, src/homelab_mcp/tool_handlers/__init__.py, src/homelab_mcp/tool_annotations.py, src/homelab_mcp/openapi_app.py</files>
  <read_first>
    - src/homelab_mcp/tool_schemas/ssh_tools_schema.py (entire file, 145 lines)
    - src/homelab_mcp/tool_handlers/ssh_handlers.py (entire file)
    - src/homelab_mcp/tool_handlers/__init__.py (focus lines 50–105)
    - src/homelab_mcp/tool_annotations.py — **specifically line 44** (`    "verify_mcp_admin",` inside `_READ_ONLY_TOOLS` list) AND **lines 90-94** (`"update_mcp_admin_groups"` entry inside `_MUTATING_ANNOTATIONS` dict). BLOCKER 2: these are two distinct structural shapes and must be handled as two distinct deletions.
    - src/homelab_mcp/openapi_app.py (focus lines 65–100 and 140–160)
    - .planning/phases/33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-/33.1-CONTEXT.md (D-05 bullet list — six file targets)
    - .planning/phases/33-keyring-single-source-of-truth/33-04-SUMMARY.md (the Phase 33 lock-step template — D-10/D-20/D-21 pattern for setup_mcp_admin/update_server_credentials/remove_server. This plan follows the same pattern for two more tools.)
  </read_first>
  <action>
    **File 1: `src/homelab_mcp/tool_schemas/ssh_tools_schema.py`**

    Delete the entire `"verify_mcp_admin"` dict entry (current lines 33–55, inclusive of the comma after the closing brace). Delete the entire `"update_mcp_admin_groups"` dict entry (current lines 118–144). Leave surrounding entries (`ssh_discover`, `ssh_execute_command`, `start_interactive_shell`) intact. Verify the file still parses as valid Python.

    **File 2: `src/homelab_mcp/tool_handlers/ssh_handlers.py`**

    In the import block (lines 7–13), remove `update_mcp_admin_groups` and `verify_mcp_admin_access` from the `from ..ssh_tools import (...)` list. The remaining imports should be `ssh_discover_system`, `ssh_execute_command`.

    Delete the entire function `handle_verify_mcp_admin` at lines 23–26.

    Delete the entire function `handle_update_mcp_admin_groups` at lines 97–100 (plus trailing blank line).

    **File 3: `src/homelab_mcp/tool_handlers/__init__.py`**

    In the import block (approximately lines 57–63) that reads `from .ssh_handlers import (...)`, remove `handle_update_mcp_admin_groups` and `handle_verify_mcp_admin`. The remaining imports should be `handle_ssh_discover`, `handle_ssh_execute_command`, `handle_start_interactive_shell`.

    In `TOOL_HANDLERS` dict, delete these two lines:
    - `"verify_mcp_admin": handle_verify_mcp_admin,` (line 81)
    - `"update_mcp_admin_groups": handle_update_mcp_admin_groups,` (line 84)

    **File 4: `src/homelab_mcp/tool_annotations.py`** — **BLOCKER 2: two distinct deletions at two distinct structural shapes.**

    Deletion 4a (list entry): Delete **line 44** (`    "verify_mcp_admin",`) from the `_READ_ONLY_TOOLS` list (lines 23-53). After deletion the `_READ_ONLY_TOOLS` list has one fewer entry; the surrounding list structure and trailing comma on the previous line stay intact.

    Deletion 4b (dict entry): Delete **lines 90-94** (the `"update_mcp_admin_groups"` entry: `"update_mcp_admin_groups": ToolAnnotations(readOnlyHint=False, destructiveHint=False, idempotentHint=False),`) from the `_MUTATING_ANNOTATIONS` dict. After deletion the dict has one fewer key; the surrounding entries (`bulk_discover_and_map` above, `ssh_execute_command` below) stay intact.

    Verify the file still parses as valid Python by running `uv run python -c "from homelab_mcp.tool_annotations import TOOL_ANNOTATIONS; print(len(TOOL_ANNOTATIONS))"`.

    **File 5: `src/homelab_mcp/openapi_app.py`**

    In `_SSH_TOOLS_WITH_HOSTNAME` tuple (lines 68–85), delete:
    - `"verify_mcp_admin",` (line 70)
    - `"update_mcp_admin_groups",` (line 73)

    In `TOOL_CATEGORIES["SSH"]` list (lines 143–149), delete:
    - `"verify_mcp_admin",` (line 145)
    - `"update_mcp_admin_groups",` (line 148)

    **Verification after File 5:**

    Run `uv run python -c "from homelab_mcp.tool_handlers import TOOL_HANDLERS; from homelab_mcp.tool_schemas.ssh_tools_schema import SSH_TOOLS; from homelab_mcp.tool_annotations import TOOL_ANNOTATIONS; from homelab_mcp.openapi_app import TOOL_CATEGORIES, _SSH_TOOLS_WITH_HOSTNAME; assert 'update_mcp_admin_groups' not in SSH_TOOLS; assert 'update_mcp_admin_groups' not in TOOL_HANDLERS; assert 'update_mcp_admin_groups' not in TOOL_ANNOTATIONS; assert 'update_mcp_admin_groups' not in _SSH_TOOLS_WITH_HOSTNAME; assert 'update_mcp_admin_groups' not in TOOL_CATEGORIES['SSH']; assert 'verify_mcp_admin' not in SSH_TOOLS; assert 'verify_mcp_admin' not in TOOL_HANDLERS; assert 'verify_mcp_admin' not in TOOL_ANNOTATIONS; assert 'verify_mcp_admin' not in _SSH_TOOLS_WITH_HOSTNAME; assert 'verify_mcp_admin' not in TOOL_CATEGORIES['SSH']; print('lock-step parity OK')"`

    This MUST print `lock-step parity OK`. If ANY assertion fails, you missed a site — fix before committing. Note the expanded assertion set includes `'verify_mcp_admin' not in TOOL_ANNOTATIONS` (it WAS in `_READ_ONLY_TOOLS` — confirms the line 44 deletion landed).

    Run `uv run pytest tests/test_tools.py tests/test_openapi_app.py -v` to confirm schema/annotation parity tests still pass. Tool count test (`test_get_available_tools`) will likely fail at this point — leave it RED (Task 2 updates it).

    Commit:
    ```
    git commit -m "feat(33.1-03): lock-step delete update_mcp_admin_groups + verify_mcp_admin_access from 5 surfaces (D-05)"
    ```
  </action>
  <verify>
    <automated>uv run python -c "from homelab_mcp.tool_handlers import TOOL_HANDLERS; from homelab_mcp.tool_schemas.ssh_tools_schema import SSH_TOOLS; from homelab_mcp.tool_annotations import TOOL_ANNOTATIONS; from homelab_mcp.openapi_app import TOOL_CATEGORIES, _SSH_TOOLS_WITH_HOSTNAME; assert 'update_mcp_admin_groups' not in SSH_TOOLS and 'update_mcp_admin_groups' not in TOOL_HANDLERS and 'update_mcp_admin_groups' not in TOOL_ANNOTATIONS and 'update_mcp_admin_groups' not in _SSH_TOOLS_WITH_HOSTNAME and 'update_mcp_admin_groups' not in TOOL_CATEGORIES['SSH']; assert 'verify_mcp_admin' not in SSH_TOOLS and 'verify_mcp_admin' not in TOOL_HANDLERS and 'verify_mcp_admin' not in TOOL_ANNOTATIONS and 'verify_mcp_admin' not in _SSH_TOOLS_WITH_HOSTNAME and 'verify_mcp_admin' not in TOOL_CATEGORIES['SSH']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The one-line python -c assertion above prints `OK` (zero assertion failures)
    - `grep -n 'update_mcp_admin_groups\|verify_mcp_admin' src/homelab_mcp/tool_schemas/ssh_tools_schema.py` returns empty
    - `grep -n 'update_mcp_admin_groups\|verify_mcp_admin' src/homelab_mcp/tool_handlers/ssh_handlers.py` returns empty
    - `grep -n 'update_mcp_admin_groups\|verify_mcp_admin' src/homelab_mcp/tool_handlers/__init__.py` returns empty
    - `grep -n 'update_mcp_admin_groups\|verify_mcp_admin' src/homelab_mcp/tool_annotations.py` returns empty (BLOCKER 2: proves BOTH the `_READ_ONLY_TOOLS` list entry AND the `_MUTATING_ANNOTATIONS` dict entry are gone)
    - `grep -n 'update_mcp_admin_groups\|verify_mcp_admin' src/homelab_mcp/openapi_app.py` returns empty
    - `uv run ruff check src/homelab_mcp/tool_schemas/ssh_tools_schema.py src/homelab_mcp/tool_handlers/ src/homelab_mcp/tool_annotations.py src/homelab_mcp/openapi_app.py` clean
    - `uv run mypy src/homelab_mcp/tool_handlers/ src/homelab_mcp/tool_annotations.py src/homelab_mcp/openapi_app.py` clean (the `ssh_tools.py` attr-defined errors from still-present function bodies are OK here — Task 2 deletes them)
  </acceptance_criteria>
  <done>Single commit; 5 files modified; import-time parity assertion passes; schema/annotation parity CI-equivalent test green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete update_mcp_admin_groups() and verify_mcp_admin_access() function bodies from ssh_tools.py + rewrite connect_to_device prompt Step 6 + update tests (D-05, D-05b)</name>
  <files>src/homelab_mcp/ssh_tools.py, src/homelab_mcp/prompt_registry.py, tests/test_ssh_tools.py, tests/test_mcp_prompts.py, tests/test_tools.py</files>
  <read_first>
    - src/homelab_mcp/ssh_tools.py lines 154–222 (verify_mcp_admin_access function body and decorator)
    - src/homelab_mcp/ssh_tools.py lines 526–660 (update_mcp_admin_groups function body including try/except and trailing "never reached" sentinel)
    - src/homelab_mcp/prompt_registry.py lines 125–153 (_build_connect_to_device_result)
    - tests/test_ssh_tools.py (search for any remaining `verify_mcp_admin_access` or `update_mcp_admin_groups` test references)
    - tests/test_mcp_prompts.py (search for existing assertions on `verify_mcp_admin` in the prompt text)
    - tests/test_tools.py **lines 11–26 specifically** — the `test_get_available_tools` function. Line 16 reads `assert len(tools) == 53` (update to 51). Line 19 is Phase 33's pattern `assert "setup_mcp_admin" not in tools  # D-10: removed in Phase 33`. Line 20 currently reads `assert "verify_mcp_admin" in tools` — WARNING 1: invert this.
    - .planning/phases/33-keyring-single-source-of-truth/33-04-SUMMARY.md (pattern for deleting function bodies in ssh_tools.py without leaving orphan imports)

    **Helpers dead-code audit (WARNING 2):** Before deleting function bodies, run these greps and record results:
    - `grep -n 'ensure_mcp_ssh_key' src/homelab_mcp/ssh_tools.py`
    - `grep -n 'get_mcp_ssh_key_path' src/homelab_mcp/ssh_tools.py`
    - `grep -n '_sudo_run' src/homelab_mcp/ssh_tools.py`
    - `grep -rn 'ensure_mcp_ssh_key\|get_mcp_ssh_key_path' src/homelab_mcp/`
    Record the audit result in Task 2's action text notes (see `<action>` below). Pre-determined status: `_sudo_run` PRESERVE (CONTEXT D-05 explicit), `get_mcp_ssh_key_path` PRESERVE (called by `ensure_mcp_ssh_key`), `ensure_mcp_ssh_key` PRESERVE (used by CLI key-path flow per Phase 33 D-09a). Confirm these three with the actual grep output before proceeding.
  </read_first>
  <action>
    **Step A: Helper dead-code audit (WARNING 2).**

    Run the greps listed in `<read_first>` and record the caller count for each helper. Expected (document the actual numbers in the SUMMARY):
    - `_sudo_run` — PRESERVE (CONTEXT D-05 explicit). Expect callers in ssh_tools.py and potentially other modules.
    - `get_mcp_ssh_key_path` — PRESERVE. Expect caller = `ensure_mcp_ssh_key`.
    - `ensure_mcp_ssh_key` — PRESERVE by default. Expect at least one caller outside the two functions being deleted (likely the CLI key-path flow established in Phase 33 D-09a).

    If the audit surfaces a helper with ZERO remaining callers after the deletions in Step B, delete it alongside the function body in Step B. If a helper has at least one non-deleted caller, PRESERVE it.

    Record the audit result as an inline comment in the commit message for Step B AND in the SUMMARY document.

    **Step B: Delete function bodies from ssh_tools.py.**

    In `src/homelab_mcp/ssh_tools.py`:

    1. Delete the entire `verify_mcp_admin_access` function — from the `@ssh_connection_wrapper(timeout_seconds=15.0)` decorator line (line 156) through the closing `)` of the final `return json.dumps(...)` block (approximately line 221). Delete the trailing blank line. Do NOT delete `get_mcp_ssh_key_path()` immediately above it (lines 124–126) or `ensure_mcp_ssh_key()` immediately above that (lines 129–153) — both are still used by other paths per the Step A audit.

    2. Delete the entire `update_mcp_admin_groups` function — from line 529 `async def update_mcp_admin_groups(...)` through the closing sentinel `return json.dumps({"status": "error", ...})` (approximately line 658). Delete trailing blank line.

    3. Audit imports at the top of the file: after deletion, check for any imports used exclusively by the two deleted functions. `from typing import cast` — still used elsewhere (e.g., `whoami_result.stdout` cast remains in other functions); keep. `json`, `sanitize_error` — still used. Do NOT prune imports without confirming they're unused elsewhere (use `grep -n "<name>" src/homelab_mcp/ssh_tools.py` to check).

    Run `uv run python -c "import homelab_mcp.ssh_tools"` — MUST succeed (no ImportError, no NameError).

    **Step C: Rewrite `_build_connect_to_device_result` Step 6 (D-05b).**

    In `src/homelab_mcp/prompt_registry.py`, replace lines 146–147 (current Step 6 text) with:

    ```python
    6. Call ssh_execute_command with hostname="{hostname}" and command="sudo -n true" to \
    confirm the registered user has passwordless sudo. A non-zero exit code means sudo \
    is not configured for the registered user.
    ```

    The rest of `_build_connect_to_device_result` (Steps 1–5 and closing "If any step fails..." sentence on line 149) is unchanged.

    **Step D: Update tests.**

    In `tests/test_ssh_tools.py`:
    - Delete any test function that references `verify_mcp_admin_access` or `update_mcp_admin_groups` (including the imports of those names if module-level). Example deletion targets: `test_verify_mcp_admin_access_*`, `test_update_mcp_admin_groups_*`. Use `grep -n "verify_mcp_admin_access\|update_mcp_admin_groups" tests/test_ssh_tools.py` to enumerate before deleting. Replace the test class docstring if needed; do NOT leave stub assertions.

    In `tests/test_mcp_prompts.py`:
    - Find the test that asserts on `connect_to_device` prompt text (likely `test_connect_to_device_prompt` or similar). Flip any assertion that `"verify_mcp_admin" in text` to `"verify_mcp_admin" not in text`. Add a new assertion: `assert "ssh_execute_command" in text and "sudo -n true" in text`. Preserve existing Phase 33 assertions about `credentials add` and the absence of `setup_mcp_admin`.

    In `tests/test_tools.py` (WARNING 1 — explicit inversion + count update):
    - **Line 16:** change `len(tools) == 53` to `len(tools) == 51`. Update the surrounding comment to reflect Phase 33.1's two removals (e.g., `# Phase 33.1 removed update_mcp_admin_groups, verify_mcp_admin`).
    - **Line 20:** invert `assert "verify_mcp_admin" in tools` → `assert "verify_mcp_admin" not in tools  # D-05: removed in Phase 33.1` (mirrors the Phase 33 line-19 pattern `assert "setup_mcp_admin" not in tools  # D-10: removed in Phase 33`).
    - **Add a new line** immediately after the inverted line 20: `assert "update_mcp_admin_groups" not in tools  # D-05: removed in Phase 33.1`.
    - Preserve line 19's existing `assert "setup_mcp_admin" not in tools  # D-10: removed in Phase 33`.

    Run `uv run pytest tests/ -m "not integration" -x`. ALL tests must pass. If a test fails due to orphaned reference that this plan was supposed to clean, fix it now (don't leave it for Plan 05).

    Commit:
    ```
    git commit -m "feat(33.1-03): delete update_mcp_admin_groups + verify_mcp_admin_access from ssh_tools.py; rewrite prompt Step 6; invert test_tools assertions (D-05, D-05b)"
    ```

    **D-13 sanity grep (acceptance criterion, not a separate task):**

    Run `grep -rn 'mcp_admin' src/ --include='*.py'`. Legitimate remaining hits (these MUST be the only ones):
    - `src/homelab_mcp/credential_store.py` — `_SERVICE_NAMES` dict key `"ssh"` maps to `homelab-mcp-ssh-<user>` namespace template (may or may not contain `mcp_admin` literal — Phase 33 D-19)
    - `src/homelab_mcp/ssh_tools.py` — `get_mcp_ssh_key_path()` and the `~/.ssh/mcp/mcp_admin_key` constant (Phase 33 D-09a)
    - `src/homelab_mcp/ssh_tools.py` — `ensure_mcp_ssh_key` comment `"mcp_admin@homelab"` as SSH key comment
    - Any import path literal (e.g., `"mcp_admin_key"`)
    - **Deferred-to-33.2 sites** (covered by Plan 05 allowlists — do NOT remove in this plan): `src/homelab_mcp/ssh_connection.py:162`, `src/homelab_mcp/service_installer.py` (8 sites), `src/homelab_mcp/tool_schemas/service_tools_schema.py` (9 tool schemas). These are expected residual hits per user's "Narrow + defer" decision.

    NOT OK: any docstring or user-facing string in the files this plan directly modifies that calls out `mcp_admin` as "the" user. If you find one in a file that this plan modifies, reword to generic "the registered user" or delete.

    If the grep surfaces an illegitimate hit inside this plan's modified files, fix it in this commit before marking the task done.
  </action>
  <verify>
    <automated>uv run python -c "import homelab_mcp.ssh_tools; assert not hasattr(homelab_mcp.ssh_tools, 'update_mcp_admin_groups'); assert not hasattr(homelab_mcp.ssh_tools, 'verify_mcp_admin_access')" && uv run pytest tests/ -m "not integration" -x && uv run mypy src/homelab_mcp/ssh_tools.py src/homelab_mcp/prompt_registry.py && uv run ruff check src/homelab_mcp/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'async def update_mcp_admin_groups\|async def verify_mcp_admin_access' src/homelab_mcp/ssh_tools.py` returns empty
    - `grep -cn 'verify_mcp_admin' src/homelab_mcp/prompt_registry.py` returns 0
    - `grep -n 'ssh_execute_command.*sudo -n true\|sudo -n true.*ssh_execute_command' src/homelab_mcp/prompt_registry.py` returns at least 1 match
    - `uv run python -c "import homelab_mcp.ssh_tools; assert not hasattr(homelab_mcp.ssh_tools, 'update_mcp_admin_groups')"` exits 0
    - `uv run pytest tests/ -m "not integration" -x` exits 0 (full sweep)
    - `uv run mypy src/homelab_mcp/ssh_tools.py` clean (no leftover attr-defined errors)
    - D-13 sanity grep (narrowed): `grep -rn 'mcp_admin' src/homelab_mcp/ --include='*.py' | grep -v 'credential_store\|ssh_tools.py\|mcp_admin_key\|mcp_admin@homelab\|ssh_connection\|service_installer\|service_tools_schema'` returns empty (only the Phase-33-legitimate and Phase-33.2-deferred hits remain)
    - Tool count test `tests/test_tools.py::test_get_available_tools` passes with expected count 51 (WARNING 1 — line 16 updated to 51)
    - `grep -n 'assert "verify_mcp_admin" not in tools' tests/test_tools.py` returns exactly 1 match (WARNING 1 — line 20 inverted)
    - `grep -n 'assert "update_mcp_admin_groups" not in tools' tests/test_tools.py` returns exactly 1 match (WARNING 1 — new assertion added)
  </acceptance_criteria>
  <done>Single commit; `ssh_tools.py` shrinks by ~195 lines (verify_mcp_admin_access ~65 + update_mcp_admin_groups ~130); prompt Step 6 rewritten; test_tools.py line 16 count → 51, line 20 inverted, new update_mcp_admin_groups negative assertion added; helpers audit recorded; full test sweep green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tools/list response | Clients may have cached the old tool names; will encounter "tool not found" errors when they try to invoke them |
| Prompt consumer → prompt text | Agent reads connect_to_device prompt; Step 6 must reference a tool that actually exists |
| Schema parity CI → server surface | Schema/annotation/handler/openapi parity is the safety net; partial removal breaks CI and blocks merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33.1-09 | Denial of Service | Client cached old tool name → MCP error "tool not found" | accept | Expected client behavior. MCP spec requires clients to refresh tools on reconnect. No server-side mitigation possible or needed. User-facing surface: error message is informative. |
| T-33.1-10 | Tampering | Partial lock-step removal (schema dropped but handler still registered, or vice versa) | mitigate | Import-time assertion in Task 1 verify command enforces 5-way parity before commit (plus the explicit `'verify_mcp_admin' not in TOOL_ANNOTATIONS` assertion catches BLOCKER 2's line-44 list-entry deletion). Schema/annotation parity CI test (v1.2 quality-gate) is a second layer — will fail if parity breaks. Task 2 adds sixth site (ssh_tools.py function body) and relies on mypy attr-defined errors to detect orphan imports. |
| T-33.1-11 | Information Disclosure | Prompt text still names a removed tool → agent tries to call it and receives a confusing error | mitigate | Step 6 rewrite (Task 2 Step C) + positive test assertion in `tests/test_mcp_prompts.py`. AST meta-test extension in Plan 05 (D-10) forbids the removed names outside tests/ as a permanent guard. |
| T-33.1-12 | Elevation of Privilege | `update_mcp_admin_groups` deletion removes a function that adjusted sudo-capable group membership. Could this be a regression? | accept | D-05a rationale (verbatim from CONTEXT): "we should drop this as we are also removing the need for the mcp_admin since again username comes from the keyring." The function operated only on the literal `mcp_admin` account. Any remaining need for group adjustment is served by `ssh_execute_command` with a generic `sudo usermod -a -G <group> <user>` command — no lost capability. |
| T-33.1-13 | Repudiation | Audit log trail loses two tool invocation signatures | accept | MCP protocol logging (if any) is client-side. Server has no separate audit log that loses granularity. |

Residual risk: **low-medium**. Medium because this is the highest-risk edit in 33.1 (~195-line deletion across 7 files). Mitigated by: depends_on Plan 01, import-time parity assertion in Task 1, full `uv run pytest` sweep requirement, helpers dead-code audit (WARNING 2), and Plan 05's regression guards.
</threat_model>

<verification>
- Two commits: Task 1 (5-file surface), Task 2 (ssh_tools.py + prompt + tests)
- Import-time assertion in Task 1 verify command enforces 5-way parity; includes explicit `'verify_mcp_admin' not in TOOL_ANNOTATIONS` check (BLOCKER 2 coverage)
- `uv run pytest tests/ -m "not integration" -x` passes after Task 2
- Tool count 53 → 51
- tests/test_tools.py line 20 inverted from `assert "verify_mcp_admin" in tools` to `assert "verify_mcp_admin" not in tools` (WARNING 1)
- tests/test_tools.py adds `assert "update_mcp_admin_groups" not in tools` (WARNING 1)
- tests/test_tools.py line 16 count updated 53 → 51 (WARNING 1)
- Helpers dead-code audit recorded (WARNING 2) — `_sudo_run`, `get_mcp_ssh_key_path`, `ensure_mcp_ssh_key` all PRESERVE with caller enumeration in SUMMARY
- D-13 sanity grep produces ONLY the Phase 33-legitimate `mcp_admin` references (credential namespace, key file directory/name, SSH key comment) and the Phase-33.2-deferred hits (ssh_connection, service_installer, service_tools_schema)
- `_sudo_run`, `get_mcp_ssh_key_path`, `ensure_mcp_ssh_key` preserved (still used by other paths — audit confirms)
</verification>

<success_criteria>
Calling `homelab-mcp --http --port 8080` and visiting `/openapi.json` shows 51 tools (down from 53).

`uv run python -c "from homelab_mcp.tool_handlers import TOOL_HANDLERS; print(len(TOOL_HANDLERS))"` prints `51` (or whatever the current post-deletion count is — the key assertion is `-2` from Phase 33's baseline).

The `connect_to_device` prompt Step 6 calls `ssh_execute_command` with `sudo -n true`. Agents that follow the prompt won't get a "tool not found" error.

AST regression scan (Plan 05 extends this) will forbid the removed names from reappearing in source.
</success_criteria>

<output>
After completion, create `.planning/phases/33.1-ssh-tool-family-keyring-uniformity-drop-hardcoded-mcp-admin-/33.1-03-SUMMARY.md` with:
- Table of all 7 file sites touched + line-count delta for each (with verify_mcp_admin_access ~65 + update_mcp_admin_groups ~130 = ~195 total ssh_tools.py delta)
- tool_annotations.py: confirm BOTH line 44 (`_READ_ONLY_TOOLS` list entry) AND lines 90-94 (`_MUTATING_ANNOTATIONS` dict entry) were deleted — two distinct shapes
- Before/after of `_build_connect_to_device_result` Step 6
- Exact tool count before → after (53 → 51)
- Helpers dead-code audit result (WARNING 2): caller count + disposition for `_sudo_run`, `get_mcp_ssh_key_path`, `ensure_mcp_ssh_key`
- Confirmation of D-13 sanity grep result (enumeration of remaining legitimate + deferred `mcp_admin` hits in src/)
- List of test functions deleted (not skipped)
- tests/test_tools.py line 16 / line 20 / new line diff (WARNING 1 coverage)
- Pattern followed: Phase 33 D-10/D-20/D-21 lock-step deletion template
</output>
</output>
