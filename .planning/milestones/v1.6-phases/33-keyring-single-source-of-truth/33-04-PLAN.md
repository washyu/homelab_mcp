---
phase: 33-keyring-single-source-of-truth
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/homelab_mcp/ssh_tools.py
  - src/homelab_mcp/tool_schemas/ssh_tools_schema.py
  - src/homelab_mcp/tool_handlers/ssh_handlers.py
  - src/homelab_mcp/tool_handlers/credential_handlers.py
  - src/homelab_mcp/tool_handlers/__init__.py
  - src/homelab_mcp/tool_annotations.py
  - src/homelab_mcp/openapi_app.py
autonomous: true
requirements: [CRED-06]

must_haves:
  truths:
    - "setup_mcp_admin MCP tool does not appear in tool_schemas, tool_handlers dispatch, tool_annotations, or openapi allow-lists"
    - "update_server_credentials MCP tool is fully removed from the tool surface (D-20)"
    - "remove_server + remove_server_preview MCP tools are fully removed (D-21 — CLI credentials remove subcommand is the replacement, landed in Plan 33-03)"
    - "setup_remote_mcp_admin function is deleted from ssh_tools.py (D-11); update_mcp_admin_groups and verify_mcp_admin_access remain"
    - "list_registered_servers is rewritten to read credential_store.list_credentials instead of the DB (D-19)"
    - "update_mcp_admin_groups error message no longer names setup_mcp_admin (D-24)"
    - "handle_register_server continues to work as a thin wrapper — register_server itself is NOT modified in this plan (that is Plan 33-05's scope)"
  artifacts:
    - path: "src/homelab_mcp/ssh_tools.py"
      provides: "setup_remote_mcp_admin DELETED; list_registered_servers rewritten to keyring; remove_server + update_server_credentials functions DELETED from module"
    - path: "src/homelab_mcp/tool_schemas/ssh_tools_schema.py"
      provides: "SSH_TOOLS dict without setup_mcp_admin, update_server_credentials, or remove_server entries"
    - path: "src/homelab_mcp/tool_handlers/ssh_handlers.py"
      provides: "No handle_setup_mcp_admin; no import of setup_remote_mcp_admin"
    - path: "src/homelab_mcp/tool_handlers/credential_handlers.py"
      provides: "No handle_update_server_credentials; no handle_remove_server; no handle_remove_server_preview"
    - path: "src/homelab_mcp/tool_handlers/__init__.py"
      provides: "TOOL_HANDLERS dict without the four removed dispatch entries; imports pruned"
    - path: "src/homelab_mcp/tool_annotations.py"
      provides: "No annotations for setup_mcp_admin / update_server_credentials / remove_server"
    - path: "src/homelab_mcp/openapi_app.py"
      provides: "_SSH_TOOLS_WITH_HOSTNAME and TOOL_CATEGORIES cleaned of removed tool names"
  key_links:
    - from: "tool_handlers/__init__.py TOOL_HANDLERS"
      to: "remaining handlers (handle_register_server, handle_list_registered_servers, etc.)"
      via: "import + dispatch dict entry"
      pattern: "TOOL_HANDLERS"
    - from: "ssh_tools.list_registered_servers"
      to: "credential_store.list_credentials"
      via: "direct call, returns JSON string"
      pattern: "list_credentials.credential_type=.ssh."
---

<objective>
Execute the tool-surface cleanup in lock-step across the five files that must track together (PATTERNS.md §Tool Removal Lock-Step Rule, RESEARCH.md Pitfall 1 schema/annotation parity). Three MCP tools are removed from the server entirely: `setup_mcp_admin` (D-10), `update_server_credentials` (D-20), `remove_server` + `remove_server_preview` (D-21). The `setup_remote_mcp_admin` function is deleted from `ssh_tools.py` (D-11). `list_registered_servers` is rewritten to read the keyring registry (D-19). `update_mcp_admin_groups` error message is rephrased (D-24). After this plan, Wave 0 openapi and tool-handler removal tests go GREEN. The AST meta-test's `setup_mcp_admin`, `setup_remote_mcp_admin`, `update_server_credentials`, `remove_server`, `add_credential`, `get_credential_by_hostname`, `update_credential`, `update_last_verified` strings are eliminated from all files in scope.

Purpose: CRED-06 — the MCP tool surface has zero credential-write paths. Passwords never enter chat. Onboarding routes through `credentials add` CLI and the rewritten `connect_to_device` prompt (Plan 33-05).

Output: Three tools removed at the five coordinated surfaces. One function deleted from ssh_tools.py. One function rewritten. One error message rephrased.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-keyring-single-source-of-truth/33-CONTEXT.md
@.planning/phases/33-keyring-single-source-of-truth/33-RESEARCH.md
@.planning/phases/33-keyring-single-source-of-truth/33-PATTERNS.md
@.planning/phases/33-keyring-single-source-of-truth/33-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Tool surface AFTER this plan -->

From src/homelab_mcp/tool_schemas/ssh_tools_schema.py:
- SSH_TOOLS dict KEEPS: ssh_discover, verify_mcp_admin, ssh_execute_command, start_interactive_shell, update_mcp_admin_groups, register_server, list_registered_servers, list_keyring_credentials
- SSH_TOOLS dict LOSES: setup_mcp_admin, update_server_credentials, remove_server, remove_server_preview

From src/homelab_mcp/tool_handlers/__init__.py TOOL_HANDLERS:
- KEEPS: handle_register_server, handle_list_registered_servers, handle_list_keyring_credentials, handle_ssh_discover, handle_ssh_execute_command, handle_start_interactive_shell, handle_update_mcp_admin_groups, handle_verify_mcp_admin
- LOSES: handle_setup_mcp_admin, handle_update_server_credentials, handle_remove_server, handle_remove_server_preview

From src/homelab_mcp/ssh_tools.py:
- KEEPS: resolve_ssh_credentials, register_server, update_mcp_admin_groups, verify_mcp_admin_access, ssh_discover_system, ssh_execute_command, ensure_mcp_ssh_key, get_mcp_ssh_key_path, list_registered_servers (rewritten)
- LOSES: setup_remote_mcp_admin (full deletion), remove_server (function body), update_server_credentials (function body)

The target rewrite for list_registered_servers (from PATTERNS.md lines 321–335):
```
def list_registered_servers() -> str:
    entries = list_credentials(credential_type="ssh")
    result = {
        "status": "success",
        "count": len(entries),
        "servers": [
            {"hostname": e["hostname"], "username": e["username"]}
            for e in entries
        ],
    }
    return json.dumps(result, indent=2)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete tool-surface entries across the 5-way lock-step (schemas, handlers, dispatch, annotations, openapi)</name>
  <files>src/homelab_mcp/tool_schemas/ssh_tools_schema.py, src/homelab_mcp/tool_handlers/ssh_handlers.py, src/homelab_mcp/tool_handlers/credential_handlers.py, src/homelab_mcp/tool_handlers/__init__.py, src/homelab_mcp/tool_annotations.py, src/homelab_mcp/openapi_app.py</files>
  <read_first>
    - .planning/phases/33-keyring-single-source-of-truth/33-PATTERNS.md §Shared Patterns - Tool Removal Lock-Step Rule (lines 1075–1086) — the 5-way rule enforced by CI
    - .planning/phases/33-keyring-single-source-of-truth/33-PATTERNS.md §tool_schemas/ssh_tools_schema.py (lines 339–358), §tool_handlers/ssh_handlers.py (lines 362–391), §tool_handlers/credential_handlers.py (lines 395–427), §tool_handlers/__init__.py (lines 431–468), §tool_annotations.py (lines 472–492), §openapi_app.py (lines 496–521)
    - .planning/phases/33-keyring-single-source-of-truth/33-CONTEXT.md §D-10 (five call sites), §D-19 (list_registered_servers keyring rewrite — handled in Task 2), §D-20 (update_server_credentials removal), §D-21 (remove_server removal — same lock-step pattern)
    - .planning/phases/33-keyring-single-source-of-truth/33-RESEARCH.md §Sources (lines 638–648 — exact line numbers for each call site)
    - src/homelab_mcp/tool_schemas/ssh_tools_schema.py (existing — find setup_mcp_admin entry at line 33 per RESEARCH; also find update_server_credentials and remove_server entries)
    - src/homelab_mcp/tool_handlers/__init__.py (existing — line 61 setup_mcp_admin dispatch, line 85 dispatch duplicate, lines 130–132 update_server_credentials / remove_server / remove_server_preview entries per PATTERNS)
    - src/homelab_mcp/tool_handlers/ssh_handlers.py (existing — line 24 handle_setup_mcp_admin, line 8–14 import of setup_remote_mcp_admin)
    - src/homelab_mcp/tool_handlers/credential_handlers.py (existing — line 7–12 imports of remove_server/update_server_credentials; line 27–30 handle_update_server_credentials; line 33–69 handle_remove_server; line 85–91 handle_remove_server_preview)
    - src/homelab_mcp/tool_annotations.py (existing — lines 92–96 setup_mcp_admin annotation)
    - src/homelab_mcp/openapi_app.py (existing — line 70 setup_mcp_admin in tuple, line 146 in TOOL_CATEGORIES SSH list)
  </read_first>
  <behavior>
    - tool_schemas/ssh_tools_schema.py: SSH_TOOLS dict loses exactly 3 key entries: "setup_mcp_admin", "update_server_credentials", "remove_server". "remove_server_preview" (if schema exists) is also removed.
    - tool_handlers/ssh_handlers.py: handle_setup_mcp_admin function deleted; `setup_remote_mcp_admin` name removed from ssh_tools import tuple
    - tool_handlers/credential_handlers.py: handle_update_server_credentials, handle_remove_server, handle_remove_server_preview functions deleted; remove_server, update_server_credentials removed from ssh_tools import tuple
    - tool_handlers/__init__.py: imports of handle_setup_mcp_admin, handle_update_server_credentials, handle_remove_server, handle_remove_server_preview deleted; TOOL_HANDLERS dispatch dict loses the matching 4 key entries; __all__ pruned if it mentions them
    - tool_annotations.py: ToolAnnotations entries for setup_mcp_admin, update_server_credentials, remove_server (and _preview if present) deleted
    - openapi_app.py: _SSH_TOOLS_WITH_HOSTNAME tuple loses setup_mcp_admin (and any of update_server_credentials/remove_server if present); TOOL_CATEGORIES["SSH"] list loses the same
    - Server still starts; `tools/list` still works; the three removed tools are absent from the response
  </behavior>
  <action>
Before editing, validate current state by running:

```
grep -rn "setup_mcp_admin\|update_server_credentials\|remove_server" src/homelab_mcp/tool_schemas/ src/homelab_mcp/tool_handlers/ src/homelab_mcp/tool_annotations.py src/homelab_mcp/openapi_app.py
```

This enumerates every call site. Confirm the list matches PATTERNS.md file-by-file table (lines 19–24).

**Step A: tool_schemas/ssh_tools_schema.py**

Find the `"setup_mcp_admin": { ... }` dict entry (RESEARCH anchors it at line 33). Delete the entire key-value pair including any trailing comma. The entry is a `{...}` block spanning multiple lines — delete from the opening `"setup_mcp_admin":` through the closing `},` line.

Repeat for any `"update_server_credentials": { ... }` entry.
Repeat for any `"remove_server": { ... }` and `"remove_server_preview": { ... }` entries.

After deletion, the SSH_TOOLS dict should have zero occurrences of those three keys. Verify: `grep -c '"setup_mcp_admin"\|"update_server_credentials"\|"remove_server"' src/homelab_mcp/tool_schemas/ssh_tools_schema.py` == 0.

**Step B: tool_handlers/ssh_handlers.py**

1. Delete the import of `setup_remote_mcp_admin` from the `from ..ssh_tools import (...)` tuple:

Find the import block (per PATTERNS.md lines 382–391) — it looks like:

  from ..ssh_tools import (
      setup_remote_mcp_admin,    # DELETE THIS LINE
      ssh_discover_system,
      ssh_execute_command,
      update_mcp_admin_groups,
      verify_mcp_admin_access,
  )

Remove the `setup_remote_mcp_admin,` line. Keep all other imports.

2. Delete the `handle_setup_mcp_admin` function (per PATTERNS.md lines 367–372, RESEARCH line 24):

  async def handle_setup_mcp_admin(arguments: dict[str, Any]) -> dict[str, Any]:
      """Handle setup_mcp_admin tool."""
      result = await setup_remote_mcp_admin(**arguments)
      return {"content": [{"type": "text", "text": result}]}

Delete the full function.

**Step C: tool_handlers/credential_handlers.py**

1. Delete imports of `remove_server` and `update_server_credentials` from the `from ..ssh_tools import (...)` tuple (PATTERNS lines 400–407):

  from ..ssh_tools import (
      list_registered_servers,
      register_server,
      remove_server,              # DELETE
      update_server_credentials,  # DELETE
  )

Result:

  from ..ssh_tools import (
      list_registered_servers,
      register_server,
  )

2. Delete `handle_update_server_credentials` function (per PATTERNS line 397, RESEARCH lines 27–30).

3. Delete `handle_remove_server` function (PATTERNS line 397, RESEARCH lines 33–69 — this is the bigger function with dry_run/DB access logic).

4. Delete `handle_remove_server_preview` function (RESEARCH lines 85–91).

5. KEEP `handle_register_server`, `handle_list_registered_servers`, `handle_list_keyring_credentials` unchanged. Leave `handle_register_server` as the thin wrapper — Plan 33-05 rewrites `register_server` itself, not this wrapper. Verify PATTERNS.md lines 420–424 confirm the wrapper stays.

**Step D: tool_handlers/__init__.py**

1. Edit the imports from credential_handlers (PATTERNS lines 435–443) — remove three entries:

  from .credential_handlers import (
      handle_list_keyring_credentials,
      handle_list_registered_servers,
      handle_register_server,
      handle_remove_server,            # DELETE
      handle_remove_server_preview,    # DELETE
      handle_update_server_credentials, # DELETE
  )

Result:

  from .credential_handlers import (
      handle_list_keyring_credentials,
      handle_list_registered_servers,
      handle_register_server,
  )

2. Edit the imports from ssh_handlers (PATTERNS lines 447–456) — remove handle_setup_mcp_admin:

  from .ssh_handlers import (
      handle_setup_mcp_admin,         # DELETE
      handle_ssh_discover,
      handle_ssh_execute_command,
      handle_start_interactive_shell,
      handle_update_mcp_admin_groups,
      handle_verify_mcp_admin,
  )

3. Delete the TOOL_HANDLERS dispatch entries (PATTERNS lines 462–465, matching RESEARCH lines 61, 85, 130–132):

  TOOL_HANDLERS = {
      ...
      "setup_mcp_admin": handle_setup_mcp_admin,                 # DELETE
      "update_server_credentials": handle_update_server_credentials,  # DELETE
      "remove_server": handle_remove_server,                     # DELETE
      "remove_server_preview": handle_remove_server_preview,     # DELETE
      ...
  }

4. Update `__all__` if it mentions any of the deleted symbols (PATTERNS line 468).

**Step E: tool_annotations.py**

1. Delete the `"setup_mcp_admin": ToolAnnotations(...)` entry (PATTERNS lines 478–484, RESEARCH lines 92–96).

2. Delete `"update_server_credentials"` and `"remove_server"` annotation entries if present. Grep first: `grep -n '"update_server_credentials"\|"remove_server"\|"setup_mcp_admin"' src/homelab_mcp/tool_annotations.py`.

**Step F: openapi_app.py**

1. Remove `"setup_mcp_admin"` from `_SSH_TOOLS_WITH_HOSTNAME` tuple at line 70 (PATTERNS lines 500–508).

2. Remove `"setup_mcp_admin"` from `TOOL_CATEGORIES["SSH"]` list at line 146 (PATTERNS lines 511–519).

3. Also remove `"update_server_credentials"` and `"remove_server"` (and `"remove_server_preview"` if present) from both collections. Grep first: `grep -n '"update_server_credentials"\|"remove_server"' src/homelab_mcp/openapi_app.py` — enumerate all occurrences and remove each.

**Step G: verify lock-step across all five files**

After all edits, run:

```
grep -rn 'setup_mcp_admin\|update_server_credentials\|remove_server\|remove_server_preview' src/homelab_mcp/tool_schemas/ src/homelab_mcp/tool_handlers/ src/homelab_mcp/tool_annotations.py src/homelab_mcp/openapi_app.py
```

Expected: zero matches anywhere in these files. If any match remains, it means the 5-way lock-step failed — identify the miss and delete.

Special case — `setup_remote_mcp_admin` (the deleted FUNCTION name, distinct from the tool name): after Task 2 of this plan deletes the function from ssh_tools.py, the import in ssh_handlers.py (already deleted in Step B) must stay deleted. Verify no stale references: `grep -rn 'setup_remote_mcp_admin' src/homelab_mcp/tool_handlers/ src/homelab_mcp/tool_schemas/ src/homelab_mcp/tool_annotations.py src/homelab_mcp/openapi_app.py` — expected zero.
  </action>
  <verify>
    <automated>uv run pytest tests/test_openapi_app.py tests/test_tools.py -k "setup_mcp_admin_absent or setup_mcp_admin_removed or update_server_credentials_absent or update_server_credentials_removed or remove_server_absent or remove_server_removed" -x</automated>
  </verify>
  <acceptance_criteria>
    - Zero matches for `"setup_mcp_admin"|"update_server_credentials"|"remove_server"` in tool_schemas, tool_handlers, tool_annotations, openapi_app: `grep -rn '"setup_mcp_admin"\|"update_server_credentials"\|"remove_server"' src/homelab_mcp/tool_schemas/ src/homelab_mcp/tool_handlers/ src/homelab_mcp/tool_annotations.py src/homelab_mcp/openapi_app.py` produces no output
    - `grep -c 'handle_setup_mcp_admin\|handle_update_server_credentials\|handle_remove_server\|handle_remove_server_preview' src/homelab_mcp/tool_handlers/__init__.py` == 0
    - `grep -c 'def handle_setup_mcp_admin' src/homelab_mcp/tool_handlers/ssh_handlers.py` == 0
    - `grep -c 'def handle_update_server_credentials\|def handle_remove_server' src/homelab_mcp/tool_handlers/credential_handlers.py` == 0
    - `grep -c 'setup_remote_mcp_admin' src/homelab_mcp/tool_handlers/ssh_handlers.py` == 0 (import removed)
    - `grep -c 'remove_server\|update_server_credentials' src/homelab_mcp/tool_handlers/credential_handlers.py` == 0 (imports removed)
    - `uv run python -c "from homelab_mcp.tool_handlers import TOOL_HANDLERS; assert 'setup_mcp_admin' not in TOOL_HANDLERS and 'update_server_credentials' not in TOOL_HANDLERS and 'remove_server' not in TOOL_HANDLERS; print('OK')"` exit 0
    - `uv run python -c "from homelab_mcp.openapi_app import _SSH_TOOLS_WITH_HOSTNAME, TOOL_CATEGORIES; assert 'setup_mcp_admin' not in _SSH_TOOLS_WITH_HOSTNAME and 'setup_mcp_admin' not in TOOL_CATEGORIES.get('SSH', [])"` exit 0
    - `uv run pytest tests/test_openapi_app.py -k "setup_mcp_admin_absent or update_server_credentials_absent or remove_server_absent" -x` exit 0 (GREEN — 3 tests)
    - `uv run pytest tests/test_tools.py -k "setup_mcp_admin_removed or update_server_credentials_removed or remove_server_removed" -x` exit 0 (GREEN — 3 tests)
    - `uv run ruff check src/homelab_mcp/tool_schemas/ src/homelab_mcp/tool_handlers/ src/homelab_mcp/tool_annotations.py src/homelab_mcp/openapi_app.py` passes
  </acceptance_criteria>
  <done>All three removed tools are absent from all five tool-surface files; Wave 0 openapi + tool-handler tests GREEN; lock-step rule enforced (zero grep matches for removed names across the five files).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete setup_remote_mcp_admin + remove_server + update_server_credentials from ssh_tools.py; rewrite list_registered_servers to keyring; update error string (D-11, D-19, D-24)</name>
  <files>src/homelab_mcp/ssh_tools.py</files>
  <read_first>
    - .planning/phases/33-keyring-single-source-of-truth/33-PATTERNS.md §list_registered_servers rewrite (lines 303–335) — paste-in target pattern
    - .planning/phases/33-keyring-single-source-of-truth/33-PATTERNS.md §update_mcp_admin_groups error string (lines 283–299) — D-24 new message
    - .planning/phases/33-keyring-single-source-of-truth/33-RESEARCH.md §Pitfall 4 (lines 326–333 — setup_remote_mcp_admin is ~490 lines starting at ~line 170, NOT lines 700–750), §Pitfall 5 (lines 336–341 — update_mcp_admin_groups error message at line 758)
    - .planning/phases/33-keyring-single-source-of-truth/33-CONTEXT.md §D-11 (delete setup_remote_mcp_admin, keep update_mcp_admin_groups + verify_mcp_admin_access), §D-19 (list_registered_servers keyring rewrite), §D-20 (update_server_credentials removal), §D-21 (remove_server removal), §D-24 (pitfall-5 resolution error text)
    - src/homelab_mcp/ssh_tools.py (existing — confirm current locations via grep before editing: line 181 setup_remote_mcp_admin, line 739 update_mcp_admin_groups, line 870 register_server). Also grep for def list_registered_servers, def remove_server, def update_server_credentials to find their current line numbers.
  </read_first>
  <behavior>
    - setup_remote_mcp_admin function (async, ~490 lines starting at line 181 per earlier grep): ENTIRELY DELETED. All its internal helpers that exist only to support it go too.
    - update_mcp_admin_groups and verify_mcp_admin_access: PRESERVED — they are valid operations against an already-bootstrapped mcp_admin user.
    - update_mcp_admin_groups error message at ~line 758: "Run setup_mcp_admin first" → D-24 replacement text (no setup_mcp_admin reference, no docs link).
    - remove_server function in ssh_tools.py: DELETED (D-21 — CLI `credentials remove` is the replacement, landed in Plan 33-03).
    - update_server_credentials function in ssh_tools.py: DELETED (D-20 — no replacement; users re-run `credentials add` which is idempotent).
    - list_registered_servers function: REWRITTEN to read keyring registry via `list_credentials(credential_type="ssh")`. Returns a JSON string. Sync function (not async) per PATTERNS line 323.
    - All now-unused imports pruned (get_database_adapter if orphaned; any helpers that supported only setup_remote_mcp_admin).
  </behavior>
  <action>
Run `grep -n "async def setup_remote_mcp_admin\|async def update_mcp_admin_groups\|def list_registered_servers\|def remove_server\|def update_server_credentials\|def register_server\|def verify_mcp_admin_access" src/homelab_mcp/ssh_tools.py` to lock in current line numbers. (Research-confirmed anchors: line 181 setup_remote_mcp_admin, line 739 update_mcp_admin_groups, line 870 register_server.)

In `src/homelab_mcp/ssh_tools.py`:

**Step A: Delete setup_remote_mcp_admin**

1. The function starts at `async def setup_remote_mcp_admin(...)` (line 181 per grep). Find the matching function end — the next `def` or `async def` or `class` at the same indentation level. Per RESEARCH.md Pitfall 4, the function runs ~490 lines through approximately line 660. The next preserved function is `update_mcp_admin_groups` at line 739, meaning anything between ~line 660 (end of setup_remote_mcp_admin) and line 739 (start of update_mcp_admin_groups) may be helper functions supporting setup_remote_mcp_admin.

2. Delete the entire `setup_remote_mcp_admin` function body — from `async def setup_remote_mcp_admin(` through the last line of its body (likely a `return` statement followed by a blank line before the next top-level `def`).

3. Examine the helper functions between setup_remote_mcp_admin's end and update_mcp_admin_groups's start. Check each: is it called by any function OTHER than setup_remote_mcp_admin? For each helper, grep: `grep -n "<helper_name>" src/homelab_mcp/ssh_tools.py`. If ONLY setup_remote_mcp_admin called it (count drops to 0 after step 2's deletion), delete the helper too. If it's called by update_mcp_admin_groups, verify_mcp_admin_access, register_server, or any preserved function, KEEP it.

4. Verify: `grep -c "def setup_remote_mcp_admin\|setup_remote_mcp_admin" src/homelab_mcp/ssh_tools.py` == 0.

**Step B: Update update_mcp_admin_groups error message (D-24)**

1. Locate line 758 (per RESEARCH): search for `"Run setup_mcp_admin first"` in the file: `grep -n "Run setup_mcp_admin first" src/homelab_mcp/ssh_tools.py`.

2. The current text (per PATTERNS lines 288–290):

  "error": "mcp_admin user does not exist. Run setup_mcp_admin first."

3. Replace with the D-24 target text (per CONTEXT.md D-24 and PATTERNS lines 294–298):

  "error": (
      "mcp_admin user does not exist on target. "
      "Create any sudo-capable user and register it via "
      "`homelab-mcp credentials add <hostname> <username>`."
  ),

4. Verify: `grep -c "Run setup_mcp_admin first" src/homelab_mcp/ssh_tools.py` == 0; `grep -c "Create any sudo-capable user" src/homelab_mcp/ssh_tools.py` >= 1.

**Step C: Delete remove_server function (D-21)**

1. Locate `def remove_server(...)` in ssh_tools.py: `grep -n "def remove_server" src/homelab_mcp/ssh_tools.py`.

2. Delete the full function body. If it's async (likely async given the SSH operations it performed), delete from `async def remove_server(` through the last line before the next top-level def.

3. Check for callers of `remove_server` inside ssh_tools.py: `grep -n "remove_server(" src/homelab_mcp/ssh_tools.py`. Expected zero after deletion (we removed the definition, and Task 1 already deleted the handler import). If any self-reference remains, investigate.

**Step D: Delete update_server_credentials function (D-20)**

1. Locate `def update_server_credentials(...)` in ssh_tools.py: `grep -n "def update_server_credentials" src/homelab_mcp/ssh_tools.py`.

2. Delete the full function body.

3. Verify no remaining reference: `grep -c "update_server_credentials" src/homelab_mcp/ssh_tools.py` == 0.

**Step E: Rewrite list_registered_servers to keyring (D-19)**

1. Locate `def list_registered_servers(...)`: `grep -n "def list_registered_servers" src/homelab_mcp/ssh_tools.py`.

2. The current implementation calls `db.list_credentials()` (or similar DB access). Delete the ENTIRE current body and REPLACE with the pattern from PATTERNS.md lines 321–335:

  def list_registered_servers() -> str:
      """List servers registered in the keyring credential registry (D-19).

      Returns a JSON string with ``status``, ``count``, and a ``servers`` list of
      ``{"hostname", "username"}`` entries sourced from
      ``credential_store.list_credentials(credential_type="ssh")``.
      """
      entries = list_credentials(credential_type="ssh")
      result = {
          "status": "success",
          "count": len(entries),
          "servers": [
              {"hostname": e["hostname"], "username": e["username"]}
              for e in entries
          ],
      }
      return json.dumps(result, indent=2)

3. Verify `list_credentials` is imported at module top of ssh_tools.py: `grep -n "from .credential_store import\|list_credentials" src/homelab_mcp/ssh_tools.py | head`. If only `list_credentials` appears lazy-imported inside resolve_ssh_credentials, move or add the top-level import — but per Plan 33-03's completion, `list_credentials` is already a module-level import. Confirm.

4. Verify `json` is imported at module top — almost certainly yes already. Grep `grep -c "^import json\|^from json" src/homelab_mcp/ssh_tools.py`.

**Step F: Prune orphaned imports**

After Steps A–E, some imports may be orphaned. Check:

1. `get_database_adapter` — if Plan 33-03 Task 2 didn't remove it and this plan's deletions orphan it: grep `grep -c "get_database_adapter" src/homelab_mcp/ssh_tools.py` — if 0 outside the import line itself, remove the import.

2. Any DB-specific helpers from `src.homelab_mcp.database` imported for CRUD (e.g. if the top of ssh_tools.py imports things like `add_credential`, `delete_credential` names matching the DELETED DB methods — which shouldn't be the case; DB methods were method names on the adapter, not module-level imports — but double-check).

3. Any helpers specifically supporting setup_remote_mcp_admin (e.g. shell-script builders, cloud-init generators used only by that function) — delete them.

**Step G: Sanity-check remaining file**

- `grep -c "^def\|^async def" src/homelab_mcp/ssh_tools.py` — note the total function count before and after this plan. Expected decrease: at least 3 (setup_remote_mcp_admin, remove_server, update_server_credentials) plus any helpers that were solo-supporters of setup_remote_mcp_admin.

- Run `uv run python -c "import homelab_mcp.ssh_tools; print('module loads')"` — must exit 0 (no syntax or import errors after deletions).

- Run `uv run pytest tests/test_ssh_tools.py -x` — `test_setup_remote_mcp_admin_absent` from Plan 33-01 must now be GREEN.

- The AST meta-test scan will stop flagging `setup_remote_mcp_admin`, `update_server_credentials`, `remove_server`, `setup_mcp_admin` after this plan and Plan 33-04 Task 1 complete. It may still flag `update_credential` if that string appears elsewhere (it shouldn't — but if it does in error messages referencing removed features, clean it up).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ssh_credentials.py::TestListRegisteredServers tests/test_ssh_tools.py::test_setup_remote_mcp_admin_absent -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'async def setup_remote_mcp_admin\|def setup_remote_mcp_admin' src/homelab_mcp/ssh_tools.py` == 0
    - `grep -c 'def remove_server' src/homelab_mcp/ssh_tools.py` == 0
    - `grep -c 'def update_server_credentials' src/homelab_mcp/ssh_tools.py` == 0
    - `grep -c 'def list_registered_servers' src/homelab_mcp/ssh_tools.py` == 1 (rewritten, still exists)
    - `grep -c 'def update_mcp_admin_groups' src/homelab_mcp/ssh_tools.py` == 1 (preserved per D-11)
    - `grep -c 'def verify_mcp_admin_access' src/homelab_mcp/ssh_tools.py` == 1 (preserved per D-11)
    - `grep -c 'def register_server' src/homelab_mcp/ssh_tools.py` == 1 (still exists; Plan 33-05 rewrites its body)
    - `grep -c 'Run setup_mcp_admin first' src/homelab_mcp/ssh_tools.py` == 0 (D-24)
    - `grep -c 'Create any sudo-capable user' src/homelab_mcp/ssh_tools.py` >= 1 (D-24)
    - `grep -c 'get_credential_by_hostname\|get_database_adapter' src/homelab_mcp/ssh_tools.py` — if nonzero, each occurrence must be explainable; ideally all occurrences removed by combined Plans 33-03 and 33-04. Re-run the AST meta-test if anything remains.
    - `uv run python -c "import homelab_mcp.ssh_tools; from homelab_mcp.ssh_tools import list_registered_servers, update_mcp_admin_groups, verify_mcp_admin_access, register_server; assert not hasattr(homelab_mcp.ssh_tools, 'setup_remote_mcp_admin')"` exit 0
    - `uv run pytest tests/test_ssh_tools.py::test_setup_remote_mcp_admin_absent -x` exit 0 (GREEN — D-11)
    - `uv run pytest tests/test_ssh_credentials.py -k "test_list_returns_keyring_entries or test_list_does_not_read_db" -x` exit 0 (GREEN — D-19)
    - `uv run pytest tests/test_ast_regression.py -x` exit 0 (GREEN — D-15/D-25 scan passes after this plan + 33-05 land; if still RED, the remaining violations must be only in files that Plan 33-05 touches — register_server, prompt_registry)
    - `uv run ruff check src/homelab_mcp/ssh_tools.py` passes
    - `uv run mypy src/homelab_mcp/ssh_tools.py` passes — may still report type issues in register_server (Plan 33-05 scope)
  </acceptance_criteria>
  <done>setup_remote_mcp_admin + remove_server + update_server_credentials deleted from ssh_tools.py; list_registered_servers rewritten to keyring; update_mcp_admin_groups error message rephrased per D-24; module imports cleanly; Wave 0 D-11 + D-19 tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool surface → credential store | Zero tools can write credentials after this plan (D-06 guiding principle) |
| update_mcp_admin_groups → mcp_admin target | Still a legitimate remote sudo-level operation, but references CLI-based credential add rather than the removed bootstrap tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-10 | Tampering | setup_mcp_admin tool re-introduced by future change — bypasses D-06 | mitigate | Wave 0 AST meta-test (`test_no_forbidden_strings_in_source`) scans for `setup_mcp_admin` and fails at test time. 5-way lock-step rule forces re-adding across all five files — schema/annotation parity CI catches partial restores |
| T-33-11 | Information Disclosure | update_server_credentials MCP tool re-introduced — passwords flow through chat | mitigate | Same AST meta-test scan (scans for `update_server_credentials`); 5-way lock-step enforced |
| T-33-12 | Spoofing | list_registered_servers returns stale DB rows instead of keyring entries | mitigate | Function rewritten to call `list_credentials(credential_type="ssh")`. Wave 0 `test_list_does_not_read_db` mocks `get_database_adapter` and asserts it is never called |
| T-33-13 | Denial of Service | update_mcp_admin_groups error message refers to a removed tool — user cannot self-diagnose | mitigate | D-24 rewording names `homelab-mcp credentials add` as the correct remediation |
</threat_model>

<verification>
After both tasks land:
- `uv run pytest tests/ -m "not integration"` — ALL Plan 33-01 Wave 0 tests touching CRED-06 scope (setup_mcp_admin_removed, setup_remote_mcp_admin_absent, openapi absence tests) go GREEN
- AST meta-test (`tests/test_ast_regression.py::test_no_forbidden_strings_in_source`) — expected to be GREEN *except* for any violations that remain in `src/homelab_mcp/prompt_registry.py` (the prompt still mentions setup_mcp_admin until Plan 33-05 rewrites it) or `src/homelab_mcp/ssh_tools.py::register_server` body (still has verify_connection param until Plan 33-05). Those are resolved in Plan 33-05.
- `uv run ruff check src/homelab_mcp/` passes
- `uv run mypy src/homelab_mcp/` passes (or produces only errors that Plan 33-05 resolves)
- Running a real `homelab-mcp` process and calling `tools/list` returns a response WITHOUT setup_mcp_admin, update_server_credentials, remove_server, remove_server_preview
</verification>

<success_criteria>
CRED-06 complete except for the prompt rewrite (Plan 33-05):
- Three removed MCP tools absent from all five lock-step files
- setup_remote_mcp_admin function deleted
- list_registered_servers reads keyring registry
- update_mcp_admin_groups error message updated per D-24
- Wave 0 tool-handler and openapi tests GREEN
- AST meta-test mostly GREEN — remaining violations are scoped to Plan 33-05 files
</success_criteria>

<output>
After completion, create `.planning/phases/33-keyring-single-source-of-truth/33-04-SUMMARY.md` documenting:
- Total deletions across the 6 files (bullet list: file → what was removed)
- list_registered_servers before/after size
- D-24 error message change before/after quotation
- Wave 0 tests flipped to GREEN: setup_mcp_admin_removed_from_tool_handlers, setup_mcp_admin_absent, update_server_credentials_absent/removed, remove_server_absent/removed, setup_remote_mcp_admin_absent, test_list_returns_keyring_entries, test_list_does_not_read_db
- Known remaining RED tests: anything under Plan 33-05 scope (register_server signature checks, prompt assertions, AST scan entries for remaining files)
</output>
