---
phase: 33-keyring-single-source-of-truth
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - src/homelab_mcp/ssh_tools.py
  - src/homelab_mcp/prompt_registry.py
  - src/homelab_mcp/tool_schemas/ssh_tools_schema.py
  - src/homelab_mcp/tool_handlers/credential_handlers.py
autonomous: true
requirements: [CRED-07]

must_haves:
  truths:
    - "register_server accepts hostname, username (required, no default), port, display_name — and nothing else"
    - "register_server calls resolve_ssh_credentials and rejects registration with actionable error when credentials are missing or invalid"
    - "register_server does not touch the database — it only verifies the SSH connection"
    - "register_server returns {status, hostname, username, verified, display_name} as JSON; verified=true only on successful SSH handshake"
    - "The connect_to_device prompt uses the D-13 six-step sequence with D-22 step-1 wording (any SSH user with sudo, not mcp_admin)"
    - "The connect_to_device prompt contains no reference to setup_mcp_admin or verify_connection=False"
    - "AST meta-test test_no_forbidden_strings_in_source is GREEN — all forbidden strings removed from source (except migration.py's allowlisted DROP reference)"
  artifacts:
    - path: "src/homelab_mcp/ssh_tools.py"
      provides: "Rewritten register_server: verify-only, resolves credentials via resolve_ssh_credentials, returns actionable errors"
      exports: ["register_server"]
    - path: "src/homelab_mcp/prompt_registry.py"
      provides: "Rewritten _build_connect_to_device_result with D-13 six-step sequence and D-22 step-1 wording"
    - path: "src/homelab_mcp/tool_schemas/ssh_tools_schema.py"
      provides: "register_server schema lists hostname, username, port, display_name only — no password or key_path or verify_connection"
    - path: "src/homelab_mcp/tool_handlers/credential_handlers.py"
      provides: "handle_register_server — unchanged thin-wrapper shape; passes arguments straight through"
  key_links:
    - from: "register_server"
      to: "resolve_ssh_credentials"
      via: "direct call in function body — single funnel pattern"
      pattern: "resolve_ssh_credentials.hostname=hostname"
    - from: "register_server"
      to: "asyncssh.connect (via ssh_connect helper or direct)"
      via: "async with ... as conn: pass — open-and-close verification"
      pattern: "async with .* as .*:"
    - from: "prompt_registry._build_connect_to_device_result"
      to: "register_server + credentials add CLI"
      via: "f-string references the CLI command and the tool name"
      pattern: "homelab-mcp credentials add.*register_server"
---

<objective>
Final plan. Rewrite `register_server` to the verify-only shape (D-03, D-04, D-05, D-07, D-23) and rewrite the `connect_to_device` prompt to the D-13 six-step sequence with D-22 step-1 wording. Update the `register_server` schema to match the new signature. Per RESEARCH.md Pitfall 1, the prompt rewrite and the test assertion flip (landed in Plan 33-01) MUST land together to avoid a RED gap — this plan achieves that by shipping both the prompt rewrite and the register_server rewrite in the final wave. After this plan, ALL Wave 0 tests go GREEN. The AST meta-test scan completes across the entire source tree.

Purpose: CRED-07 — `register_server` validates credentials via the standard `resolve_ssh_credentials()` path; no verify-bypass exists; registrations with missing/invalid credentials are rejected with actionable error. Onboarding prompt reflects the new D-18/D-22 "any SSH user with sudo" workflow.

Output: register_server rewritten verify-only; schema trimmed; prompt rewritten; Wave 0 fully GREEN; AST meta-test fully GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-keyring-single-source-of-truth/33-CONTEXT.md
@.planning/phases/33-keyring-single-source-of-truth/33-RESEARCH.md
@.planning/phases/33-keyring-single-source-of-truth/33-PATTERNS.md
@.planning/phases/33-keyring-single-source-of-truth/33-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- register_server target signature and return shape -->

From src/homelab_mcp/ssh_tools.py (AFTER rewrite):
```python
async def register_server(
    hostname: str,
    username: str,                 # D-23: required, no default
    port: int = 22,
    display_name: str | None = None,
    # REMOVED: password, key_path, verify_connection
) -> str:
    """Verify SSH connectivity using keyring credentials. Does NOT write credentials.

    Returns a JSON string:
      {"status": "success|error", "hostname": ..., "username": ...,
       "verified": bool, "display_name": str|None, "error": str|None}
    """
```

From src/homelab_mcp/tool_schemas/ssh_tools_schema.py (register_server schema AFTER):
```json
{
  "type": "object",
  "properties": {
    "hostname": {"type": "string", ...},
    "username": {"type": "string", ...},
    "port": {"type": "integer", "default": 22, ...},
    "display_name": {"type": "string", ...}
  },
  "required": ["hostname", "username"]
}
```

From src/homelab_mcp/tool_handlers/credential_handlers.py (unchanged — thin wrapper):
```python
async def handle_register_server(arguments: dict[str, Any]) -> dict[str, Any]:
    result = await register_server(**arguments)
    return {"content": [{"type": "text", "text": result}]}
```

From src/homelab_mcp/prompt_registry.py (_build_connect_to_device_result AFTER):
```python
def _build_connect_to_device_result(args: dict[str, str]) -> types.GetPromptResult:
    hostname = args.get("hostname", "<hostname>")
    text = f"""Follow these steps to onboard {hostname} into your homelab:
    ... D-13 six-step sequence, D-22 step-1 wording ...
"""
    return types.GetPromptResult(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite register_server to verify-only + trim its schema (D-03, D-04, D-05, D-07, D-23)</name>
  <files>src/homelab_mcp/ssh_tools.py, src/homelab_mcp/tool_schemas/ssh_tools_schema.py</files>
  <read_first>
    - .planning/phases/33-keyring-single-source-of-truth/33-PATTERNS.md §register_server rewrite (lines 204–279) — paste-in target body
    - .planning/phases/33-keyring-single-source-of-truth/33-RESEARCH.md §Current register_server (lines 403–423 — what to replace) and §Architecture Patterns data-flow (lines 141–157 — verify-only flow)
    - .planning/phases/33-keyring-single-source-of-truth/33-CONTEXT.md §D-03, §D-04, §D-05, §D-07, §D-23
    - .planning/phases/33-keyring-single-source-of-truth/33-RESEARCH.md §Anti-Patterns (line 263 — Don't leave register_server with a different import surface; credential_handlers.py imports register_server from ssh_tools)
    - src/homelab_mcp/ssh_tools.py lines 870–969 (current register_server — verify signature before editing)
    - src/homelab_mcp/tool_schemas/ssh_tools_schema.py — find register_server schema (not explicitly line-numbered; grep `grep -n '"register_server"' src/homelab_mcp/tool_schemas/ssh_tools_schema.py`)
    - src/homelab_mcp/tool_handlers/credential_handlers.py — confirm handle_register_server still works as `await register_server(**arguments)` (PATTERNS line 420–424 confirms it stays unchanged)
  </read_first>
  <behavior>
    - register_server signature: (hostname: str, username: str, port: int = 22, display_name: str | None = None) — async; returns str
    - Body: calls resolve_ssh_credentials(hostname=hostname, username=username, port=port); on CredentialNotFoundError returns JSON error with actionable message
    - On successful resolve: opens SSH connection via asyncssh (or existing helper like `ssh_connect`) using the resolved SSHCredentials; on success returns {"status": "success", "verified": True, ...}; on SSH connect exception returns {"status": "error", "verified": False, "error": "..."}
    - NO database access — does NOT call get_database_adapter
    - NO optional parameters for password, key_path, or verify_connection
    - Schema matches signature: properties = {hostname, username, port, display_name}; required = [hostname, username]
  </behavior>
  <action>
Run `grep -n "async def register_server\|def register_server" src/homelab_mcp/ssh_tools.py` to confirm current start line (expected ~line 870). Then `grep -n '"register_server"' src/homelab_mcp/tool_schemas/ssh_tools_schema.py` to find the schema entry.

**Step A: Understand how current register_server opens SSH**

Before writing the new body, inspect the current implementation to see which SSH helper or library call it uses. Grep `grep -n "asyncssh\.connect\|ssh_connect\|conn.run\|async with" src/homelab_mcp/ssh_tools.py | head -30`. Pick the same primitive used by existing verify-type functions (verify_mcp_admin_access is a perfect analog — PATTERNS line 211 notes it as the shape-match).

Look at verify_mcp_admin_access's SSH connect pattern — whichever helper/call it uses, reuse it exactly in the new register_server. This keeps the test mock path consistent with existing test conventions (Plan 33-01 Task 2 mocks `src.homelab_mcp.ssh_tools.asyncssh.connect` OR the helper name — whichever exists; if it doesn't match, update the test patch target in Plan 33-01 Task 2 test file to match the actual helper).

**Step B: Replace register_server body**

1. Locate `async def register_server(...)` at line 870. The current signature (per RESEARCH.md lines 406–413) is:

  async def register_server(
      hostname: str,
      username: str = "mcp_admin",
      key_path: str | None = None,
      port: int = 22,
      display_name: str | None = None,
      verify_connection: bool = True,
  ) -> str:

2. REPLACE the entire function (signature + body) — from `async def register_server(` through the function's last line before the next top-level def — with this target (adapted from PATTERNS.md lines 231–279):

  async def register_server(
      hostname: str,
      username: str,
      port: int = 22,
      display_name: str | None = None,
  ) -> str:
      """Verify SSH connectivity using keyring credentials. Does NOT write credentials.

      Phase 33 (v1.6) — verify-only (D-03, D-04, D-07, D-23).

      Resolves credentials via ``resolve_ssh_credentials`` (keyring-only after D-08),
      opens one SSH connection to prove the credential works, then returns the
      result. The server database is not touched.
      """
      import json  # noqa: PLC0415 (only import needed at top already likely present — verify)

      try:
          creds = resolve_ssh_credentials(hostname=hostname, username=username, port=port)
      except CredentialNotFoundError as e:
          return json.dumps({
              "status": "error",
              "hostname": hostname,
              "username": username,
              "verified": False,
              "display_name": display_name,
              "error": str(e),
          })

      try:
          <SSH CONNECT — match existing pattern, see Step A>
          # For reference — asyncssh direct form:
          # async with asyncssh.connect(
          #     host=hostname,
          #     username=creds.username,
          #     password=creds.password,
          #     client_keys=[creds.key_path] if creds.key_path else None,
          #     port=creds.port,
          #     known_hosts=None,
          # ) as _conn:
          #     pass
      except Exception as e:
          return json.dumps({
              "status": "error",
              "hostname": hostname,
              "username": username,
              "verified": False,
              "display_name": display_name,
              "error": (
                  f"SSH verification failed: {e}. "
                  "Re-add credentials with: "
                  f"homelab-mcp credentials add {hostname} {username}"
              ),
          })

      return json.dumps({
          "status": "success",
          "hostname": hostname,
          "username": username,
          "verified": True,
          "display_name": display_name,
      })

The `<SSH CONNECT>` block must match the exact primitive the project already uses. Options (pick the one matching existing code in ssh_tools.py):

Option 1 — direct asyncssh.connect (if verify_mcp_admin_access uses it):

  async with asyncssh.connect(
      host=hostname,
      username=creds.username,
      password=creds.password,
      client_keys=[creds.key_path] if creds.key_path else None,
      port=creds.port,
      known_hosts=None,
  ) as _conn:
      pass

Option 2 — existing project helper (ssh_connect or similar). Grep to confirm: `grep -n "^async def ssh_connect\|^def ssh_connect" src/homelab_mcp/ssh_tools.py`. If a helper exists with a compatible signature, use it for consistency with other tools.

Option 3 — existing project helper on an SSHCredentials object (e.g. `creds.connect()` if SSHCredentials has a method). Unlikely but possible — inspect the dataclass definition.

Use the SAME primitive used by the test monkeypatch in Plan 33-01 Task 2 — those tests patch `src.homelab_mcp.ssh_tools.asyncssh.connect`. If the actual code uses a different primitive, coordinate with the test file: either use `asyncssh.connect` directly in register_server (preferred — matches the test patch path) or update the test patch target. Prefer the direct-asyncssh approach to keep the test mock path simple.

3. DELETE all of:
   - The `key_path` parameter (D-03)
   - The `verify_connection` parameter and its conditional branch (D-07)
   - The `username: str = "mcp_admin"` default — becomes `username: str` required (D-23)
   - Every DB call (`get_database_adapter`, `db.connect`, `db.init_schema`, `db.add_credential`, `db.get_credential_by_hostname`, etc.) — D-03
   - The "already registered" guard that queried db.get_credential_by_hostname

4. Verify: `grep -c 'get_database_adapter\|db.add_credential\|db.init_schema\|verify_connection' src/homelab_mcp/ssh_tools.py` — all occurrences related to register_server must be zero. `get_database_adapter` may still appear if some other non-credential function needs it (e.g. devices CRUD). Grep by function context: `awk '/^async def register_server/,/^async def [a-zA-Z]|^def [a-zA-Z]/' src/homelab_mcp/ssh_tools.py | grep -c 'get_database_adapter\|db\.'` == 0.

5. Verify module imports at the top of ssh_tools.py: `asyncssh` (or equivalent primitive), `json`, `CredentialNotFoundError`, `resolve_ssh_credentials`, `SSHCredentials`. All should already be imported — confirm with grep.

**Step C: Update register_server schema**

1. Locate the `"register_server"` entry in `src/homelab_mcp/tool_schemas/ssh_tools_schema.py`.

2. REPLACE the schema with one matching the new signature. Match the shape of other tool schemas in the file (per PATTERNS lines 344–355 for the reference shape):

  "register_server": {
      "description": (
          "Verify SSH connectivity to a registered server using keyring credentials. "
          "Does not write any credentials — credentials must already be stored via "
          "`homelab-mcp credentials add <hostname> <username>` in the CLI. "
          "Returns a status object with verified=true on successful handshake."
      ),
      "inputSchema": {
          "type": "object",
          "properties": {
              "hostname": {
                  "type": "string",
                  "description": "Hostname or IP address of the target server",
              },
              "username": {
                  "type": "string",
                  "description": "SSH username to verify; must match a keyring entry",
              },
              "port": {
                  "type": "integer",
                  "description": "SSH port (default 22)",
                  "default": 22,
              },
              "display_name": {
                  "type": "string",
                  "description": "Optional human-readable label",
              },
          },
          "required": ["hostname", "username"],
      },
  },

3. Verify there is NO `password`, `key_path`, or `verify_connection` property in the schema. Grep within the register_server entry: run `awk '/"register_server":/,/^    \},/' src/homelab_mcp/tool_schemas/ssh_tools_schema.py | grep -cE '"password"|"key_path"|"verify_connection"'` — expected 0.

**Step D: Confirm handle_register_server still works**

Plan 33-04 Task 1 already cleaned credential_handlers.py. Confirm `handle_register_server` (PATTERNS lines 420–424) remains as the 4-line thin wrapper:

  async def handle_register_server(arguments: dict[str, Any]) -> dict[str, Any]:
      """Handle register_server tool."""
      result = await register_server(**arguments)
      return {"content": [{"type": "text", "text": result}]}

The `**arguments` unpacking means the handler automatically adapts to the new signature — no change needed as long as the incoming dict has the right keys (which the new schema enforces via `required: [hostname, username]`).

Verify: `grep -A 4 "async def handle_register_server" src/homelab_mcp/tool_handlers/credential_handlers.py` — output matches the 4-line shape above.

**Step E: Run the Wave 0 register_server tests**

All should now be GREEN:
- `test_register_verify_success` (D-04)
- `test_register_missing_keyring_error` (D-05)
- `test_register_server_schema_no_write_params` (D-03)
- `test_register_no_verify_connection_flag` (D-07)
- `test_register_username_required` (D-23)
- `test_register_does_not_write_db` (D-03/D-04)
- `test_register_server_handler_no_verify_connection_param` (AST meta-test signature check)

If the test patch path for `asyncssh.connect` doesn't match the register_server implementation, expect RED on test_register_verify_success and test_register_does_not_write_db. Fix by aligning either the register_server code (use direct `asyncssh.connect`) or the test patch target (use whatever primitive register_server actually calls — edit Plan 33-01 Task 2 output to match).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ssh_credentials.py::TestRegisterServer tests/test_ast_regression.py::test_register_server_handler_no_verify_connection_param -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'async def register_server' src/homelab_mcp/ssh_tools.py` == 1
    - `awk '/^async def register_server/,/^async def [a-zA-Z_]|^def [a-zA-Z_]/' src/homelab_mcp/ssh_tools.py | grep -cE 'verify_connection|key_path|password'` — allowed occurrences are inside creds.password / creds.key_path accesses and the error-message string; direct function parameters must be 0
    - `uv run python -c "import inspect; from homelab_mcp.ssh_tools import register_server; sig = inspect.signature(register_server); assert list(sig.parameters.keys()) == ['hostname', 'username', 'port', 'display_name'], f'got {list(sig.parameters.keys())}'; assert sig.parameters['username'].default is inspect.Parameter.empty"` exit 0
    - `awk '/"register_server":/,/^    \},$/' src/homelab_mcp/tool_schemas/ssh_tools_schema.py | grep -cE '"password"|"key_path"|"verify_connection"'` == 0
    - `uv run pytest tests/test_ssh_credentials.py::TestRegisterServer -x` exit 0 (GREEN — all 6 register_server tests)
    - `uv run pytest tests/test_ast_regression.py::test_register_server_handler_no_verify_connection_param -x` exit 0 (GREEN)
    - `uv run ruff check src/homelab_mcp/ssh_tools.py src/homelab_mcp/tool_schemas/ssh_tools_schema.py` passes
    - `uv run mypy src/homelab_mcp/ssh_tools.py` passes
  </acceptance_criteria>
  <done>register_server is verify-only with the exact signature required by D-03/D-07/D-23; schema matches; the database is never touched; all Wave 0 register_server tests GREEN; handle_register_server unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite _build_connect_to_device_result prompt with D-13 six-step sequence and D-22 step-1 wording</name>
  <files>src/homelab_mcp/prompt_registry.py</files>
  <read_first>
    - .planning/phases/33-keyring-single-source-of-truth/33-PATTERNS.md §prompt_registry.py (lines 525–574) — target f-string text
    - .planning/phases/33-keyring-single-source-of-truth/33-RESEARCH.md §Pitfall 1 (lines 295–303 — this rewrite MUST land together with Plan 33-01 Task 3's prompt-test assertion flip; they co-execute to avoid RED gap)
    - .planning/phases/33-keyring-single-source-of-truth/33-CONTEXT.md §D-13 (six-step sequence), §D-14 (no setup_mcp_admin, no verify_connection=False), §D-22 (step 1 wording — any SSH user with sudo), §D-18 (no mcp_admin specificity)
    - src/homelab_mcp/prompt_registry.py lines 125–147 (current _build_connect_to_device_result — verify current state)
  </read_first>
  <behavior>
    - _build_connect_to_device_result produces a GetPromptResult with messages[0] containing text that:
      - Step 1: "Ensure you have an SSH-accessible user on {hostname} with sudo privileges. The username can be anything — you will specify it in the next step."
      - Step 2: Names `homelab-mcp credentials add {hostname} <username>` (password path) AND `--key-path <path>` (key-auth path)
      - Step 3: Names `register_server` with `hostname="{hostname}"` and `username="<username>"`; explicitly describes it as verification (no verify_connection reference)
      - Step 4: Names `ssh_discover` with `hostname="{hostname}"`
      - Step 5: Names `discover_and_map` with `hostname="{hostname}"`
      - Step 6: Names `verify_mcp_admin` with `hostname="{hostname}"`
    - Prompt does NOT contain the strings `setup_mcp_admin` or `verify_connection`
    - Prompt DOES contain `credentials add`, `register_server`, `ssh_discover`, `discover_and_map`, `verify_mcp_admin` (per Plan 33-01 Task 3 positive assertions)
    - Prompt DOES contain `<username>` placeholder (per D-18 — not mcp_admin specifically)
    - Function signature unchanged; return type unchanged
  </behavior>
  <action>
Run `grep -n "def _build_connect_to_device_result" src/homelab_mcp/prompt_registry.py` to confirm current location (RESEARCH says lines 125–147).

In `src/homelab_mcp/prompt_registry.py`:

1. Locate `_build_connect_to_device_result` function. Inspect the current f-string body (RESEARCH lines 125–147).

2. REPLACE the `text = f"""..."""` block — keep the function signature, the hostname extraction from args, and the final `return types.GetPromptResult(...)` — with the D-13 + D-22 sequence (adapted from PATTERNS.md lines 544–561):

  text = f"""Follow these steps to onboard {hostname} into your homelab:

1. Ensure you have an SSH-accessible user on {hostname} with sudo privileges. The username can be anything — you will specify it in the next step.

2. Run the CLI command in your terminal: homelab-mcp credentials add {hostname} <username> — this stores the SSH credential in your OS keyring. For key-based auth: homelab-mcp credentials add {hostname} <username> --key-path <path>.

3. Call register_server with hostname="{hostname}" and username="<username>" to verify the stored credential end-to-end.

4. Call ssh_discover with hostname="{hostname}" to collect hardware and system info and record it in the database.

5. Call discover_and_map with hostname="{hostname}" to add the device to the network sitemap.

6. Call verify_mcp_admin with hostname="{hostname}" to confirm that the registered user has sudo access.

If any step fails, fix the issue before proceeding to the next step."""

3. Verify the function's wrapper structure (per PATTERNS lines 566–574) is preserved — DO NOT change:
   - Function signature: `def _build_connect_to_device_result(args: dict[str, str]) -> types.GetPromptResult:`
   - Docstring (or restore one referencing TOFU-03 if the original had one)
   - `hostname = args.get("hostname", "<hostname>")` extraction line
   - `return types.GetPromptResult(description="Full device onboarding workflow", messages=[_make_user_message(text)])` final return

4. Verify the text does NOT contain the forbidden strings:
   - `grep -c 'setup_mcp_admin' src/homelab_mcp/prompt_registry.py` == 0
   - `grep -c 'verify_connection' src/homelab_mcp/prompt_registry.py` == 0

5. Verify the text DOES contain the required references:
   - `grep -c 'credentials add' src/homelab_mcp/prompt_registry.py` >= 1
   - `grep -c 'register_server' src/homelab_mcp/prompt_registry.py` >= 1
   - `grep -c '<username>' src/homelab_mcp/prompt_registry.py` >= 2 (step 2 password + step 2 key-path + step 3)
   - `grep -c '\-\-key-path' src/homelab_mcp/prompt_registry.py` >= 1

6. Wave 0 prompt tests (from Plan 33-01 Task 3) should all flip to GREEN:
   - `test_connect_to_device_prompt` — asserts setup_mcp_admin NOT in, credentials add IN, register_server IN
   - `test_connect_to_device_prompt_parameter_names` — for-loop over (register_server, ssh_discover, discover_and_map, verify_mcp_admin) all present
   - `test_connect_to_device_no_verify_bypass` — verify_connection NOT in
   - `test_connect_to_device_mentions_credentials_cli` — `homelab-mcp credentials add` present; mcp_admin not mandated

7. Run the AST meta-test once more. With this task + Task 1 complete, `test_no_forbidden_strings_in_source` should be fully GREEN because:
   - `ssh_credentials` — only in migration.py (allowlisted in Plan 33-02 Task 1 Step 6)
   - `add_credential`, `get_credential_by_hostname`, `update_credential`, `update_last_verified` — removed by Plan 33-02
   - `setup_remote_mcp_admin` — removed by Plan 33-04
   - `setup_mcp_admin` — removed across five files by Plan 33-04 AND from prompt_registry.py by THIS task
   - `update_server_credentials`, `remove_server` — removed by Plan 33-04
   - `verify_connection` — removed from register_server signature by this plan's Task 1

Note: a non-test file legitimately referencing `verify_connection` elsewhere (e.g. in a Python SDK re-export) could still trigger the AST meta-test. If that happens, the D-25 "narrowly in register_server context only" guidance applies — refine the test to be register_server-specific. Cross-check by running `grep -rn 'verify_connection' src/homelab_mcp/ --include='*.py'` — if any non-register-server reference exists, decide whether to remove it or refine the AST scan to skip it. Either way, test_register_server_handler_no_verify_connection_param (which uses inspect.signature) already covers the D-25 narrow check.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mcp_prompts.py tests/test_ast_regression.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'setup_mcp_admin' src/homelab_mcp/prompt_registry.py` == 0
    - `grep -c 'verify_connection' src/homelab_mcp/prompt_registry.py` == 0
    - `grep -c 'credentials add' src/homelab_mcp/prompt_registry.py` >= 1
    - `grep -c 'register_server' src/homelab_mcp/prompt_registry.py` >= 1
    - `grep -c '\-\-key-path' src/homelab_mcp/prompt_registry.py` >= 1
    - `grep -c '<username>' src/homelab_mcp/prompt_registry.py` >= 1
    - `uv run python -c "from homelab_mcp.prompt_registry import _build_connect_to_device_result; r = _build_connect_to_device_result({'hostname': 'test.local'}); txt = r.messages[0].content.text; assert 'setup_mcp_admin' not in txt; assert 'verify_connection' not in txt; assert 'credentials add' in txt; assert 'register_server' in txt; print('OK')"` exit 0
    - `uv run pytest tests/test_mcp_prompts.py -k "connect_to_device" -x` exit 0 (GREEN — all 4 prompt tests: test_connect_to_device_prompt, test_connect_to_device_prompt_parameter_names, test_connect_to_device_no_verify_bypass, test_connect_to_device_mentions_credentials_cli)
    - `uv run pytest tests/test_ast_regression.py -x` exit 0 (GREEN — D-15/D-25 meta-test fully passes)
    - `uv run ruff check src/homelab_mcp/prompt_registry.py` passes
    - `uv run mypy src/homelab_mcp/prompt_registry.py` passes
  </acceptance_criteria>
  <done>Prompt uses D-13 six-step sequence; step 1 has D-22 wording; no setup_mcp_admin or verify_connection reference; all Wave 0 prompt tests GREEN; AST meta-test fully GREEN.

> **RED→GREEN transition (companion to Plan 33-01 Task 3 note):** Running the test suite after this task should show `test_connect_to_device_prompt`, `test_connect_to_device_prompt_parameter_names`, `test_connect_to_device_no_verify_bypass`, and `test_connect_to_device_mentions_credentials_cli` all GREEN. These four tests were intentionally RED from Wave 0 (Plan 33-01 Task 3) through Wave 3 per RESEARCH.md Pitfall 1 — this task closes the loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool surface → SSH connect | register_server opens an SSH handshake on user-supplied hostname/username — credential resolved server-side via keyring |
| prompt text → AI agent → user | The connect_to_device prompt shapes the AI's tool-call sequence; wrong instructions can lead AI to call removed or dangerous tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-14 | Spoofing | register_server accepts a username for a host it has keyring entries for — wrong username passes resolve but SSH fails | mitigate | resolve_ssh_credentials exact-match on hostname; keyring lookup is on (hostname, stored_username); if caller's username mismatches stored_username, behavior depends on D-08 — resolved_username = username or stored_username. Caller-supplied mismatch leads to SSH auth failure → error response with actionable CLI remediation (D-05). Still safe |
| T-33-15 | Information Disclosure | SSH connect exception string may leak server hostnames/IPs in error message | accept | Error message already echoes hostname/username (user-supplied); exception text logged but returned verbatim only when helpful. Homelab scope — no multi-tenant leakage risk |
| T-33-16 | Elevation of Privilege | AI agent follows stale prompt telling it to call setup_mcp_admin (no longer exists) — tool-call fails | mitigate | D-13 rewrite removes setup_mcp_admin from prompt; D-14 assertion flip in Plan 33-01 Task 3 guards against regression; AST meta-test also guards |
| T-33-17 | Denial of Service | Hostile or misbehaving user calls register_server in a tight loop against a down target — resource exhaustion via SSH retries | accept | register_server uses whatever timeout the existing SSH primitive enforces; project's deferred SSH-04 (per-call timeout handshake) would harden this further but is explicitly scoped to v1.7+ |
</threat_model>

<verification>
Full Phase 33 verification after this plan lands:
- `uv run pytest tests/ -m "not integration"` — ENTIRE unit test suite GREEN
- `uv run pytest tests/test_ast_regression.py -x` — GREEN (D-15 + D-25 meta-test passes)
- `uv run ruff check src/ tests/` — GREEN
- `uv run mypy src/` — GREEN
- `uv run bandit -r src/` — no new high-severity findings introduced by this phase
- Manual: `uv run homelab-mcp` — server starts without errors; tools/list response excludes setup_mcp_admin, update_server_credentials, remove_server, remove_server_preview
- Manual: onboarding demo — run `homelab-mcp credentials add` + call register_server via MCP client, verify status:success returned for valid creds and status:error + actionable message for missing creds
</verification>

<success_criteria>
Phase 33 complete — all four CRED requirements and all 17 + 8 addendum decisions implemented:
- CRED-04: ssh_credentials DB table gone + methods deleted (Plan 33-02); AST meta-test proves nothing in source references DB CRUD method names (Plans 33-02 + 33-04)
- CRED-05: two-tier keyring-only resolver; no mcp_admin default fallback; key-path CLI parity (Plan 33-03)
- CRED-06: setup_mcp_admin tool removed across five surfaces (Plan 33-04); setup_remote_mcp_admin function deleted (Plan 33-04); update_server_credentials + remove_server tools removed (Plan 33-04); list_registered_servers reads keyring (Plan 33-04); update_mcp_admin_groups error rephrased (Plan 33-04); prompt rewritten (this plan Task 2)
- CRED-07: register_server verify-only, calls resolve_ssh_credentials, returns actionable errors, no database access (this plan Task 1); schema trimmed (this plan Task 1)
- All Wave 0 tests GREEN
- AST meta-test fully GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/33-keyring-single-source-of-truth/33-05-SUMMARY.md` documenting:
- register_server before/after line count + signature diff
- register_server schema diff (properties removed, required tightened)
- Prompt step-by-step before/after quotation
- Full-phase test outcome: Wave 0 test count + GREEN count
- AST meta-test violation-count-final (expected 0 outside migration.py allowlist)
- Any remaining Phase 33 deferred items (e.g. manual-only verifications from VALIDATION.md — key-path round-trip via real TTY, legacy DB migration notice)
</output>
