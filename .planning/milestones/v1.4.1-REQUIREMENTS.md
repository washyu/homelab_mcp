# Requirements Archive: v1.4.1 Security Patch

**Archived:** 2026-04-01
**Original scope:** v1.5 Security & Correctness Hardening (9 requirements from CodeRabbit PR #39 review)
**Shipped as:** v1.4.1 patch release (2/9 requirements — security-critical subset)

## Completed Requirements

- [x] **SEC-01**: The `setup_mcp_admin` and `add_to_sudoers` commands never interpolate public key content directly into remote shell strings — keys are transferred via SFTP tmpfile delivery
  - **Outcome:** Validated — SFTP-based key delivery replaces all f-string shell interpolation; grep reads from file, cat reads from tmpfile
  - **Phase:** 30, Plan 02

- [x] **SEC-02**: The TOFU existence check and append both execute under `_tofu_lock` — concurrent first-connections cannot write conflicting keys for the same host
  - **Outcome:** Validated — `threading.Lock` covers full TOCTOU window in `validate_host_public_key`; concurrency test confirms single entry after race
  - **Phase:** 30, Plan 01

## Deferred Requirements (not shipped in v1.4.1)

- [ ] **SSH-01**: Keyring auto-injection raises disambiguation error for multiple credentials per hostname
- [ ] **SSH-02**: Per-call `timeout` forwarded to `ssh_connect()` handshake
- [ ] **SSH-03**: `verify_mcp_admin_access()` uses resolved port/credentials from `resolve_ssh_credentials()`
- [ ] **ERR-01**: `resolve_ssh_credentials()` called inside error-handled section — returns JSON error, not unhandled exception
- [ ] **ERR-02**: WebSocket PTY reader closes websocket on EOF/error
- [ ] **QUAL-01**: Proxmox VM creation schema enforces `iso`/`cdrom` mutual exclusivity
- [ ] **QUAL-02**: `test_http_app.py` EOF regression test drives production `handle_shell_websocket`

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| SEC-01 | Phase 30 | ✅ Complete |
| SEC-02 | Phase 30 | ✅ Complete |
| SSH-01 | — | Deferred |
| SSH-02 | — | Deferred |
| SSH-03 | — | Deferred |
| ERR-01 | — | Deferred |
| ERR-02 | — | Deferred |
| QUAL-01 | — | Deferred |
| QUAL-02 | — | Deferred |

**Coverage:** 2/9 requirements shipped; 7 deferred to next milestone

---
*Archived: 2026-04-01*
