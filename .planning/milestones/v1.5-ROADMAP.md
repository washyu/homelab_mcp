# Milestone v1.5: Critical Bug Fixes

**Status:** ✅ SHIPPED 2026-04-20 (tech_debt accepted)
**Phases:** 31-32
**Total Plans:** 7 (2 in Phase 31 + 5 in Phase 32)

## Overview

Close all critical and high-priority bugs identified in CodeRabbit review of PR #39, with revert-proof regression tests preventing recurrence of each. Scope strictly limited to fixes — no new features, no refactoring beyond PR findings.

## Phases

### Phase 31: Bug Fixes

**Goal**: All 5 critical and high-priority bugs from PR #39 review are corrected in production code
**Depends on**: Nothing (independent fixes)
**Requirements**: WS-01, ERR-01, SSH-01, SSH-02, SCH-01
**Plans**: 2 plans

Plans:
- [x] 31-01-PLAN.md — Fix timeout error message, test assertion, and credential_type schema (ERR-01, SSH-02, SCH-01) — completed 2026-04-19
- [x] 31-02-PLAN.md — Fix zombie WebSocket PTY sessions and extract _sudo_run helper (WS-01, SSH-01) — completed 2026-04-19

**Success Criteria (all satisfied functionally; phase-level VERIFICATION.md missing — tech debt):**
1. Closing a WebSocket PTY session (EOF or connection error) cancels the paired reader task and closes the socket — no zombie sessions accumulate ✓
2. A timeout error response reports the actual computed effective timeout value, not the raw timeout_seconds parameter ✓
3. `_sudo_run` with `check=True` raises on non-zero exit code regardless of whether a password was provided — password branch behaves identically to no-password branch ✓
4. The password propagation test in `test_ssh_tools.py` fails when password is absent — assertion is not an always-passing ternary ✓
5. Passing an arbitrary string as `credential_type` to the credentials tool is rejected by schema validation — only "ssh" and "proxmox" accepted ✓

### Phase 32: Regression Tests

**Goal**: All 5 fixed bugs have dedicated regression tests that will catch any recurrence before it ships
**Depends on**: Phase 31
**Requirements**: REG-01
**Plans**: 5 plans

Plans:
- [x] 32-01-PLAN.md — WS-01 E2E regression test in tests/test_http_app.py (closes QUAL-02) — completed 2026-04-20
- [x] 32-02-PLAN.md — SSH-01 + SSH-02 regression tests in tests/test_ssh_tools.py (includes AST meta-test with D-05 mutation experiment) — completed 2026-04-20
- [x] 32-03-PLAN.md — ERR-01 regression test in tests/test_error_handling.py (monkeypatched asyncio.wait_for) — completed 2026-04-20
- [x] 32-04-PLAN.md — SCH-01 regression test in tests/test_tools.py (credential_type enum shape) — completed 2026-04-20
- [x] 32-05-PLAN.md — Gap closure: extend SSH-02 AST detector to catch d25c915 pre-fix Compare(Constant in X) form + D-10 note — completed 2026-04-20

**Success Criteria (all 5 verified by `32-VERIFICATION.md`):**
1. WebSocket PTY reader regression test — reverting the fix causes the test to fail ✓
2. Timeout error message regression test — reverting the fix causes the test to fail ✓
3. `_sudo_run(check=True)` regression test — reverting the fix causes the test to fail ✓
4. `test_ssh_tools.py` password assertion regression guard — extended AST detector catches both disjunctive-Name and Compare-Constant-In forms ✓
5. Credentials schema enum regression test — reverting the fix causes `prop["enum"]` KeyError ✓

---

## Milestone Summary

**Decimal Phases:** None.

**Key Decisions (carried into PROJECT.md):**
- [31-01] ERR-01 fix is minimal single-variable substitution in `error_handling.py:58` — `effective_timeout` already in scope from lines 50-53, no helper refactor needed
- [31-01] SCH-01 enum goes on property definition inside `list_keyring_credentials.inputSchema.properties.credential_type` — MCP framework validates JSON Schema before handler runs
- [31-01] SSH-02 assertion simplified to single check on "No credentials" — paired `status == "error"` assertion at line 189 already guards the error-path precondition
- [31-02] `contextlib.suppress(Exception)` around `websocket.close()` — cleaner idiom than try/except, matches module's existing contextlib usage
- [31-02] Quoted `_sudo_run` return annotation as `'asyncssh.SSHCompletedProcess'` — class is not subscriptable at runtime, mypy stubs have no generic support; quoting defers evaluation safely
- [32-02] AST meta-test pattern — parse `tests/test_ssh_tools.py` itself, walk AST, fail if `BoolOp(Or)` contains structurally-always-true operand; extends lint-style detector pattern
- [32-05] Extended `_is_structurally_always_true` to treat `Compare(left=Constant, ops=[In()], comparators=[<any>])` as a tautology — catches the exact `d25c915` pre-fix mutation form

**Issues Resolved:**
- All 5 CodeRabbit critical/high findings closed
- QUAL-02 deferred item closed (WS-01 regression uses production `handle_shell_websocket`)
- SSH-02 AST detector gap closed in 32-05 (pre-fix Compare-Constant-In form now covered)

**Issues Deferred (tech_debt accepted at close — see MILESTONES.md):**
- Missing `31-VERIFICATION.md` — Phase 31 merged on plan-SUMMARY evidence; Phase 32 regression tests indirectly re-prove each fix via revert-proof integration, but the formal Phase-31 gate was skipped
- `31-VALIDATION.md` remains `status: draft`, `nyquist_compliant: false`, `wave_0_complete: false`
- Missing `32-VALIDATION.md` entirely
- SUMMARY frontmatter inconsistency: `32-01` uses flat `requirements-completed: [REG-01]`; `32-02..05` use nested `requirements:\n  - REG-01` — both parse but extraction picks up only the flat form

**Technical Debt Incurred:**
- See `Issues Deferred` above — 4 process-level bookkeeping items. All functional coverage is sound; the debt is paperwork/gate items compensated for by Phase 32's revert-proof wiring.

**Audit Result:** `tech_debt` — 6/6 requirements functionally satisfied; 5/5 integrations WIRED; 5/5 revert-proof flows verified. See `.planning/milestones/v1.5-MILESTONE-AUDIT.md` for full analysis (integration checker agent: `a7a1d352d4dae20c6`).

---

*For current project status, see `.planning/ROADMAP.md`.*
